Status: current
Last verified: 2026-07-23
Review cadence days: 14
Source paths: scripts/runtime/battle_runtime.gd, scripts/data/pokemon_catalog.gd, scripts/app/main.gd, scripts/app/smoke_scenarios.gd, scripts/domain/world_generator.gd, scripts/domain/biome_defs.gd, scripts/domain/biome_encounters.gd, scripts/domain/material_drops.gd, scripts/domain/recipes.gd, scripts/domain/pokemon_rules.gd, scripts/runtime/camping_runtime.gd, docs/registry/art-anchors.toml, docs/references/miss-postmortem-protocol.md, docs/product-specs/building-and-placement.md, docs/product-specs/camping-crafting-survival.md

# Tech Debt Tracker

## Open items

- 142 of 299 catalog moves have no source animation set and use the synthesized lunge/flash fallback (157 of 299 play a per-move source set — recomputed via the `attack_anims` resolver over the live catalog, not hardcoded); extend coverage or enrich the fallback.
- Forced moves (rampage/encore) spend PP on the chosen slot rather than the forced move; the domain exposes `forced_move` for a future runtime fix.
- One unreproduced anomaly: a single `visual_sweep` battle shot showed an enemy level of 0 while every code path clamps levels to >= 1 and the encounter trace agreed with the HUD in 3 subsequent runs. Keep an eye on battle HUD level text.
- Building Phase 1 deferred behavior (spec: [product-specs/building-and-placement.md](product-specs/building-and-placement.md)): STORAGE-BOX INTERACTIVITY (definition + placement + occupancy + cost only — no contents, no interact; Phase 3 fills it in) and full PER-BIOME SPRITE completeness (default `house1_*` + Desert stone/ruins ship; snow/savanna/spooky variants from the `pokewilds/tiles/buildings/` families are partial). BED INTERACTIVITY is NO LONGER deferred: it landed with Phase 2 (bed = full heal + status cure + revive via interact-rest; the campfire placeholder is filled too — light source + crafting station + runtime `lit` toggle). The "indoors" enclosure FLOOD-FILL stays DEFERRED: Phase 2 needs indoor gating only for the kiln (itself deferred — no kiln structure in any ported phase), so no enclosure computation landed and the sleeping-bag indoor heal bonus waits on it (below). DEMOLITION landed with Phase 1 — Cut refunds the full `cost_for` (hard-stone shells need Smash), wired into the harvest Z-action flow, removing the placement entry only, plus the would-trap guard.
- Camping/crafting Phase 2 deferred behavior (spec: [product-specs/camping-crafting-survival.md](product-specs/camping-crafting-survival.md)): (1) KILN + GREAT BALL (and all kiln balls — Ultra/Dusk/Net/… from the scraped Items page) — `great_ball` is defined at station `kiln` (poke_ball + metal_coat, faithful) but UNAVAILABLE; no kiln structure exists in any ported phase (a later building phase adds it, with the indoor enclosure flood-fill it needs). (2) INTERIM INGREDIENT SOURCE — `material_drops.gd` battle-end type drops (ELECTRIC→magnet, WATER→hard_shell, STEEL→metal_coat, BUG→silky_thread, FLYING→soft_feather; 100% on a type-matched victory/capture, pure/no-rng) REPLACE the faithful happy-Pokemon HABITAT DROPS (`.firecrawl/wiki-materials.md:463-471/:543/:571`, `wiki-habitat.md`); Phase 5 owns the faithful system and retires the interim — it must preserve the log/hard_stone exclusion the witness invariant below rides on. (3) SLEEPING-BAG indoor-effectiveness bonus + the original's "certain terrain / near aggressive Pokemon" use restrictions (`fresh-sleepingbag.md:145`) — deferred with the enclosure flood-fill (indoor bonus) and the Phase-6 aggressive-chase system (restrictions); the bag is flat 50% with no status cure (the faithful cure split: bed/berries/faint-then-bag). (4) OVERWORLD GHOST/SHADOW SPRITES — the night hazard is a forced battle only (`ghost_sheet1.png`/`shadow1.png` exist for a later overworld-sprite phase); the original's Damp/Mystic/Cursed-Soil DWELL trigger (`wiki-overworld-encounters.md:274`) is replaced by night-without-light (tiles the port does not generate; the dev-attested framing is documented). (5) ROD USABILITY — rods craft into the bag but do not fish (fishing = Phase 5); crafted BALL TIERS are bag inventory only until the Phase-8 ball tiers (`battle_runtime` BALL_ID stays `poke_ball`). (6) FAILOPEN EVOLUTION CONTEXT — `pokemon_rules._time_requirement_met` (:247) still passes on EMPTY context (flipping to fail-closed costs 1 line the 320/320 file does not have and changes soak expectations); GUARANTEE: the battle caller passes real `time_of_day` and future callers of `check_level_evolution` MUST too (code comment at :247). OUT entirely (documented in the spec): repel crafting (conflicts with the Phase-4 field-move model), apricorn balls, the Dusk Ball night-catch bonus, the Flash field-move ACTION (Phase 4 — the passive Fire-type light READ landed), torch fence/interior-wall mounting (Phase 3 interiors).
- Building region-enclosure escape is a LOAD-BEARING INVARIANT, not a coded guarantee (spec: [product-specs/building-and-placement.md](product-specs/building-and-placement.md)): the would-trap guard (`build_runtime._would_trap_player`) deliberately ALLOWS a region-scale seal (a closed wall ring, a sealed island chokepoint) and refuses only self-enclosure on the player's own tile. Escape from a permitted seal rests on the party always being demolition-capable, which holds ONLY while `build_runtime.unwitnessed_demolish_moves()` stays empty — every shell class has a cost material solely its demolish move yields (log→cut, hard_stone→smash). Phase 2 added a SECOND material source (the interim battle-end type drops) and a starting-bag sleeping_bag; the witness STILL HOLDS because `material_drops.TYPE_MATERIALS` NEVER contains `log` or `hard_stone` (the witnesses stay harvest-only) and the sleeping bag is not a shell material — mechanized by `unwitnessed_demolish_moves` + `placement_flow`'s `_check_demolish_invariant`, which now asserts the drop table's exclusion on every run. If ANY future material source (the Phase-5 habitat drops, a shop/gift) adds log or hard_stone, or a party-release/box mechanic lands, extend the trap guard to forbid region-scale enclosure (or re-prove escape) BEFORE shipping that change — a non-empty witness result would turn a permitted seal into a permanent self-trap.
- Line budgets AT CEILING (flagged for the next edit): `scripts/app/main.gd` (220/220), `scripts/app/placement_flow_scenario.gd` (220/220), `scripts/domain/pokemon_rules.gd` (320/320 — untouched by Phase 2 by design), and `scripts/runtime/game_runtime.gd` (stays AT 320/320: Phase 2 freed the campsite accessors + encounter level/fallback helpers into `camping_runtime.gd`/`encounter_selection.gd` FIRST, then spent the freed lines on the night/camping/crafting wiring) have zero-or-near-zero headroom; `scripts/runtime/battle_runtime.gd` stays WITHIN its 320 budget after the `_move_priority`→`BattleRules.move_priority` swap paid for the evolution context + retreat-callable additions; `scripts/domain/world_generator.gd` (318/320) has two lines and `scripts/app/visual_sweep_baselines.gd` (219/220) has one. Any follow-up edit to these files must extract first (the building slice extracted `field_action_router.gd`/`placement_flow_demolition.gd`/`world_invariants.gd`; the camping slice extracted `encounter_selection.gd` and moved the campsite accessors + `_move_priority`).
- Replace remaining dictionary-heavy runtime contracts with more explicit typed boundaries once the gameplay slice stabilizes.
- Add subsystem entries and specs immediately when new systems such as NPCs, PC storage, or building are introduced.
- Slice 6 DoD #7 (legibility/vision plan): the four optional verification spikes stay DEFERRED, explicitly off the critical path — Movie Writer PNG-burst motion lane, DAP `godot/put_msg` bridge, ScriptBacktrace (#91006) structured error frames, and the live NDJSON-over-TCP introspection endpoint ("built ONLY IF in-process sidecars prove insufficient" — they have not). Status recorded in docs/superpowers/plans/2026-07-20-agent-legibility-and-vision-verification.md; adopt each behind its own registry entry only if ever needed; none gates CI. (The box-mode `battle_message` judgment was documented at flip time, so no deferral there.)

## Resolved this cycle

- Camping, crafting & night survival (Phase 2, feature-completion plan): the Phase-1 placement system's two placeholders became gameplay and the night became dangerous. The campfire is now a LIGHT SOURCE + CRAFTING STATION (lit by default, no fuel mechanic — faithful; an additive `"lit": false` placement-entry field carries the extinguished state with ZERO validator changes since `is_valid_placement` checks only kind + structure_id) and the bed is a FULL HEAL + STATUS CURE + revive via interact-rest; the TORCH joined `structures.IDS` appended last (faithful 1 Log + 1 Grass → log + dry_soil; always lit; fence/wall mounting documented as a deviation). Campfire crafting landed with a faithful recipe table (`recipes.gd` — Poke Ball, Soft Bedding, Old/Good/Super Rod scraped from the wiki Campfire/Items pages with line citations; Great Ball DEFINED at the kiln station but UNAVAILABLE — the exec-plan parenthetical that conflated it with the Poke Ball recipe was corrected in the spec, not flattened), a consume/grant `crafting_runtime.gd` with reason-carrying refusals (`no_station`/`wrong_station`/`missing`/`no_recipe`), and a camp menu UI (Craft / Extinguish-or-Light / Demolish — demolition stays reachable so the region-seal escape holds). Ingredients come from a minimal deterministic INTERIM (`material_drops.gd` battle-end type drops mapped 1:1 from the wiki Materials drop table; pure/no-rng; witness-safe — never log/hard_stone, asserted by craft_flow every run via crafting_runtime.drop_witness_clean) that the Phase-5 faithful habitat drops retire. The sleeping bag is a starting-bag reusable (faithful): 50% HP + fainted revive, NO status cure (the spec's open "or partial" question resolved to the faithful bed-only cure), with the indoor bonus + terrain/aggressive restrictions deferred. Resting advances the clock (night → next 07:00, day → +4h) and ESTABLISHES the campsite anchor the overflow-mon hold reads; blackout now returns to the CAMPSITE instead of the world origin (same-line edit; the defeat toast is the only main.gd change). Night danger: `day_phase.gd` is the single day/night source of truth mirroring the tint keyframes (night_cycle asserts the 269/270/1229/1230 boundaries agree); a light model (lit campfire/torch within Manhattan 4, or a Flash-capable Fire-type party member — a capability READ per the dev interview, not the Phase-4 Flash action); ghost encounters at half the light-less night cadence on the shared `_rng` (sorted type-derived pool, deterministic under `seed_for_smoke`; the original's soil-dwell trigger replaced by night-without-light — documented); shadow battles block retreat for their duration via an injected callable (the layering holds — battle_runtime never imports night_system). Nocturnal spawns: `filter_species_ids` gained an additive `time_of_day` parameter (UMBREON night-only in Savanna; GHOST type admitted to every night pool). Time evolutions fixed CALLER-SIDE only — `battle_runtime._try_evolve` passes `{"time_of_day": …}` into the existing context parameter (`pokemon_rules.gd` untouched at 320/320; the `_move_priority` one-liner moved to `battle_rules.gd` as `BattleRules.move_priority` to keep battle_runtime within 320), so Espeon/Umbreon/Frosmoth gates work with an `evolution_time_gate` trace in both directions; the empty-context fail-open stays with a documented guarantee. The static-gradient glow composes inside `structure_layer.gd` (the design's documented fallback path — night-only, no per-frame animation, byte-stable baselines; `world_view.gd` untouched). All Phase-2 state is v3-additive (no version bump, no new top-level key, no recipe discovery state). Budget-full files touched only by extraction/same-line edit (game_runtime keeps its 320 budget by freeing the campsite accessors + encounter helpers into `camping_runtime.gd`/`encounter_selection.gd` before the new wiring; battle_runtime stays within 320 via the `_move_priority`→`BattleRules.move_priority` swap; main.gd one same-line setup argument, toast wording unchanged; pokemon_rules zero). Covered by four new scenarios (`camp_survival`/`craft_flow`/`night_cycle`/`time_evolution`) + `visual_sweep_camping` baselines (shots 15-17; 09-12 stay battle-reserved by the sidecar canary contract) + the full doc payments, under verify_all. Spec: `docs/product-specs/camping-crafting-survival.md`; the deferred behavior (kiln/Great Ball, habitat drops, bag indoor bonus + restrictions, overworld ghost sprites, rod usability, crafted-ball use, fail-open guarantee) is logged in Open items.
- Building placement (Phase 1, feature-completion plan): the harvest economy's FIRST CONSUMER landed on top of the world-mutation layer, closing the "building gets its own spec next" item. Pure-domain structure definitions + faithful per-biome costs in `scripts/domain/structures.gd` (the original Buildings-page numbers, with the documented Grass→dry_soil secondary mapping and the Desert hard_stone shell swap); Fighting-type Build capability reused from `field_moves.gd` (no change needed — scenarios swap in a Build-capable mon, e.g. MACHOP, since the starting Chikorita cannot Build); a runtime placement orchestrator (`scripts/runtime/build_runtime.gd`) that consumes bag materials through the session item API and emits four traces with reason-carrying refusals; a ghost-preview/build-mode/occupancy layer (`scripts/runtime/structure_layer.gd`) that COMPOSES `world_view.gd` without extending it; and overworld-context + party-screen `FIELD MOVE` entry points extracted into `scripts/app/field_action_router.gd` (the party-screen move id is now consumed, closing the Phase-0 decorative-FIELD-MOVE flag). Placements ride the sparse override map as a "placed" entry kind and persist under a NEW ADDITIVE v3 `structures` save key (version stays 3; clears key untouched), rendered in place via a merged `mutations_for_view()` mirror so occupancy agrees across logic/render/collision by construction. A walled house with a door is buildable from harvested materials (the exit object); covered by `placement_flow`, the extended `world_consistency_audit`/`save_migration`, and the deterministic `13_built_house.png` visual baseline. Spec: `docs/product-specs/building-and-placement.md`; the deferred Phase 2-3 behavior (bed/box interactivity, indoor flood-fill, demolition, per-biome sprite completeness, the unbuildable bed) is logged in Open items.
- Vision-fidelity systemic fix — the self-referential-check gap that let the 11 stage-px HP-bar misalignment ship green (G1 + G2). Root cause: every automated check class measured the code against its own earlier output or a human transcription (pixel baselines freeze whatever the code renders; the sidecar-consistency reviewer diffs code-output vs code-output; the art-anchored render model only covered what someone hand-modeled, and the bars were never modeled), and the one rubric question that would have caught it ("Are HP bars on their baked tracks?") had no answering agent. Resolved at a SYSTEMIC level per the governing directive (fix the REASON it was missed, never the specific miss), not by an HP-bar test: (G1) a source-art anchor registry (`docs/registry/art-anchors.toml`) whose `stage_rect`s are a pure integer function of the art BYTES re-derived at check time by `tools/art_geometry.py` — the dark-run scan of `battle_screen2.png` reproduces the fixed HP tracks (32,18,48,4)/(96,74,48,4) and excludes the misaligned 43/107, baseline-independent on a fresh checkout — behind a mechanical `[[scenes]]` overlay inventory (any future overlay is caught on day one), a `tools/check_art_anchors.py` RED static gate folded into `check_repo_contracts` (completeness + art-freshness re-derivation + the `art_sha256` pin, since art is a submodule), an `anchor_drift` Lane-4 class grounded on the first art-truth `anchor:<id>` region (quarantine, graduating per-anchor), and an `apply_anchor_gate` baseline-regeneration refusal gate that structurally closes the "wrong baseline frozen green for 3+ days" hole; (G2) the Lane-4 `--reviewer-cmd` socket is populated by `tools/vlm_reviewer.py` (Qwen3-VL 8b, pure-stdlib, quarantine-forever witness, composite degrade so coverage never depends on the model) and a rubric-coverage ledger makes "unanswered" a first-class COUNTED state (`q1-` question ids, `QUESTION_ANSWERERS`, `EXPECTED_QUESTION_COUNTS` backstop, `rubric_coverage` manifest block). A miss-postmortem protocol + tracked ledger (`docs/references/miss-postmortem-protocol.md`, `docs/generated/miss-postmortems.json`) binds every future miss to a class-level mechanism; the HP-bar episode is entry #1. Spec: `docs/product-specs/vision-fidelity.md` § Source-art anchor registry / VLM reviewer lane / Rubric coverage ledger.
- Phase 0 defect & hygiene batch (feature-completion plan 0.1–0.9): the standing data-integrity and repo-hygiene gaps are closed with no new features. Full-party captures are no longer losing — overflow is relocated to a non-losing campsite HOLD (`mon_relocated`) and retrieved from the party screen (`mon_retrieved`); New Game is confirm-gated and closes the menu; save writes are atomic (temp+rename) with traced, non-destructive recovery (`.corrupt.bak` + `save_recovery`) and a non-destructive future-version refusal (`.newer.bak` preserved before the fresh-start autosave); the blackout heal now clears status and `sleep_turns`; `world_view._tile_cache` is window-evicted each `sync_visible` (bounded to the visible window); the exit-time ObjectDB/resource leak (OGG playing at process exit) is fixed at the source with a headless audio gate; the action_b/run `KEY_X` double-bind is documented as a deliberate mutually-exclusive-context shared key; and the doc-drift batch (113→114 items, 298→299 moves, capability-model wording, README refresh) is settled. A dedicated `save_migration` fixture now covers v1/v2→v3 migration and future-version refusal, closing the RELIABILITY open risk.
- Deleted the deprecated `is_field_move_unlocked`/`unlock_field_move` shims from `game_runtime.gd` (nothing had called them since the harvest rework; removed on the Phase-0 runtime touch that added the campsite/version-refusal accessors).
- Removed the dead `is_field_move_unlocked` context-callable reader from `scripts/ui/party_screen.gd` (it fell back harmlessly to species-flag rows; cleaned up on the Phase-0 party-screen touch that added the campsite RETRIEVE action).
- One-command local gate (Workstream L.1): `tools/verify_all.py` closes the standing "verify_all.py absent / one-command local gate missing" gap. It ORCHESTRATES (never forks) the four static gates, the determinism pins/canary, the transport-honest headless full suite, the windowed pixel lanes (`ui_render_audit` + `visual_sweep`), and the legibility report in one run-all command, and MECHANIZES the refuse-on-mismatch + freshness refusals that were "human/agent-enforced policy until verify_all" — R1 report `head_sha`==git HEAD, R3 windowed `godot_version`/`renderer`/`window` vs the baseline capture_env + canonical 1152x648 window, R4/R5 windowed entries present with correct transport (`ui_render_audit` not headless), R6 `vision-review.json` freshness via `review_is_fresh`. Four honest exit codes (0 GREEN / 1 STEP_FAILURE / 2 REFUSAL / 3 TOOL_ERROR — a transport skip is never a failure, a stale/mismatched report is a refusal, a missing binary is a tool error); `--skip-windowed` reports the pixel lanes SKIP, never PASS. Documented as the pre-push ritual in RELIABILITY.md § Local gate and AGENTS.md; stays a LOCAL ritual (CI lint/contract-only, unchanged).
- Removed the synthetic `SMOKE_MON` fallback: the catalog reliably loads 954 species, and an empty catalog now skips encounters with a `warning` trace instead of fabricating a Pokemon.
- Battle mechanics replaced the simplified damage math with mainline formulas: 18-type effectiveness chart, stat stages, status effects, capture formula with catch rates, growth-curve EXP, and level/item/happiness evolution.
- Encounter tables are species-level, parsed from source `wilds_data.asm` spawn biomes with a source-token alias map; type-based matching remains the fallback.
- Save schema v2 persists the bag, time of day, and step count, with v1 migration on load; the session clock advances per step and drives day/night presentation.
- The start menu grew real party and bag screens with swap-lead, summary, field-move, and item-use flows.
- Player avatar renders from the source walking/running sprite sheets; biome music and the wild-battle theme are wired through the music router.
- Automated playtesting: `playtest_journey` scripts the full player loop with a save round-trip check, and `playtest_soak` runs a seeded bot asserting HP/PP/bag invariants every iteration; `tools/run_playtests.py` executes the suite over DAP or headless and writes a JSON report.
- Visual verification: `visual_sweep` captures 16 screenshots per run. Its first pass caught real defects the trace suite could not: source tile/prop PNGs use white/black backgrounds the original engine color-keyed (now flood-fill keyed at load with caching, composited over per-biome ground colors), sprite-less species could enter battle (now filtered from encounter tables, with an intentional `?` placeholder as backstop), battle message text overflowed into the command box, species names garbled the baked `:L` glyph (level markers now redraw dynamically after measured name width), the moves screen overlapped sprites and HUD plates, the menu dim double-stacked over sub-screens, and bag items showed raw ids (`Poké Ball` now resolves).
- Housekeeping: every smoke scenario now backs up and restores the player's save file (no more test-state clobbering); input routing is extracted into `scripts/app/input_router.gd` so `main.gd` has headroom again; bag item ids are canonical i18n keys (`poke_ball`) with legacy-save migration; the legacy music-router wrappers are deleted.
- Harvest & world mutation: a sparse per-tile override map (save schema v3) makes cut/dig/smash permanent; field moves use party capability (species flags + type auto-ability) instead of stored unlocks; harvesting yields materials (log, soils, stones) through one resolver driven by context-Z and the party screen; the world view mirrors overrides live via a `world_overridden` signal.
- Audit suite: `nav_audit` (traversal agreement, battle/menu reachability, model/cursor consistency), `texture_audit` (all 633 battle sprites + 27 overworld textures through the real loaders), `data_audit` (encounter-pool battle viability, instance integrity, bag/catalog resolution), and `layout_audit` (worst-case label fit + cursor alignment) run headlessly in seconds. `visual_sweep` is now deterministic (fixed seed, crafted party with a strip-sprite canary, fixed battle) and diffs against committed baselines via `tools/visual_diff.py` (0.5% pixel threshold; `visual_sweep_update` accepts intentional changes).
- Model-based oracle lanes (spec: docs/superpowers/specs/2026-07-18-autonomous-playtesting-oracles-design.md): `world_consistency_audit` (tile logic/render/collision three-way plus spatial, z-order, and tall-grass contracts) and `ui_render_audit` (art-anchored expected strings, label overlap, cursor pairs, plus a calibrated pixel lint whose findings are quarantine-tier). The soak asserts per-step spatial invariants. First runs caught and fixed: player drawing over tree canopies (now y-sorted), item-menu two-column text overlap, HUD status/level collision, and wide TYPE values bleeding over the side-box border.
- Agent vision review: `docs/references/vision-review-rubric.md` structures a per-state visual read of every sweep shot; findings land in `.godot-smoke/vision-review.json` (quarantine-tier). First pilot: zero defects, plus one coverage note — no deterministic state yet places the player north of a tall prop, so the canopy contract lacks a pixel canary.
- Battle presentation: attack animations play from the source metadata (157 of 299 moves with sets; synthesized fallback for the other 142), species cries sound at battle start and on faint, and the top ten unhandled move-effect families (confusion, heal, trap, rampage, protect, fury cutter, encore, attract, OHKO, priority) are implemented with soak warnings measurably reduced.
- Battle surface scales to the largest integer factor (fractional scales aliased the pixel font at some window sizes), and KO blows now play their animation before the battle screen closes instead of skipping it.
- The player HP bar now stays visible during move selection (name/level yields to the side window) and during attack animations (the source's healthbar-hide metadata is deliberately unhonored since the port's animation frames carry no replacement HUD).
- First audit pass caught and fixed: 5 non-conformant sprite strips (COTTONEE, MINIOR, WHIMSICOTT, two ROTOM backs) that defeated frame detection; sprite-less-data species with zero catch rates or empty learnsets in encounter pools (EGG, GMRMIME, ROTOM forms, CORSOLA_GALARIAN, SHELLOS_*) — eligibility now requires battle viability; `potion` missing from the source i18n (runtime item supplement added); long alternate-form names overflowing the GSC HUD (short-form fallback with measured metrics); moves-screen cursor/row misalignment; the battle font now matches the baked art (source `fonts.ttf` at 7px); tall grass is real patches that own encounters.
