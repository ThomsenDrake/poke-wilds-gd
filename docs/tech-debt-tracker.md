Status: current
Last verified: 2026-08-13
Review cadence days: 14
Source paths: scripts/runtime/battle_runtime.gd, scripts/data/pokemon_catalog.gd, scripts/app/main.gd, scripts/app/smoke_scenarios.gd, scripts/domain/world_generator.gd, scripts/domain/biome_defs.gd, scripts/domain/biome_encounters.gd, scripts/domain/material_drops.gd, scripts/domain/recipes.gd, scripts/domain/pokemon_rules.gd, scripts/runtime/camping_runtime.gd, scripts/runtime/storage_runtime.gd, scripts/domain/field_moves.gd, scripts/runtime/night_system.gd, scripts/runtime/player_avatar.gd, scripts/domain/structures.gd, scripts/runtime/session_state.gd, scripts/runtime/stone_evolution_runtime.gd, scripts/runtime/breeding_runtime.gd, scripts/runtime/habitat_runtime.gd, scripts/domain/habitat_drops.gd, scripts/ui/bag_screen.gd, docs/registry/art-anchors.toml, docs/references/miss-postmortem-protocol.md, docs/product-specs/building-and-placement.md, docs/product-specs/camping-crafting-survival.md, docs/product-specs/storage-and-party.md, docs/product-specs/field-moves.md, docs/product-specs/breeding-shinies-drops-fishing.md, scripts/runtime/structure_layer.gd, scripts/app/input_router.gd, scripts/ui/message_box.gd, .github/workflows/repo-contracts.yml, .github/workflows/playtests-headless.yml

# Tech Debt Tracker

## Open items

- Pokémon systems (Phase 5, feature-completion plan features #13/#14/#15 + shiny half of #5; spec: [product-specs/breeding-shinies-drops-fishing.md](product-specs/breeding-shinies-drops-fishing.md)) — PROGRESS: shinies (1/256 `is_shiny` at creation, battle palette swap + sparkle, status/egg badge, faithful overworld silence), breeding (sealed-fence pens + pasture abstraction, egg-group pairing + Ditto, father egg moves via the catalog `egg_moves` field, pre-hatch egg visibility, step-based hatch at level 5), the FAITHFUL habitat drops (primary source; the Phase-2 interim flips to documented secondary), rod-tiered fishing (bite roll + cumulative Beach-anchored pools + fixed per-tier level bands + pending-encounter seam), and the evolution-stone bag-use seam (WIRED 2026-07-26 — `stone_evolution_runtime.gd` behind `game_runtime.use_stone_on_mon`; the bag screen's stone branch; `evolution_stone_used`'s first live caller; see Resolved). The 2026-07-26 review fixes also landed: the breeding lay gate rides the ONE shared habitat scan (`HabitatDrops.pen_habitat_tags` interior + ring + `types_satisfied` per parent — the divergent second table deleted from `breeding.gd`; drops-comfortable ⟺ lay-gate passes), the pasture store is ONE session key held BY REFERENCE (habitat's daily +10 reaches the 220 gate the same step; the deep-copy rollback hazard gone), and `breed_flow` gained the wrong-egg-group negative + the Flying/Water ring-scan proofs. Stone ACQUISITION is ALSO RESOLVED this cycle (item 11 — see Resolved; only the ice_stone/dawn_stone world source stays open as the item-11 residual). DEFERRED PIECES recorded by design: (1) USER-ADJUSTABLE SHINY ODDS — RESOLVED 2026-08-04 by the new-game-flow slice: the creation screen's SHINY RATE step landed the FAQ's promised knob over a FLAGGED ladder `SHINY_ODDS_CHOICES := [32, 64, 128, 256, 512, 1024]` around the faithful 1/256 (see Resolved). (2) HATCH-CYCLE DATA is not in the parsed asm dump — every egg uses `DEFAULT_STEPS_TO_HATCH := 2560` (Gen-2 minimum); the per-species Eevee/Dratini incubation spread awaits an importer/catalog extension (the catalog `egg_hatch_steps` field is already read when present). (3) WILD EGG NESTS + egg-theft aggression + Attack-on-egg shiny check (`wiki-overworld-encounters.md:248`) are the Phase-6 overworld twin of these rules (no overworld-mon entities until Phase 6 — the pasture abstraction + visible pen eggs are the Phase-5 model). (4) The HOLDING-ITEM FARM LOOP (a mon visibly holding an item "can deliver more items, regardless of whether a full day has passed") + the talk-to-the-mon interaction need overworld-mon entities; the port grants the daily drop directly to the bag. (5) The separation rule is FAITHFUL, not debt — a third COMPATIBLE mon breaks attraction (`find_pair` returns {} unless exactly one compatible pair forms); extra INCOMPATIBLE mons are ignored (the entry records the decision against the design's first-pair simplification). (6) GHOST (cursed soil/gravestones) and STEEL (light clay) habitats are BASIC-GROUND PLACEHOLDERS until those structures/tiles exist; the Dragonite-line water exception IS modeled. (7) DARK's night-secondary-habitat nuance is not modeled (pure Dark → basic; dual Dark's other tile is demanded by the dual-type AND rule anyway). (8) PERIODIC COMFORT RE-EVALUATION (the day-boundary habitat tick, +10 happiness per comfortable in-game day, no decay — ~9.5x slower than the design's first hourly +4/-2 model) replaces the original's deposit-time freeze — a documented divergence so "becomes uncomfortable → forfeits the generated drop" is observable at all. (9) FISHING TABLES are BIOME-AGNOSTIC — only the Beach table was scraped (`fresh-beach.md:63`); per-biome pools need fresh scrapes (firecrawl credits exhausted). The table holds to the five Beach-scraped species with FIXED per-tier level bands (Old 3-7, Good 8-14, Super 15-22); the Goldeen/Seaking/Seadra additions the design first sketched were dropped with the distance-based level model. (10) LURE BALL (3× fished) / DIVE BALL (3.5× water) catch bonuses wait on the Phase-8 ball tiers. (11) EVOLUTION STONE ACQUISITION — RESOLVED this cycle (BOTH halves; see Resolved). Bag USE: `stone_evolution_runtime.gd` / `game_runtime.use_stone_on_mon` (`breed_flow`'s stone group ARMED, `stone_ok: true`). ACQUISITION: implemented 2026-07-26 as the FAITHFUL Beach-Dig pool (SAND → water_stone among big_pearl/clear_glass/revive — the `fresh-beach.md` "Dig Items" set, exact order) PLUS flagged PORT ADDITIONS, each DIVERGENCE-flagged in code + spec: the other-biome Dig pools (GRASSLAND `[leaf_stone]` / FOREST `[leaf_stone, moon_stone]` / SAVANNA `[fire_stone, thunderstone]` / DESERT `[sun_stone]` / SWAMP `[dusk_stone]`; the scrapes cite NO non-Beach stone source), the invented `DIG_BONUS_RARITY := 8` rate (the wiki documents pool membership, no rate), and the Steel-type shiny_stone cadence drop (`habitat_drops.STEEL_STONE_DROP := "shiny_stone"` / `STEEL_STONE_CADENCE := 4`; `wiki-materials.md:393` documents Metal Coat ONLY — the exec plan's :149 "Steel-type drops" is uncited intent, so the stone choice + cadence are inventions; `TYPE_MATERIALS["STEEL"]` stays exactly `[metal_coat]`). The earlier "needs a seeded rng threaded into `harvest_runtime.setup`" blocker was solved WITHOUT touching the shared `_rng`: every draw is a pure step-counter function (`harvest_resolver._dig_draw` over total_steps+tile; `habitat_drops.steel_stone_due` over types+day+last_stone_day), so the night_system determinism guarantee holds and NO build pins were re-stamped (seven scenarios re-ran byte-identical, the shiny_odds 6/1024 pin holding). Extending the Steel row stayed witness-safe exactly as predicted (the audit checks only log/hard_stone; `is_habitat_drop_material` now walks the stone so the audit is honest). RESIDUAL (the ONLY remaining item-11 piece, deliberately open): ice_stone + dawn_stone have NO world source — SNOW is not a diggable biome — and stay scenario-grant-only; never papered over with a bad thematic fit. Proven end-to-end by `field_moves_flow`'s dig group (Beach pool faithful / divergent pool pinned to the SHIPPED table / PLAINS negative control) + `habitat_drops`' `run_steel_drop_control` (day-20 drop / day-21-23 suppression / day-24 re-arm). (12) ROCK→HARD_STONE drops are WITHHELD to keep the building-loop witness invariant (below); the faithful row restores when the trap guard extends to forbid region-scale enclosure. (13) `ALLOW_UNDISCOVERED_BREEDING := false` is the FAITHFUL default for genderless/Undiscovered/legendary breeding (wiki Note b); the exec plan's breedable workaround stays one flagged const flip away — never silent, witnessed by `breed_flow`'s unbreedable group.
- Field-move completion (Phase 4, feature-completion plan feature #7; spec: [product-specs/field-moves.md](product-specs/field-moves.md)) — PROGRESS: the eight rules-only moves (Flash, Teleport + Way Stones, Ride, Fly, Attack, Charm, Repel, Power) gain runtime callers in the NEW `field_move_runtime.gd` (extracted because `game_runtime.gd` is at budget), with the `field_moves_flow` / `build_house_flow` / `playtest_field_soak` scenarios driven by the `FIELD_MOVES_PARTY` fixture (the DP-proven 6-species minimum cover). DEFERRED PIECES recorded by design: (1) ATTACK / CHARM are HOOKS only (`overworld_attack` / `charm_used` traces + Phase-6 hook callables) — the overworld-Pokémon ENTITIES they target (spawn/roam/flee/aggro, recruit, nests, Alphas) land in Phase 6; the scenarios assert the hook is reached + traced + refuses gracefully with no target, never any mon state. (2) FLY is to-VISITED-Way-Stone only — edge-fly (Surf/Fly off a map edge generates an adjacent world) is the Phase 7 world-chaining trigger, NOT built. (3) TELEPORT is intra-world only — world-edge **Teleport Beacons** for world chaining are Phase 7 (the Way-Stone/Beacon naming split is kept airtight). (4) RIDE is traversal SPEED only (mount speed mode + sprite) — the original's ledge-climbing is out (no ledge terrain). (5) POWER is movable-boulder props only (distinct from Smash's destructible rock) — tying boulders to a landmark gate is Phase 7 if useful. (6) REPEL fidelity gap — the original is a CRAFTED ITEM (campfire: 1 Charcoal + 1 Manure; Max Repel = Poison Barb + Repel) repelling LOW-LEVEL mons for a while; the port scopes the field-move version suppressing ALL encounters for N steps (`session_state.repel_steps`). (7) The ACTIVE Flash is a port convenience over the faithful PASSIVE light (a Fire-type party member lights `LIGHT_RADIUS=4`, "the same range as a campfire" — the original "can't be selected like Cut"); the active caller must not diverge from the campfire range. (8) fly/ride stay EXPLICIT-FLAG-ONLY (the fidelity decision resolving the exec plan's "AUTO_TYPES gains fly/ride" — no broadening; fly stays 30 species, ride 32). All Phase-4 state is additive on save schema v4 (no bump — v5 is reserved for the Phase-7 `world_id` world-chaining schema).
- Field-move player-facing ACTIVATION gap (Phase 4 follow-up; spec: [product-specs/field-moves.md](product-specs/field-moves.md)) — two deviations from the original's activation model, surfaced honestly in `CONTROLS.md` ("Activation reality") rather than silently: (1) FLY has no way-stone SELECTION MENU — FIELD: Fly warps to the LAST-REGISTERED stone (`field_move_runtime.way_stone_tiles` is registration-step-ordered, so "last" is chronological, NOT a coordinate max); a selection list over visited stones is the faithful behavior and is an additive party-screen / `field_move_actions` UI change. (2) The auto-typed moves (Flash/Teleport/Repel/Power/Attack/Charm) have working runtime callers but NO player-facing entry: the party-screen FIELD menu lists species-flag `1` moves only (`party_screen._eligible_field_moves`), and for the playtest party those moves are all type-based, so they never render — the scenarios/smoke harness drive them via the runtime seams. Surfacing them (a FIELD menu that also lists auto-typed-capable moves, or an overworld `Z` route for Power/Teleport/Repel) is additive UI work deferred until it can land without perturbing the green scenarios; until then `CONTROLS.md` states the reachable set exactly.
- 142 of 299 catalog moves have no source animation set and use the synthesized lunge/flash fallback (157 of 299 play a per-move source set — recomputed via the `attack_anims` resolver over the live catalog, not hardcoded); extend coverage or enrich the fallback.
- Forced moves (rampage/encore) spend PP on the chosen slot rather than the forced move; the domain exposes `forced_move` for a future runtime fix.
- One unreproduced anomaly: a single `visual_sweep` battle shot showed an enemy level of 0 while every code path clamps levels to >= 1 and the encounter trace agreed with the HUD in 3 subsequent runs. Keep an eye on battle HUD level text.
- Building Phase 1 deferred behavior (spec: [product-specs/building-and-placement.md](product-specs/building-and-placement.md)): full PER-BIOME SPRITE completeness REMAINS deferred (default `house1_*` + Desert stone/ruins ship; snow/savanna/spooky variants from the `assets/source/tiles/buildings/` families are partial). STORAGE-BOX INTERACTIVITY is NO LONGER deferred: it landed with Phase 3 (spec: [product-specs/storage-and-party.md](product-specs/storage-and-party.md)) — each box is an INDEPENDENT per-placement container (contents ride an additive field on the placement entry; save schema v4) with deposit/withdraw, a confirm-gated release, and a non-empty-box Cut refusal. BED INTERACTIVITY is NO LONGER deferred: it landed with Phase 2 (bed = full heal + status cure + revive via interact-rest; the campfire placeholder is filled too — light source + crafting station + runtime `lit` toggle). The "indoors" enclosure FLOOD-FILL stays DEFERRED — and storage deliberately does NOT need it: faithful boxes live outside/right next to an entrance as manual walk-to containers (no indoor gating involved), so the kiln remains the flood-fill's ONLY consumer (itself deferred — no kiln structure in any ported phase), and the sleeping-bag indoor heal bonus waits on it (below). DEMOLITION landed with Phase 1 — Cut refunds the full `cost_for` (hard-stone shells need Smash), wired into the harvest Z-action flow, removing the placement entry only, plus the would-trap guard.
- Input latch MessageBox quirk (latent, documented in-code): `MessageBox.show_message` while a confirm is pending emits `cancelled` (to drop the owner's awaiting state), which sets `input_router`'s generalized latch; if a toast ever fired during a pending confirm in `_process` (after `poll_context_action` reset the latch), the NEXT frame's first `Z`/`Enter` press would be swallowed once. Post-six-fix no `_process` toast can reach a confirm — the avatar is disabled during the RELEASE confirm (`field_action_router` disables it on open), `_menu_open` guards `_on_player_blocked` during the NEW GAME confirm, and the confirm timer is stopped — so the quirk is LATENT (documented at `input_router.gd:78-81` + `message_box.gd:27-28`). A fix must neither suppress the `cancelled` emit (it IS the owner's drop-state signal — suppressing it strands `_awaiting_confirm`) nor restructure the verified generalized latch (set it only from the input-phase answer keys); both risk the six-fix more than the latent one-frame swallow. Deferred unless a future `_process` toast path can interrupt a pending confirm.
- Storage & party Phase 3 deferred behavior (spec: [product-specs/storage-and-party.md](product-specs/storage-and-party.md)): (1) NICKNAME/RENAME — the source lists "store, release, and nickname" (`.firecrawl/buildings-scrape.md:294`); store + release ship, and mons already carry a `name` that survives deposit/withdraw round-trips, so a RENAME entry on the storage screen is purely additive. (2) RELEASE PERMANENCE — the port's release is permanent removal; the original "drops" a mon into the world ("no guarantee a dropped Pokemon will stick around", `.firecrawl/lchan-review.md:45`), which needs overworld-mon entities (Phase 6); the double-emphasized confirm wording ("It will be gone for good.") is the interim compensation. (3) WORLD-GENERATOR REACH — `storage_runtime` writes box contents through `world_gen.get("_placements")[tile]["contents"]` (the documented reach the campfire `"lit"` toggle already uses, because `world_generator.gd` is at 318/320 with no room for a mutator); reads use the public `placements_for_save()` — promote the reach to typed accessors when world_generator next has budget. (4) NO PER-BOX MON CAP is a faithful decision, not debt (mons "stack on top of one-another... until they're moved", `.firecrawl/house-building-scrape.md:261`); if soak ever shows a save-payload problem, add a traced soft WARNING — never a refusal (documented assumption).
- Camping/crafting Phase 2 deferred behavior (spec: [product-specs/camping-crafting-survival.md](product-specs/camping-crafting-survival.md)): (1) KILN + GREAT BALL (and all kiln balls — Ultra/Dusk/Net/… from the scraped Items page) — `great_ball` is defined at station `kiln` (poke_ball + metal_coat, faithful) but UNAVAILABLE; no kiln structure exists in any ported phase (a later building phase adds it, with the indoor enclosure flood-fill it needs). (2) INTERIM INGREDIENT SOURCE — `material_drops.gd` battle-end type drops (ELECTRIC→magnet, WATER→hard_shell, STEEL→metal_coat, BUG→silky_thread, FLYING→soft_feather; 100% on a type-matched victory/capture, pure/no-rng) stood in for the faithful happy-Pokemon HABITAT DROPS (`.firecrawl/wiki-materials.md:463-471/:543/:571`, `wiki-habitat.md`); Phase 5 LANDED the faithful system as the PRIMARY source (`habitat_drops.gd`/`habitat_runtime.gd`) and the interim is KEPT as the documented SECONDARY source (header flipped; never deleted) — both tables preserve the log/hard_stone exclusion the witness invariant below rides on (the interim via `is_drop_material`, the habitat table via `HabitatDrops.witness_clean()`). (3) SLEEPING-BAG indoor-effectiveness bonus + the original's "certain terrain / near aggressive Pokemon" use restrictions (`fresh-sleepingbag.md:145`) — deferred with the enclosure flood-fill (indoor bonus) and the Phase-6 aggressive-chase system (restrictions); the bag is flat 50% with no status cure (the faithful cure split: bed/berries/faint-then-bag). (4) OVERWORLD GHOST/SHADOW SPRITES — the night hazard is a forced battle only (`ghost_sheet1.png`/`shadow1.png` exist for a later overworld-sprite phase); the original's Damp/Mystic/Cursed-Soil DWELL trigger (`wiki-overworld-encounters.md:274`) is replaced by night-without-light (tiles the port does not generate; the dev-attested framing is documented). (5) ROD USABILITY — RESOLVED by Phase 5 (rods now fish from shore; a bag rod-USE entry is still not wired — the overworld Z is the only trigger); crafted BALL TIERS are bag inventory only until the Phase-8 ball tiers (`battle_runtime` BALL_ID stays `poke_ball`). (6) FAILOPEN EVOLUTION CONTEXT — `pokemon_rules._time_requirement_met` (:247) still passes on EMPTY context (flipping to fail-closed costs 1 line the 320/320 file does not have and changes soak expectations); GUARANTEE: the battle caller passes real `time_of_day` and future callers of `check_level_evolution` MUST too (code comment at :247). OUT entirely (documented in the spec): repel crafting (conflicts with the Phase-4 field-move model), apricorn balls, the Dusk Ball night-catch bonus, the Flash field-move ACTION (Phase 4 — the passive Fire-type light READ landed), torch fence/interior-wall mounting (Phase 3 interiors).
- Building region-enclosure escape is a LOAD-BEARING INVARIANT, not a coded guarantee (spec: [product-specs/building-and-placement.md](product-specs/building-and-placement.md)): the would-trap guard (`build_runtime._would_trap_player`) deliberately ALLOWS a region-scale seal (a closed wall ring, a sealed island chokepoint) and refuses only self-enclosure on the player's own tile. Escape from a permitted seal rests on the party always being demolition-capable, which holds ONLY while `build_runtime.unwitnessed_demolish_moves()` stays empty — every shell class has a cost material solely its demolish move yields (log→cut, hard_stone→smash). Phase 2 added a SECOND material source (the interim battle-end type drops) and a starting-bag sleeping_bag; the witness STILL HOLDS because `material_drops.TYPE_MATERIALS` NEVER contains `log` or `hard_stone` (the witnesses stay harvest-only) and the sleeping bag is not a shell material — mechanized by `unwitnessed_demolish_moves` + `placement_flow`'s `_check_demolish_invariant`, which now asserts the drop table's exclusion on every run. Phase 3 shipped the party-release/box mechanic WITH the guard's DYNAMIC EXTENSION: `storage_runtime` refuses deposits and party releases that would strand the party's last witnessed demolition move while structures stand (`storage_refused{reason: would_strand_demolition}` — the PARTY pool counts, box contents NEVER, since a box may be sealed away behind the player's own wall ring; `storage_flow` asserts the dynamic half on every run), so the escape guarantee now holds while mons DO leave the party. The REMAINING obligation: if ANY future material source (a shop/gift) adds log or hard_stone, extend the trap guard to forbid region-scale enclosure (or re-prove escape) BEFORE shipping that change — a non-empty witness result would turn a permitted seal into a permanent self-trap. PHASE 5 RECONCILIATION (landed): the faithful habitat drops resolved the collision the Rock-type row posed — ROCK drops are WITHHELD (`HabitatDrops.TYPE_MATERIALS["ROCK"] == []`; dual Rock/Ground mons keep Soft Sand) and the guarantee is MECHANIZED over the habitat table (`HabitatDrops.witness_clean()` + `is_habitat_drop_material`, asserted every `habitat_drops` run alongside `craft_flow`'s interim-table audit). Restoring the faithful Rock→hard_stone row requires extending the trap guard first (the escape route above); the `breeding-shinies-drops-fishing.md` spec § Witness invariant records the decision.
- Line budgets AT CEILING (flagged for the next edit): `scripts/app/main.gd` (still 220/220 after Phase 3 — storage used FOUR same-line edits: the `bind_ui_consumers` line in `_ready`, the `_field_router.setup` argument gaining `$UI/StorageScreen`, the `$UI/StorageScreen.visible` check in the `overworld_idle` poll expression, and the `$UI/StorageScreen.visible` guard in `_toggle_menu`), `scripts/app/placement_flow_scenario.gd` (220/220), `scripts/domain/pokemon_rules.gd` (320/320 — untouched by Phases 2-3 by design), and `scripts/runtime/game_runtime.gd` (Phase 3 freed ~23 lines by extracting `harvest_tile` + its tile-payload helper into the NEW `scripts/runtime/harvest_runtime.gd` FIRST — one-line forwarder, all existing callers unchanged — then spent them on the storage wiring, ~303/320) have zero-or-near-zero headroom; `scripts/app/phase0_scenarios.gd` (was 220/220) extracted `run_wild_battle` + helpers into the NEW `scripts/app/wild_battle_scenario.gd` (dispatch name `wild_battle` stays stable) then spent the headroom on the v4 migration fixtures (~170/220); `scripts/ui/party_screen.gd` extracted its action-list construction into the NEW `scripts/ui/party_actions.gd` then added the MOVE/DEPOSIT surface (~217/220); `scripts/runtime/battle_runtime.gd` stays WITHIN its 320 budget (untouched by Phase 3 — the overflow no-routing decision keeps the 0.1 relocation path exactly as-is); `scripts/domain/world_generator.gd` (318/320) has two lines and `scripts/app/visual_sweep_baselines.gd` (219/220) has one — storage writes box contents via the documented `_placements` reach rather than spending either line. Any follow-up edit to these files must extract first (the building slice extracted `field_action_router.gd`/`placement_flow_demolition.gd`/`world_invariants.gd`; the camping slice extracted `encounter_selection.gd` and moved the campsite accessors + `_move_priority`; the storage slice extracted `harvest_runtime.gd`/`wild_battle_scenario.gd`/`party_actions.gd`). The Phase 0 static-gate cleanup (2026-08-09, see Resolved) leaves `scripts/domain/battle_rules.gd` AT its 320 ceiling exactly (the effect tables + `OHKO_EFFECT` moved to `battle_effect_tables.gd`) — same extract-first rule on its next edit.
- Replace remaining dictionary-heavy runtime contracts with more explicit typed boundaries once the gameplay slice stabilizes.
- Add subsystem entries and specs immediately when new systems such as NPCs or overworld-mon entities are introduced (PC storage got its `storage` subsystem entry + `docs/product-specs/storage-and-party.md` spec with Phase 3; building got `building_loop` + its spec with Phase 1).
- Slice 6 DoD #7 (legibility/vision plan): the four optional verification spikes stay DEFERRED, explicitly off the critical path — Movie Writer PNG-burst motion lane, DAP `godot/put_msg` bridge, ScriptBacktrace (#91006) structured error frames, and the live NDJSON-over-TCP introspection endpoint ("built ONLY IF in-process sidecars prove insufficient" — they have not). Status recorded in docs/superpowers/plans/2026-07-20-agent-legibility-and-vision-verification.md; adopt each behind its own registry entry only if ever needed; none gates CI. (The box-mode `battle_message` judgment was documented at flip time, so no deferral there.)
- Title/creation visual baselines (new-game-flow slice; FLAGGED scope, NOT debt): the splash/title + creation screens ship with trace-only assertions (the `new_game_flow` scenario, green headless AND windowed); NO sweep baselines exist yet — a future sweep slice can add title shots (next free numbers 37+; 17/33/35/36 stay the whitelisted retired holes, `scripts/app/visual_sweep_baselines.gd:36`). HOST/JOIN multiplayer likewise stays NOT ported — flagged scope (`strings.properties:13`), not debt.
- Legendary-dungeon deferred pieces (legendary-dungeon slice, 2026-08-10; spec: [product-specs/world-depth.md](product-specs/world-depth.md) § Legendaries): (1) DUNGEON ENCOUNTER TABLES are FLAGGED curations (`dungeon_layouts.gd` — one- or two-species curated tables + a level band per dungeon over bare TYPE sentinels, the `{VOLCARONA}` `RUINS_INNER_CURATED` precedent), untuned by real playtesters; catalog presence/battle viability/nonempty membership are mechanically gated, while the 38-58 bands are pinned off the entrance ring-≥60 progression floor, not any source. (2) `REGI_RESTAND_STEPS := 2880` (the tablet-Regi KO re-stand cooldown, ~2 in-game days off the 1-step = 1-minute clock) is pinned TUNABLE — the valve's duration awaits real playtesting. The dungeon entrance/interior states are now covered by the windowed 44/45 baselines. (3) The chunk-hash dungeon-SITE placement leg of the world-gen audit fix path is SUPERSEDED (not debt): the entrance model stamps the seven hand-authored dungeons at the frozen legendary anchors, so no procedural site placement is owed; the `spawn_disc_intrusion` + §19(c) reachability advisories stay open as LANDMARK-footprint gates ([product-specs/bootstrap-and-overworld.md](product-specs/bootstrap-and-overworld.md) § Comprehensive world-gen audit).

## Resolved this cycle

- Building build-mode ENTRY OVER-FIRE (closed 2026-08-13; spec: [product-specs/building-and-placement.md](product-specs/building-and-placement.md)): entering build is now overworld `C` (`build_toggle`), a disjoint key from place-confirm `Z` (`action_a`), so `structure_layer._process` cannot see `action_a` just-pressed on the enter frame. The old walkable+Build `Z` fallthrough is deleted from `field_action_router.on_context_action` (Dig stays the ordinary harvest path, including the dig-silence QoL). `dig_silence` parts D/E/F drive the player-facing path: `Z` on a diggable+placeable tile Digs and never opens build; `C` opens the overlay with no `structure_placed` on the enter frame; overlay `Z` places a wall. `SCENARIO_REQUIREMENTS` pins `build_mode_entered`.

- `verify_all.py` parallel-checkout routing and S8.5 tool-error reporting (2026-08-11): the local gate accepts an explicit DAP host/port for its windowed `run_playtests` lanes, repeats that selected endpoint in the R5 recovery directive, and maps `commandcode_play_agent` exit 2 to the valid `tool_error` status instead of crashing the report printer with `KeyError("error")`.

- Agent-neutral Godot integration (2026-08-09, the agent-neutral-integration plan Phases 1-4; the deep-research findings folded into repo idioms — protocols, not vendors): (1) AGENT SURFACE CONTRACT — `docs/registry/agent-surface.toml` (the machine-readable "how to drive this repo": DAP 6006 / LSP 6005 protocols, scenario CLI, trace stream, visual baselines, determinism conventions, golden save, preflight, error-as-directive) + the prose companion `docs/references/agent-integration.md` (linked from AGENTS.md's Read First), mechanically gated by the new `agent_surface_issues` fold into `check_repo_contracts.run()` (sections/ports/referenced-paths; progressive arming when absent, refuse-on-unreadable when corrupt) and comment-owned by the `app_bootstrap` registry entry (the art-anchors.toml precedent — docs artifacts never ride code_paths). (2) RUNTIME LEGIBILITY — the `ui_tree_dump` scenario (`scripts/app/ui_tree_dump_scenario.gd`, the accessibility-snapshot analog: title/menu/party/bag/battle → per-screen Control-tree JSON in `.godot-smoke/ui_tree/`, the symmetric `ui_tree_dump_passed`/`ui_tree_dump_failed` pair on PLAYTEST_SCENARIOS + SCENARIO_REQUIREMENTS only) and the three custom Performance monitors (`game/party_size`/`game/world_seed`/`game/current_screen` in the new `scripts/runtime/performance_monitors.gd` + the `performance_monitor_values.gd` child node whose `_exit_tree` re-arms the callables — both lambda forms segfaulted at engine teardown on 4.6.1; `game/current_screen` mirrors the dump's screen ids so the surfaces agree by construction); the three seed_convention_advisories are CLOSED (battle_anim/smoke_biome/wild_battle self-pin `seed_for_smoke` BEFORE pass-gating draws — PINs 2026080901/02/03 — the new_game_flow precedent, none made a double-run consumer) and the miss-002 ledger re-stamped. (3) EDITOR PLUGIN — the opt-in `addons/agent_trace/` plugin (disabled by default, `project.godot` untouched; debugger tab + bottom-panel activity log over the additive `EngineDebugger.send_message("agent_trace:event", [line])` hook in `trace_logger.gd`, a strict no-op without a debugger attached), registered on the `session_runtime` entry; visualizes the repo's own JSONL contract, zero vendor coupling, gates nothing. (4) ACCESSKIT ANNOTATIONS — the GBC widget/stage library (`gbc_widgets.gd`/`gbc_stage.gd`, title_flow entry) sets `accessibility_name`/`accessibility_description`/`accessibility_live` on every widget it builds — PROPERTY-ONLY (the engine's per-class update handlers rewrite the low-level RID element, so handwritten role/state overrides would race it), metadata-only (no geometry/visual/input change; the committed baselines are unaffected by construction), one contract serving the player-facing screen-reader tree AND the agent-facing ui_tree dump; contract + experimental status in `docs/references/accessibility.md`.
- Pre-existing static-gate reds from f98a7cab (QoL polish, 2026-08-09; surfaced by the source-asset-vendoring gate run, verified pre-existing by running both gates on a f98a7cab worktree; CLEARED same-day by the agent-neutral-integration plan's Phase 0): (1) the four blown `check_architecture` line budgets fixed by EXTRACTION ONLY, the house extract-before-edit pattern — `scripts/domain/battle_rules.gd` (326>320) moved the pure-data effect tables (`HIT_STATUS_EFFECTS`/`PURE_STATUS_EFFECTS`/`MULTI_HIT_EFFECTS`/`SELF_TARGET_EFFECTS`, plus the `OHKO_EFFECT` id constant of the same pure-data family — the four named tables alone left 321) into the NEW `scripts/domain/battle_effect_tables.gd` (back to exactly 320/320); `scripts/ui/party_rows.gd` (262>220) moved the style constants + the threshold-colored HP-bar builder into the NEW `scripts/ui/party_row_style.gd` (209/220); `scripts/app/field_action_router.gd` (227>220) moved the camp/storage placed-object routes (`_route_camp_object`/`_open_camp_menu`/`_open_storage_screen` + their exclusive `_rest_at_bed`/`_toggle_campfire` helpers and the CAMPFIRE/BED/BOX ids) into the NEW `scripts/app/field_object_routes.gd` (153/220), the router keeping one `route_camp_object` delegation at the same precedence tier; `scripts/ui/creation_screen.gd` (226>220) moved the step-label accessor cluster (the `step_title_label`/`step_value_label`/`seed_edit_active` scenario seams + the `_committed_name`/`_seed_line` value accessors) into the EXISTING `scripts/ui/creation_screen_render.gd` sibling (218/220), with the three app readers (`new_game_flow_checks`/`visual_sweep_title`/`play_agent_scenario`) retargeted to the `CreationRender` statics with zero assertion changes. (2) `check_repo_contracts` registry coverage closed: `scripts/app/dig_silence_scenario.gd` (f98a7cab's dig-silence QoL guard scenario) and the three new modules registered in `docs/registry/subsystems.toml` (`building_loop`/`battle_loop`/`menu_ui`; `creation_screen_render.gd` was already title_flow-listed). Behavior byte-identical by construction (pure moves; the campfire `campfire_lit` trace deliberately keeps its `"App.FieldActionRouter"` source string). All four static gates green; the three RNG-pinning advisories stay advisory-only (the plan's Phase 2 owns them).
- User-adjustable shiny odds (new-game-flow slice, 2026-08-04; closes the Phase 5 DEFERRED piece (1) above; spec: [product-specs/infinite-world.md](product-specs/infinite-world.md) § Flagged divergences item 9): the creation screen's SHINY RATE step landed the FAQ's promised knob ("1 in 256. This may be adjustable by the user in a subsiquent update", `.firecrawl/fresh-faq.md:150`) — ←/→ cycles `SessionState.SHINY_ODDS_CHOICES := [32, 64, 128, 256, 512, 1024]`, a FLAGGED invention ladder around the faithful 1/256 (the sources anchor ONLY 1/256; 256 carries the `(DEFAULT)` marker), and `game_runtime.begin_created_game` applies the choice as `PokemonRules.shiny_odds =` BEFORE the pinned `new_game` draw order (the existing `Breeding.set_shiny_odds` hook lineage's effect, one line; the choice consumes ZERO rng — the starter shiny draw stays one `randi_range` at stream index 0, range argument only), with the load path re-applying the saved odds in `_apply_loaded_payload`. The choice persists as the additive v6 `shiny_rate` save key (write-only-when-non-default, NO schema bump — the `encounter_settings` precedent). Covered by the `shiny_odds` scenario's hook window + the `new_game_flow` scenario's `shiny_rolled{origin: "starter", odds: 64}` witness (the knob applied before the pinned roll); the pause-menu path never rides the creation seam.
- Evolution-stone bag-use seam (Phase 5, exec plan pokewilds-feature-completion.md :141-156 exit criterion; spec: [product-specs/breeding-shinies-drops-fishing.md](product-specs/breeding-shinies-drops-fishing.md) § Evolution stones): the previously-uncalled `check_item_evolution` (`pokemon_rules.gd:176-183`, unchanged) gained its live caller — the NEW `scripts/runtime/stone_evolution_runtime.gd` (validate → consume → swap → trace — a refused bag decrement is the distinct `no_item` refusal and applies nothing; `is_shiny` + level/exp/moves/happiness ride the `duplicate(true)` swap while stats/max_hp/current_hp/catch_rate RECOMPUTE off the target entry — the exact level-evolution path, `battle_runtime._try_evolve`: `build_stats` at the mon's level, `max_hp` from the new stats, `current_hp` scaled by the pre-swap ratio; name/types/front/back paths re-resolve to the target entry) behind the one-line `game_runtime.use_stone_on_mon` delegation (the exact `breed_flow_checks.STONE_SEAM_METHODS[2]` name, so `run_stone_case` auto-arms via `runtime.has_method`), with a bag-screen stone branch reusing the existing party picker (Potion pattern; confirms through `/root/GameRuntime`, the sleeping-bag convention; closes on success, stays open on no-effect). `evolution_stone_used {item_id, species_id, evolved}` (source `GameRuntime`, registry-required on `pokemon_progression`) now emits on success ONLY; no-effect uses — an egg, the wrong stone, a line with no ITEM-method evolution, an already-final species — show "It would have no effect." and consume nothing (the Potion precedent), an out-of-range index is the distinct `no_such_mon` refusal, and a missing target entry is a warning-tier `bad_target` that also consumes nothing. All ten stone ids resolve from `item.properties:80-89` (thunderstone has NO underscore — the brief's `thunder_stone` spelling would silently no-effect every Electric-stone evolution; there is NO `RUNTIME_ITEM_SUPPLEMENTS` stone entry), and the explicit `STONE_ITEM_IDS` set gate is load-bearing in BOTH the module and `bag_screen` (a suffix predicate would misroute CRACKEDPOT/SWEET_APPLE/TART_APPLE ITEM evolutions and the `hard_stone` building material). No rng is threaded, so seeded scenario streams stay byte-stable; `breed_flow` double-ran byte-identical with `stone_ok: true` and the shiny_odds pin held. ACQUISITION is RESOLVED 2026-07-26 (this cycle — see the stone-acquisition entry directly below): the faithful Beach-Dig pool + flagged port additions (the other-biome Dig pools + the Steel-type shiny_stone cadence), all pure step-counter draws that consume NO shared `_rng`, so NO build pins were re-stamped (seven scenarios re-ran byte-identical).
- Evolution-stone ACQUISITION (Phase 5, exec plan pokewilds-feature-completion.md :149 "items from Dig drops and Steel-type drops"; spec: [product-specs/breeding-shinies-drops-fishing.md](product-specs/breeding-shinies-drops-fishing.md) § Evolution stones; Open item 11 RESOLVED): the bag-USE seam's deferred world-acquisition half landed, split honestly between the documented original and flagged port additions. (1) FAITHFUL — Beach Dig: `harvest_runtime.harvest_tile`'s dig branch rolls a bonus find parallel to the base dry sand via `harvest_resolver.bonus_for` (pure over total_steps+tile); the SAND pool is the `fresh-beach.md` "## Dig Items" set `{big_pearl, water_stone, clear_glass, revive}` in exact wiki order — NOT flagged. (2) DIVERGENCE — other-biome Dig pools in `harvest_resolver.DIG_BONUS_POOLS` (each per-key flagged; the scrapes cite NO non-Beach stone source): GRASSLAND `[leaf_stone]`, FOREST `[leaf_stone, moon_stone]`, SAVANNA `[fire_stone, thunderstone]`, DESERT `[sun_stone]`, SWAMP `[dusk_stone]`; PLAINS deliberately absent (dry_soil-only, keeps `harvest_flow` byte-identical). (3) DIVERGENCE — the rate `DIG_BONUS_RARITY := 8` (invented even for the faithful Beach pool; wiki documents membership, no rate). (4) DIVERGENCE — Steel-type habitat drop: `habitat_drops.STEEL_STONE_DROP := "shiny_stone"` / `STEEL_STONE_CADENCE := 4`, a second cadence gate in `habitat_runtime._day_tick` (NEVER `drops_for`'s daily path; `TYPE_MATERIALS["STEEL"]` stays exactly `[metal_coat]` — `wiki-materials.md:393` documents Metal Coat ONLY; the exec plan's "Steel-type drops" :149 is uncited intent). LOAD-BEARING DETERMINISM: NO acquisition draw consumes the shared `_rng` (the wild-encounter stream) — `harvest_runtime.setup` / `habitat_runtime.setup` signatures are UNCHANGED, the `game_runtime.gd:59` no-rng seam holds; `_dig_draw` is integer SplitMix (explicit sub-`INT64_MAX` constants + 63-bit masks, no engine hash), `steel_stone_due` is pure over (types, day, last_stone_day). The per-mon `last_stone_day` rides the additive habitat sub-dict (`-CADENCE` at deposit ⇒ first drop day stone-due; loaded mons self-heal via `.get` default — no sanitizer, no save-schema bump). Witness-safe by construction (shiny_stone is neither log nor hard_stone; `witness_clean()` stays true; `is_habitat_drop_material` walks the stone). Traces: the dig bonus emits `dig_item_found {tile [x,y], item_id, biome}` (source `GameRuntime`, parallel to the unchanged `field_move_used`); the steel drop REUSES `item_dropped` (same `HabitatRuntime` source + `{species_id, item_id, tile [x,y], day}` keys — no new habitat event). Proven end-to-end by `field_moves_flow`'s new dig group (`field_moves_dig_checks.gd`: contract witness → Beach water_stone hunt via resolver prediction → first-found divergent biome pinned to the SHIPPED table → PLAINS negative control; `dig_acquisition_ok` in `field_moves_passed`) + `habitat_drops`' `run_steel_drop_control` (a crafted MAGNEMITE — ELECTRIC/STEEL both basic-recessive, same grass-free pen; day-20 magnet+metal_coat+shiny_stone, days 21-23 suppressed, day 24 re-armed; `steel_ok` in `habitat_drops_passed`). Seven scenarios (field_moves_flow, habitat_drops, breed_flow, shiny_odds, build_house_flow, harvest_flow, playtest_breed_soak) double-ran byte-identical with the shiny_odds 6/1024 pin holding — zero shared-stream drift. RESIDUAL (still open, by design): ice_stone + dawn_stone have NO world source (SNOW is not diggable) and stay scenario-grant-only.
- Storage boxes & party management (Phase 3, feature-completion plan feature #10): the Phase-1 storage-box placeholder became an interactive Pokemon container, and NO Pokemon is ever lost to a full party. Each craftable Storage Box (2 Log, placed like any structure) is an INDEPENDENT, NON-SHARED container — box identity IS its placement tile and there is no global box store, so two boxes can never share by construction (faithful: "the contents of the box are not shared with other built boxes", `.firecrawl/buildings-scrape.md:294`). Contents live as an additive array ON the placement entry under SAVE SCHEMA V4 (purely additive — the bump the building spec pre-pinned; absent `contents` backfills to `[]`; `_normalize_structures` sanitizes corrupt contents to an EMPTY BOX, never a crash or a torn entry; `WorldOverrides.is_valid_placement` checks only kind + structure_id, so the round-trip needed ZERO validator/generator plumbing changes, the `"lit"` precedent). A v3 build refuses a v4 save non-destructively to `.newer.bak` rather than loading box semantics it has no UI for. `scripts/runtime/storage_runtime.gd` (RefCounted) drives open (`box_opened`), deposit (`mon_deposited`), withdraw (`mon_withdrawn`), and release (`mon_released`) through reason-carrying `storage_refused` refusals (`no_box`/`no_such_mon`/`last_party_member`/`party_full`/`would_strand_demolition` — never silent); the storage screen (`scripts/ui/storage_screen.gd` + `scenes/ui/StorageScreen.tscn`, ≤220/≤250, ui-references-only, self-wired via `/root/GameRuntime`) is WIRED through the campfire-menu Z-route pattern in `field_action_router.gd` — the `BOX_ID` arm in `_route_camp_object` disables the avatar + opens the screen, the `closed` signal re-enables + saves, `_overlay_open` covers BOTH overlays, and the screen's `closed` feeds `input_router`'s generalized latch via `main.gd`'s `bind_ui_consumers` (the `set_battle` poll-gate workaround is gone — `storage_flow` drives the box loop through the real Z seam) — and release is CONFIRM-GATED through the MessageBox sibling (the 0.2 destructive-action contract), with the release-confirm mouse-bypass gated at `storage_screen.gd`'s `_on_entry_clicked` (a click on the still-visible action list mid-confirm cannot mutate the box). Demolition can NEVER destroy contents: a non-empty box refuses Cut (`demolish_refused{box_not_empty}` — faithful to `.firecrawl/pokewilds-build.md:38`'s filled-chest rule reduced to the single-floor port), and an empty box Cuts to the full 2 Log refund with the entry + contents removed atomically. Party management: ARBITRARY REORDER (MOVE replaces swap-lead-only; `session_state.move_party_member`/`set_party_order`, `set_party_lead` delegates) + DEPOSIT to the nearest adjacent box (`box_tile_near` deterministic scan — the "right next to an entrance" rule, `.firecrawl/house-building-scrape.md:261`). Two sourced POLICY DECISIONS recorded in the spec: overflow captures stay on the CAMPSITE HOLD (faithful — mons are confined to the last slept place, `.firecrawl/pokewilds-getting-started.md:76`; the always-exists no-loss anchor beats nearest-box routing; `battle_runtime.gd` untouched) — the retrieve→deposit loop is the player's path from hold to box; and NO per-box mon cap (faithful stacking). The building spec's load-bearing demolition-witness invariant was extended DYNAMICALLY (deposit/party-release refuse to strand the party's last witnessed Cut/Smash carrier while structures stand — the party pool counts, box contents never; the static `unwitnessed_demolish_moves` audit unchanged). Budget-full files touched only by extraction + same-line edit (`harvest_runtime.gd`, `wild_battle_scenario.gd`, `party_actions.gd` extracted first; main.gd four same-line edits — the `bind_ui_consumers` line, the `_field_router.setup` argument, the `overworld_idle` StorageScreen check, and the `_toggle_menu` StorageScreen guard; start_menu dict-only; world_generator untouched via the documented `_placements` reach). Covered by the `storage_flow` scenario (box round-trip, both release branches, every refusal, the non-empty Cut refusal, box independence in the world AND across save, save round-trip + corrupt-contents normalization, the overflow no-routing PROOF, the dynamic witness guard, and the reorder), `save_migration`'s v4 fixture, the deterministic `18_storage_box` visual baseline (`visual_sweep_storage`, numbered after 17 per the sidecar canary contract), and a `storage` `ui_render_audit` state — all under verify_all. Spec: `docs/product-specs/storage-and-party.md`; the deferred behavior (nickname, release permanence pending overworld-mon entities, the world_generator reach promotion) is logged in Open items.
- Camping, crafting & night survival (Phase 2, feature-completion plan): the Phase-1 placement system's two placeholders became gameplay and the night became dangerous. The campfire is now a LIGHT SOURCE + CRAFTING STATION (lit by default, no fuel mechanic — faithful; an additive `"lit": false` placement-entry field carries the extinguished state with ZERO validator changes since `is_valid_placement` checks only kind + structure_id) and the bed is a FULL HEAL + STATUS CURE + revive via interact-rest; the TORCH joined `structures.IDS` appended last (faithful 1 Log + 1 Grass → log + dry_soil; always lit; fence/wall mounting documented as a deviation). Campfire crafting landed with a faithful recipe table (`recipes.gd` — Poke Ball, Soft Bedding, Old/Good/Super Rod scraped from the wiki Campfire/Items pages with line citations; Great Ball DEFINED at the kiln station but UNAVAILABLE — the exec-plan parenthetical that conflated it with the Poke Ball recipe was corrected in the spec, not flattened), a consume/grant `crafting_runtime.gd` with reason-carrying refusals (`no_station`/`wrong_station`/`missing`/`no_recipe`), and a camp menu UI (Craft / Extinguish-or-Light / Demolish — demolition stays reachable so the region-seal escape holds). Ingredients come from a minimal deterministic INTERIM (`material_drops.gd` battle-end type drops mapped 1:1 from the wiki Materials drop table; pure/no-rng; witness-safe — never log/hard_stone, asserted by craft_flow every run via crafting_runtime.drop_witness_clean) that the Phase-5 faithful habitat drops flipped to the documented SECONDARY (kept, never deleted). The sleeping bag is a starting-bag reusable (faithful): 50% HP + fainted revive, NO status cure (the spec's open "or partial" question resolved to the faithful bed-only cure), with the indoor bonus + terrain/aggressive restrictions deferred. Resting advances the clock (night → next 07:00, day → +4h) and ESTABLISHES the campsite anchor the overflow-mon hold reads; blackout now returns to the CAMPSITE instead of the world origin (same-line edit; the defeat toast is the only main.gd change). Night danger: `day_phase.gd` is the single day/night source of truth mirroring the tint keyframes (night_cycle asserts the 269/270/1229/1230 boundaries agree); a light model (lit campfire/torch within Manhattan 4, or a Flash-capable Fire-type party member — a capability READ per the dev interview, not the Phase-4 Flash action); ghost encounters at half the light-less night cadence on the shared `_rng` (sorted type-derived pool, deterministic under `seed_for_smoke`; the original's soil-dwell trigger replaced by night-without-light — documented); shadow battles block retreat for their duration via an injected callable (the layering holds — battle_runtime never imports night_system). Nocturnal spawns: `filter_species_ids` gained an additive `time_of_day` parameter (UMBREON night-only in Savanna; GHOST type admitted to every night pool). Time evolutions fixed CALLER-SIDE only — `battle_runtime._try_evolve` passes `{"time_of_day": …}` into the existing context parameter (`pokemon_rules.gd` untouched at 320/320; the `_move_priority` one-liner moved to `battle_rules.gd` as `BattleRules.move_priority` to keep battle_runtime within 320), so Espeon/Umbreon/Frosmoth gates work with an `evolution_time_gate` trace in both directions; the empty-context fail-open stays with a documented guarantee. The static-gradient glow composes inside `structure_layer.gd` (the design's documented fallback path — night-only, no per-frame animation, byte-stable baselines; `world_view.gd` untouched). All Phase-2 state is v3-additive (no version bump, no new top-level key, no recipe discovery state). Budget-full files touched only by extraction/same-line edit (game_runtime keeps its 320 budget by freeing the campsite accessors + encounter helpers into `camping_runtime.gd`/`encounter_selection.gd` before the new wiring; battle_runtime stays within 320 via the `_move_priority`→`BattleRules.move_priority` swap; main.gd one same-line setup argument, toast wording unchanged; pokemon_rules zero). Covered by four new scenarios (`camp_survival`/`craft_flow`/`night_cycle`/`time_evolution`) + `visual_sweep_camping` baselines (shots 15-17; 09-12 stay battle-reserved by the sidecar canary contract) + the full doc payments, under verify_all. Spec: `docs/product-specs/camping-crafting-survival.md`; the deferred behavior (kiln/Great Ball, habitat drops, bag indoor bonus + restrictions, overworld ghost sprites, rod usability, crafted-ball use, fail-open guarantee) is logged in Open items.
- Building placement (Phase 1, feature-completion plan): the harvest economy's FIRST CONSUMER landed on top of the world-mutation layer, closing the "building gets its own spec next" item. Pure-domain structure definitions + faithful per-biome costs in `scripts/domain/structures.gd` (the original Buildings-page numbers, with the documented Grass→dry_soil secondary mapping and the Desert hard_stone shell swap); Fighting-type Build capability reused from `field_moves.gd` (no change needed — the starter IS the faithful level-5 MACHOP since the new-game-flow slice, Build-capable by the same auto-type rule, `.firecrawl/wiki-getting-started.md:60`; the scenario swaps stay harmless); a runtime placement orchestrator (`scripts/runtime/build_runtime.gd`) that consumes bag materials through the session item API and emits four traces with reason-carrying refusals; a ghost-preview/build-mode/occupancy layer (`scripts/runtime/structure_layer.gd`) that COMPOSES `world_view.gd` without extending it; and overworld-context + party-screen `FIELD MOVE` entry points extracted into `scripts/app/field_action_router.gd` (the party-screen move id is now consumed, closing the Phase-0 decorative-FIELD-MOVE flag). Placements ride the sparse override map as a "placed" entry kind and persist under a NEW ADDITIVE v3 `structures` save key (version stays 3; clears key untouched), rendered in place via a merged `mutations_for_view()` mirror so occupancy agrees across logic/render/collision by construction. A walled house with a door is buildable from harvested materials (the exit object); covered by `placement_flow`, the extended `world_consistency_audit`/`save_migration`, and the deterministic `13_built_house.png` visual baseline. Spec: `docs/product-specs/building-and-placement.md`; the deferred Phase 2-3 behavior (bed/box interactivity, indoor flood-fill, demolition, per-biome sprite completeness, the unbuildable bed) is logged in Open items.
- Vision-fidelity systemic fix — the self-referential-check gap that let the 11 stage-px HP-bar misalignment ship green (G1 + G2). Root cause: every automated check class measured the code against its own earlier output or a human transcription (pixel baselines freeze whatever the code renders; the sidecar-consistency reviewer diffs code-output vs code-output; the art-anchored render model only covered what someone hand-modeled, and the bars were never modeled), and the one rubric question that would have caught it ("Are HP bars on their baked tracks?") had no answering agent. Resolved at a SYSTEMIC level per the governing directive (fix the REASON it was missed, never the specific miss), not by an HP-bar test: (G1) a source-art anchor registry (`docs/registry/art-anchors.toml`) whose `stage_rect`s are a pure integer function of the art BYTES re-derived at check time by `tools/art_geometry.py` — the dark-run scan of `battle_screen2.png` reproduces the fixed HP tracks (32,18,48,4)/(96,74,48,4) and excludes the misaligned 43/107, baseline-independent on a fresh checkout — behind a mechanical `[[scenes]]` overlay inventory (any future overlay is caught on day one), a `tools/check_art_anchors.py` RED static gate folded into `check_repo_contracts` (completeness + art-freshness re-derivation + the `art_sha256` pin, since art is a submodule), an `anchor_drift` Lane-4 class grounded on the first art-truth `anchor:<id>` region (quarantine, graduating per-anchor), and an `apply_anchor_gate` baseline-regeneration refusal gate that structurally closes the "wrong baseline frozen green for 3+ days" hole; (G2) the Lane-4 `--reviewer-cmd` socket is populated by `tools/vlm_reviewer.py` (Qwen3-VL 8b, pure-stdlib, quarantine-forever witness, composite degrade so coverage never depends on the model) and a rubric-coverage ledger makes "unanswered" a first-class COUNTED state (`q1-` question ids, `QUESTION_ANSWERERS`, `EXPECTED_QUESTION_COUNTS` backstop, `rubric_coverage` manifest block). A miss-postmortem protocol + tracked ledger (`docs/references/miss-postmortem-protocol.md`, `docs/generated/miss-postmortems.json`) binds every future miss to a class-level mechanism; the HP-bar episode is entry #1. Spec: `docs/product-specs/vision-fidelity.md` § Source-art anchor registry / VLM reviewer lane / Rubric coverage ledger.
- Phase 0 defect & hygiene batch (feature-completion plan 0.1–0.9): the standing data-integrity and repo-hygiene gaps are closed with no new features. Full-party captures are no longer losing — overflow is relocated to a non-losing campsite HOLD (`mon_relocated`) and retrieved from the party screen (`mon_retrieved`); New Game is confirm-gated and closes the menu; save writes are atomic (temp+rename) with traced, non-destructive recovery (`.corrupt.bak` + `save_recovery`) and a non-destructive future-version refusal (`.newer.bak` preserved before the fresh-start autosave); the blackout heal now clears status and `sleep_turns`; `world_view._tile_cache` is window-evicted each `sync_visible` (bounded to the visible window); the exit-time ObjectDB/resource leak (OGG playing at process exit) is fixed at the source with a headless audio gate; the action_b/run `KEY_X` double-bind is documented as a deliberate mutually-exclusive-context shared key; and the doc-drift batch (113→114 items, 298→299 moves, capability-model wording, README refresh) is settled. A dedicated `save_migration` fixture now covers v1/v2→v3 migration and future-version refusal, closing the RELIABILITY open risk.
- Deleted the deprecated `is_field_move_unlocked`/`unlock_field_move` shims from `game_runtime.gd` (nothing had called them since the harvest rework; removed on the Phase-0 runtime touch that added the campsite/version-refusal accessors).
- Removed the dead `is_field_move_unlocked` context-callable reader from `scripts/ui/party_screen.gd` (it fell back harmlessly to species-flag rows; cleaned up on the Phase-0 party-screen touch that added the campsite RETRIEVE action).
- One-command local gate (Workstream L.1): `tools/verify_all.py` closes the standing "verify_all.py absent / one-command local gate missing" gap. It ORCHESTRATES (never forks) the four static gates, the determinism pins/canary, the transport-honest headless full suite, the windowed pixel lanes (`ui_render_audit` + `visual_sweep`), and the legibility report in one run-all command, and MECHANIZES the refuse-on-mismatch + freshness refusals that were "human/agent-enforced policy until verify_all" — R1 report `head_sha`==git HEAD, R3 windowed `godot_version`/`renderer`/`window` vs the baseline capture_env + canonical 1152x648 window, R4/R5 windowed entries present with correct transport (`ui_render_audit` not headless), R6 `vision-review.json` freshness via `review_is_fresh`. Four honest exit codes (0 GREEN / 1 STEP_FAILURE / 2 REFUSAL / 3 TOOL_ERROR — a transport skip is never a failure, a stale/mismatched report is a refusal, a missing binary is a tool error); `--skip-windowed` reports the pixel lanes SKIP, never PASS. Documented as the pre-push ritual in RELIABILITY.md § Local gate and AGENTS.md; the renderer-stamped windowed lanes stay local while CI runs the same gate with `--skip-windowed` plus the Godot-free checks.
- Removed the synthetic `SMOKE_MON` fallback: the catalog reliably loads 954 species, and an empty catalog now skips encounters with a `warning` trace instead of fabricating a Pokemon.
- Battle mechanics replaced the simplified damage math with mainline formulas: 18-type effectiveness chart, stat stages, status effects, capture formula with catch rates, growth-curve EXP, and level/item/happiness evolution.
- Encounter tables are species-level, parsed from source `wilds_data.asm` spawn biomes with a source-token alias map; type-based matching remains the fallback.
- Save schema v2 persists the bag, time of day, and step count, with v1 migration on load; the session clock advances per step and drives day/night presentation.
- The start menu grew real party and bag screens with swap-lead, summary, field-move, and item-use flows.
- Player avatar renders from the source walking/running sprite sheets; biome music and the wild-battle theme are wired through the music router.
- Automated playtesting: `playtest_journey` scripts the full player loop with a save round-trip check, and `playtest_soak` runs a seeded bot asserting HP/PP/bag invariants every iteration; `tools/run_playtests.py` executes the suite over DAP or headless and writes a JSON report.
- Visual verification: `visual_sweep` captures 16 screenshots per run. Its first pass caught real defects the trace suite could not: source tile/prop PNGs use white/black backgrounds the original engine color-keyed (now flood-fill keyed at load with caching, composited over per-biome ground colors), sprite-less species could enter battle (now filtered from encounter tables, with an intentional `?` placeholder as backstop), battle message text overflowed into the command box, species names garbled the baked `:L` glyph (level markers now redraw dynamically after measured name width), the moves screen overlapped sprites and HUD plates, the menu dim double-stacked over sub-screens, and bag items showed raw ids (`Poké Ball` now resolves).
- Housekeeping: every smoke scenario now backs up and restores the player's save file (no more test-state clobbering); input routing is extracted into `scripts/app/input_router.gd` so `main.gd` has headroom again; bag item ids are canonical i18n keys (`poke_ball`) with legacy-save migration; the legacy music-router wrappers are deleted.
- Harvest & world mutation: a sparse per-tile override map (save schema v3) makes cut/dig/smash permanent; field moves use party capability (species flags + type auto-ability) instead of stored unlocks; harvesting yields materials (log, soils, stones) through one resolver driven by context-Z and the party screen; the world view mirrors overrides live via a `world_overridden` signal.
- Audit suite: `nav_audit` (traversal agreement, battle/menu reachability, model/cursor consistency), `texture_audit` (all 633 battle sprites + 27 overworld textures through the real loaders), `data_audit` (encounter-pool battle viability, instance integrity, bag/catalog resolution), and `layout_audit` (worst-case label fit + cursor alignment) run headlessly in seconds. `visual_sweep` is now deterministic (fixed seed, crafted party with a strip-sprite canary, fixed battle) and diffs against committed baselines via `tools/visual_diff.py` (0.5% pixel threshold; `visual_sweep_update` accepts intentional changes).
- Model-based oracle lanes (spec: docs/superpowers/specs/2026-07-18-autonomous-playtesting-oracles-design.md): `world_consistency_audit` (tile logic/render/collision three-way plus spatial, z-order, and tall-grass contracts) and `ui_render_audit` (art-anchored expected strings, label overlap, cursor pairs, plus a calibrated pixel lint whose findings are quarantine-tier). The soak asserts per-step spatial invariants. First runs caught and fixed: player drawing over tree canopies (now y-sorted), item-menu two-column text overlap, HUD status/level collision, and wide TYPE values bleeding over the side-box border.
- Agent vision review: `docs/references/vision-review-rubric.md` structures a per-state visual read of every sweep shot; findings land in `.godot-smoke/vision-review.json` (quarantine-tier). First pilot: zero defects, plus one coverage note — no deterministic state yet places the player north of a tall prop, so the canopy contract lacks a pixel canary.
- Battle presentation: attack animations play from the source metadata (157 of 299 moves with sets; synthesized fallback for the other 142), species cries sound at battle start and on faint, and the top ten unhandled move-effect families (confusion, heal, trap, rampage, protect, fury cutter, encore, attract, OHKO, priority) are implemented with soak warnings measurably reduced.
- Battle surface scales to the largest integer factor (fractional scales aliased the pixel font at some window sizes), and KO blows now play their animation before the battle screen closes instead of skipping it.
- The player HP bar now stays visible during move selection (name/level yields to the side window) and during attack animations (the source's healthbar-hide metadata is deliberately unhonored since the port's animation frames carry no replacement HUD).
- First audit pass caught and fixed: 5 non-conformant sprite strips (COTTONEE, MINIOR, WHIMSICOTT, two ROTOM backs) that defeated frame detection; sprite-less-data species with zero catch rates or empty learnsets in encounter pools (EGG, GMRMIME, ROTOM forms, CORSOLA_GALARIAN, SHELLOS_*) — eligibility now requires battle viability; `potion` missing from the source i18n (runtime item supplement added); long alternate-form names overflowing the GSC HUD (short-form fallback with measured metrics); moves-screen cursor/row misalignment; the battle font now matches the baked art (source `fonts.ttf` at 7px); tall grass is real patches that own encounters.
