Status: current
Last verified: 2026-08-01
Review cadence days: 21
Source paths: scenes/app/Main.tscn, scripts/app/main.gd, scripts/app/input_router.gd, scripts/app/input_gate_scenario.gd, scripts/app/input_gate_menu_checks.gd, scripts/app/battle_end_input_scenario.gd, scripts/app/smoke_tap.gd, scripts/app/smoke_scenarios.gd, scripts/app/phase0_scenarios.gd, scripts/app/snapshot_capture.gd, scripts/app/visual_sweep.gd, scripts/app/visual_sweep_baselines.gd, scripts/app/render_introspection.gd, scripts/runtime/world_view.gd, scripts/runtime/player_avatar.gd, scripts/runtime/music_router.gd, scripts/runtime/structure_layer.gd, scripts/domain/world_generator.gd, scripts/domain/world_overrides.gd, scripts/domain/biome_defs.gd, scripts/domain/biome_encounters.gd, scripts/domain/day_phase.gd

# Boot And Overworld

## Supported behavior

- The app boots into `res://scenes/app/Main.tscn`.
- The autoload runtime initializes source data, session state, and save state before the main scene starts normal play.
- The overworld is rebuilt from the saved seed and centered on the saved player tile.
- The player moves on a 16x16 tile grid with continuous hold-to-move stepping and a faster run modifier on `X`, rendered from the source `ben-walking.png` / `ben-running.png` sprite sheets with direction-correct frames. Draw order is y-sorted: the player renders behind tall prop canopies when standing north of them and in front when standing south.
- Grass tiles can trigger wild encounters. Water, trees, cacti, swamp trees, rock cliffs, snow trees, and lava block movement.
- Entering battle clears transient overworld message UI before the battle screen appears, plays the wild species' cry, and switches to the battle theme; when the battle ends, the fainted side's cry sounds (victory or defeat) and the biome track resumes.
- When movement is blocked the player avatar surfaces a biome-specific reason from the world view (for example "A tall tree blocks the way.").
- Each completed step advances the session clock by one minute and the step counter by one; both persist in the save. The world view tints for time of day (night, dawn, dusk) via `set_time_of_day`. Since Phase 2 the day/night phase is GAMEPLAY, not just presentation: `scripts/domain/day_phase.gd` is the single source of truth (night = minutes >= 1230 or < 270; dawn/dusk/day all label `"DAY"`) MIRRORING the world view's `TIME_OF_DAY_KEYFRAMES` (the `night_cycle` scenario asserts the boundary minutes 269/270/1229/1230 agree with the keyframes, so the duplication cannot drift silently), and it drives the night-danger / nocturnal-spawn / time-evolution systems in [camping-crafting-survival.md](camping-crafting-survival.md). At night a static warm glow is composed inside `structure_layer.gd` around lit campfires/torches — a generated gradient texture with NO per-frame animation (night-only, refreshed on `world_overridden` and player tile change), so the visual sweep stays byte-stable; `world_view.gd` itself is untouched.
- The music router plays a biome-specific overworld track on boot and whenever the player crosses into a new biome, switches to the wild-battle theme during battle, and resumes the biome track afterward.

## Biomes and traversal

- The world is generated from a seeded elevation, moisture, and biome noise field.
- Biome selection is progressive: tiles near the origin favor plains and grassland; forests and savanna appear farther out; desert, swamp, and rock appear at greater distance; snow and lava appear deepest in the world. This makes rarer biomes harder to reach, matching the original exploration incentive.
- Biomes are defined in `scripts/domain/biome_defs.gd` as a pure data table: base texture, optional atlas region, encounter flag, walkability, block reason, and scatter props with their own block reasons.
- `world_generator.gd` picks a biome per tile and scatters biome props deterministically from the seed, then overrides high-elevation tiles with rock cliffs. Tile logic (`get_tile_logic`) is separated from texture loading (`get_tile`) so navigability and traversal probes run without touching the GPU.
- Traversal rules are data-driven: each blocked tile carries a human-readable `block_reason` with a hint and a `requires_field_move` key. Harvestable blockers (trees, cacti, swamp trees, snow trees, rock cliffs) open only by being cleared through harvesting; water opens passively while a surf-capable Pokemon is in the party. See [harvest-and-mutation.md](harvest-and-mutation.md).
- The world generator's sparse per-tile override map carries TWO independent entry kinds: the harvest "cleared/dug" facts (owned here) and, since the building slice, "placed" structures added by the `building_loop` subsystem (a documented cross-subsystem co-modification of `world_overrides.gd` / `world_generator.gd`; this spec and those files stay owned by `world_runtime`). Placements apply at the same single `get_tile_logic` boundary as clears, so a built wall/door/fence participates in traversal, render, and collision exactly like a world prop — occupancy agrees by construction. Placements persist under a separate additive v3 `structures` save key (the clears `world_overrides` key is untouched); see [building-and-placement.md](building-and-placement.md).
- `world_view.gd` exposes `get_tile_biome`, `get_traversal_block_reason`, `tile_requires_field_move`, `set_time_of_day`, and `validate_world_invariants` for the player avatar and field-move flows, and emits a `biome_entered` trace whenever the center tile crosses into a new biome. Its per-tile data cache (`_tile_cache`) is window-evicted on every `sync_visible` pass — tiles outside the visible window are dropped AFTER the inactive-node cleanup, never mid-pass — so the cache stays bounded to the visible window (about 61×41 tiles) instead of growing without bound as the player walks; transient off-window audit queries are reclaimed on the next per-step sync. Since Phase 2 the lit light-source glow is composed inside `structure_layer.gd` (the building loop's layer) rather than the world view — the design's documented fallback path, chosen so the camping visuals never depend on which compositor landed: one static generated gradient per lit campfire/torch in the visible window, night-only, refreshed on `world_overridden` (the campfire lit toggle mirrors that signal) and player tile change, so `world_view.gd` and `main.gd` stay untouched. See [camping-crafting-survival.md](camping-crafting-survival.md) § Light rendering.

## Navigable spawn

- `world_generator.find_walkable_spawn` searches outward from the origin in expanding rings and returns the first walkable tile that has at least two walkable neighbors and one non-encounter neighbor. New games spawn the player there instead of at `(0,0)`, which may be water or a blocked tile. **Beach preference (infinite-world slice 4):** the scan runs a beach FIRST pass — the first SAND (beach-band, elevation-driven) tile that is walkable in resolved logic and has a cardinal neighbor whose `requires_field_move == "surf"` (WATER), gated by the same `_spawn_meets_neighbors` (WATER is non-walkable, so a water-only sandbar is rejected and `SPAWN_REACH_MIN` reachability holds); only when no beach is within `SPAWN_SEARCH_RADIUS` does it fall back to the existing walkable scan byte-identical. Faithful "start on a beach"; deterministic (rng-free, `_ring_tiles` order, fixed direction order). Witnessed by `seed_choice` (beach spawn) and the re-pinned spawn-dependent baselines.
- `world_generator.reachable_walkable_count` performs a bounded flood fill from a start tile to prove the spawn is not walled off (optional `blocked` parameter treats one extra tile as unwalkable — the build trap guard's hypothetical stamp, so the guard never mutates the live placements map). `validate_invariants` asserts the spawn reaches at least `SPAWN_REACH_MIN` tiles within `SPAWN_REACH_BUDGET`.

## Biome-specific encounters

- `scripts/domain/biome_encounters.gd` builds encounter tables from the per-species `spawn_biomes` parsed out of the source `wilds_data.asm` files. Source biome tokens that differ from world biome ids (for example `DEEP_FOREST` or `TIDAL_BEACH`) are mapped through `SOURCE_BIOME_ALIASES`; species carrying the source `TYPE` sentinel (or no spawn data) fall back to the type-based biome table (for example `FOREST` draws from `BUG`, `GRASS`, `POISON`, `FLYING`).
- Encounter eligibility requires battle-viable species: battle sprites, a catch rate, base stats, and a learnset. The literal `EGG` never spawns. Phase 7 ([world-depth.md](world-depth.md) § Legendaries) additionally bars the frozen seven (`LEGENDARY_IDS` — MEWTWO + the six Regis) from EVERY pool: they are static ring-≥60 stationary entities, never random encounters. No legendary has any `wilds_data.asm` spawn line, but they are battle-viable AND carry the `TYPE` sentinel, so the exclusion guards BOTH the type fallback AND the otherwise-unguarded full-catalog fallback (belt-and-suspenders beside the no-token fact); the roster is frozen in `scripts/domain/legendary_placement.gd`.
- In grass biomes (GRASSLAND, FOREST, SAVANNA) encounters are tied to visible tall-grass patches scattered deterministically by the generator; other encounter biomes keep biome-wide encounters. (Since the configurable-encounters slice this per-step random roll is OPT-IN and OFF by default — wild battles come only from a shared-tile sprite collision until the player enables it; [overworld-pokemon.md](overworld-pokemon.md) § Configurable encounters. The `encounter` tile flag + this eligibility model are unchanged; only whether the roll fires is gated.)
- `game_runtime.generate_wild_encounter` accepts an optional biome id and rolls from the filtered table. If no species match, the filter falls back to the full catalog and emits a `warning` trace so the escape hatch stays observable. If the catalog itself is empty, the encounter is skipped with a `warning` trace instead of fabricating a synthetic Pokemon.
- Since Phase 2 the filter gains a time-of-day dimension: `filter_species_ids(species_dict, biome, time_of_day := "DAY")` (the additive default kept the sole caller compiling until `game_runtime` passed the real `DayPhase.time_of_day_label`) excludes the one wiki-anchored night-only species (UMBREON, Savanna night) from DAY pools and extends every biome's NIGHT pool with the GHOST type (faithful: ghosts seek you out everywhere at night). Separately, `game_runtime._pick_encounter_species` delegates to the night system at night without light — half of those encounters become a battle-viable Ghost-type from a sorted type-derived pool (`night_hazard_spawned`), riding the existing shared `_rng` so `seed_for_smoke` pins the stream with no new seam. The level-from-distance and fallback-species helpers were extracted to `scripts/domain/encounter_selection.gd` to free the `game_runtime.gd` budget (same-line call-site edits only). Full contract in [camping-crafting-survival.md](camping-crafting-survival.md) § Night danger and § Nocturnal spawns.

## Input map

- Movement: arrow keys or `WASD`
- Confirm: `Z`
- Cancel (in menus/battle) / run modifier (in overworld): `X` — one physical key deliberately shared across two actions in mutually exclusive contexts. UI screens consume `action_b` via `_unhandled_input` only while visible; `run` is polled only during overworld movement (`is_action_pressed` while input is enabled). The contexts never overlap, so the shared key never collides (no rebind needed).
- Start menu: `Enter`
- Overworld context action: `Z` on the faced tile, in precedence order — camp-object interaction FIRST (`field_action_router._route_camp_object` reads the faced structure: a placed campfire opens the camp menu — Craft / Extinguish-or-Light / Demolish; a placed bed rests the party — see [camping-crafting-survival.md](camping-crafting-survival.md); a placed storage box opens the storage screen — see [storage-and-party.md](storage-and-party.md)); then harvest (Cut a tree → `log`, Smash a cliff → `hard_stone`, Dig ground → `dry_soil`; see [harvest-and-mutation.md](harvest-and-mutation.md)); then build mode on a walkable faced tile with a Build-capable (FIGHTING auto-type) party member (see [building-and-placement.md](building-and-placement.md)). While ANY router-opened overlay is visible (`field_action_router._overlay_open` — the camp menu OR the storage screen), the context route no-ops so `Z` stays owned by the overlay.
- Overlay ownership: while ANY input-phase overlay is open (the camp menu, the start menu, the storage screen, or a MessageBox confirm), `Z`/`X`/`Enter` BELONG to it (`_unhandled_input` during the input phase, which runs before the `_process` polls). A press that closes or confirms an overlay there must not also fire the same-frame polls, so a GENERALIZED same-frame latch in `input_router.gd` (`_ui_ate_press`, set via `bind_ui_consumers` AND by `main._on_battle_finished` via `note_press_consumed`) suppresses BOTH `poll_menu_toggle` and `poll_context_action`. `main.gd`'s `_ready` calls `bind_ui_consumers([$UI/CampMenu, _start_menu, _message_box, $UI/StorageScreen])`, connecting the ARGLESS `closed`/`confirmed`/`cancelled` signals of all four UIs to the latch (the argless contract is load-bearing — a producer that grew an argument would silently stop setting it). One latch, set by every producer, consumed by both polls — this kills every same-press double-fire at the shared root: Enter no longer opens the start menu under a closing camp menu; Z-on-Demolish no longer re-fires harvest/build on the bare former campfire tile; a start-menu CLOSE no longer re-harvests the faced tile; a MessageBox NEW GAME confirm no longer harvests the spawn-facing tile on the brand-new world (superseding the "New game started." toast); and an inert party-screen FIELD MOVE no longer harvests the faced cut-tile its capability message just declined. The latch ALSO covers the one input-phase event that is not an overlay close/confirm: a press that ENDS A BATTLE. Z-on-RUN (`run_from_battle`) and a ball-select Z capture (`use_pokeball`) — the only SYNCHRONOUS end paths — resolve inside `battle_view._unhandled_input`, emitting `battle_finished` within the input phase (victory/defeat emit AFTER the turn animation, from `battle_turn_player`, with no press step); `main._on_battle_finished` re-enables the overworld there (`_in_battle = false` shares its line with the `note_press_consumed()` call — same-line edit, the 220-line budget), so the same-frame poll can no longer harvest/build the faced tile (pre-fix the harvest toast superseded "Got away safely!", or build mode opened on the faced tile). It is set UNCONDITIONALLY at the single `battle_finished` endpoint covering every path (RUN, capture, victory, defeat — the only end classes; any future press-driven end, e.g. a message advance if one lands, is covered by the same unconditional latch with no new wiring): the context poll resets the latch every frame, so a press-less animated end strands it for one frame — that frame's toggle poll ALSO early-returns once (a coincidental Enter loses one menu toggle) — and the press after it still fires. The latch is consumed by the context poll — `Main._process` calls the toggle poll FIRST and the context poll second, every frame (the order contract; the context poll captures + resets the latch unconditionally, so a toggle-only frame strands it for one harmless frame). The `input_gate` scenario drives the camp-menu races (parts A/B) plus the start-menu CLOSE, inert-FIELD-MOVE, and NEW-GAME-confirm leak paths (parts C/E/D in `input_gate_menu_checks.gd`) through real input injection (the shared `smoke_tap.gd`); the `battle_end_input` scenario drives the battle-end races the same way — a real Z on RUN and a real ball-select Z capture facing cut-harvestable tiles (no re-fire: the trees stand, the escape/capture toasts survive) PLUS the over-suppression guard (a fresh deliberate Z the next frame still harvests).
- Build mode (while active): movement keys cycle the selected structure, `Z` places, `X` exits.

## Capture contract

- Windowed screenshot captures in `visual_sweep`, `ui_render_audit`, and `display_matrix` delegate to `scripts/app/snapshot_capture.gd` (subsystem `vision_fidelity`; see [vision-fidelity.md](vision-fidelity.md)) for a `RenderingServer.frame_post_draw` readback guard (always ADDED AFTER each scenario's existing settle waits, never a substitute), a validity oracle (minimum PNG size, luminance floor, uniform-color and magenta-frame checks), and a root-viewport-crop fallback when the battle SubViewport readback is magenta/stale (Godot 4.6 regression #115402).
- Valid captures emit `snapshot_captured`; invalid captures emit a quarantine-tier `capture_invalid` warning trace classified `transport` (headless display) or `regression` (windowed, including all magenta frames). `visual_sweep` still fails red on an invalid capture in a windowed run; `ui_render_audit` pixel-half findings route per `GRADUATED_STATES` (Slice 4; Slice 6 flipped all four battle keys true on five consecutive clean windowed runs recorded in `docs/generated/graduation-ledger.json`) — red for a graduated battle state, quarantine-tier otherwise; menu/party/bag have no keys and can never graduate. The flip changes the verification harness's finding tiering only, never game behavior (see [vision-fidelity.md](vision-fidelity.md) § Legibility oracles).
- `visual_sweep` additionally writes a canonical semantic sidecar next to each valid shot PNG (`<shot>.png.sidecar.json`; schema in [../references/snapshot-sidecar.md](../references/snapshot-sidecar.md), built by `scripts/app/render_introspection.gd` from the existing `smoke_context()` ctx dict — no `main.gd` edits), and committed sidecars sit alongside the baseline PNGs. `visual_sweep_passed` carries `sidecar_paths` and `invalid_captures`; the per-region verdict fields (`region_failures`, `clusters_explained`, `clusters_unexplained`, `region_artifacts`) are recorded by the playtest runner into `.godot-smoke/playtest-report.json` — NOT the JSONL trace — by the `tools/visual_region_diff.py` post-step (see [vision-fidelity.md](vision-fidelity.md) § Explainable per-region diff).
- Under `PLAYTEST_FORCE_HEADLESS=1`, windowed-only scenarios are reported skipped-with-reason instead of failed; the playtest report is stamped with `head_sha` plus `godot_version`/`window`/`renderer` harvested from `snapshot_captured` traces (null when no windowed capture ran).

## Smoke validation

- `boot` proves the app reaches a ready state and rebuilds the world.
- `overworld_step` proves the player can take at least one safe step and persist movement state, and asserts the world view's `_tile_cache` stays bounded to the visible window after N steps (window eviction reclaims off-window tiles).
- `save_migration` drives the runtime's real load path against the FULL migration ladder (v1/v2/v3/v4/v5 + future-version fixtures written to the live save path inside the runner's backup/restore guard), asserting field migration, the non-destructive refusal of a newer schema, AND — infinite-world slice 1 — the v5→v6 flatten: the pinned NON-TRIVIAL v5 fixture is CHAIN-LESS (active_chain `"0,0"`, a `chained_worlds["0,0"]` nest holding a Mansion landmark_state row) and flattens LOSSLESSLY (delta EXACTLY `{version: 6}`, the landmark_state hoisted to its top-level seat + re-keyed to its instance seat, byte-verbatim; `migrate()` never mutates its argument), while a truly CHAINED v5 fixture (active_chain `"0,-1"`) fails `can_represent_infinite` and is refused non-destructively (preserved to `.chained.bak` + fresh start). It emits `save_migration_passed` (payload carries `v1_ok`…`v5_ok`, `future_refused`, `chained_refused`, and checks). See [menu-and-save.md](menu-and-save.md) + [infinite-world.md](infinite-world.md) § Save v6.
- Phase 7 Build 3 extracts the `save_stability` v5 work into `scripts/app/save_stability_support.gd` for the app 220-line budget (the `wild_battle_scenario` precedent): the update-mode downshift guard (the golden always regenerates v4-shaped; a v5-shaped write is refused + traced) and the world_chain round-trip sublane (mutate origin → cross → mutate chained → reload → both worlds re-canonicalize byte-identically — the v5 save shape only; the runtime cross mechanics are pinned by `world_chain_scenario`).
- `biome_probe` drives `world_view.validate_world_invariants` and emits `biome_probe_passed` when the generated world satisfies determinism, ring progression, navigable spawn, and reachability invariants.
- `biome_traverse` walks the player across a biome boundary (or teleports across one), triggers a traversal-gate block on a field-move-locked tile, then starts a biome-aware wild battle. Requires `biome_entered` or `traversal_blocked` plus a completed `encounter_started`/`battle_finished` pair.
- `field_move` finds a `cut`-gated tile, drives the party-capability field-move flow from the party screen (species flags + type auto-ability; there is no stored unlock state), and confirms the tile becomes walkable with the `field_move_used` trace.
- `nav_audit` proves traversal agreement (blocked tiles reject with a reason, walkable tiles accept, a cut gate rejects even a cut-capable party until the resolver clears it, a surf gate opens only for a surf-capable party), battle-menu navigation (every enabled option d-pad reachable with the selection matching the layout model, move row 0 performs the lead mon's actual first move, RUN ends the battle — trap-tolerantly: a legitimate "Can't escape!" refusal is PROVEN, the trap expired, and escape re-proven, so an in-game escape refusal can never be a suite failure), and start-menu navigation. The battle half pins BOTH runtime rngs via `game_runtime.seed_for_smoke(NAV_AUDIT_SEED)` before the battle starts (the house seeding convention — visual_sweep's `BATTLE_RNG_SEED`, playtest_soak's `SOAK_SEED`), so the wild draw and every battle roll are a pure function of (code, save, seed), reproducible across subprocesses and cold/warm starts: the pass event is never gated on per-process wall-clock-seeded RNG (miss-002). A non-pass emits `nav_audit_failed` carrying the failures array alongside `push_error` — part of the suite-wide total-exit contract: any trace event ending in `_failed` is collected by BOTH harness transports as a named failure and `push_error`'s "ERROR: " lines are captured as exceptions, so a red can never be silent (miss-002).
- `world_consistency_audit` samples tiles around spawn and proves logic/render/collision agreement, player-vs-prop spatial contracts, z-order, and tall-grass/encounter alignment.
- `ui_render_audit` verifies battle and menu screens against the art-anchored render model (expected strings, label overlap, cursor pairs). Its windowed pixel half now calls the `vision_fidelity` glyph template oracle (`scripts/app/text_oracle.gd`) per battle state — expected strings rasterized to ink masks and XOR-matched at the modeled rects, with per-glyph bboxes and min-stroke checks — merged with the existing visual lint into one findings list. Findings route per `GRADUATED_STATES`: a graduated battle state's glyph mismatch / garble / low_ink fails red, non-graduated states stay quarantine-tier; at Slice 6 all four battle states graduated on recorded evidence — `battle_moves` + `battle_item` (anchor, glyph template match) and `battle_action` (lint cleanliness on ACTION_ROWS) on five consecutive clean windowed runs (`tools/graduation_ledger.py` runs 1-5 at HEAD 7b733946), `battle_message` last (box mode, lint cleanliness; glyph match excluded by design, with the documented engine-owned ~1px pen judgment in the ledger flips[] entry) — while menu/party/bag never graduate (Godot default-theme font at 12/14/20 — a different raster than fonts.ttf@7, no equivalence proof; a future author must not add menu keys). The flip is an in-place constant edit to `ui_render_audit.gd` that changes the verification harness's tiering, not the game. A zero-glyph-mismatch run emits the coded `text_oracle_passed` trace. WCAG contrast (`tools/contrast_check.py`) and color-vision-deficiency simulation (`tools/cvd_sim.py`) evidence is reported via `tools/generate_legibility_report.py` (Contrast + CVD sections) and the `visual_sweep` runner post-step; see [vision-fidelity.md](vision-fidelity.md) § Legibility oracles for the full contract.
- `display_matrix` resizes the window across six sizes and proves the battle surface renders without scale degradation at each; `visual_sweep` applies a canonical 1152x648 window size so baselines are window-size-stable. All three capture through the capture contract above but at different depths: `visual_sweep` runs the full pipeline, so every shot emits `snapshot_captured` or a quarantine-tier `capture_invalid`; `ui_render_audit`'s pixel half runs the guard plus oracle and emits `capture_invalid` only on an invalid verdict (a valid pixel-half shot emits no per-shot trace); `display_matrix` adopts the guard alone. `visual_sweep_passed` carries the canonical `window` plus `dup_checked`, `sidecar_paths`, and `invalid_captures`.

## Phase 4 field-move completion (cross-subsystem)

Phase 4 (spec: [field-moves.md](field-moves.md)) touches this subsystem's code only:
- `scripts/app/smoke_scenarios.gd` gains the `playtest_field_soak` match arm; `scripts/app/playtest_scenarios.gd` grows `run_field_soak` (a thin wrapper delegating to the new `playtest_field_soak_scenario.gd`); `scripts/app/qa_scenarios.gd` registers `field_moves_flow` + `build_house_flow`; `scripts/runtime/playtest_bot.gd` gains `try_random_field_move` (the seeded field-move soak band). The fresh-game starter-party `playtest_soak` is untouched (the field soak is its own `playtest_` scenario).
- `scripts/runtime/player_avatar.gd` gains the Ride mount speed mode (`set_mounted`/`is_mounted`, `mount_step_seconds` faster than the run gait; the run sheet is the documented mount-sprite fallback — no dedicated mount art ships in the submodule). Ledge-climbing is out of scope (traversal speed only).


## Phase 5 Pokemon systems co-modification (cross-subsystem)

Phase 5 (spec: [breeding-shinies-drops-fishing.md](breeding-shinies-drops-fishing.md)) touches this subsystem's code only:
- `scripts/app/qa_scenarios.gd` registers the Phase 5 proofs (`breed_flow`, `shiny_odds`, `habitat_drops`, `fishing_flow`, the self-guarded `playtest_breed_soak`) and the pokemon-state sweep (`visual_sweep_pokemon` + `_update`, shots 20-21; the shared `_foreign_shot` prune guard in `visual_sweep_baselines.gd` extends to `20_`/`21_` so the main sweep never prunes them). The runtime adapters + pen/water site helpers live in `scripts/runtime/phase5_support.gd` + `phase5_sites.gd`; the soak band in `scripts/runtime/playtest_bot_breeding.gd`.
- `scripts/runtime/world_view.gd` renders ground eggs (the breeding runtime's `ground_egg_at` on the prop layer, y-sort contract).
- `scripts/data/species_file_parser.gd` gains the static `parse_egg_moves` (`SpeciesEggMoves:` label + `db MOVE` lines; absent file -> []); `scripts/data/pokemon_catalog.gd` carries `egg_moves` + the shiny asset paths (front.pal/shiny.pal colors, overworld-shiny path) on the species entry.

## Phase 6 integration note (overworld Pokémon)

Phase 6 (`overworld-pokemon.md`) adds the EntityLayer Node2D to `scenes/app/Main.tscn` (child index 2, after Player — the y-sort composition for roaming mons/eggs/guardians; self-wired via the `/root/GameRuntime` convention, ZERO lines in `main.gd`), the `overworld_mons` gate scenario + `visual_sweep_overworld` (shots 22-23, plus `30_night_roamers` from the expansion below) dispatch in `qa_scenarios.gd`, the `22_`/`23_` same-line `_foreign_shot` prune guard in `visual_sweep_baselines.gd`, and the one-line activation opt-out in `smoke_scenarios.gd` (opt-in set: `overworld_mons` + the two sweep variants — every other baseline runs entity-free, byte-stable).

## Pre-Phase-7 suite expansion (user-approved full deep-dive expansion)

The verification suite expanded to pin the interacting-systems determinism surface before Phase 7 (full detail + landed-status annotation: [../exec-plans/active/pokewilds-feature-completion.md](../exec-plans/active/pokewilds-feature-completion.md) § Phase 7; lanes + run-when lists: [../RELIABILITY.md](../RELIABILITY.md)). This subsystem's code changes:
- New miss-002-compliant scenarios dispatched here: `rng_joint_pin` (`scripts/app/rng_joint_pin_scenario.gd` — ONE `seed_for_smoke` seed, seeded BEFORE `new_game` since the world-seed draw rides `_rng`; a fixed interleaving of encounter draws + fishing casts (fishing shares `game_runtime._rng`) + harvest dig steps (step-pure), driven TWICE in-scenario with the canonicalized joint sequence byte-compared; `rng_joint_pin_passed{seed, cycles, draws, hooks, digest}` / `rng_joint_pin_failed{seed, reasons, digest_a, digest_b}`) and `save_stability` (`scripts/app/save_stability_scenario.gd` + `save_stability_checks.gd` — scripted v4-surface mutations → save → reload → save → byte-compare of canonicalized save JSON (key-sorted, ts/version-stripped); commits ONE golden v4 fixture `docs/generated/golden-saves/v4_golden.json` and asserts load + canonical stability).
- `scripts/app/visual_sweep.gd` (at its 220 budget) is paid by EXTRACTION: the battle shots, the day/night + menu shots (now 24/25 + 28/29), the report/compare pass, and the menu fixtures split into `visual_sweep_battle.gd`, `visual_sweep_day_menu.gd`, `visual_sweep_report.gd`, and `ui_render_fixtures.gd`.
- `scripts/app/visual_sweep_baselines.gd` gains the single-sourced shot-range REGISTRY const (per-sweep number ranges + per-sweep seeds + retired numbers — shot 17 formally RETIRED, the sole whitelisted numbering gap; the satellites' `_foreign_shot` guards derive from it; biome-shots floor ≥3 loud-fail).
- `seed_for_smoke` (`game_runtime.gd:207`, session_runtime-owned) now also pins `player_avatar._rng` (wall-clock previously; `player_avatar.gd:42,47`; trigger draw :205), and `boot` / `overworld_step` / `menu_save` gain `seed_for_smoke` calls in `smoke_scenarios.gd`.
- `playtest_entity_soak` (the `scripts/runtime/playtest_bot_entity.gd` band, registry-owned by `overworld_mons`) and `visual_sweep_fishing` (shots 26/27, seed 2026072804, registry-owned by `fishing`) dispatch through the same `qa_scenarios.gd` router; the five menu-group rubric questions convert to deterministic answerers (no new machinery — see [vision-review-rubric.md](../references/vision-review-rubric.md) § Answerers table).

## Phase 7 Build 1 (landmarks) co-modification note

Phase 7 Build 1 ([world-depth.md](world-depth.md) § Landmarks) stamps three deterministic-from-seed landmark footprints (Mansion / Desert Ruins / Heart Tower) into base tile logic at `world_generator.gd`'s single mutation boundary via a pure resolver the runtimes wire (`Landmarks.tile_logic_for(world_seed, chain, map_pos, base)`; byte-identical OUTSIDE every footprint — the generator consults, never owns). `world_generator.gd` pays its one consult line at the 320 budget by EXTRACTION FIRST: `_pick_prop`/`_coord_noise`/the rock-cliff test move to `scripts/domain/landmarks.gd` (`pick_prop`/`coord_noise`/`is_rock_cliff`) and are delegated with identical constants (prop/cliff layouts byte-stable). `world_view.gd` wires the same resolver on `rebuild` so render/movement/audit share the single choke point; `game_runtime.gd`'s `generate_wild_encounter` reads the active tile's `encounter_token` through `landmark_runtime.encounter_scope_for` — outside footprints the token is `""` and the shared `_rng` stream stays byte-identical to pre-Phase-7 (spec § Determinism). `world_invariants.gd`'s determinism diff now copies the resolver onto its second generator (the invariant compares LIKE-WIRED generators; the resolver is wiring, not setup state). The app-owned `world_consistency_audit.gd` failure path now emits `world_consistency_audit_failed` (failures + seed payload) instead of stderr-only `push_error` (miss-002 loudness), and `qa_scenarios.gd`/`visual_sweep_baselines.gd` gain the `world_depth` satellite (shots 31/32). The anchor search keeps sibling footprints mutually disjoint (borders included), so one step past ANY footprint edge is clean ground; the spawn-region reachability + ring bands are unchanged (spec gate (c)).

## Phase 7 R4/R11 (post-Build-3 suite audit) co-modification note

The post-Phase-7 audit found the world-depth landmark shots pixel-UNDER-gated (only the 0.5% global drift; the pre-capture asserts read tile-logic dicts, not pixels) and three Phase-7 states UNSHOT (the Heart Tower, any non-origin chained world, and a legendary guardian sprite). This subsystem's code changes close both, all windowed-only and rng-free (byte-stable):
- R4 — the REGIONAL PIXEL ORACLE: `scripts/app/visual_sweep_world_depth_oracle.gd` (extracted at the app-220 wall) projects the in-scenario landmark/beacon/legendary TILES (already derived off the frozen seams) to screen rects off the camera tile + the canonical 1152x648 window (world_view `TILE_SIZE` 16 × Camera2D zoom 3 = 48 device px/tile; the camera parents the player and centers the window on `player.position`), and `visual_sweep_world_depth.gd` / `visual_sweep_world_depth_beacon.gd` merge the result into each shot's `expected_regions.ink` + `canary_rect` so a dropped statue/decor/beacon/guardian sprite reds via `tools/visual_region_diff.py`'s red tier instead of hiding under the global gate. The gated sprites use the feet-origin trick at integer tiles, so the strict (tolerance-0) canary self-equals on re-capture (measured 0 region failures / 0 quarantine on the compare run).
- R11 — the UNSHOT states, numbered after 33: shot `34_heart_tower` joins the origin `world_depth` satellite (registry range [31,34]; the Heart Tower base chamber, dedicated tileset, asserted via the rendered wall/decor props since the tower has NO encounter token — `scripts/app/visual_sweep_world_depth_expand.gd`, extracted at the wall). The derived-world shots are their OWN satellite `visual_sweep_world_chain` (registry range [35,36], seed = `world_seed_for(2026072907,(0,-1))` single-sourced in the registry) because R3's run_playtests sidecar gate (`_sidecar_seed_equality_violations`: exactly one deterministic world per run) forbids a derived `world_seed` sharing a shots dir with the origin 31-34 shots: `35_chained_world` (the non-origin overworld with its RE-ANCHORED landmarks — the chained anchor is set through `session.active_chain` + `world_seed` BEFORE `rebuild` so the frozen landmark resolver re-derives the derived world's three) + `36_legendary_guardian` (a ring≥60 legendary static at rest — the shot opts the entity layer IN and calls `stamp_legendaries()` since `craft_state` bypasses it, framing one guardian out of its widened sight so the step-driven chase never moves it and the two-sample rest probe settles byte-stable). Both new satellites register in `qa_scenarios.gd` + the smoketest windowed sets + `SCENARIO_REQUIREMENTS` + `run_playtests.SATELLITE_SWEEP_SCENARIOS`/`PLAYTEST_SCENARIOS` (so `verify_all`'s S9 loop + the R3 RED gate cover them by construction), and the shared rest probe + legendary-framing math + the R3 registry single-source live in the extracted `visual_sweep_world_depth_expand.gd` / `visual_sweep_registry_support.gd`. The beacon shot (33) now `close_selector()`s after capture so the picker overlay (a separate node from the message box) does not bleed into the later shots. Baselines captured in update mode and re-compared at 0.0% drift (global + per-region); `check_architecture`/`check_repo_contracts`/`check_quality_docs` green. Scores unchanged: verification coverage + pixel-gating, not game behavior.

## Showcase capture + WayStone selector (co-modification note)

Two Phase-7 follow-up wirings touch this subsystem's boot/dispatch surface (no boot-behavior change, no capture-contract change):
- `scenes/app/Main.tscn` instances `scenes/ui/WayStoneSelector.tscn` under `UI` (the multi-way-stone teleport selector — a registration-ordered listing of the world's way stones with a REAL input pick; hidden at boot, opened only by the warp request). INFINITE-WORLD SLICE 1 renamed it from the retired `BeaconSelector` (the edge-beacon concept is gone with world chaining; way stones are the ONLY warp points on the seamless plane — [infinite-world.md](infinite-world.md)). The screen is DUMB by layer contract (ui may not import domain): the opener (`field_move_actions`) supplies the rows off `field_move_runtime.way_stone_tiles()` plus a resolve Callable, and the screen SELF-WIRES the input latch at boot via `current_scene._input_router.bind_ui_consumers` (the documented reach-in precedent — `main.gd` stays at its frozen 220 budget with ZERO added lines).
- `qa_scenarios.gd` registers `showcase_capture` (`scripts/app/showcase_capture_scenario.gd` + the `showcase_*` satellites): a WINDOWED-ONLY capture driver that deterministically crafts the world-depth locales (origin seed 2026072907 at noon — spawn vista, mansion, sewer, ruins exterior + underground guardian, heart tower; pen seed 2026072605 — the EEVEE pasture + ground egg) and writes frames + crafted-state sidecars to `docs/generated/showcase/`. The LIVE frames are 01-05, 07, 08; frames 06 (edge beacons), 09/10 (a chained world + its stationary legendary) RETIRED with world chaining (infinite-world slice 1) and their files are deleted. Deliberately NOT a baseline sweep: no SHOT_REGISTRY entry, no `reconcile()`/`visual_diff`/region-diff gate, no writes to `docs/generated/visual-baselines/`; under `PLAYTEST_FORCE_HEADLESS` it transport-skips with a reason (the headless display server renders no pixels). The registration adds it to `run_playtests.PLAYTEST_SCENARIOS` + the smoketest windowed sets + `SCENARIO_REQUIREMENTS` (the symmetric `showcase_capture_passed`/`showcase_capture_failed` markers ride the standard contract), so the headless full suite counts 56 scenarios (43 playtest + 13 smoke; 10 skip headless honestly). See [../RELIABILITY.md](../RELIABILITY.md) § Visual verification.

## Configurable encounters (co-modification note)

The configurable-encounters slice ([overworld-pokemon.md](overworld-pokemon.md) § Configurable encounters) touches this subsystem's boot/dispatch + avatar surfaces (no boot-behavior change, no capture-contract change):
- `player_avatar.gd`'s `_try_trigger_encounter` (the per-step random roll) is now GATED on `session.encounter_settings` read live from `/root/GameRuntime` (the `_try_edge_cross` autoload precedent): mode `off` — the DEFAULT — returns before any rng is touched (so the avatar's pinned trigger stream is provably unchanged); `classic` keeps the `is_encounter_tile` gate; `anywhere` drops it; both roll at the configured rate (the legacy `encounter_chance = 0.12` stays the no-runtime fallback; the fast-gait modifier unchanged). The walk/run frame build EXTRACTED to `scripts/runtime/player_sprite_frames.gd` at the 320 wall (pure presentation; null-on-missing keeps the scene-default frames exactly as before).
- `qa_scenarios.gd` registers the `encounter_config` gate scenario (`scripts/app/encounter_config_scenario.gd` — the collision-only default + the opt-in modes + the additive save round-trip; overworld-active, the eleventh double-run consumer; the symmetric `encounter_config_passed`/`_failed` markers ride the standard contract on `app_bootstrap`). `smoke_scenario_runner.scenario_uses_overworld` gains it (so entities are live for the contact case). The start-menu `OPTIONS` entry at index 3 re-pins the `ENTRY_*` indices, so the two index-driven menu smokes re-target their rows (`_scenario_menu_save` four `move_down` to `NEW GAME`; `input_gate_menu_checks.gd` five to `CLOSE` / four to `NEW GAME`).

## Comprehensive world-gen audit (co-modification note)

The comprehensive world-generation workflow lands as an AUDIT HARNESS first (the world-gen fixes follow as separate gated slices). It mechanically MEASURES three properties of the procedural world across a fixed 9-seed list (`1337, 1, 42, 20260101, 31337, 999983, 2026072907, 2026072913, 2026073001`), consuming NO rng (a pure function of code + catalog + seeds — deterministic by construction; its `determinism_density` check is the in-engine determinism proof, so it is deliberately NOT a double-run consumer):
- **Cohesion / blending** (`scripts/domain/world_gen_cohesion.gd`): one cached disc scan/seed (radius 110) feeds disc determinism density, the biome-adjacency histogram + hostile-pair count, region-size/speck distribution, and the whole-disc biome histogram + SNOW/LAVA extreme presence; the biome-distribution contract rides a wide stride-sampled window (radius 400, stride 4 — one 110-disc reads as ONE climate region). Infinite-world slice 2 replaced the radial ring model with the climate field (`scripts/domain/biome_field.gd` — temperature/moisture/volcanism channels; WATER/SAND/ROCK stay elevation-driven), so ring admission and the ring-seam metric RETIRED. MEASURED TODAY: biomes are hard-edged step-function regions with NO transition/blending logic, hostile adjacencies occur (e.g. SNOW↔LAVA), and LAVA generates as the rare joint tail (~0.02-0.9% of tiles per seed, present in 8 of the audit's 9 windows — the radial model's quantization gap is RESOLVED; a cold-climate window may legitimately lack it, so per-seed LAVA absence is not a failure — presence is a cross-seed contract, `LAVA_WINDOWS_MIN`). Visual edge-blending is recorded as a named advisory; its pixel probe rides the later blending fix's windowed verification.
- **Spawn↔stats** (`scripts/domain/world_gen_spawns.gd`): the FIRST base-stat-total computation anywhere in the codebase; type↔biome coherence + the legendary/EGG no-leak invariant (enforcing), plus fallback-disjoint leaks, BST-by-biome, and level-band↔strength analysis (advisory; the radial BST↔depth tiering RETIRED with the ring model). MEASURED TODAY: stats play NO role in spawn assignment (species by type/biome + source lines, level by distance only) — high-BST mons (≥540) are reachable at level 2-5 wherever their biome touches the inner region, and BST is uncorrelated with biome.
- **Dungeon sites** (`scripts/domain/world_gen_dungeons.gd`): per-biome footprint site availability over the playable disc (incl. LAVA), spawn-disc exclusion, spawn reachability, the chunk-hash content-density sample (infinite-world slice 3), and the unimplemented spec §19(c) gen-time reachability gate. MEASURED TODAY: every land biome hosts fitting sites; LAVA sites are nonzero-but-rare (small pockets; slice 2's climate field made LAVA generate — the radial headline gap is RESOLVED), all seven legendaries anchor (the guided min-margin scan, slice 2), a footprint can reach manhattan ≤24 of origin (`spawn_disc_intrusion`), and footprint reachability from spawn is not verified (§19(c) unimplemented).

**TIER RULE:** `world_gen_audit_passed` gates on the ENFORCING tier ONLY (structural invariants green today: disc determinism, the biome-distribution contract (the ten common biomes per seed + LAVA in at least `LAVA_WINDOWS_MIN` of 9 windows cross-seed), pool no-leak, spawn reachability; ring admission and landmark-in-extent RETIRED with the radial/chain models); every gap needing a future fix rides the warning-tier `world_gen_audit_advisory` event which NEVER gates — the `advisory_findings` ARE the fix-slice punch-list. The `spawn_disc_exclusion` check is advisory (not enforcing) because the world does not yet guarantee it (a real finding, demoted per the day-one-green guard).

**Files:** `scripts/domain/world_gen_audit.gd` (shared contract) + the three families above + `scripts/runtime/world_gen_audit_runner.gd` (runtime facade — builds a fresh `WorldGenerator` per seed, never the live `_world_gen`/shared `_rng`; re-exposes the domain consts the app reaches) + `scripts/app/world_gen_audit_scenario.gd`. New registry subsystem `world_gen_audit`. Findings: `res://.godot-smoke/world-gen-audit-findings.json` (per-run) + the curated `docs/generated/world-gen-audit-findings.md` (committed).

**Fix path (later slices):** blending fix → transition bands + no hostile adjacencies + visual edge-blending, promoting `hostile_adjacency` to enforcing + a windowed probe; spawn fix → stats-aware spawn gating, promoting `fallback_disjoint` + pinning BST thresholds; dungeon fix → place the pre-generated structures at the chunk-hash dungeon SITES + a gen-time spawn-disc/reachability gate, promoting the site + reachability checks (the audit advisories are that fix's regression net). The LAVA generation + all-seven anchoring already LANDED with the slice-2 climate field (`legendary_spawn_checks.gd` `EXPECTED_ANCHORED` re-pinned to the full seven in lockstep). No boot-behavior, capture-contract, or determinism change this round.

## Infinite-world slice (co-modification note)

The bounded-disc world model is RETIRED for a **seamless infinite procedural plane** (a flagged modernization / divergence — research found the original was actually chained bounded maps; the user chose seamless anyway):
- **No world edge.** `WORLD_RADIUS` / `is_outside` / the Surf/Fly edge-cross are gone (`world_chain.gd` + `world_chain_runtime.gd` deleted). The noise plane was always unbounded; `get_tile_logic` computes any coordinate, and a far tile now refuses on its OWN terrain (water/cliff/prop) exactly like an inland tile. The slice-2 climate field (`biome_field.gd`) replaced the radial rings: biomes derive from temperature/moisture/volcanism channels everywhere on the plane (WATER/SAND/ROCK stay elevation-driven), and slice 3 scatters landmark instances + repeating legendary lairs beyond the origin core.
- **v6 save.** `SAVE_VERSION 5→6` drops the chain-identity keys (`root_seed`/`active_chain`/`chained_worlds`) and restores `landmark_state` to its top-level seat. A chain-less v4/v5 save flattens LOSSLESSLY (delta == `{version: 6}`); a TRULY CHAINED v5 save is structurally unrepresentable on one plane, so the load path gates on `SaveMigration.can_represent_infinite` and REFUSES + PRESERVES it (`.chained.bak`, miss-002 loud) + starts fresh. The frozen `world_seed_for(root,(0,0))==root` identity guarantees a chain-less save's visible terrain does not shift. The migration scenario extracted to `save_migration_scenario.gd`.
- **Creation-time seed choice (follow-up):** the original's creation-time "world size" choice maps to a world-SEED choice (random/custom) for the infinite world — deferred to its own slice, with a faithful BEACH spawn.
