Status: current
Last verified: 2026-08-04
Review cadence days: 21
Source paths: scripts/core/trace_logger.gd, scripts/runtime/game_runtime.gd, scripts/runtime/battle_runtime.gd, scripts/runtime/save_store.gd, scripts/runtime/world_view.gd, scripts/runtime/build_runtime.gd, scripts/runtime/camping_runtime.gd, scripts/runtime/crafting_runtime.gd, scripts/runtime/night_system.gd, scripts/runtime/storage_runtime.gd, scripts/runtime/player_avatar.gd, scripts/domain/structures.gd, scripts/runtime/session_state.gd, scripts/app/main.gd, scripts/app/smoke_scenarios.gd, scripts/app/phase0_scenarios.gd, scripts/app/snapshot_capture.gd, scripts/app/ui_render_audit.gd, scripts/app/placement_flow_scenario.gd, scripts/app/placement_flow_demolition.gd, scripts/app/camp_survival_scenario.gd, scripts/app/craft_flow_scenario.gd, scripts/app/night_cycle_scenario.gd, scripts/app/time_evolution_scenario.gd, scripts/app/visual_sweep_camping.gd, scripts/app/storage_flow_scenario.gd, scripts/app/storage_flow_party_checks.gd, scripts/app/wild_battle_scenario.gd, scripts/app/visual_sweep_storage.gd, scripts/app/input_gate_scenario.gd, scripts/app/input_gate_menu_checks.gd, scripts/app/battle_end_input_scenario.gd, scripts/app/storage_release_mouse_check.gd, scripts/runtime/breeding_runtime.gd, scripts/runtime/habitat_runtime.gd, scripts/runtime/fishing_runtime.gd, scripts/runtime/stone_evolution_runtime.gd, scripts/domain/breeding.gd, scripts/domain/habitat_drops.gd, scripts/domain/fishing.gd, scripts/app/breed_flow_scenario.gd, scripts/app/breed_flow_checks.gd, scripts/app/breed_flow_habitat_checks.gd, scripts/app/shiny_odds_scenario.gd, scripts/app/habitat_drops_scenario.gd, scripts/app/habitat_drops_checks.gd, scripts/app/fishing_flow_scenario.gd, scripts/runtime/phase5_sites.gd, scripts/ui/bag_screen.gd, scripts/runtime/harvest_runtime.gd, scripts/runtime/harvest_resolver.gd, scripts/app/field_moves_flow_scenario.gd, scripts/app/field_moves_dig_checks.gd, tools/run_playtests.py, tools/vision_review.py, tools/vlm_reviewer.py, docs/registry/art-anchors.toml, scripts/domain/overworld_mons.gd, scripts/runtime/overworld_mons_runtime.gd, scripts/runtime/overworld_mons_sim.gd, scripts/runtime/overworld_mons_probe.gd, scripts/runtime/entity_layer.gd, scripts/domain/encounter_selection.gd, scripts/app/overworld_entity_actions.gd, scripts/app/overworld_mons_scenario.gd, scripts/app/overworld_mons_checks.gd, scripts/app/overworld_mons_battle_checks.gd, scripts/app/world_entity_audit.gd, scripts/app/visual_sweep_overworld.gd, scripts/app/rng_joint_pin_scenario.gd, scripts/app/save_stability_scenario.gd, scripts/app/visual_sweep_fishing.gd, scripts/runtime/playtest_bot_entity.gd, scripts/app/playtest_entity_soak_scenario.gd, docs/generated/golden-saves/v4_golden.json, scripts/domain/landmarks.gd, scripts/runtime/landmark_runtime.gd, scripts/runtime/session_payload.gd, scripts/app/landmark_actions.gd, scripts/app/landmark_flow_scenario.gd, scripts/app/world_depth_checks.gd, scripts/app/visual_sweep_world_depth.gd, scripts/domain/save_migration.gd, scripts/app/showcase_capture_scenario.gd, scripts/app/save_migration_scenario.gd, scripts/app/world_gen_audit_scenario.gd, scripts/runtime/world_gen_audit_runner.gd, scripts/domain/world_gen_audit.gd, scripts/domain/world_gen_cohesion.gd, scripts/domain/world_gen_spawns.gd, scripts/domain/world_gen_dungeons.gd, scripts/domain/contact_encounter.gd, scripts/runtime/player_sprite_frames.gd, scripts/runtime/overworld_mons_actions.gd, scripts/ui/options_screen.gd, scripts/ui/menu_context.gd, scripts/ui/waystone_selector.gd, scripts/domain/content_scatter.gd, scripts/domain/landmark_scatter.gd, scripts/runtime/overworld_mons_lairs.gd, scripts/app/content_scatter_scenario.gd, scripts/app/waystone_selector_scenario.gd, scripts/app/seed_choice_scenario.gd, scripts/app/new_game_flow_scenario.gd, scripts/app/new_game_flow_checks.gd, scripts/ui/title_screen.gd, scripts/ui/creation_screen.gd, scripts/ui/name_entry.gd, scripts/ui/avatar_picker.gd
# Trace Events

All runtime traces are JSONL records with `event`, `ts_msec`, `source`, and `payload`.

| Event | Source | Meaning |
| --- | --- | --- |
| `boot_started` | `App.Main` | Main scene startup has begun. |
| `boot_ready` | `App.Main` | Main scene finished boot wiring and is ready for play. |
| `session_loaded` | `GameRuntime` | Existing save state loaded successfully. |
| `session_created` | `GameRuntime` | A new session was created because no valid save was available. |
| `splash_shown` | `TitleScreen` | The startup splash-card phase activated on a PLAYER boot — the black card reading "POKÉWILDS" over "a fan remake of PokeWilds — original game by SheerSt" (FLAGGED invention text: no logo/copyright/credit strings exist anywhere in sources, so the card copy is port invention; the two label strings are pinned in the scene). `main.gd` calls `TitleScreen.begin_boot(has_save)` on player boots ONLY — scenario boots (a `.godot-smoke/scenario.json` request present) keep the pre-slice boot path EXACTLY and bypass the screen entirely, so `main.gd` never fires this on a scenario boot — the EXCEPTION is the `new_game_flow` gate scenario, which drives the player-boot seam directly (`begin_boot` on purpose) and so emits it in-scenario. Payload is empty. The card shows for 2.0s or until any action press skips it (`splash_closed`); the splash is a PHASE of `TitleScreen.tscn` (it shares the title's input handler and the single `$UI/TitleScreen.visible` guard in main.gd), not a separate scene. |
| `splash_closed` | `TitleScreen` | The splash card closed and the title phase came up (`show_title` runs inside the same transition — `title_shown` follows immediately); payload carries reason — `"key"` (any of the `SPLASH_SKIP_ACTIONS` presses: move_up/move_down/move_left/move_right/action_a/action_b/start) or `"timeout"` (the 2.0s one-shot `SplashTimer`). Key and timer race the same transition and the `_in_splash` flag gates it (title_screen.gd:104-113), so the FIRST one wins and exactly one `splash_closed` fires per splash. |
| `title_shown` | `TitleScreen` | The title phase came up over the COMPOSED title art (the 160x144 stage's gsc/background1.png background + white wordmark plate + textbox_bg2 entry band — the shipped `pokewilds/title_bg1.png` RETIRED by pixel probe, GBC restyle wave 0; spec § Title entries) with the entry list rebuilt and the first entry selected; payload carries has_save (whether boot loaded a save — `game_runtime.has_loaded_save()` threaded through `begin_boot`) and entries (the exact entry-label array: `["CONTINUE", "NEW GAME"]` when a save loaded, `["NEW GAME"]` otherwise). Re-emitted when the player backs out of creation (`CreationScreen.cancelled` → `show_title` — the title is the creation flow's cancel destination). The CONTINUE/NEW-GAME menu is a FLAGGED concretization over the original's NEW/LOAD + save-file list (single-save port; the original's HOST/JOIN multiplayer is NOT ported — `strings.properties:13`). |
| `title_continued` | `TitleScreen` | CONTINUE was activated (only ever present when a save loaded); the screen hides and `continue_chosen` fires — `main.gd._enter_world` then applies the saved avatar, syncs the world, and re-enables input. Payload carries party_size (the loaded session's party count read off `/root/GameRuntime`'s session at emit time — `title_screen.gd:181-184`; 0 if no session is reachable). |
| `title_new_game_chosen` | `TitleScreen` | NEW GAME was activated and the title screen hid (`new_game_chosen` then opens the CreationScreen — a direct signal→method connect in main.gd). Payload is empty. With a loaded save the choice FIRST passes through the MessageBox sibling's save-wipe confirm — the pinned wording "Start a new game? Your current save will be erased." (the exact pause-menu NEW GAME confirm; `X` cancels back to the title, and the shared `confirmed`/`cancelled` signals gate on `_awaiting_confirm` exactly like start_menu, since StorageScreen RELEASE rides the same confirm); without a save there is nothing to erase and the event fires directly (the original asked nothing either). Emitted AFTER the confirm, if any — never before. |
| `creation_confirmed` | `CreationScreen` | The GO! step completed: the summary rows (NAME/PLAYER/SHINY RATE/WORLD SEED) gave way to the faithful "Generating... please wait..." value line (i18n `generating_please_wait`, verbatim) for the 0.6s `GenTimer` beat (FLAGGED: the string is faithful, the duration is invented theater — world generation is instant), then the creation payload committed; payload carries player_name (the NameEntry grid's text — 7×4 A-Z+DEL+OK, cap `PLAYER_NAME_MAX := 8` — falling back to `DEFAULT_PLAYER_NAME` "PLAYER" when the name was never entered or confirmed empty), player_avatar (the AvatarPicker's choice — one of the 24 shipped `pokewilds/player/` sets, default `ben`), shiny_odds (the chosen `SessionState.SHINY_ODDS_CHOICES` ladder value [32, 64, 128, 256, 512, 1024], default 256), and world_seed (the seed typed into the creation screen's in-stage digit row — the SeedPrompt digit-edit semantics ported verbatim, GBC restyle wave 0 — or `-1` = RANDOM: the runtime's world-seed draw stays in charge, the seed_choice contract). `main.gd` answers the same-moment `creation_confirmed` signal with `game_runtime.begin_created_game(creation)` — identity/odds commit BEFORE the pinned `new_game(world_seed)` draw order (the starter shiny draw rides stream index 0 regardless of the odds VALUE, so the ladder shifts no pins). Step order SEED → SHINY RATE → NAME → AVATAR → GO! follows the i18n dump (`strings.properties` shiny_rate/name/player/go); the original's file-name creation step is NOT ported (single-save port — FLAGGED). |
| `world_rebuilt` | `App.Main` | World view rebuilt around the current seed and tile position. |
| `biome_entered` | `WorldView` | Player crossed into a different biome; payload carries biome id and tile. |
| `traversal_blocked` | `App.Main` | Player attempted to move onto a blocked tile; payload carries tile, reason, and required field move. |
| `biome_probe_passed` | `SmokeScenarios` | The `biome_probe` smoke scenario confirmed world-generation invariants (determinism, ring progression, navigable spawn, reachability); payload carries seed, spawn, and reachable count. |
| `menu_opened` | `App.Main` | The start menu became visible. |
| `menu_closed` | `App.Main` | The start menu was closed and control returned to the overworld. |
| `encounter_started` | `GameRuntime` | A wild battle started. |
| `legendary_encounter` | `GameRuntime` | Phase 7 Build 2 (registry-REQUIRED, world-depth.md § Legendaries :51): a static legendary battle started — emitted on the battle-start path (`generate_wild_encounter`'s pending take, the SINGLE owner), DISTINCT from the generic `overworld_mon_spawned` stationary-spawn so the ring gate is provable. Payload carries species_id (one of the frozen seven `LEGENDARY_IDS`), tile `[x, y]` (the anchor — ring ≥ `LEGENDARY_RING_MIN := 60` in the affinity biome), biome (the affinity biome — SNOW/LAVA, climate-field blobs under infinite-world slice 2), ring (the anchor's Manhattan distance from origin — `ring >= LEGENDARY_RING_MIN := 60` is the provable PROGRESSION floor: re-justified in slice 2 as a pure distance gate so no legendary anchors near spawn, no longer a biome-admission band), and battle_kind: `"legendary"` (the first real caller of `music_router`'s `BATTLE_TRACKS["legendary"]` — kind plumbing only; `play_track_path` is headless-gated, so tests never play audio). Re-battle semantics: a white-out leaves the legendary standing + re-battleable with persisted HP/stats (faithful :284/:288, Phase-6 precedent); a KO/capture removes it gone-for-good PER-INSTANCE via the v4-additive ANCHOR-keyed `legendary_removals` key (flagged PORT DECISION inverting wiki :224 — the original's stationary legendaries ARE re-battleable after a KO; infinite-world slice 3 re-keyed removals per-instance so repeating lairs each track their own) — stamp-time suppression re-derives from that set. Non-legendary forced battles (guardians, mons) never emit this. |
| `battle_finished` | `BattleRuntime` | A battle ended with a final outcome payload. |
| `attack_animation_played` | `BattleView` | A battle move animation played (source set or synthesized fallback); payload carries move_id, anim_key, frames, sound, fallback. |
| `battle_anim_passed` | `SmokeScenarios` | The `battle_anim` scenario verified a scripted animated move plays its frame set with sound and resolves its turn. |
| `display_matrix_passed` | `SmokeScenarios` | The `display_matrix` scenario verified battle content renders without scale degradation across a matrix of window sizes (block-uniformity plus round-trip consistency); payload carries sizes_checked and max drift percent. |
| `save_written` | `GameRuntime` | Runtime state was written to disk. |
| `field_move_used` | `GameRuntime` | A harvest field move cleared or dug a tile; payload carries move_id, tile `[x, y]`, and `yield` (the base yielded item id). The base yield is unchanged by the Phase-5 dig bonus (below) — a dig emits this for its dry soil/sand FIRST, then `dig_item_found` for any bonus find. |
| `dig_item_found` | `GameRuntime` | A Dig harvest turned up a bonus item alongside its base yield (Phase 5 stone acquisition; `harvest_runtime.harvest_tile`'s dig branch via `harvest_resolver.bonus_for`). Payload carries tile `[x, y]`, item_id (the bonus item — a stone for the pooled biomes, or big_pearl/clear_glass/revive on the beach), and biome (the tile's biome key string). The bonus is a PURE step-counter draw (`harvest_resolver._dig_draw` over total_steps + tile, an integer SplitMix — NO shared `_rng` consumed, the night_system determinism guarantee holds), gated by `DIG_BONUS_RARITY := 8`, so most digs emit only `field_move_used` and no `dig_item_found`. FAITHFUL only for biome SAND (the beach — `fresh-beach.md` "## Dig Items" pool `{big_pearl, water_stone, clear_glass, revive}`, exact wiki order); GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP pools are flagged PORT DIVERGENCES (the scrapes cite no non-Beach stone source); PLAINS has no pool (dry_soil-only, so a PLAINS dig never emits this). ice_stone/dawn_stone are unassigned (SNOW is not diggable). Witnessed by `field_moves_flow`'s dig group (`field_moves_dig_checks.gd`). |
| `field_move_scenario_passed` | `SmokeScenarios` | The `field_move` smoke scenario confirmed a cut-gated tile cleared and stayed walkable after a save round-trip; payload carries move id, tile, yield, and save check. |
| `harvest_flow_passed` | `SmokeScenarios` | The `harvest_flow` scenario verified harvest refusal without capability, cut/dig/smash yields, cleared logic, and save/reload persistence; payload carries the three tiles and save check. |
| `build_mode_entered` | `BuildRuntime` | Build mode opened on a target tile with a default-selected structure; payload carries tile `[x, y]`, structure_id (the default selection), and biome. |
| `structure_placed` | `BuildRuntime` | A structure was placed on a tile (materials already consumed); payload carries structure_id, tile `[x, y]`, and biome. |
| `structure_refused` | `BuildRuntime` | A placement was refused and consumed nothing; payload carries structure_id, tile `[x, y]`, and reason — one of `not_capable` (no Build-capable party member), `missing_materials` (bag cannot cover `cost_for`), `tile_occupied` (tile already carries a placement), `not_placeable` (non-walkable tile or a prop still present), `would_trap` (the would-trap guard: a flood-fill from the player tile treating the candidate tile as unwalkable — the `reachable_walkable_count` `blocked` seam, a hypothetical stamp that never mutates the live placements map — proved the solid placement would leave the player walled in on their own tile; pens enclosing mons are intentional, only PLAYER-tile reachability is guarded), or `cap_reached` (the shared clears+placements 10k override cap). |
| `materials_consumed` | `BuildRuntime` | The bag was charged for a placement; payload carries structure_id, tile `[x, y]`, and items `{item_id: count, ...}` (the exact `cost_for` amounts removed via the session item API). |
| `structure_demolished` | `BuildRuntime` | A built structure was demolished (Cut on log/dry_soil-cost structures, Smash on hard_stone-cost shells — the faithful original refund rule); payload carries structure_id, tile `[x, y]`, and refund `{item_id: count, ...}` (the full `cost_for(id, biome)` returned to the bag). The PLACEMENT entry alone is removed — the tile's clear (if any) persists by the two-map v3 design, so demolished ground reverts to cleared ground, never a respawned prop. |
| `materials_refunded` | `BuildRuntime` | The bag was refunded for a demolition; payload carries structure_id, tile `[x, y]`, and items `{item_id: count, ...}` (the symmetric `materials_consumed`; the exact `cost_for` amounts added via the session item API). |
| `placement_flow_passed` | `SmokeScenarios` | The `placement_flow` scenario verified capability/material refusals, wall+door placement with exact `structure_placed`/`materials_consumed` payloads, occupancy blocking pathing (wall non-walkable + step rejected, door walkable + step accepted), save/reload persistence of the `structures` map, the would-trap guard (the enclosing fourth wall of a four-sided self-trap refused `would_trap`), the build->demolish->refund round-trip (exact refunds, clear fact intact), and the load-bearing material->demolition witness invariant (every shell class has a cost material only its demolish move yields — the region-seal escape); payload carries wall_tile `[x, y]`, door_tile `[x, y]`, save_ok, and demolish_ok. |
| `campfire_lit` | `App.FieldActionRouter` | A placed campfire's light state toggled (the CampMenu Extinguish/Light entry, dispatched by the field-action router's toggle callable); payload carries tile `[x, y]` and lit (the NEW state — false = extinguished). The state rides an additive `"lit": false` field on the `structures` save entry (absent = lit, so Phase-1 campfires load lit); the toggle mutates the generator's live placement entry, mirrors `world_overridden(tile)` so the light rendering refreshes, and saves via the existing save path. An extinguished campfire is still a crafting station but emits no light. Torches are always lit and never emit this event. |
| `rested` | `CampingRuntime` | The party rested (sleeping bag from the bag, or interact on a placed bed); payload carries kind (`"bag"` / `"bed"`), tile `[x, y]` (the rest site), minutes_advanced, woke_at (the post-rest `time_of_day_minutes` — night rest advances to the next 07:00 `WAKE_MINUTES`, day rest advances `REST_DAY_MINUTES` 4h), healed (members whose HP rose), and revived (fainted members brought back). Bag rest heals `ceil(max_hp * 0.5)` per member with NO status cure (faithful: the bag revives but does not cure live status); bed rest is `heal_party_full()` semantics (full HP + status cure + revive). |
| `campsite_established` | `CampingRuntime` | Resting wrote the campsite anchor (`session.campsite_tile = session.player_tile`); payload carries tile `[x, y]` and kind (`"bag"` / `"bed"`). The overflow-mon hold (`mon_relocated`) and the blackout return (battle defeat → campsite) both read this anchor; pre-first-rest it defaults to spawn (`session_state.reset_for_new_game`). |
| `item_crafted` | `CraftingRuntime` | A recipe was crafted at a station (every ingredient consumed, the output granted — all-or-nothing, checked before the first removal); payload carries output_id, station (`"campfire"`), and ingredients `{item_id: count, ...}` (the exact recipe amounts removed via the session item API). |
| `craft_refused` | `CraftingRuntime` | A craft attempt was refused and consumed nothing; payload carries output_id, station (the faced station id, `""` for `no_station`), reason — one of `no_recipe` (no such recipe), `no_station` (no crafting station faced), `wrong_station` (the recipe needs another station — Great Ball at a campfire is refused "That needs a kiln.", the kiln gate), or `missing` (the bag cannot cover the recipe) — plus missing `{item_id: need, ...}` (the shortfall; empty unless `missing`). |
| `material_dropped` | `GameRuntime` | A battle victory/capture granted its interim type-drop material (100% on a type-matched species, pure/no-rng — `material_drops.drop_for` walking the defeated species' types primary→secondary); payload carries species_id and item_id (one of magnet/hard_shell/metal_coat/silky_thread/soft_feather; the table NEVER contains log or hard_stone — the demolition-witness invariant the region-seal escape in building-and-placement.md rides on, asserted via `crafting_runtime.drop_witness_clean` (`is_drop_material`) on every `craft_flow` run). The documented SECONDARY material source since Phase 5: the faithful happy-Pokemon habitat drops (`item_dropped`, breeding-shinies-drops-fishing.md) are the PRIMARY; this battle-end table is KEPT as the secondary source, and the witness audit extends over BOTH tables (neither ever yields log or hard_stone). |
| `night_hazard_spawned` | `NightSystem` | Emitted by the night system (during `game_runtime._pick_encounter_species`'s delegation to `night_system.try_ghost_species`) when a night (minutes >= 1230 or < 270) draw without active light (no lit campfire/torch within Manhattan 4, no Flash-capable Fire-type party member) rolls ghost (50% of the light-less night cadence, `GHOST_CHANCE`, on the shared `_rng`); payload carries species_id (a battle-viable Ghost-type from the sorted type-derived pool), tile `[x, y]`, and minutes. The draw marks a PENDING shadow battle — the next `start_wild_battle` resolves it via `night_system.begin_battle(wild_mon)` (a non-ghost encounter clears the mark) so `retreat_blocked` can fire. Day and lit draws consume no rng, so daytime encounter streams are byte-identical to pre-Phase-2 behavior. |
| `retreat_blocked` | `NightSystem` | A run attempt in a shadow battle was refused by the injected `retreat_check` gate (`battle_runtime.run_from_battle` → `game_runtime._retreat_allowed` → `night_system.retreat_allowed()`); payload carries species_id. Emitted ONCE per shadow battle (the block lasts the battle's duration — time freezes mid-battle in the port, so "until dawn" ports as "for the whole shadow battle"; the escapes are victory, capture, or blackout → campsite). |
| `evolution_time_gate` | `BattleRuntime` | The post-battle evolution check ran with real time context (the caller-side fix for the Espeon/Umbreon/Frosmoth gates); payload carries species_id, time_of_day (`"DAY"` / `"NIGHT"` from `session.time_of_day_label()` — the one-line delegation to `DayPhase.time_of_day_label`; dawn/dusk/day all map to DAY), and evolved (the evolution target id, or `""` when no evolution fired). Emitted on EVERY `_try_evolve` call so the gate is proven in both directions (DAY + no evolve, NIGHT + evolve). `pokemon_rules._time_requirement_met` stays fail-open on EMPTY context (legacy); future callers of `check_level_evolution` MUST pass `time_of_day`. |
| `camp_survival_passed` | `SmokeScenarios` | The `camp_survival` scenario verified the rest/heal/campsite/blackout loop: bag rest (50% HP, PSN retained, fainted revived, `rested{kind:"bag"}`, `campsite_established`), bed rest after crafting soft_bedding + placing the bed (full heal + PSN cleared, `rested{kind:"bed"}`), blackout returning to the campsite (player_tile == campsite_tile, explicitly != world origin when the campsite != ZERO), and a save round-trip preserving campsite + bag; payload carries bag_ok, bed_ok, blackout_ok, campsite `[x, y]`, and save_ok. |
| `craft_flow_passed` | `SmokeScenarios` | The `craft_flow` scenario verified campfire crafting end to end: the three refusals (`no_station`, `missing` with the missing list, `wrong_station` for Great Ball at a campfire — the kiln gate), poke_ball/soft_bedding/old_rod crafts with exact bag deltas and `item_crafted` payloads, `craftable_at_station("campfire")` == exactly the 5 recipes, `can_afford("bed")` true after soft_bedding, and crafted counts surviving a save round-trip; payload carries refused, crafted, station_count, and save_ok. |
| `night_cycle_passed` | `SmokeScenarios` | The `night_cycle` scenario verified the night danger loop: dark draws spawn >= 1 Ghost-type with `night_hazard_spawned`; a lit campfire, an extinguish-then-relight contrast, a torch, and a Flash-capable (Fire) party member each zero the ghosts; the shadow battle blocks retreat once (`retreat_blocked`, run refused) then ends by victory; dawn clears the hazard; `filter_species_ids` SAVANNA-night carries UMBREON while day excludes it (fallback: the night pool strictly contains a Ghost-type the day pool lacks); and `DayPhase.is_night` agrees with the world_view tint keyframes at 269/270/1229/1230; payload carries dark_ghosts, lit_clean, torch_clean, flash_clean, shadow_blocked, dawn_clean, umbreon_night, and boundary_ok. |
| `time_evolution_passed` | `SmokeScenarios` | The `time_evolution` scenario verified the time-of-day evolution gate DISCRIMINATES (fires and blocks on the real clock): EEVEE (happiness 255) carries the contrast — the same seeded level-up evolves to ESPEON at DAY (`evolution_time_gate{time_of_day:"DAY", evolved:"ESPEON"}` — its MORNDAY entry passes in the DAY window, since "MORNDAY" contains "DAY"; verified against the parsed asm) and to UMBREON at NIGHT (`evolution_time_gate{evolved:"UMBREON"}`); SNOM carries the literal block/fire pair on the identical TR_NITE gate — its NITE-only evolution leaves species SNOM at DAY (`evolved:""`) and evolves to FROSMOTH at NIGHT. Payload carries day_species (ESPEON), night_species (UMBREON), snom_day_species (SNOM), snom_night_species (FROSMOTH), and gate_events. |
| `visual_sweep_camping_passed` | `SmokeScenarios` | The `visual_sweep_camping` scenario captured its deterministic camping shots (15_camp_night_lit / 16_craft_menu — numbered after the building shots; shot 17 (`17_dawn_after_rest`) is formally RETIRED — camping-reserved, never committed, the sole whitelisted numbering gap in the single-sourced shot-range registry in `visual_sweep_baselines.gd`; the leading digits 09-12 stay reserved for battle shots by the sidecar canary contract) and matched baselines within threshold (or updated them in update mode); payload carries shots, compared, mismatched, max_drift_pct, and mode. `light_layer` is a generated STATIC gradient (no per-frame animation) and all placement is seeded grant+try_place, so the shots are byte-stable; the existing 04_night/05_dawn baselines carry no lit structures, so `light_layer` draws nothing there and their bytes are unchanged. |
| `playtest_journey_passed` | `SmokeScenarios` | The `playtest_journey` scripted playthrough completed the full loop (new game, steps, battle, menu, save round-trip); payload carries outcome, steps, party size, and save check. |
| `playtest_soak_passed` | `SmokeScenarios` | The `playtest_soak` seeded bot completed its iteration budget with all invariants holding; payload carries seed, iterations, steps, battles, and outcome counts. |
| `nav_audit_passed` | `SmokeScenarios` | The `nav_audit` scenario verified traversal agreement (blocked/walkable tiles and field-move gates), battle menu reachability and model/cursor consistency, and start-menu navigation; payload carries per-area check counts. |
| `nav_audit_failed` | `SmokeScenarios` | The `nav_audit` scenario's total exit contract (symmetric with `nav_audit_passed`, emitted alongside `push_error`): a non-pass ALWAYS names its cause; payload carries the `failures` array so a red can never surface as a blank "missing required events". Any trace event ending in `_failed` is collected by both harness transports (run_playtests.py TraceCollector / godot_dap_smoketest.py failed_event_entry) as a named failure — the honest-reporting mechanism landed by miss-002. |
| `input_gate_passed` | `SmokeScenarios` | The `input_gate` regression scenario proved BOTH same-press double-fires dead, driven by REAL injected input (press and release in separate frames via `Input.use_accumulated_input`, flushed in the real input phase BEFORE `_process` — the exact race frame; `smoke_scenarios._press`'s same-frame press+release can never fire a poll): (a) Enter with the camp menu open closes ONLY the camp menu — the start menu does NOT open, no `menu_opened` since the cursor, the avatar is re-enabled with no menu layered on top, and the no-dual-control invariant `not (start_menu.visible and player.input_enabled)` holds; (b) Z-on-Demolish demolishes the campfire (tile walkable, `structure_demolished`) and does NOT re-fire harvest/build on the bare former tile — build mode never opens (`structure_layer.is_active() == false`), no `structure_placed`/`materials_consumed`/`field_move_used` since the cursor, and the bag delta equals exactly the refund. Every injected key carries an injection witness (an observable state change only `_unhandled_input`/poll delivery can produce), so a delivery degrade fails red rather than passing vacuously; payload carries enter_only_close, demolish_no_refire, and seed. |
| `input_gate_failed` | `SmokeScenarios` | The `input_gate` scenario's total exit contract (symmetric with `input_gate_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array (the first-failure latch list) so a red names which half of the race re-opened. |
| `battle_end_input_passed` | `SmokeScenarios` | The `battle_end_input` regression scenario proved the battle-END same-press leak dead (the generalized latch's battle arm, `input_router.note_press_consumed` set from `main._on_battle_finished`), driven by REAL injected input (press and release in separate frames via `Input.use_accumulated_input` + `smoke_tap.gd` — the exact race frame; `smoke_scenarios._press`'s same-frame press+release can never fire a poll): (A) a Z on RUN facing a cut-harvestable tile ends the battle (`battle_finished{outcome: escaped}`) and does NOT re-fire the overworld context action the same frame — no `field_move_used`/`structure_placed`/`materials_consumed` since the cursor, the tree stands, the "Got away safely!" toast survives, the bag is unchanged; (B) a fresh deliberate Z on a LATER frame still harvests the faced tree (`field_move_used` — the latch is reset by `poll_context_action` unconditionally, so the fix never over-suppresses past the battle-end frame); (C) a ball-select Z on a guaranteed-capture mon (1 HP + asleep + catch_rate ≥ 192, deterministic) ends on capture (`battle_finished{outcome: caught}`) with no re-fire — the second tree stands and the "Gotcha!" toast survives. Every tap carries an injection witness (battle view opened/closed, RUN/POKE-BALL row selected, outcome traced), so a delivery degrade fails red rather than passing vacuously; payload carries escape_no_refire, deliberate_press_harvests, capture_no_refire, and seed. |
| `battle_end_input_failed` | `SmokeScenarios` | The `battle_end_input` scenario's total exit contract (symmetric with `battle_end_input_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array so a red names which battle-end race re-opened (escape re-fire, over-suppressed deliberate press, or capture re-fire). |
| `texture_audit_passed` | `SmokeScenarios` | The `texture_audit` scenario verified every battle sprite (frame-crop shape/ink) and every overworld tile/prop texture (keying, no opaque borders) through the real loaders; payload carries species, frame, and tile counts. |
| `data_audit_passed` | `SmokeScenarios` | The `data_audit` scenario verified encounter-pool battle viability (sprites, catch rate, learnset, known types), instance integrity across levels, and starting-bag catalog resolution; payload carries biome, species, and instance counts. |
| `layout_audit_passed` | `SmokeScenarios` | The `layout_audit` scenario verified worst-case label fit and cursor/row alignment across battle, start menu, party, and bag screens; payload carries label, cursor, and screen counts. |
| `world_consistency_audit_passed` | `SmokeScenarios` | The `world_consistency_audit` scenario verified tile logic/render/collision agreement plus spatial, z-order, and tall-grass contracts around spawn; payload carries tiles_checked, movement_checked, spatial_checked, failures. |
| `ui_render_audit_passed` | `SmokeScenarios` | The `ui_render_audit` scenario verified expected strings, label overlap, and cursor pairs against the art-anchored render model across battle and menu states; payload carries states_checked, labels_checked, cursors_checked, quarantined. |
| `text_oracle_passed` | `SmokeScenarios` | Coded tier: the `ui_render_audit` pixel half's glyph template oracle (`scripts/app/text_oracle.gd`) rasterized the ANCHOR-mode expected strings (TextServer glyph-atlas readback; the box-mode `battle_message` string is lint-verified, never glyph-matched) and XOR-matched them against the engine readback with zero `glyph_mismatch` findings across the checked states (the event is gated on every battle spec verification-complete — `states_checked == specs.size()`, where a state counts only when its mask readback succeeded or it had nothing to rasterize — AND `glyphs_checked > 0`, so an empty/blank readback mask — zero glyphs verified — cannot mint the clean-run signal, and a mid-run double readback failure leaves its state uncounted, withholding the pass rather than minting it from partial coverage; that state's lint evidence still runs); payload carries states_checked (verification-complete states), strings_checked, glyphs_checked, and the calibrated XOR tolerance (T_str = 2 px/string, T_glyph = 1 px/glyph — derived from the measured duplicate-run raster noise floor; see vision-fidelity.md § Legibility oracles). Emitted in addition to `ui_render_audit_passed` as the clean-run signal; never blocks a `quarantine_finding` (the defect signal) from firing in the same run. |
| `quarantine_finding` | `SmokeScenarios` | A quarantined heuristic pixel check reported a possible visual defect; payload carries state, kind (`low_ink`/`forbidden_ink`/`garble`/`lint_unavailable`, and from Slice 4 `glyph_mismatch` — a per-glyph template XOR over the calibrated tolerance — plus `clipped`, payload additionally carrying text and glyph_index), and region. Never fails a scenario until graduated — and from Slice 4 graduation is PER STATE: a finding in a state whose `GRADUATED_STATES` key is true fails the scenario red (`ui_render_audit`), while non-graduated states stay quarantine-tier; menu/party/bag states never graduate (default-theme font at 12/14/20 — a different raster family than fonts.ttf@7, for which the equivalence proof does not hold). The playtest runner additionally records kinds `region_drift` (non-red per-region drift and unexplained change clusters from the region diff), `contrast_low` (WCAG rendered-pixel contrast below 4.5:1 from tools/contrast_check.py), `cvd_collapse` (Machado-2009 color-vision-deficiency palette collapse from tools/cvd_sim.py — quarantine-forever accessibility evidence, never red), and from Slice 5 `vision_review` (grounded Lane-4 findings from tools/vision_review.py — the deterministic sidecar-consistency default reviewer or a --reviewer-cmd plugin; every emitted finding cites a sidecar region id, intersects its bbox, and carries the `finding_id` Slice-6 graduation-ledger join key — quarantine-forever, never red; only a vision_review tool ERROR fails red, fail-closed), and from the art-anchor slice `anchor_drift` (a programmatic-on-baked node whose LIVE `draw_order` stage-px rect violates its source-art anchor `stage_rect` from `docs/registry/art-anchors.toml` — reviewer_kind `deterministic-art-anchor`, the first ART-TRUTH check class, grounded on the `anchor:<id>` region; quarantine-tier at first and graduating to coded RED PER-ANCHOR via the graduation ledger — determinism proof + clean streak + a recorded seeded plant; the baseline-regeneration refusal gate additionally fails RED on an anchored baseline rewrite, separately from this quarantine signal — see `anchor_gate_refused` below) in the playtest-report quarantine section — report-tier, not emitted in the JSONL trace. The art-anchor/VLM slice also adds an ADVISORY `rubric_coverage_gap` (a shot-group whose rubric questions have no fresh capable reviewer pass — counted WITH reason, e.g. overworld "no fresh reviewer of kind [model-qwen3-vl] ran; zero groundable regions"; never red, never flips ok; see `vision_review_written` below). |
| `vision_review_written` | report (NOT JSONL) | Not a trace event — a playtest-report.json REPORT FIELD recorded by the runner's `apply_vision_review` post-step on the visual_sweep scenario entry (Slice 5, Lane 4): `{path, shots_reviewed, shots_changed, findings, grounded, dropped, reviewer}` summarizing the `.godot-smoke/vision-review.json` the post-step regenerates on every visual_sweep compare run (schema vision-review/2; deterministic default reviewer, no model). `shots_reviewed` counts the shots COVERED by the review manifest (every baselined shot, changed or not — the manifest is the freshness authority); `shots_changed` counts the subset whose fresh bytes differed from baseline (the shots actually re-reviewed; 0 on a zero-drift sweep — the per-shot `changed` flags inside the review file are the granular source). Never emitted in the JSONL trace; kind-`vision_review` quarantine entries carry the per-finding detail and `finding_id` join keys (see `quarantine_finding` above and vision-fidelity.md § Lane 4 automation). From the art-anchor/VLM slice the same post-step ALSO records a `rubric_coverage` block (schema `rubric-coverage/1`: `reviewer_kinds_ran`, `totals {questions_total, questions_answered, unanswered, unassigned, groups_with_gap}`, and per-group `{answered:[{id,text,by}], unanswered:[{id,text,reason}], needs_reviewer_kinds}`) and the reviewer may be the composite `tools/vlm_reviewer.py` (Qwen3-VL 8b, reviewer_kind `model-qwen3-vl`, quarantine-forever WITNESS that answers the rubric judgment + non-baked-UI questions, wired via the env-gated `--reviewer-cmd`); findings (deterministic and model) ground on the new `anchor:<id>` art-truth regions. The unanswered count is HONESTY, not failure — `verify_all` surfaces a `rubric_coverage_gap` WARN that degrades under `--skip-windowed`, and `review_is_fresh` covers the block because it rides the sha256 manifest. |
| `anchor_gate_refused` | report (NOT JSONL) | Not a trace event — a playtest-report.json REPORT FIELD plus a RED run refusal recorded by the runner's `apply_anchor_gate` post-step in `tools/run_playtests.py` (the ONLY post-step that runs in `visual_sweep` `update`/`auto_update` mode): when a baseline regeneration would write a baseline whose anchored node (`draw_order` stage rect) violates its source-art anchor `stage_rect` (`docs/registry/art-anchors.toml`, ±`tol_px`, stage-to-stage with no mapping), the gate RESTORES the prior baseline PNG + sidecar bytes (snapshot/restore) and fails the run RED ("baseline regeneration refused: EnemyHPBar [43,18,48,4] violates anchor battle/enemy_hp_track [32,18,48,4] — fix the alignment, not the baseline"). This structurally prevents a misalignment from being frozen into a baseline — the exact hole that held the 11px HP-bar defect green for 3+ days. Fires regardless of the completeness gate's arming (a wrong baseline is worse than a missing one). Never emitted in the JSONL trace; the live drift itself rides the `anchor_drift` quarantine kind above. |
| `snapshot_captured` | `App.SnapshotCapture` | A windowed capture passed the validity oracle; payload carries shot, shot_seq, ts_msec, trace_cursor (join key into user://logs/agent_trace.jsonl — the record lands at cursor+1), window [w, h], renderer, godot_version, and sidecar_path (the canonical JSON sidecar written next to the PNG, e.g. res://.godot-smoke/shots/09_battle.png.sidecar.json; "" when the capture wrote none — ui_render_audit/display_matrix pass no metadata). The sidecar mirrors this payload's ts_msec and trace_cursor; schema and join protocol in docs/references/snapshot-sidecar.md. |
| `capture_invalid` | `App.SnapshotCapture` | A capture failed the validity oracle or a duplicate pair differed; payload carries shot, kind (blank/uniform/magenta/undersize/headless/nondeterministic_pair), classification (transport/regression — magenta is always regression), luminance (0.0-1.0), and detail with the identified cause. Warning tier: never fails a scenario on its own. |
| `visual_sweep_passed` | `SmokeScenarios` | The `visual_sweep` scenario captured its deterministic screenshot set and matched baselines within threshold (or updated them in update mode); payload carries shots, compared, mismatched, max drift percent, mode, window, dup_checked, sidecar_paths (res:// sidecar path per shot), and invalid_captures (captures that failed the validity oracle — structurally 0 on a passing sweep, mirroring `validity.uniform`: every invalid capture appends a failure that stops the sweep before this trace fires, so the field reads as a pass-time invariant while the `capture_invalid` trace carries the per-capture count and detail). The per-region verdict fields region_failures / clusters_explained / clusters_unexplained / region_artifacts are NOT emitted in the trace — they are recorded by the playtest runner into .godot-smoke/playtest-report.json (the region diff is a runner post-step, never in-engine; see vision-fidelity.md § Explainable per-region diff). |
| `mon_relocated` | `BattleRuntime` | A full-party capture relocated the overflow Pokemon to the player's campsite instead of losing it (the non-losing campsite HOLD; retrievable from the party screen); payload carries species_id, name, level, and campsite `[x, y]`. |
| `mon_retrieved` | `GameRuntime` | The player retrieved a campsite-held Pokemon into the party via the party screen RETRIEVE action; payload carries species_id, name, level, campsite `[x, y]`, and party_size. |
| `box_opened` | `StorageRuntime` | A placed storage box's contents were opened via the storage screen — the overworld context `Z` route through `field_action_router`'s `BOX_ID` arm (the router disables the avatar, then the screen's `closed` signal re-enables it + saves), which `storage_flow` drives through the real seam (`visual_sweep_storage` opens the screen directly for its capture); payload carries tile `[x, y]` (the box's placement tile — the box identity) and count (contents length at open). |
| `mon_deposited` | `StorageRuntime` | A party Pokemon was deposited into a storage box (MANUAL — the storage screen's party-side DEPOSIT or the party-screen DEPOSIT-to-nearest-box action); payload carries tile `[x, y]` (the box), species_id, name, level, party_size (the party count AFTER the move), and box_count (the box contents count AFTER the move). The mon dict moves party→entry `contents` ON the placement entry (save schema v4 additive field; absent = empty), so the box and its contents can never desync. |
| `mon_withdrawn` | `StorageRuntime` / `BreedingRuntime` | A boxed Pokemon was withdrawn into the party (the storage screen's box-side WITHDRAW); payload carries tile `[x, y]` (the box), species_id, name, level, party_size (AFTER the move), and box_count (AFTER the move). Refused `party_full` at party size 6 — the no-loss contract: a withdrawal can never strand a mon (it either lands in the party or stays in the box). Since Phase 5 the SAME event is emitted by `BreedingRuntime` for a PEN withdrawal (overworld `Z` facing a pen interior with no ground eggs — the most-recently penned mon returns): that variant's payload carries pen (the `"x,y"` anchor key), species_id, name, level, party_size (AFTER), and pen_count (pasture mons AFTER) — `party_full` is refused via `breeding_refused`, so the penned mon is never lost. |
| `mon_released` | `StorageRuntime` | A Pokemon was released PERMANENTLY, after the MessageBox sibling CONFIRM (the 0.2 destructive-action contract; the runtime executes confirm-agnostic, confirmation is UI-owned); payload carries source (`"box"` / `"party"`), tile `[x, y]` (the box tile on a box release, the current player tile on a party release), species_id, name, and level. Reachability: the wired UI only ever emits `"box"` (the storage screen's confirm-gated RELEASE); `"party"` is a runtime capability (`storage_runtime.release_from_party`) with NO wired UI caller yet — party-release is deferred (spec: storage-and-party.md § Documented deviations), so the reachable source set today is `{"box"}`. Documented deviation: the original "drops" a released mon into the world ("no guarantee a dropped Pokemon will stick around", `.firecrawl/lchan-review.md:45`); with no overworld-mon entities until Phase 6, release is permanent removal — hence the double-emphasized confirm wording. |
| `storage_refused` | `StorageRuntime` | An open/deposit/withdraw/release was refused and changed nothing; payload carries action (`"open"` / `"deposit"` / `"withdraw"` / `"release"`), tile `[x, y]`, reason — one of `no_box` (no storage box at the tile), `no_such_mon` (index out of range), `last_party_member` (depositing the party's only mon — "You can't deposit your last Pokemon."), `party_full` (withdraw at party size 6), or `would_strand_demolition` (the dynamic demolition-witness guard: with structures standing, removing this mon would leave the party with no carrier of a required demolish move — `Structures.demolish_move_for` over the live placements; the party pool counts, never box contents) — plus species_id. `"open"` is the `open_box` `no_box` refusal (`storage_runtime.open_box` on a tile with no box); it fires on EVERY `ui_render_audit` run (the audit opens the origin tile, which carries no box, to capture the empty two-column state) and via `storage_flow`'s non-box-tile refusal check. Never silent, mirroring `structure_refused`/`craft_refused`. |
| `demolish_refused` | `BuildRuntime` | A demolition was refused with the placement intact and nothing refunded; payload carries structure_id, tile `[x, y]`, and reason — `box_not_empty` (a storage box with non-empty contents cannot be Cut — "The storage box isn't empty. Take your Pokemon out first."; faithful to `.firecrawl/pokewilds-build.md:38`'s filled-chest rule, which the original states across same-tile chests on upper/lower floors and the single-floor port reduces to this per-box check; the check runs AFTER resolving `structure_id == "storage_box"` and BEFORE `remove_placement`, so contents can never be destroyed by demolition). An EMPTY box demolishes through the existing path (`structure_demolished` + `materials_refunded`). |
| `storage_flow_passed` | `SmokeScenarios` | The `storage_flow` scenario verified the storage box + party-management loop end to end: box placement + open (`box_opened`), deposit/withdraw with exact payloads and party/box deltas, the confirm-gated release on BOTH branches (cancel leaves the mon in actions; confirm emits `mon_released`), every `storage_refused` reason (`last_party_member`, `party_full`, `no_such_mon`), the non-empty Cut refusal (`demolish_refused{box_not_empty}`, refund untouched) then the empty-box Cut (2 Log refund, tile open ground), box INDEPENDENCE (a mon deposited in one of two side-by-side boxes never appears in the other, in the world AND across a save round-trip), save persistence (contents survive load; a corrupt `contents` normalizes to an empty box, never a crash), the overflow NO-ROUTING proof (full-party capture → campsite hold `mon_relocated`, never a box), the dynamic witness guard (last-Cut-carrier deposit refused with a wall standing; demolish the wall → the deposit proceeds), and the party reorder (MOVE commits the new order; cancel restores the snapshot); payload carries booleans per check — box_ok, deposit_ok, withdraw_ok, release_ok, refused_ok, independence_ok, save_ok, overflow_ok, witness_ok, reorder_ok — and seed. |
| `storage_flow_failed` | `SmokeScenarios` | The `storage_flow` scenario's total exit contract (symmetric with `storage_flow_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array so a red can never surface as a blank "missing required events". |
| `storage_mouse_fallback` | `SmokeScenarios` | A degrade-only diagnostic from the `storage_release_mouse_check` group (the S2 release-confirm mouse-bypass regression): the synthesized LEFT mouse click on the action list did NOT deliver `item_clicked` under the current transport (headless GUI hit-testing unavailable), so the check fell back to a direct `_on_entry_clicked` call — which still proves the gate, since the direct call IS the handler the click invokes. Never fires in a passing run where the click hit-tests (the synthesized click did land under headless in the green runs); traced so the degrade is honest, never silent. Not a required event. |
| `visual_sweep_storage_passed` | `SmokeScenarios` | The `visual_sweep_storage` scenario captured its deterministic storage shot (`18_storage_box` — numbered after the camping shots; the leading digits 09-12 stay reserved for battle shots by the sidecar canary contract) and matched baselines within threshold (or updated them in update mode); payload carries shots, compared, mismatched, max_drift_pct, and mode. The shot is seeded grant+try_place with the storage screen open at a fixed box (no rng in the capture path), so it is byte-stable; the existing baselines carry no boxes, so their bytes are unchanged. Windowed-only; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1` like `visual_sweep_camping`. |
| `save_recovery` | `SaveStore` | A save could not be used and a fresh start was recovered non-destructively; payload carries reason (`corrupt` / `absent`) and preserved_path (the `.corrupt.bak` the corrupt file was moved to; `""` when absent). Corrupt is warning-tier, absent is event-tier. |
| `save_migration_passed` | `SmokeScenarios` | The `save_migration` scenario verified the FULL migration ladder (v1/v2→v3→v4→v5→v6 field migration — infinite-world slice 1: a CHAIN-LESS v5 flattens LOSSLESSLY to v6, delta == {version: 6}) and the non-destructive refusal of a future version PLUS a truly CHAINED v5 (`SaveMigration.can_represent_infinite` false — a non-origin active world or any beyond-origin `chained_worlds` state is unrepresentable on one seamless plane; the production load path preserves it to `.chained.bak` and starts fresh); payload carries v1_ok, v2_ok, v3_ok, v4_ok, v5_ok, future_refused, chained_refused, and checks. |
| `flash_lit` | `FieldMoveRuntime` | The active Flash field move lit the area around the player (the Phase-4 caller for the move that was previously a passive capability READ only); payload carries tile `[x, y]` (the player tile the light centers on), radius (Manhattan `LIGHT_RADIUS` = 4 — "the same range as a campfire", the pinned fidelity contract), and species_id (the Flash-capable Fire-type the move was invoked on). The faithful effect stays PASSIVE (a Fire-type party member is a traveling light per `night_system._party_has_flash`; the original "can't be selected like Cut"), so this trace proves the caller + light DELTA, never a divergence from the campfire range. |
| `waystone_registered` | `FieldMoveRuntime` | A Way Stone was placed/registered as an intra-world warp point (the new `way_stone` structure, `structures.gd`); payload carries tile `[x, y]` (the stone's placement tile — the registry key). The registry IS the placed `way_stone` entries themselves — they ride the `structures` save key (no separate `session_state` key), so a stone persists for free as a placement (`field_move_runtime.way_stone_tiles` reads them back in registration-step order). Way Stones are Phase-4 INTRA-world warp targets — NOT the Phase-7 world-edge Teleport Beacons (themselves RETIRED with world chaining in infinite-world slice 1 — way stones are the ONLY warp points on the seamless plane; see field-moves.md § Infinite-world slice and infinite-world.md). |
| `teleport_used` | `FieldMoveRuntime` | The player Teleported to a registered Way Stone (PSYCHIC-capable party member); payload carries from `[x, y]` (the origin tile) and tile `[x, y]` (the destination Way Stone — the player tile after the warp, resynced via the runner). Intra-world only; teleporting between worlds rode the Phase-7 Build-3 edge-beacon chaining, RETIRED in infinite-world slice 1 — teleport stays plain intra-world warp on the seamless plane (infinite-world.md). |
| `mount_summoned` | `FieldMoveRuntime` | A Ride-capable party member was mounted/dismounted (explicit `ride = 1` flag); payload carries species_id and mounted (true on summon — the avatar switches to the faster mount speed mode + mount sprite; false on dismount — walk/run restored). Ride state is runtime-only (not saved). Ledge-climbing is out of scope (traversal speed only). |
| `fly_used` | `FieldMoveRuntime` | The player Flew to a VISITED (registered) Way Stone (explicit `fly = 1` flag), reusing the Way-Stone registry; payload carries from `[x, y]` and tile `[x, y]` (the visited destination). Fly REFUSES an unvisited/unregistered stone (no `fly_used`, an empty/refused result) — "to visited way stones only" proven both directions. Edge-fly world chaining LANDED in Phase 7 Build 3 and was RETIRED with world chaining in infinite-world slice 1 — Fly is intra-world only. |
| `overworld_attack` | `FieldMoveRuntime` | The Attack field move fired the overworld-combat HOOK (DARK-capable party member); payload carries tile `[x, y]` and target_species_id (the targeted overworld Pokémon, or `""` when no target is present). Phase 4 wires the hook + trace only — the overworld-Pokémon ENTITIES land in Phase 6, so with no entity present the move refuses gracefully (well-formed payload, no crash) rather than asserting any mon state. |
| `charm_used` | `FieldMoveRuntime` | The Charm field move fired the pacify/recruit HOOK (FAIRY-capable party member); payload carries tile `[x, y]`, target_species_id (or `""` with no target), and level_gate_met (the faithful level-gated pacify — "can prevent the attack altogether if the Pokemon using it is at a high enough level"). Phase 6 consumes the hook; Phase 4 asserts the hook is reached + traced, never any overworld-mon state. |
| `repel_active` | `FieldMoveRuntime` | The Repel field move set the session encounter-suppression counter (POISON-capable party member); payload carries steps (the N steps wild encounters are suppressed for). The counter rides the additive `session_state.repel_steps` key, decrements per step, and short-circuits `game_runtime.generate_wild_encounter`/`_pick_encounter_species` BEFORE consuming encounter rng (so a suppressed draw consumes no rng — structural suppression, deterministic under seed). Documented fidelity gap: the original Repel is a crafted ITEM repelling LOW-LEVEL mons; the port suppresses ALL encounters for N steps (field-moves.md § Repel). |
| `power_used` | `FieldMoveRuntime` | The Power field move pushed a movable boulder one tile (ELECTRIC-capable party member — the smallest strength task); payload carries from `[x, y]` (the boulder's origin tile) and to `[x, y]` (its destination). The boulder is a placements-map prop (structure_id `boulder`, distinct from Smash's destructible `rock_small1.png`), so it renders, blocks traversal, and survives save; the move notifies `world_overridden` so the view re-renders both tiles. |
| `field_move_refused` | `FieldMoveRuntime` | A Phase-4 field move was refused and changed nothing; payload carries move_id (`flash`/`teleport`/`fly`/`ride`/`repel`/`power`/`attack`/`charm`), reason — one of `not_capable` (no capable party member), `no_way_stone` (teleport with no registered way stone), `unvisited_way_stone` (fly to an unregistered tile), `edge_suppressed` (Phase 7 Build 3: teleport/fly attempted inside the world-edge band — `manhattan(player_tile) >= WORLD_RADIUS - TELEPORT_EDGE_MARGIN` — so Teleport can't skip the chain mechanic; `fresh-faq.md:190` "move away from the edge a bit", the numbers flagged invention — **RETIRED in infinite-world slice 1:** the seamless plane has no edge band, so this reason no longer fires), `no_boulder` (power with no faced boulder), `blocked_destination` (power push blocked), `no_target` (attack/charm with no target), `not_placeable`/`cap_reached` (way stone/boulder placement) — and the relevant tile `[x, y]` where applicable. Never silent, mirroring `structure_refused`/`storage_refused`. |
| `field_moves_passed` | `SmokeScenarios` | The `field_moves_flow` scenario verified the eight Phase-4 moves under the `FIELD_MOVES_PARTY` fixture (RHYPERIOR/CHARIZARD/MACHOP/CALYREX/DEDENNE/DRAPION): FIRST the precondition witness (`runtime.party_has_field_move_ability` true for all 13 moves — catalog/AUTO_TYPES drift fails loud, never a vacuous pass), then flash (`flash_lit` + the campfire-equal light delta), teleport↔way stone (`waystone_registered` + `teleport_used` + save survival), ride (`mount_summoned` + the avatar speed-mode change), fly (visited-only `fly_used` + unvisited refusal), repel (structural suppression: control draws ≥1 mon, reseeded repel draws exactly 0, expiry resumes), power (boulder moved one tile + no-boulder refusal + world_overrides survival), and the charm/attack hooks (reached + well-formed trace + graceful no-target refusal), and the Phase-5 DIG ACQUISITION group LAST (`field_moves_dig_checks.gd` — the stone-pool contract witness `stone_pool_contract_clean`, a Beach SAND water_stone hunt where every dig is predicted via `resolver.bonus_for` at `session.total_steps` before it lands (faithful pool, `fresh-beach.md`), the first-found DIVERGENT biome's pool membership pinned to the SHIPPED `DIG_BONUS_POOLS` table read live (a flagged port divergence — never the wiki), and a PLAINS negative control — no `dig_item_found`, all ten stone bags unchanged, dry_soil +1 only); payload carries flash_ok, teleport_ok, ride_ok, fly_ok, repel_ok, power_ok, charm_ok, attack_ok, dig_acquisition_ok, and seed. |
| `field_moves_failed` | `SmokeScenarios` | The `field_moves_flow` scenario's total exit contract (symmetric with `field_moves_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array so a red can never surface as a blank "missing required events". |
| `build_house_passed` | `SmokeScenarios` | The `build_house_flow` scenario verified the building playtest end to end under the `FIELD_MOVES_PARTY` fixture: harvest EARNED through the real resolver (cut→log, dig→dry_soil, smash→hard_stone with exact bag gains + `field_move_used`), a fixed HOUSE_PATTERN stamped (roof cap + wall ring + one door) with per-piece `structure_placed` + exact `materials_consumed` cost drops, occupancy (wall non-walkable + step rejected, door walkable + step passed), and demolish-with-refund (every piece down via `build_runtime.try_demolish`, exact full `cost_for` `materials_refunded`, tile reverted to open ground with its clear fact intact, bag after == bag before build); payload carries harvest_ok, placed, door_walkable, refund_ok, and save_ok. |
| `build_house_failed` | `SmokeScenarios` | The `build_house_flow` scenario's total exit contract (symmetric with `build_house_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array. |
| `playtest_field_soak_passed` | `SmokeScenarios` | The `playtest_field_soak` seeded bot (the `playtest_bot.try_random_field_move` extension) completed its iteration budget under the `FIELD_MOVES_PARTY` fixture with all invariants holding — field moves exercised off the seeded rng (repel sets/decrements its counter, flash lights the tile, teleport/fly return to the registered way stone, ride toggles the avatar speed mode), a no-capable-move roll degrading to a walk (never a soft-lock), interleaved with the existing walk/battle/menu/save bands, save round-trip + spatial_violations == 0; payload carries seed, iterations, field_moves_used, and outcome counts. Self-guarded by `PlaytestBot.backup_save`/`restore_save` (the `playtest_` prefix). |
| `playtest_field_soak_failed` | `SmokeScenarios` | The `playtest_field_soak` scenario's total exit contract (symmetric with `playtest_field_soak_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array. |
| `shiny_rolled` | `GameRuntime` / `BreedingRuntime` / `FishingRuntime` / `OverworldMonsRuntime` | A Pokemon instance was created and its shiny draw resolved — the 1/256 roll at instance creation (the additive `is_shiny` field; `pokemon_rules.roll_shiny(rng)` on the injected shared `_rng` when one is threaded — every production creation path, fishing included — else a deterministic hash of the per-instance gender nonce for legacy fixtures; both exactly the current odds, both pinned by `seed_for_smoke`); payload carries species_id, is_shiny (the verdict — the event fires on EVERY creation, so the odds are provable in BOTH directions), origin (`"wild"` / `"starter"` from `GameRuntime`, `"egg"` at lay time from `BreedingRuntime`, `"fish"` at hook-up from `FishingRuntime` — a hatch carries its lay-time flag, no separate emission — and `"overworld"` at entity-instance build time from `OverworldMonsRuntime`, the spawn-time roll stamped onto the instance and never re-rolled), and odds (the `PokemonRules.shiny_odds` denominator in effect — 256 by default; `Breeding.set_shiny_odds(n)` changes it, the original FAQ's planned user-adjustable hook — proven by the `shiny_odds` scenario). Egg shinies are rolled at LAY time and visible before hatch (faithful). The overworld renders shinies identically to normals for anything WILD (faithful — no overworld difference until battle/status); the battle palette swap + sparkle and the status/egg badge are the render surfaces. A user-facing odds MENU entry is future UI; the domain hook exists today. |
| `egg_laid` | `BreedingRuntime` | A penned compatible pair produced an egg (the seeded cadence: every `LAY_CHECK_STEPS` 120 steps each pen with ≥2 mons, a `Breeding.find_pair` pair, BOTH happiness ≥ `BREED_HAPPY_THRESHOLD` 220, the SHARED habitat scan satisfied for BOTH parents (`HabitatDrops.pen_habitat_tags` over the pen interior PLUS its one-tile 4-neighbor ring, evaluated per parent by `HabitatDrops.types_satisfied` and AND'd — the ONE scan the drops comfort check rides, so drops-comfortable ⟺ the lay gate passes), and fewer than `EGG_GROUND_CAP` 7 ground eggs rolls `LAY_CHANCE` 0.5 on the shared rng); payload carries pen (the `"x,y"` anchor key), species_id (the offspring — the MOTHER's species, or the non-Ditto parent in a Ditto pair), mother_species_id + father_species_id (the pair's roles), gender (the offspring's, rolled at lay time), tile `[x, y]` (the egg's interior pen tile), is_shiny (visible pre-hatch), steps_to_hatch, and eggs_in_pen (the count after the lay). Egg contents (gender/moveset/shiny) are fixed at lay time and shown on the egg's status screen before hatch (faithful); the egg dict is `is_egg: true` + an `egg: {species_id, display_name, gender, is_shiny, moves, father_moves, steps_to_hatch}` payload with max_hp/current_hp 0 — NEVER built from the sprite-only `EGG` catalog entry (its base_stats is garbage). |
| `egg_hatched` | `BreedingRuntime` | A party egg's step countdown (`steps_to_hatch` decremented per player step — ground eggs in a pen do NOT incubate; faithful "walk with it") reached zero and the pre-computed instance hatched in-place at LEVEL 5 (`Breeding.hatch_egg` over the REAL species entry); payload carries species_id, name, gender, and is_shiny (the shiny flag carries from the lay-time `shiny_rolled{origin: "egg"}` — no separate hatch emission). The moveset is the FATHER's inherited egg moves (his known moves ∩ the child's parsed `egg_moves`) filled to four with the child's earliest level-up moves. `pokewilds/sounds/egg_noise1.ogg` plays. |
| `egg_picked_up` | `BreedingRuntime` | A ground egg was picked up into the party from a pen (overworld `Z` facing a pen interior — eggs take priority over mon withdrawal); payload carries pen (the anchor key), species_id (the egg's pre-hatch species), tile `[x, y]` (the egg's tile), is_shiny, and party_size (after the pickup; `party_full` at six is refused via `breeding_refused`). "You picked up the Egg. It feels warm." |
| `mon_penned` | `BreedingRuntime` | A party Pokemon was deposited into the nearest pen (`breeding_runtime.deposit_to_nearest_pen` — the party-screen DROP action, standing next to a pen); payload carries pen (the anchor key), species_id, name, level, party_size (AFTER the move), and pen_count (pasture mons AFTER the move). Refusals (`no_pen` "There is no pen here.", `no_such_mon`, `last_party_member` "You can't pen your last Pokemon.", `eggs_stay_with_you` "You keep the Egg with you.") trace `breeding_refused`, never silent. |
| `breeding_refused` | `BreedingRuntime` | A pen deposit/withdraw/egg-pickup was refused and changed nothing; payload carries action (`"deposit"` / `"withdraw"` / `"egg_pickup"`) and reason — `no_pen` (no fence enclosure near the player tile), `no_such_mon` (party index out of range), `last_party_member` (penning the party's only mon), `eggs_stay_with_you` (eggs ride party slots — never penned), or `party_full` (egg pickup / withdrawal at party size six — the mon/egg stays penned, never lost). |
| `habitat_happiness_changed` | `HabitatRuntime` | A penned Pokemon's habitat-happiness evaluation changed (on DEPOSIT — the faithful drop-time check, delta 0 — and on each CROSSED in-game day boundary, `DAY_STEPS` 1440; the load-time first tick is skipped, so only crossed boundaries count); payload carries species_id, tile `[x, y]` (the pen anchor as an x/y pair), happiness (the new clamped 0-255 value), delta (`HABITAT_HAPPINESS_GAIN` +10 on a comfortable day tick, 0 on an uncomfortable tick — there is NO decay; 0 on the deposit evaluation), and satisfied (the type-matched pen-tile verdict — dual-types need BOTH tiles; a mon that becomes uncomfortable forfeits its generated drop, faithful quirk). The 220 threshold doubles as the happy gate for breeding AND drops and feeds the wired happiness evolution. |
| `item_dropped` | `HabitatRuntime` | A comfortable happy (≥220) penned Pokemon produced its daily material (the FAITHFUL primary material source — once per in-game day per mon on the crossed day boundary, `DAY_STEPS` 1440, the per-mon `last_drop_day` stamp; the Phase-2 battle-end `material_dropped` table stays the documented SECONDARY); payload carries species_id, item_id, tile `[x, y]` (the pen anchor as an x/y pair), and day (the in-game day index). Dual-types emit ONE event PER type material (`HabitatDrops.drops_for`); the species overrides apply (Miltank → `moo_moo_milk` replacing its Normal manure; Dragon drops both scale and fang; Honey/Soft Wool/Star Piece/Berry Juice/Ancientpowder families per the spec). The table NEVER yields `log` or `hard_stone` — Rock-type drops are WITHHELD to keep the building_loop region-seal escape witness invariant (asserted via `HabitatDrops.witness_clean()` on every `habitat_drops` run, the `craft_flow` pattern). An uncomfortable mon forfeits the day (no event). DIVERGENCE (Phase 5 stone acquisition): the SAME event is REUSED for a Steel-type mon's cadence-gated shiny_stone drop — on a drop day, a happy comfortable Steel-type mon also yields ONE `shiny_stone` per ≥`STEEL_STONE_CADENCE` (4) in-game-day window (`habitat_drops.steel_stone_due`, pure over types+day+the per-mon `last_stone_day` stamp), emitting a second `item_dropped` with the IDENTICAL payload keys (species_id, item_id=`shiny_stone`, tile `[x, y]`, day) — NO separate event. This is a flagged port divergence (`wiki-materials.md:393` documents the Steel drop as Metal Coat ONLY, which stays the daily material; `TYPE_MATERIALS["STEEL"]` is exactly `[metal_coat]`, the stone rides a second `_day_tick` gate, never `drops_for`'s daily path). Witness-safe (shiny_stone is neither log nor hard_stone; `is_habitat_drop_material` walks it so the audit stays honest). Witnessed by `habitat_drops`' `run_steel_drop_control`. |
| `pasture_deposited` | `HabitatRuntime` | A party mon was moved into a pen via the habitat subsystem's drop seam (`habitat_runtime.deposit_mon` — evaluates comfort AT DEPOSIT and stamps the mon's cached `habitat` state); payload carries species_id and tile `[x, y]` (the pen anchor as an x/y pair). The deposit-time comfort verdict rides the alongside delta-0 `habitat_happiness_changed` (emitted immediately after, which DOES carry `satisfied`). |
| `pasture_withdrawn` | `HabitatRuntime` | A pastured mon was returned to the party via the habitat subsystem's seam (`habitat_runtime.withdraw_mon` — party cap 6; happiness accrued in the pen rides with it); payload carries species_id and tile `[x, y]` (the pen anchor). The pasture entry is erased only when BOTH its mons AND its eggs are gone — the ONE shared session store (below) means breeding's ground eggs ride the same entry and are never destroyed by a withdrawal. |
| `pasture_refused` | `HabitatRuntime` | A habitat-seam deposit/withdrawal was refused and changed nothing; payload carries action (`"drop"` / `"take_out"`), reason — `no_such_mon` (party/pen index out of range), `egg_guard` (eggs ride party slots — never penned), `last_party_member` (penning the party's only mon), `not_in_pen` (no fence enclosure bounds the tile), or `party_full` (withdrawal at party size six) — plus species_id and tile `[x, y]`. |
| `fishing_cast` | `FishingRuntime` | A fishing rod cast facing water rolled its bite check (emitted on EVERY rng-consuming cast); payload carries rod_id (`"old_rod"` / `"good_rod"` / `"super_rod"` — the BEST bagged rod casts), tier (1/2/3), tile `[x, y]` (the water tile fished), and bite (the per-tier `BITE_CHANCE` roll — Old 0.55 / Good 0.70 / Super 0.85; the wiki describes no minigame, so the wait is this single seeded roll). A false bite shows "Not a bite... The water settles." and consumes no encounter. |
| `fishing_refused` | `FishingRuntime` | A fishing attempt was refused and consumed no rng; payload carries reason — `no_rod` (faced water with no bagged rod — "You need a Fishing Rod to fish here."), `no_water` (defensive — the router pre-gates water tiles), or `no_species` (the viability filter emptied the tier pool — "The water is quiet. Nothing bit.") — plus rod_id (`""` without a rod). Never silent, mirroring `field_move_refused`. |
| `fish_hooked` | `FishingRuntime` | A bite became a hooked encounter; payload carries rod_id, species_id, level, and tile `[x, y]` (the water tile fished); the rod tier rides the `fishing_cast` event instead. The species is drawn from the rod tier's CUMULATIVE pool (Old: MAGIKARP/TENTACOOL; Good + HORSEA/CORSOLA; Super + QWILFISH — the documented Beach-anchored approximation, `fresh-beach.md:63`, five species only) filtered to battle-viable species; level rides the FIXED per-tier band (`TIER_LEVEL_RANGE`: Old 3-7, Good 8-14, Super 15-22). The shiny roll APPLIES and rides the injected shared `_rng` (water shiny hunts are real) — `shiny_rolled{origin: "fish"}` fires on every hook-up (the EVERY-creation contract). The hooked mon rides the PENDING-ENCOUNTER seam — `game_runtime.generate_wild_encounter` returns it BEFORE the wild draw, so Repel + unlit-night ghosts never touch fishing. |
| `fish_caught` | `GameRuntime` | A battle that began from a fishing hook ended in a CAPTURE; payload carries species_id and rod_id (the rod that hooked it — `fishing_runtime.last_rod_used`). `consume_fishing_battle` resets the fishing mark on EVERY finished battle, so the event can never fire for an ordinary encounter. |
| `evolution_stone_used` | `GameRuntime` | A bag-used evolution stone evolved a party Pokemon (the bag screen's stone branch → `game_runtime.use_stone_on_mon` → `stone_evolution_runtime.use_stone_on_mon` → `pokemon_rules.check_item_evolution` :176-183); payload carries item_id (the stone bag id — one of fire_stone/water_stone/thunderstone/leaf_stone/moon_stone/sun_stone/ice_stone/dawn_stone/dusk_stone/shiny_stone, the explicit `STONE_ITEM_IDS` set shared by the bag screen; thunderstone has NO underscore), species_id (the PRE-evolution species), and evolved (the target species id). Emitted with source `GameRuntime` ONLY on a successful stone evolution — the stone is consumed BEFORE the swap (a refused decrement is the distinct `no_item` refusal and applies nothing), `is_shiny` + level/exp/moves/happiness ride the `duplicate(true)` species swap unchanged, stats/max_hp/current_hp/catch_rate RECOMPUTE off the target entry (the exact level-evolution path, `battle_runtime._try_evolve`: `build_stats` at the mon's level, `max_hp` from the new stats, `current_hp` scaled by the pre-swap ratio and clamped ≥ 1), and name/types/front/back paths re-resolve to the target entry. A no-effect use (an egg, the wrong stone, a line with no ITEM-method evolution, an already-final species, an unknown species) shows "It would have no effect.", consumes nothing, and emits no trace (the Potion no-effect precedent); an out-of-range party index is the distinct `no_such_mon` refusal, and a missing target entry is a warning-tier `bad_target` that also consumes nothing. Stone ACQUISITION is implemented (tech-debt item 11 RESOLVED): the FAITHFUL Beach-Dig pool (SAND → water_stone among big_pearl/clear_glass/revive, `fresh-beach.md`; surfaced via `dig_item_found`) plus flagged PORT ADDITIONS — the other-biome Dig pools (GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP), the invented `DIG_BONUS_RARITY` rate, and the Steel-type shiny_stone cadence drop (the `item_dropped` reuse above; `wiki-materials.md:393` documents Metal Coat ONLY). Ice/Dawn Stones stay scenario-grant-only (SNOW isn't diggable — the item-11 residual). |
| `breed_flow_passed` | `SmokeScenarios` | The `breed_flow` scenario verified the breed→hatch loop end to end under the `FIELD_MOVES_PARTY` fixture: FIRST the precondition contract witness (`Phase5.contract_problem(runtime)` empty — runtimes wired, fences buildable, catalog drift fails loud), then a sealed fence ring built from EARNED materials, a happy compatible EEVEE pair penned (habitat-satisfied basic pen), the seeded cadence producing an egg (`egg_laid` with the pre-hatch gender/moveset/shiny visible on the egg status; a forced-shiny egg witnessed via the pinned seed), the DITTO pairing rule (`run_ditto_case` — Ditto + male breeds; the non-Ditto parent's species is the offspring), the UNBREEDABLE case (`run_unbreedable_case` — a genderless MAGNEMITE pair WITHOUT Ditto produces nothing under the faithful `ALLOW_UNDISCOVERED_BREEDING := false` default), the WRONG-EGG-GROUP case (`run_wrong_group_case` — a happy female EEVEE (EGG_FIELD) × male ABRA (EGG_HUMANLIKE) lays nothing across `UNBREEDABLE_WINDOW` 960 steps, eight lay cadences; BOTH parents are basic-recessive (NORMAL / PSYCHIC need no habitat tag), so the standard basic pen satisfies them and the disjoint egg groups are the ONLY gate that can hold the pair back — the group DISJOINTNESS and the habitat-neutrality are asserted off the live catalog inside the check, so a catalog edit or a re-confounding habitat requirement fails loud instead of passing vacuous), the FLYING habitat proof (`run_flying_case` — a PIDGEY pair penned AROUND a live tree prop lays, the shared interior-plus-ring scan catching the "tree" tag the old interior-only scan never saw), the WATER habitat proof (`run_water_case` — ALWAYS a domain-layer proof: synthesized tile logics through `HabitatDrops.pen_habitat_tags` + `types_satisfied` satisfy MAGIKARP with a pond tile and NOT without one (the vacuous-proof control); PLUS the runtime MAGIKARP pen when the seeded world offers an isolated pond site — otherwise the NAMED warning-tier skip `"skipped_no_water_site"` on top of the passed domain proof, never a silent pass), egg pickup (`egg_picked_up`) and hatch at level 5 (`egg_hatched`) with the father's egg move in the moveset, the evolution-stone group (`run_stone_case` — ARMED via the `use_stone_on_mon` seam: a granted fire stone → `evolution_stone_used{item_id: fire_stone, evolved: FLAREON}` with `is_shiny` preserved through the species swap, the stats/max_hp recomputed off FLAREON's base stats (the level-path parity pin), and the stone consumed; PLUS the no-effect negative — a fire stone on MAGIKARP (no ITEM-method evolution) refuses `no_effect`, consumes nothing, and emits no trace; a missing seam would still skip NAMED `"skipped_no_seam"` — never silent, never red), and a save round-trip preserving the pasture + a party egg; payload carries pair_ok, egg_ok, hatch_ok, ditto_ok, unbreedable_ok, wrong_group_ok, flying_ok, water_ok (a bool OR the `"skipped_no_water_site"` string), stone_ok (a bool OR the `"skipped_no_seam"` string), save_ok, and seed. |
| `breed_flow_failed` | `SmokeScenarios` | The `breed_flow` scenario's total exit contract (symmetric with `breed_flow_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array so a red can never surface as a blank "missing required events". |
| `shiny_odds_passed` | `SmokeScenarios` | The `shiny_odds` scenario verified the shiny draw STATISTICALLY but SEEDED (deterministic, never flaky): N instance creations on the pinned rng yield a shiny count EQUAL to the exact precomputed expectation for the pinned seed (a pure function of (code, save, seed) — no tolerance band, no wall-clock; re-calibrate only with a seeded re-run), every instance carries a well-formed `is_shiny`, `shiny_rolled` fires on EVERY creation with `odds: 256` (the `is_shiny: true` subset equals the shiny count), and the adjustable-odds HOOK is proven (`Breeding.set_shiny_odds(2)` on a reseeded window flips the rate to the exact 1/2 expectation — `odds_hook_ok` — then the odds are restored); payload carries draws, shinies, expected, odds_hook_ok, and seed. |
| `shiny_odds_failed` | `SmokeScenarios` | The `shiny_odds` scenario's total exit contract (symmetric with `shiny_odds_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array. |
| `habitat_drops_passed` | `SmokeScenarios` | The `habitat_drops` scenario verified the faithful habitat-drop loop: a MILTANK deposited in a satisfied basic-floor pen (`pasture_deposited` + the alongside delta-0 `habitat_happiness_changed{satisfied: true}`) vs a GRASS mon in a pen WITHOUT tall grass (unsatisfied control — gains NO happiness and drops nothing; there is no decay), the crossed-day-boundary drop (`item_dropped{miltank, moo_moo_milk}` with the exact bag gain — the species override replacing manure; the dual-type double-drop and the Rock-empty row witnessed against `HabitatDrops.drops_for`), the once-per-day cadence (a second same-day rollover grants nothing — `last_drop_day`), the uncomfortable-forfeit rule, and the witness invariant (`HabitatDrops.witness_clean()` — the habitat table never yields log/hard_stone, asserted every run), and the Steel-type STONE control (`run_steel_drop_control` — a flagged DIVERGENCE proof pinning the SHIPPED cadence, not the wiki: a crafted MAGNEMITE (ELECTRIC/STEEL both basic-recessive, same grass-free pen) drops magnet + metal_coat + shiny_stone on its first drop day (deposit stamps `last_stone_day := -CADENCE`), materials ONLY on the next three days (window <4 suppressed), and shiny_stone re-armed on the fourth (window 4); domain pins — `drops_for(MAGNEMITE) == [magnet, metal_coat]` exactly, `steel_stone_due` boundaries, non-Steel MILTANK false, `is_habitat_drop_material(shiny_stone)` true); payload carries satisfied_ok, drop_ok, milk_ok, cadence_ok, unsatisfied_ok, steel_ok, witness_ok, and seed. |
| `habitat_drops_failed` | `SmokeScenarios` | The `habitat_drops` scenario's total exit contract (symmetric with `habitat_drops_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array. |
| `fishing_flow_passed` | `SmokeScenarios` | The `fishing_flow` scenario verified rod-tiered fishing: an Old Rod crafted through the real resolver, a shore cast facing water under the pinned seed (`fishing_cast{bite: true}` + `fish_hooked{old_rod, ...}`) with the species in the Old pool and a battle started through the PENDING-ENCOUNTER seam (Repel active → the hook still lands — the seam proven), the TIER GATE under reseed (Super's pool strictly contains Good's strictly contains Old's — Horsea/Corsola unreachable on Old, Qwilfish on Super only; the table holds to the five Beach-scraped species), the refusals (`fishing_refused{no_rod}` facing water rodless; the seeded bite-miss path `no_bite`), the shiny roll applying to hooked mons, and seeded determinism (same seed → same cast outcomes); payload carries cast_ok, tier_ok, refused_ok, battle_ok, and seed. |
| `fishing_flow_failed` | `SmokeScenarios` | The `fishing_flow` scenario's total exit contract (symmetric with `fishing_flow_passed`, emitted alongside `push_error` on any non-pass); payload carries the `failures` array. |
| `visual_sweep_pokemon_passed` | `SmokeScenarios` | The `visual_sweep_pokemon` scenario captured its deterministic Phase-5 shots (`20_egg_in_pen` — a sealed fence ring with a gold-egg entity on the prop layer honoring the y-sort/world_draw_order contract; `21_shiny_battle` — a forced-shiny wild battle with the palette-swapped enemy sprite + sparkle overlay; numbered after the `18_storage_box` / `19_storage_screen` storage shots — the leading digits 09-12 stay reserved for battle shots by the sidecar canary contract) and matched baselines within threshold (or updated them in update mode); payload carries shots, compared, mismatched, max_drift_pct, and mode. The shiny is forced via the odds hook (never rolled) and all placement is seeded grant+try_place, so the shots are byte-stable; existing baselines carry no pens/shinies, so their bytes are unchanged. Windowed-only; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1` like `visual_sweep_camping`/`visual_sweep_storage`. |
| `overworld_mon_spawned` | `OverworldMonsRuntime` | A roaming slot first materialized inside the active window (spawn traces are window-ENTRY events, never a world-wide storm — the runtime simulates only the player's window + margin); payload carries slot (the entity id, e.g. `"mon_3,5"` / `"guardian_3,5"`), species_id, tile `[x, y]`, disposition (one of TIMID/FRIENDLY/IRRITABLE/AGGRESSIVE — resolved off the live catalog where the mon stands), level (the distance band + hash jitter), and is_shiny (rolled at SPAWN on the derived hash stream — never re-rolled, never the shared `_rng`; wild entities render identical shiny or not, faithful :230). |
| `overworld_mon_despawned` | `OverworldMonsRuntime` | A roaming/stationary mon left the overworld (NEVER fired for eggs — they trace `egg_stolen`/`egg_cleared`); payload carries slot, species_id, tile `[x, y]`, and reason — `"ko"` / `"caught"` (faithful :288 — KO or capture removes the entity; recruitment removes as `"caught"`), `"fled"` (faithful :254-258 — a timid mon ran its `FLEE_STEPS` and vanished), `"distance"` (the invented un-materialization beyond `DESPAWN_CELLS` cells — re-derives identically on return at the same step band), or `"recompute"` (a DAY<->NIGHT pool re-filter dropped a now-illegal species). The reason set is exhaustive (the audit pins it). |
| `recruit_attempted` | `OverworldMonsRuntime` | An overworld Z-interact ran the dialogue-recruit path; payload carries species_id, tile `[x, y]`, path (`"dialogue"` — the only recruit path; Charm is flee/attack PREVENTION whose calm-then-interact recruit is flagged DIVERGENCE #6), party_free, ok, and reason (`""` on success, `"party_full"` for the :262 hard rule — a full party refuses and the mon never warps to camp). |
| `recruit_succeeded` | `OverworldMonsRuntime` | A friendly (or Charmed-within-the-calm-window) overworld mon joined the party; payload carries species_id, tile `[x, y]`, level, is_shiny (the SPAWN-TIME rolls, stamped onto the instance — never re-rolled), and party_size (post-add). The entity is removed (`overworld_mon_despawned{reason: "caught"}` alongside) and `shiny_rolled{origin: "overworld"}` fires on the build (the EVERY-creation contract). |
| `nest_found` | `OverworldMonsRuntime` | A nest cell first entered the active window (once per session per nest); payload carries tile `[x, y]` (the guardian's tile, or the cell center without one), guardian_species_id, and eggs (the wild-egg count materialized, pinned `NEST_EGGS := 2`). THE NEST/ALPHA MODEL IS A FLAGGED DIVERGENCE (not scrape-backed — spec § Nests; shipped per exec-plan mandate, justified against the +3-stage buff :284; the faithful strewn single egg stays the common case). |
| `egg_stolen` | `OverworldMonsRuntime` | A wild egg was TAKEN into the party (overworld Z — the Phase-5 egg payload, hatched by the existing step-incubation at level 5); payload carries tile `[x, y]`, species_id, is_shiny, and provoked_count (entities within `EGG_PROVOKE_RADIUS` passing the parents-OR-egg-group rule — flagged DIVERGENCE #5 toward the broader reading). Fires BEFORE the provocation traces (the collection pass runs first). DISTINCT from pen eggs (Phase 5 ground eggs, fence-Z pickup, persisted pastures) — never conflated. |
| `alpha_provoked` | `OverworldMonsRuntime` | A nest guardian was provoked (by egg theft OR by chasing down the player — a guardian is forced AGGRESSIVE with widened sight, so an aggressive spot to adjacency also forces its battle) and forced its battle through the pending seam; payload carries tile `[x, y]`, species_id, and attack_stages (pinned `PROVOKED_ATTACK_STAGES := 3` — the :284 verbatim buff, consumed by `battle_runtime.start_wild_battle` post `reset_stages`). A player-initiated Attack never provokes (:280 — `provoked: false`, no buff). The scenario pins the egg-theft path. |
| `mon_provoked` | `OverworldMonsRuntime` | Auxiliary (payload-variant, documented, not registry-required): an overworld mon turned CHASING from provocation; payload carries slot, species_id, tile `[x, y]`, and cause — `"egg_theft"` (the TAKE provocation rule) or `"interact"` (an irritable's second consecutive Z within `INTERACT_MEMORY_STEPS`). Chase-catch from aggressive spotting does NOT trace this (the pending seam + `alpha_provoked` are its witnesses). |
| `egg_cleared` | `OverworldMonsRuntime` | Auxiliary (payload-variant, documented, not registry-required): a wild egg was cleared by the Attack field move — the :248 shiny-check + clear with NO provocation and NO battle (the faithful binary the scenario witnesses by asserting this trace arrives with ZERO `mon_provoked`/`alpha_provoked` and an empty pending seam); payload carries tile `[x, y]`, species_id, and is_shiny (the verdict — "The egg gleams oddly... It's SHINY!" / "The egg breaks apart. It was not shiny."). The :250 wild-egg BATTLE path is dropped (DIVERGENCE #13), so this is the ONLY egg-clear surface. |
| `overworld_mons_passed` | `SmokeScenarios` | The `overworld_mons` gate scenario verified Phase 6 end to end: FIRST the determinism proof (identical `entity_set_hash` across a double run on the pinned walk script + the CONTROL that N seeded wild draws are byte-identical with entities active vs inert — the shared `_rng` provably unconsumed), per-biome spawn (every one of the 11 biomes yields a live roamer at a deterministic band anchor; species in the live pool of the spawn cell's biome; the named wiki dispositions — PIDGEY timid, PRIMEAPE aggressive-on-SAVANNA, TAUROS irritable, JUMPLUFF friendly — resolved off the live catalog), charm-recruit (Dedenne pacifies — `charm_used{level_gate_met:true}`; the calm window holds the flee; dialogue Z recruits with the spawn-time rolls; the full-party refusal :262), hostile engage (Attack → `provoked:false`/NO buff :280 through the pending seam; chase-catch → `provoked:true` + the +3 stages :284; Repel active → the provoked battle still lands, the grass stream stays suppressed), egg steal → Alpha (`nest_found`; Attack-on-egg → `egg_cleared` + zero provocation; TAKE → `egg_stolen` + `mon_provoked` + `alpha_provoked{stages:3}` + the guardian forced battle; the stolen egg hatches via the existing step-incubation at level 5), EVERY-creation `shiny_rolled{origin: "overworld"}`, and the transient save round-trip (party recruit + hatched child persist, NO entity key rides the payload); payload carries determinism_ok, spawn_ok, charm_ok, hostile_ok, egg_ok, shiny_ok, save_ok, and seed. The scenario OWNS the forced-battle seam (the presentation bridge is disconnected for the run, reconnected on exit). |
| `overworld_mons_failed` | `SmokeScenarios` | The `overworld_mons` scenario's total exit contract (symmetric with `overworld_mons_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed. |
| `encounter_config_passed` | `SmokeScenarios` | The `encounter_config` gate scenario verified the configurable-encounter slice end to end: (a) the collision-only DEFAULT — a fresh save reports mode `"off"`, the avatar's per-step roll fires ZERO encounters on an encounter tile, yet a shared-tile CONTACT with an idle wild mon forces a `provoked:false` / no-buff battle (the `contact_encounter.gd` predicate through the pending seam) and an egg on the player's tile does NOT; (b) `classic` — at a high rate the roll fires on an encounter tile and never off one; (c) `anywhere` — the roll fires on a non-encounter walkable tile; (d) ROUND-TRIP — a non-default `encounter_settings` survives save → reload exactly, and switching back to off drops the additive key (byte-preservation — the v4 golden carries none). payload carries default_off_ok, classic_ok, anywhere_ok, round_trip_ok, and seed. The scenario owns the forced-battle seam AND main's `encounter_requested` handler for the run (reconnected on exit). |
| `encounter_config_failed` | `SmokeScenarios` | The `encounter_config` scenario's total exit contract (symmetric with `encounter_config_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed. |
| `world_gen_audit_passed` | `SmokeScenarios` | The comprehensive world-gen audit (infinite-world slice) measured the procedural world across a fixed seed list and every ENFORCING-tier invariant held: disc determinism density (two same-seed generators agree tile-for-tile), the biome-distribution contract (ten common biomes per 400-radius stride-4 seed window + LAVA present in at least LAVA_WINDOWS_MIN windows cross-seed — infinite-world slice 2 retired ring admission with the radial model), pool legendary/EGG no-leak, and spawn reachability. The audit consumes NO rng (a pure function of code + catalog + seeds), so it is deterministic by construction and is NOT a double-run consumer. Payload carries seeds (count), tiles_checked, advisory_count. |
| `world_gen_audit_failed` | `SmokeScenarios` | The world-gen audit's enforcing-tier exit contract (symmetric with `world_gen_audit_passed`, emitted alongside `push_error`): a STRUCTURAL invariant regressed. Payload carries the `failures` array (each named `[goal] seed N: <cause>`) and seed count. Advisory-tier gaps NEVER appear here — they ride `world_gen_audit_advisory`. |
| `world_gen_audit_advisory` | `SmokeScenarios` | WARNING-tier, NEVER gates — the future-fix punch-list of the world-gen audit: `lava_site_gap` (re-fires only if the LAVA-dungeon-site count drops to zero — LAVA GENERATES under the slice-2 climate field, so nonzero-but-rare is expected), `hostile_adjacency_count` + `speck_count` + `biome_distribution`/`extreme_presence` (no biome blending/transition logic; the radial `ring_seam` RETIRED with the ring model), `bst_by_biome`/`level_band_strength` (stats play no role in spawn assignment; the radial `bst_depth` tiering RETIRED), `content_density` (infinite-world slice 3: scattered landmarks / repeating legendary lairs / dungeon-site rates across the beyond-core chunk sample), `legendary_no_anchor`, `spawn_disc_intrusion`, and `landmark_reachability` (spec §19(c) unimplemented). Payload carries advisory_count + the finding kinds; the full findings ride `res://.godot-smoke/world-gen-audit-findings.json` + `docs/generated/world-gen-audit-findings.md`. |
| `visual_sweep_overworld_passed` | `SmokeScenarios` | The `visual_sweep_overworld` scenario captured its deterministic Phase-6 shots — `22_roaming_mons` (a seeded walk band with live roamers mid-window honoring the y-sort/`world_draw_order` depth contract) and `23_nest_alpha` (a seeded nest: the generated ground ring, two wild eggs, the stationary Alpha guardian with the badge — numbered after `21_shiny_battle`; the leading 09-12 stay battle-reserved by the sidecar canary contract), and — since the pre-Phase-7 suite expansion — `30_night_roamers` (tod=0, entities ACTIVE — closing the night×entities×tint triple the main sweep's entity-inert `04_night` leaves open) — and matched baselines within threshold (or updated them in update mode, never pruning other sweeps' shots via the `_foreign_shot` guard DERIVED from the single-sourced shot-range registry in `visual_sweep_baselines.gd`); payload carries shots, mode, auto_update, compared, mismatched, max_drift_pct, threshold_pct, base_dir, crafted, and sidecar_paths. NO rng rides the capture path (entities are pure step functions of `(world_seed, total_steps, slot)`), so the shots are byte-stable; the subsystem is opted-in for this sweep and every non-overworld baseline stays entity-free (the activation gate). FAITHFUL NO-SHINY-VISUAL (:230): wild mon AND wild-egg sprites are identical shiny or not — the 579 overworld-shiny sheets never load. Windowed-only; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1` like the other sweeps. |
| `rng_joint_pin_passed` | `SmokeScenarios` | The `rng_joint_pin` scenario (pre-Phase-7 suite expansion) proved the shared-`_rng` consumer INTERLEAVING byte-stable: under ONE `seed_for_smoke` seed it drives a FIXED interleaving of wild-encounter draws (the grass stream), fishing casts (fishing shares `game_runtime._rng`), and harvest dig steps (step-pure — `harvest_resolver._dig_draw` consumes NO `_rng`), runs the full script TWICE in-scenario (seed BEFORE each `new_game` — the world-seed draw rides `_rng` too), and asserts the canonicalized joint sequence is byte-identical across the two runs; payload carries seed, cycles (the interleaving repetition count), draws (draw entries in the joint sequence), hooks (fishing hook count), and digest (the canonical sequence hash — the byte-stability witness). |
| `rng_joint_pin_failed` | `SmokeScenarios` | The `rng_joint_pin` scenario's total exit contract (symmetric with `rng_joint_pin_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries seed, the `reasons` array (named causes), and digest_a / digest_b (the two runs' canonical sequence hashes) so a red names which consumer's draw stream diverged. |
| `save_stability_passed` | `SmokeScenarios` | The `save_stability` scenario (pre-Phase-7 suite expansion) proved the v4 save surface CANONICAL-STABLE: a `new_game` under `seed_for_smoke`, scripted v4-surface mutations (deposit/withdraw, craft, `repel_steps`, pastures, stone/rod bag), save → reload → save, and a byte-compare of the CANONICALIZED save JSON (keys sorted, `ts`/`version`-stripped) across both writes; it additionally commits ONE golden v4 fixture (`docs/generated/golden-saves/v4_golden.json`) and asserts `load(golden)` succeeds AND its canonical form is stable; payload carries the pinned seed, the save↔reload↔save byte-compare verdict, and the golden-fixture load + canonical-stability verdicts. |
| `save_stability_failed` | `SmokeScenarios` | The `save_stability` scenario's total exit contract (symmetric with `save_stability_passed`, emitted alongside `push_error` on any non-pass); payload carries the `reasons` array so a red names which surface mutation destabilized the canonical save bytes. |
| `playtest_entity_soak_passed` | `SmokeScenarios` | The `playtest_entity_soak` scenario (pre-Phase-7 suite expansion) ran the NEW entity bot band (`scripts/runtime/playtest_bot_entity.gd` — extracted from the at-320-budget `playtest_bot.gd` exactly like `playtest_bot_breeding.gd`) looping steal-egg / provoke-Alpha / charm-recruit / flee-despawn actions over hundreds of seeded iterations with ALL postconditions holding: entity sprite count bounded, y-sort keys never NaN, the pending-encounter seam never left armed, despawn hygiene (no entity past `DESPAWN_CELLS`), zero `entity_at` on the player's tile, and the R11 legendary witness (the harness reset wipes new_game's stamped statics, so the soak RE-STAMPS the frozen seven via `stamp_legendaries` — the whole soak runs with the window-exempt statics in the store — and every anchored legendary is engaged through the forced-battle seam under the `set_battle` latch; zero encounters is LOUD); payload carries seed, iterations, and the per-postcondition counts (incl. `legendary_encounters`). |
| `playtest_entity_soak_failed` | `SmokeScenarios` | The `playtest_entity_soak` scenario's total exit contract (symmetric with `playtest_entity_soak_passed`, emitted alongside `push_error` on any non-pass); payload carries the `reasons` array so a red names which postcondition broke (sprite bound, NaN y-sort, armed seam, despawn hygiene, player-tile entity, or the R11 legendary re-stamp/encounter witness). |
| `visual_sweep_fishing_passed` | `SmokeScenarios` | The `visual_sweep_fishing` satellite (pre-Phase-7 suite expansion) captured its deterministic fishing shots (`26_fishing_cast` + `27_fishing_bite` — per-sweep frozen date-shaped seed 2026072804, a fixed Beach water tile via ring-scan, an Old Rod crafted through the REAL resolver, `encounter_chance` 0, the pending-encounter seam DISARMED at capture) and matched baselines within threshold (or updated them in update mode, never pruning other sweeps' shots via the `_foreign_shot` guard derived from the shot-range registry in `visual_sweep_baselines.gd`); payload carries shots, compared, mismatched, max_drift_pct, and mode. Windowed-only; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1` like the other satellites; rides PLAYTEST_SCENARIOS + WINDOWED_SUBPROCESS_SCENARIOS + WINDOWED_ONLY_SCENARIOS. |
| `visual_sweep_fishing_failed` | `SmokeScenarios` | The `visual_sweep_fishing` satellite's total exit contract (symmetric with `visual_sweep_fishing_passed`, emitted alongside `push_error` on any non-pass); payload carries the `reasons` array. |
| `landmark_entered` | `LandmarkRuntime` | The player first crossed into a landmark footprint OR a sub-region of one (session-scoped one-shot per landmark|region — the runtime's `_visited` memory; a new session re-fires first entries); payload carries landmark_id (`"pkmn_mansion"` / `"desert_ruins"` / `"heart_tower"`), tile `[x, y]` (the player tile at the crossing), region (the footprint region entered — e.g. mansion `"courtyard"` / `"room"` / `"sewer"`, ruins outer/inner/underground, the tower base chamber), first_entry (true on the session's first crossing of that instance|region), and instance (infinite-world slice 3: the per-instance key — BOTH origin and scattered instances carry their `id@ax,ay` content-scatter key, origin tiles stamped off the frozen origin anchor (`landmarks.tile_logic_for`), scattered copies off their chunk anchor — so first_entry is session-scoped per instance|region and EVERY scattered copy of a landmark gets its own first entry). A Heart Tower entry additionally switches the field music to `Wilds_HeartTower.ogg` on first entry (this event + the auxiliary `landmark_music{trigger: "entry"}` witness), with the host-biome restore on exit asserted via the `music_track_selected{kind: <host biome>}` seam (R10 — the biome-track half of the router's kind→track witness); both halves are trace-asserted, never audio (headless never plays sound). The Mansion's sewer region emits this with `region: "sewer", first_entry: true` once on the first crossing AFTER the statue puzzle unlocks it. |
| `puzzle_state_changed` | `LandmarkRuntime` | Every Mansion statue-puzzle transition (the PURE state machine in `landmarks.gd`, emitted by `landmark_runtime` over its returned payloads — no argument mutation); payload carries landmark_id (`"pkmn_mansion"`), puzzle_id (`"statues"`), state — `"statue_<n>_on"` / `"statue_<n>_off"` for a single toggle (`MANSION_STATUES := 3` switches, `pkmnmansion_statue1.png`), or `"unlocked"` when all three are simultaneously ON — and solved (true ONLY on the unlock transition, false for every toggle). On unlock the sewer sub-region's door tiles flip walkable via the world-generator resolver overlay and the state stays unlocked (no wrong-order fail state — a toggle-all puzzle; flagged port invention over faithful assets). Puzzle state persists through the frozen location-keyed state seam (`SessionState.landmark_state_for(chain)` / `set_landmark_state(chain, state)` — infinite-world: the `chain` param is VESTIGIAL on the seamless plane, always origin) — Build 1 resolves to the v4-additive top-level `landmark_state` key (absent → `{}`, NO `SAVE_VERSION` bump); a solved Mansion stays solved across reload. Infinite-world slice 3 re-keyed the dict PER-INSTANCE (`"pkmn_mansion@ax,ay"` content-scatter keys), so each scattered mansion instance keeps its own puzzle state through the SAME whole-dict seam. |
| `landmark_entity_spawned` | `LandmarkRuntime` | The Ruins Underground DUSCLOPS guardian materializes under the guard band: the Phase-6 DORMANT disposition woken AGGRESSIVE by the new sub-region (`wiki-overworld-encounters.md:270` always-aggressive; KO/catch removal is PERMANENT per world, a whiteout leaves it re-battleable). Rides the Phase-6 stationary-entity seam (there is NO public spawn API), gated on `overworld_mons_runtime.active` so non-opt-in scenarios stay entity-free; payload carries species_id (`\"DUSCLOPS\"`), tile [x,y] (the guardian's world tile), landmark_id (`\"desert_ruins\"`), disposition (`\"AGGRESSIVE\"`). Witnessed by `landmark_flow`'s ruins case (the Repair-1 nest-oracle fix's centerpiece trace). |
| `landmark_music` | `LandmarkRuntime` | Auxiliary (documented, NOT registry-required): the Heart Tower field-music switch armed on a first entry; payload carries landmark_id (`\"heart_tower\"`), track (`res://pokewilds/music/Wilds_HeartTower.ogg`), trigger (`\"entry\"`). Emitted in `_note_region` BEFORE `music_router.play_track_path` — the router gates audio under headless and emits no success trace itself, so this seam is the headless witness for the spec's field-music switch (the Calm variant + trigger stays FLAGGED #7). The host-biome theme restores on footprint exit via `play_biome_track`, which now emits `music_track_selected{kind: <host biome>}` BEFORE the headless gate (the R10 seam — previously "no seam", mirroring `play_battle_track`); `landmark_flow`'s tower case steps one row outside the footprint and asserts that event (`world_depth_checks.gd`), so the exit restore is trace-asserted, never audio. |
| `key_item_used` | `LandmarkRuntime` | Auxiliary (documented, NOT registry-required): a landmark key item was used at its target; payload carries item_id (`"mansion_key"` — the `pokemon_mansion_key.png` bag item picked up from a Mansion interior journal table) and landmark_id (`"pkmn_mansion"`). The key unlocks the interior ROOM door (the outer ruin is open; the key gates the room grid that contains the statues + journals) — the door overlay flips walkable through the resolver, witnessed by `landmark_flow`. |
| `landmark_flow_passed` | `SmokeScenarios` | The `landmark_flow` scenario (Phase 7 Build 1) verified landmarks end to end on the FIXED seed 2026072907 (`seed_for_smoke` BEFORE `new_game` — the save_stability precedent; the double-run lane reds without it; every tile DERIVED from `Landmarks.landmarks_in_world`, never hardcoded): the Mansion courtyard entry (`landmark_entered{pkmn_mansion, courtyard, first_entry: true}`), the mansion_key pickup at the journal table + its turn on the room door (`key_item_used`), the three statue toggles → `puzzle_state_changed{state: unlocked, solved: true}` on the third (the sewer door overlay flips walkable via the resolver), the sewer crossing (`landmark_entered{sewer, first_entry: true}`), the footprint-LOCAL encounter scope (an inside-footprint draw from the `PKMNMANSION` token — DITTO present, the mansion-exclusive proof cross-asserted against `breed_flow`'s Ditto-universal-parent reliance, + GROWLITHE/KOFFING/MAGMAR; a tile one step outside the footprint reverts to the world-biome pool — aliasing PKMNMANSION to a world biome in `SOURCE_BIOME_ALIASES` reds at the outside assert), the Ruins outer/inner/underground scope (BELDUM/SOLROCK grounds, Lunatone inner + the flagged curated VOLCARONA/GOLURK high-level table, the aggressive DUSCLOPS underground), the Heart Tower footprint + entry (`landmark_entered{heart_tower}` + the `landmark_music` entry switch) and the EXIT host-biome restore (`music_track_selected{kind: <host biome>}` — the R10 seam, asserted one row outside the footprint), all trace-asserted not audio, footprint protection (`can_place_on` refuses landmark tiles), and the puzzle-state save round-trip THROUGH THE FROZEN SEAM (a reload finds the basement still open — the v4-additive-key decision proven); payload carries mansion_ok, loot_ok, puzzle_ok, mansion_pool_ok, ruins_ok, guardian_ok, tower_ok, save_ok, and seed (`loot_ok` = the R9 one-shot loot grant; `guardian_ok` = the R2 +3 chase-catch witness). Joins the double-run determinism lane (drives the shared encounter stream; Build 2's `legendary_spawn` completed the verify_all regroup to the spec's [4,4] — Build 3's `world_chain`, the ninth consumer, regrouped to [4,5], and the Phase-7 audit's `night_cycle`, the tenth, regroups [5,5]; SUPERSEDED — the configurable-encounters slice added `encounter_config` as the ELEVENTH consumer, so the lane is now [5,6], outer [240,300]s; SUPERSEDED again — infinite-world slice 1 RETIRED the `world_chain` consumer with world chaining, leaving TEN consumers regrouped [5,5], outer [240,240]s, and the infinite-world scenarios (content_scatter, seed_choice) deliberately stay OUT of the lane). |
| `landmark_flow_failed` | `SmokeScenarios` | The `landmark_flow` scenario's total exit contract (symmetric with `landmark_flow_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `legendary_spawn_passed` | `SmokeScenarios` | The `legendary_spawn` scenario (Phase 7 Build 2) verified the frozen seven end to end on the FIXED pin 2026073001 (`seed_for_smoke` BEFORE `new_game` → `rebuild` — the breed_flow precedent; legendary stamping is pure `_mix`, NO rng, so the scenario self-pins into the double-run lane): every `LEGENDARY_IDS` species DERIVED off the live seed — an anchor resolves → exactly one stationary entity stamped on that tile (kind/battle_kind `"legendary"`, disposition `AGGRESSIVE`, resolver biome == affinity, `ring >= LEGENDARY_RING_MIN := 60`, entity tile/ring == the derived anchor); `NO_ANCHOR` → NO entity (the negative proof); the anchored/`NO_ANCHOR` partition is PINNED to the exact derived set (`anchored` == all seven in `LEGENDARY_IDS` order, `lava_absent` == [] — gate-enforced, not a floor). ALL SEVEN anchored (infinite-world slice 2 RESOLVED the empirical flag: the climate field generates LAVA — the retired radial model's quantization tail never did — so the LAVA four anchor like the SNOW three; `lava_absent` is pinned EMPTY, and the NO_ANCHOR negative proof rides a SYNTHETIC reach-1 witness since the natural case is vacuous on the pinned seed). Also proven: the never-encounter EXCLUSION on every pool path (`filter_species_ids` DAY+NIGHT over all biomes, the FORCED full-catalog fallback via a typeless biome — the only path that could surface one, the night-ghost `is_battle_viable` TYPE-fallback guard the pool shares); the chase-catch provoked +3 (`PROVOKED_ATTACK_STAGES`) with the `legendary_encounter{battle_kind: "legendary"}` battle-start trace and the `main.gd:92` kind seam — a verbatim MIMIC `play_battle_track("legendary")` run against `music_router`, the live path being latch-bypassed under test (kind plumbing only, headless never plays audio; the auxiliary `music_track_selected {kind, track_path}` makes the selection observable + a static grep pin proves `main.gd:92` reads `battle_kind`); the WHITE-OUT rule (defeat leaves the entity standing with its damage + provoked stages persisted (:284) and idle, re-battleable — a second `legendary_encounter` on the rematch; the :280 no-buff witness rides the KO case's player-initiated FIRST engagement, stages 0); and the KO rule (gone-for-good per instance — `overworld_mon_despawned{reason: ko}`, the ANCHOR-keyed `"<ax>,<ay>:<SPECIES>"` key in `session.legendary_removals`, a second attempt finds no target, a re-stamp re-derives suppression while the untouched anchored return, and the removal key survives the `to_payload`/`apply_into` save round-trip into a fresh session). Payload carries anchor_ok, exclusion_ok, whiteout_ok, ko_ok, pin, seed (the derived world seed), anchored, and lava_absent. |
| `legendary_spawn_failed` | `SmokeScenarios` | The `legendary_spawn` scenario's total exit contract (symmetric with `legendary_spawn_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `waystone_selector_passed` | `SmokeScenarios` | The `waystone_selector` scenario (infinite-world slice; succeeds the RETIRED `beacon_selector` — world chaining's edge beacons are gone, way-stone teleport stays as plain intra-world warps) verified the multi-stone CHOICE on the FIXED pin 2026073101 (`seed_for_smoke` BEFORE `new_game`; `FIELD_MOVES_PARTY`, `encounter_chance` 0): TWO way-stones registered on open ground (`waystone_registered` x2) with `way_stone_tiles()` == REGISTRATION order (the selector's `_rows` source — a listing drift reds named); the PRODUCTION party-screen route driven at the signal layer (menu toggle -> POKEMON submenu -> the Calyrex row -> `party_screen.field_move_requested` -> `start_menu._on_field_move_requested` -> `main._on_field_move_requested` -> the router) opens the WayStoneSelector with the start menu closed AND the avatar input-DISABLED under the modal (the avatar-ownership pin: the field-move emit lands the selector's disable BEFORE `start_menu` hides, so `main._on_menu_closed`'s re-enable — which re-checks the selector's visibility — must not clobber it); a REAL `smoke_tap` pick of the SECOND-registered stone (`move_down` + `action_a`) warps the avatar to THAT tile (`teleport_used{tile}` == stone[1], never index 0 — an index->tile mis-mapping reds here) with input re-enabled after the resolve; and a cancel control (X) moves nothing, closes the selector, and re-enables input. Payload carries party_ok, order_ok, pick_ok, cancel_ok, seed, and stones. |
| `waystone_selector_failed` | `SmokeScenarios` | The `waystone_selector` scenario's total exit contract (symmetric with `waystone_selector_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `content_scatter_passed` | `SmokeScenarios` | The `content_scatter` scenario (infinite-world slice 3) witnessed the chunk-hash scattering beyond the origin core on the FIXED pin 2026080301 (`seed_for_smoke` BEFORE `new_game`): ORIGIN-CORE PRESERVATION (`landmarks_in_world` returns exactly the three byte-frozen origin anchors — scatter never pollutes the core), SCATTER DISCOVERY (a scattered mansion instance derives beyond the core; its `id@x,y` instance key parses), PER-INSTANCE STATE (crafting the scattered mansion's puzzle through the frozen seam opens ITS doors while the origin mansion stays sealed — independent keys, never a whole-dict clobber), and LAIR LIFECYCLE (a repeating lair stamps window-scoped; a KO removes it gone-for-good PER INSTANCE via the anchor-keyed removal; a same-species sibling lair elsewhere still spawns — removals are per-instance, never per-species). Payload carries origin_ok, scatter_ok, lair_ok, pin, seed. NOT a double-run consumer (the origin pins ride the lane); deterministic by construction (pure `_mix` + FastNoiseLite). |
| `content_scatter_failed` | `SmokeScenarios` | The `content_scatter` scenario's total exit contract (symmetric with `content_scatter_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `seed_choice_passed` | `SmokeScenarios` | The `seed_choice` scenario (infinite-world slice 4) witnessed creation-time seed choice end to end: CUSTOM-SEED DETERMINISM on the FIXED pin 2026080401 (`seed_for_smoke` re-pinned BEFORE EACH of the TWO `new_game(SEED)` runs — each consumes one starter shiny draw from the shared `_rng`, so the re-pin keeps both runs on the pinned stream): both runs derive `world_seed == SEED` (the custom path skips ONLY the world_seed draw; the starter draw above it always runs first) and agree byte-identically on the committed spawn tile + the `world_invariants` stride-sample fingerprint (SCALAR tile-logic fields only — biome/walkable/encounter/requires_field_move/block_reason/prop_path; the Rect2 regions never marshal, and the party is NEVER compared since the starter shiny roll legitimately differs per pin); BEACH SPAWN under the SAME seed — the committed spawn IS the beach pass's first match (SAND + walkable + a cardinal neighbor requiring surf, read off the PURE generator's `get_tile_logic`, never the render cache); and the RANDOM PATH — a fresh pin 2026072810 re-armed IMMEDIATELY before the no-arg `new_game()` (the world_seed draw is stream index 1 after the starter draw) lands the HARD-PINNED world_seed 1319840800 (the `playtest_entity_soak` precedent pair). Payload carries determinism_ok, beach_ok, random_ok, pin, seed, random_pin, and random_seed. NOT a double-run consumer; deterministic by construction. |
| `seed_choice_failed` | `SmokeScenarios` | The `seed_choice` scenario's total exit contract (symmetric with `seed_choice_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `new_game_flow_passed` | `SmokeScenarios` | The `new_game_flow` gate scenario (title-flow slice) drove the REAL title/creation screens end to end through the player-boot seam (`TitleScreen.begin_boot`, never main-internal state): splash skip (`splash_closed{reason:key}`), the no-save `[NEW GAME]` / with-save `[CONTINUE, NEW GAME]` entry lists + `title_shown` payloads, CONTINUE into the world (`title_continued` + the avatar re-enabled by `main._enter_world`), then NEW GAME -> the MessageBox save-wipe confirm -> the CreationScreen steps — WORLD_SEED 2026080602 (the probed beach spawn) typed into the in-stage seed digit row as unicode digit events (the loop kept verbatim through the wave-0 seam retarget), the shiny ladder walked 256->128->64, the NameEntry grid cursor math A(0)->S(18)->H(7)->OK(27) spelling ASH, the AvatarPicker sorted-list walk ben(0)->kris(11) — ending on the EXACT `creation_confirmed{player_name: ASH, player_avatar: kris, shiny_odds: 64, world_seed: 2026080602}` payload; WORLD witnesses (`get_world_seed() == WORLD_SEED`, `session_created`, the level-5 MACHOP starter party of one + `field_move_capable("build")`, `shiny_rolled{origin: starter, odds: 64}`, and the beach spawn re-derived off the PURE generator — SAND + walkable + a cardinal surf neighbor, the seed_choice shape); PERSISTENCE witnesses (the save carries `player_name`/`player_avatar`/`shiny_rate`, `_apply_loaded_payload` restores the identity session vars, and the load-path re-apply returns `PokemonRules.shiny_odds` to 64 from a scrubbed 256). Every driving tap is a REAL input-phase event (SmokeTap + caller-owned `Input.use_accumulated_input`) carrying an injection witness, so degraded delivery fails red, never vacuous. Determinism posture: SELF-PINNED (`seed_for_smoke(2026080601)` BEFORE driving creation — the starter shiny draw is the ONE pinned-stream consumer; the custom WORLD_SEED skips the world-seed draw), NOT a double-run consumer (the seed_choice precedent). Payload carries name_ok, avatar_ok, starter_ok, odds_ok, beach_ok, persist_ok, pin, seed, and world_seed. |
| `new_game_flow_failed` | `SmokeScenarios` | The `new_game_flow` scenario's total exit contract (symmetric with `new_game_flow_passed`, emitted alongside `push_error` on any non-pass — the miss-002 honest-reporting contract); payload carries the `failures` array and seed so a red can never surface as a blank "missing required events". |
| `music_track_selected` | `MusicRouter` | Auxiliary (documented, NOT registry-required): the kind→track selection for BOTH track families, emitted by `play_battle_track` AND `play_biome_track` BEFORE the headless gate so the mapping is observable under test (`play_track_path` is headless-gated and emits no success trace itself — the same seam `landmark_music` covers for the Heart Tower ENTRY switch); payload carries kind (a battle kind — `"wild"`/`"trainer"`/`"legendary"`, resolved post-fallback — on the battle path, or the HOST BIOME id — e.g. `"desert"` — on the biome path) and track_path (the resolved `res://pokewilds/music/…` track — `BATTLE_TRACKS["legendary"]` = `gsc-vs-legendary-beasts.ogg`). Two scenario witnesses: `legendary_spawn` asserts it for kind `"legendary"` (the first real `BATTLE_TRACKS["legendary"]` caller — world-depth.md § Legendaries music seam) alongside a static grep pin that `main.gd:92` reads `battle_kind` off the pending payload (the live bridge is latch-bypassed under the `set_battle` seam, so that line never executes in-scenario); and `landmark_flow`'s tower case asserts the biome-path event for the Heart Tower EXIT host-biome restore (R10 — `play_biome_track` gained the emit so the `landmark_runtime` exit branch's restore is no longer an untraceable seam; `world_depth_checks.gd` reds if the exit event is absent). |
| `world_consistency_audit_failed` | `SmokeScenarios` | The `world_consistency_audit` scenario's total exit contract (symmetric with `world_consistency_audit_passed`, emitted alongside `push_error` on any non-pass — the Phase 7 Build 1 miss-002 loudness repair; the failure path was stderr-only before, so a red surfaced as a blank "missing required events"); payload carries the `failures` array (the audit violation records: `kind` + `tile` + `entity_id`), the world `seed` (the audit rides the harness-pinned boot seed), and `tiles_checked` so a red names its cause and reproduces. |
| `visual_sweep_overworld_failed` | `SmokeScenarios` | The `visual_sweep_overworld` satellite's drift/failure contract (symmetric with `visual_sweep_overworld_passed`, emitted alongside `push_error` — the Phase 7 Build 1 miss-002 loudness repair; drift was previously withheld from the report as stderr-only); payload carries `mismatched` (the drifted shot filenames), `per_shot_pct` (per-shot pixel-drift percentages), `errors`, and `threshold_pct` so a red names its shots + percentages. |
| `visual_sweep_pokemon_failed` | `SmokeScenarios` | The `visual_sweep_pokemon` satellite's drift/failure contract (symmetric with `visual_sweep_pokemon_passed`, emitted alongside `push_error` — the Phase 7 Build 1 miss-002 loudness repair; drift was previously stderr-only); payload carries `mismatched`, `per_shot_pct`, `errors`, and `threshold_pct` so a red names its shots + percentages. |
| `visual_sweep_world_depth_passed` | `SmokeScenarios` | The `visual_sweep_world_depth` satellite (Phase 7 Build 1) captured its deterministic landmark shots — `31_ruins_interior` (the `RUINS_INNER` chamber with its statue props, `RUINS_INNER` token canary) + `32_mansion` (the mansion room camera on the statue grid — puzzle state CRAFTED through the frozen `SessionState.set_landmark_state(Vector2i.ZERO, ...)` seam, NEVER played live, with a rebuild so the resolver door overlay re-stamps both doors open, loud-asserted pre-capture) + `33_beacon` (Phase 7 Build 3: a world EDGE with two registered way-stone beacons + the multi-beacon SELECTOR open — driven by `scripts/app/visual_sweep_world_depth_beacon.gd` (extracted at the sweep's app-220 wall), the beacons placed by a BOUNDED edge-band search over the view's tile logic and the selector node opened with the registry's `beacon_tiles()` in registration order — **RETIRED (infinite-world slice 1):** the beacon shot is gone with world chaining; 33 is a whitelisted numbered gap on the registry's `retired` list and the sweep captures 31, 32, 34 only) + `34_heart_tower` (R11: the Heart Tower base chamber — the largest landmark, dedicated `heart_tower` tileset, zero prior pixel coverage; probed by `visual_sweep_world_depth_expand.gd` and asserting the rendered wall/decor props since the tower has NO encounter token) — numbered after `30_night_roamers` per the sidecar canary contract (09-12 stay battle-reserved; shot range [31, 32] + 34, 33 RETIRED, + seed 2026072907 single-sourced in the `visual_sweep_baselines.gd` registry) — and matched baselines within threshold (or updated them in update mode, never pruning other sweeps' shots via the `_foreign_shot` guard derived from the registry). R4 populates the sidecars' `expected_regions.ink` (the regional pixel oracle in `visual_sweep_world_depth_oracle.gd`: tiles projected to screen rects off the camera tile + canonical window) — a strict `canary_rect` too for `31_ruins_interior`/`32_mansion`, while `33_beacon` WAS ink-only (retired with the shot — `canary_rect` empty: the selector overlay geometry is not pinned, so the ink overlap gate was its red tier, `visual_region_diff.py:40` / `visual_sweep_world_depth_oracle.gd:90`) — so a dropped statue/decor/beacon sprite reds instead of hiding under the 0.5% global gate; payload carries shots, mode, auto_update, compared, mismatched, max_drift_pct, threshold_pct, base_dir, crafted, and sidecar_paths. NO rng rides the capture path (footprints discovered by spiraling from world origin over the resolver's `landmark_id` stamp — the app never preloads the domain), so the shots are byte-stable. Windowed-only; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1` like the other satellites. The satellite has NO symmetric `_failed` trace — capture/diff failures ride `push_error`, captured by both transports' ERROR_MARKERS (miss-002 loud, never silent). |
| `showcase_capture_passed` | `ShowcaseCapture` | The `showcase_capture` scenario (NOT a baseline sweep) crafted + captured the coolest locales deterministically to `docs/generated/showcase/NN_name.png` + a crafted-state sidecar each: the Mansion statue room with the puzzle seam-crafted SOLVED + both doors open (01), the unlocked sewer/basement (02), the Desert Ruins exterior with its glowing statues (03), the ruins-underground AGGRESSIVE DUSCLOPS guardian (04 — entity layer opted in + spawned via `note_player_step`, framed out of its Manhattan-8 sight with the two-sample rest probe), the Heart Tower base chamber (05), a world edge with two registered Teleport Beacons + the multi-beacon selector (06 — RETIRED with world chaining, infinite-world slice 1), a fenced pen with a deterministic EEVEE pair + a ground egg (07, seed 2026072605), the origin spawn-meadow vista (08), a CHAINED world `(0,-1)` framed on its re-anchored Heart Tower (09 — the production `try_cross_edge` crossing, never a manual seam swap; RETIRED with world chaining, infinite-world slice 1), and a stationary legendary at rest in its ring (10 — a LAVA-affinity four sought via a bounded northward chain search with a committed SNOW REGICE fallback; RETIRED with world chaining, infinite-world slice 1 — its craft rode the retired chain search). The LIVE showcase frames are 01-05, 07, 08; 06/09/10 are retired numbers. Deliberately OUTSIDE the baseline gate machinery: no `SHOT_REGISTRY` entry, no `reconcile()`/region-diff, no `docs/generated/visual-baselines` writes (the foreign-shot prune only protects registry numbers). NO rng rides the capture path, so frames are byte-stable; windowed-only (the headless display server renders no pixels — `SnapshotCapture.classify` → `"headless"`, no PNG written; transport-skipped honestly under `PLAYTEST_FORCE_HEADLESS=1`). Payload carries `shots`, `base_dir`, and `sidecar_paths`. |
| `showcase_capture_failed` | `ShowcaseCapture` | The `showcase_capture` scenario's total exit contract (symmetric with `showcase_capture_passed`, emitted alongside `push_error` on any missed locale frame — the miss-002 honest-reporting contract); payload carries the `missed` array (each a named locale + cause, never a silent skip), the `captured` shots that DID land, and `base_dir`, so a red names its cascade. |
| `warning` | multiple | Non-fatal warnings worth surfacing to agents. Specific documented cases are listed below the table. |

## Warning-tier cases (specific `warning` events)

These are emitted through the existing `warning` event above; they are documented here so the structured payloads are discoverable.

- Save-version refusal — `trace.warning("SaveStore", "Save uses a newer schema; preserved it and starting fresh.", {found_version, supported_version, preserved_path})`. Fired when a save's `version` exceeds `SessionState.SAVE_VERSION`; the unreadable/newer file is renamed to `SAVE_PATH + ".newer.bak"` (reported as `preserved_path`) BEFORE the runtime starts a fresh game and autosaves, so the per-step autosave can never overwrite the preserved newer save. If the preserve rename fails, the refusal is a HARD STOP (`_live_path_protected`): the runtime does not fall through to a fresh-start overwrite of the still-live file.
- Unusable-save preservation — `trace.warning("GameRuntime", "Save parsed but could not be applied; preserved it and starting fresh.", {preserved_path})`. Fired when a version-OK payload fails `apply_loaded_state` (e.g. structurally unusable); the file is renamed to `SAVE_PATH + ".unusable.bak"` before the fresh start, so the parseable-but-unapplicable save is never clobbered either.
- Chained-save refusal — `trace.warning("GameRuntime", "Chained save cannot represent a single infinite world; preserved it and starting fresh.", {preserved_path})`. Fired at boot (infinite-world slice 1) when a parseable legacy save below `SAVE_VERSION` fails `SaveMigration.can_represent_infinite` — a TRULY CHAINED v5 save (a non-origin `active_chain`, or `chained_worlds` holding more than the origin nest's `landmark_state`) is structurally unrepresentable on one seamless plane: the file is renamed to `SAVE_PATH + ".chained.bak"` BEFORE the fresh start (the `.newer.bak` precedent), and a failed preserve arms the live-path protection so the autosave can never clobber the un-preserved chained save. Chain-LESS v4/v5 saves flatten LOSSLESSLY to v6 and load normally.
- Atomic-rename failure — `trace.warning("SaveStore", "Atomic save rename failed; the previous save is untouched.", {error})`. Fired when the temp-file→final atomic rename in `write_payload` fails; the previous save is left intact and the write returns false.
- Failed-preserve write refusal — `trace.warning("SaveStore", "Save write refused: a newer/corrupt save still occupies the live path and could not be preserved; not clobbering it.", {})`. Fired (once per store) when a newer/corrupt save EXISTS but its preserve rename failed, so the file is still at `SAVE_PATH`; the store refuses every subsequent write rather than rename a fresh save over the un-preserved file. The in-memory game keeps running and persistence resumes on the next launch once a fresh store preserves the file; in this case the `.newer.bak`/`.corrupt.bak` refusal warning above carries `preserved_path=""`.
- Empty fishing pool — `trace.warning("FishingRuntime", "Fishing pool was empty after the viability filter; the cast yielded nothing.", {rod_id, tier})`. Fired when a bite rolled but EVERY species in the tier pool failed the catalog viability gate (sprites/catch rate/learnset) — the cast then refuses `no_species` (`fishing_refused`). Watch for it after touching `scripts/data/` or the tier pools in `scripts/domain/fishing.gd`.
- Egg species missing — `trace.warning("BreedingRuntime", "Egg species missing from the catalog; hatch skipped.", {species_id})`. Fired when a party egg's pre-computed species no longer resolves in the live catalog at hatch time (catalog drift); the egg stays un-hatched rather than hatching into garbage. The `egg/` catalog entry itself is sprite-only and never hatched (the REAL species entry is used).
- Pen lost — `trace.warning("BreedingRuntime", "A pen no longer encloses; mons relocated to the campsite.", {pen, mons_relocated, eggs_lost})`. Fired from `breeding_runtime.apply_save_state` when a pen's enclosure no longer floods — on load OR LIVE the moment fences are built/demolished (`note_structures_changed` runs the same pass immediately, so a broken ring never strands its mons until the next load): its mons return to the campsite hold (`campsite_pokemon`, the capture-relocation never-lose precedent — NO mon is ever lost to a demolished pen) and its ground eggs are lost; the pasture entry is erased and the change persisted.
- Stone bad target — `trace.warning("GameRuntime", "Evolution stone target species is missing from the catalog.", {item_id, target_id})`. Fired from `stone_evolution_runtime.use_stone_on_mon` when `check_item_evolution` resolves a target the live catalog no longer carries (catalog drift); the use refuses `bad_target`, consumes nothing, and the mon is unchanged.
- Pen not breedable — `trace.warning("BreedingRuntime", "Pen holds Pokemon that cannot breed.", {reason: "not_breedable", pen})`. Fired ONCE per pen (until the geometry changes) when a lay cadence finds mons but no `Breeding.find_pair` pair can form among them (genderless without Ditto, disjoint egg groups, a third compatible mon breaking attraction).
- Pasture action refused — `trace.warning("HabitatRuntime", "Pasture action refused.", {action, reason, species_id})`. Fired on every `habitat_runtime` deposit/withdraw refusal (`no_such_mon` / `egg_guard` / `last_party_member` / `not_in_pen` / `party_full`); the alongside `pasture_refused` event carries the player-facing payload.
- Fishing refused — `trace.warning("FishingRuntime", "Fishing refused.", {rod_id, reason})`. Fired on every `fishing_runtime.try_fish` refusal (`no_rod` / `no_water` / `no_species`); consumes no rng, mirrored by the `fishing_refused` event.
- Overworld pool fallback — `trace.warning("OverworldMonsRuntime", "Overworld pool fell back to the full catalog.", {biome, time})`. Fired when `biome_encounters.filter_species_ids` yields nothing for a cell-center biome/time pair and the spawn pool falls back to the full catalog (catalog/biome-table drift); the slot still spawns, off the fallback pool. Watch for it after touching `scripts/domain/biome_encounters.gd` or `scripts/data/`.
- Entity species missing — `trace.warning("OverworldMonsRuntime", "Entity species missing from the catalog; cannot build an instance.", {species_id})`. Fired from `_stamp_instance` when a live entity's species no longer resolves in the catalog at instance-build time (catalog drift); that Attack/recruit/guardian-battle attempt refuses `no_species` and builds nothing rather than a garbage instance.
- Overworld sprite missing — `trace.warning("EntityLayer", "Overworld sprite missing for <species_id>; using the placeholder.", {species_id})`. Fired ONCE per species when a spawned species lacks an overworld sheet under `pokewilds/pokemon/` (asset-coverage gap — 281/990 species); the render falls back to the source game's own `overworld_not_found.png` placeholder, never a blank sprite. Loud and named, never silent-passed.
