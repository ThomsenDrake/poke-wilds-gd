Status: current
Last verified: 2026-07-27
Review cadence days: 14
Source paths: docs/registry/subsystems.toml, docs/registry/art-anchors.toml, scripts, scenes, tools

# Quality Score

Scores use `0-3` where `3` means strong, mechanically supported coverage.

| Subsystem | Layer | Quality bucket | Legibility | Validation | Architecture | Product completeness | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `app_bootstrap` | `app` | `bootstrap` | 3 | 3 | 3 | 2 | Boot and scene wiring are explicit, trace-backed, and smoke scenarios are extracted into `smoke_scenarios.gd`. The oracle suite runs four lanes: journey/soak playtests, nav/texture/data/layout audits, world-consistency and UI-render-model audits, and a deterministic visual sweep with baseline diffing plus agent vision review. Sweep, UI-render, and display-matrix captures now delegate to the `vision_fidelity` capture contract (frame_post_draw readback guard, validity oracle, transport-honest skips), and the sweep's determinism is pinned in `project.godot` (Slice 2: `msaa_2d=0` + `snap_2d_transforms_to_pixel=true`, measured byte-identical consecutive sweeps; the Nearest-default-filter + integer-stretch candidate was rejected on measured pixel deltas). Slice 3 adds per-shot semantic sidecars (canonical JSON, trace-correlated) and a runner-recorded explainable per-region gate on top of the unchanged global diff — seeded canary and deleted-label perturbations turn the suite red via region gates with no per-bug assertion. Slice 4 wires the `ui_render_audit` pixel half to the `vision_fidelity` glyph template oracle (`scripts/app/text_oracle.gd`): the single GRADUATED flag becomes a per-state `GRADUATED_STATES` dict, so a graduated battle state's glyph mismatch / garble / low_ink fails red with no per-bug assertion while non-graduated states stay quarantine-tier; the audit sheds its pixel-half detail by delegation and stays ≤220 lines. Slice 6 lands the graduation machinery: `tools/graduation_ledger.py` (`record`/`status`/`calibration`) banks each windowed `ui_render_audit` run — head_sha, boot-delimited session identity, per-state finding counts, `text_oracle_passed` payload — into the committed evidence binder `docs/generated/graduation-ledger.json`, so streaks and flippability are COMPUTED from recorded entries, never asserted (unstamped historical sessions are never backfilled into flip-qualifying streaks). Per-state `GRADUATED_STATES` flips (line 16) then proceed on ledger-recorded evidence only — 5 consecutive clean windowed runs at the current HEAD, in the documented order (battle_moves + battle_item FIRST: ANCHOR glyph-template match; battle_action NEXT: lint cleanliness on ACTION_ROWS; battle_message LAST: BOX mode with a required documented judgment) — and each flip graduates that state's entire pixel lint (glyph + lint findings go red together); the flip changes the verification harness's tiering, not the game. Slice-6 Graduate phase (2026-07-21, HEAD 7b733946): all four battle states flipped on ledger runs 1-5 (moves + item anchor, action lint, message box with the documented engine-owned ~1px pen judgment), run 6 the post-flip clean confirmation (streak 6/5); the Graduate-phase seeded-defect proof went RED via the graduated gate with no per-bug assertion while the same defect class stayed quarantine in a temporarily un-graduated state. Product completeness stays 2: the flips change the verification harness's tiering only, not game behavior. NOTE (Workstream L.1): the one-command local gate `tools/verify_all.py` now MECHANIZES the refuse-on-mismatch (report `head_sha`==HEAD; windowed `godot_version`/`renderer`/`window` vs the baseline capture_env plus the canonical window) and freshness (`vision-review.json` via `review_is_fresh`) refusals previously "human/agent-enforced policy until verify_all" — the rubric's "mechanically supported" bar; scores unchanged (it changes verification orchestration and tiering, not game behavior). NOTE (miss-002, 2026-07-22): the nav_audit false-red closed at the class level — the battle half pins BOTH runtime rngs via `seed_for_smoke(NAV_AUDIT_SEED)` (the house seeding convention; the pass event is a pure function of (code, save, seed), never per-process wall-clock rng), the RUN check is total (a legitimate "Can't escape!" trap refusal is proven, the trap expired, and escape re-proven — new coverage, never a suite failure), and the shared harness now collects any `<scenario>_failed` trace plus captured `push_error` "ERROR: " lines as named failures (ERROR_MARKERS single-sourced across both transports), so a red can never be reasonless; the two-part class-level plant executed and reverted byte-identical (ledger entry #2). Scores unchanged: verification determinism and reporting honesty, not game behavior. Phase-3 controls (exec plan A1–A5, generalized in the Phase 3 final review): `input_router.gd`'s same-frame latch is now GENERALIZED (`_ui_ate_press`, set by `bind_ui_consumers` connecting the argless closed/confirmed/cancelled signals of all four input-phase UIs — CampMenu, StartMenu, MessageBox, StorageScreen), killing EVERY same-press double-fire at the shared root (Enter no longer opens the start menu under a closing camp menu; Z-on-Demolish no longer re-fires harvest/build on the bare former campfire tile; a start-menu CLOSE no longer re-harvests the faced tile; a MessageBox NEW GAME confirm no longer harvests the brand-new world's spawn-facing tile; an inert party-screen FIELD MOVE no longer harvests the faced cut-tile its capability message just declined — `main.gd::_toggle_menu` and the `overworld_idle` poll additionally guard on camp-menu AND StorageScreen visibility for direct-call paths, four same-line edits only), the party-screen FIELD MOVE entries for non-harvest moves are explicitly capability display with a degrade message (verified species-flag counts + the inert-move deferrals in menu-and-save.md), and the `input_gate` scenario (real input-phase injection via `Input.use_accumulated_input` and the shared `smoke_tap.gd`: press/release in separate frames, injection witnesses, proven red-before/green-after) covers the camp-menu race frames (parts A/B) plus the start-menu CLOSE / inert-FIELD-MOVE / NEW-GAME-confirm leak paths (parts C/E/D in `input_gate_menu_checks.gd`) that `smoke_scenarios._press` structurally cannot reach. The latch's BATTLE-END arm (the surviving same-press leak the menu arm left open): a press that ENDS A BATTLE re-fired the overworld context action the same frame — Z-on-RUN / a ball-select Z capture resolve SYNCHRONOUSLY in `battle_view._unhandled_input` (input phase) and `main._on_battle_finished` re-enables the overworld within it, so the same-frame `poll_context_action` harvested/built the faced tile (the harvest toast superseded "Got away safely!") — is closed at the shared root: `input_router.gd` gains the public `note_press_consumed()` latch setter and `main._on_battle_finished`'s `_in_battle = false` line calls it (one same-line edit; main.gd stays 220/220), covering EVERY end path (RUN, capture, victory, defeat — the only end classes; any future press-driven end, e.g. a message advance, is covered by the same unconditional single-endpoint latch) uniformly, and the new `battle_end_input` scenario (same real-input-phase mechanism via `smoke_tap.gd`: press/release in separate frames, injection witnesses, seeded by `seed_for_smoke`, proven red-before/green-after) proves the escape AND capture end-presses no longer re-fire (the faced cut trees stand, the escape/capture toasts survive) AND that a fresh deliberate press the next frame still harvests (the latch is reset by the context poll, so the fix never over-suppresses). Scores unchanged: a bug fix + regression scenario + doc co-mods, no new game behavior. |
| `session_runtime` | `runtime` | `runtime` | 3 | 3 | 3 | 2 | Save schema v4 (bag, time of day, steps, world overrides, the campsite hold, placed structures, and since Phase 3 per-placement storage-box `contents`) with v1/v2/v3 migration (v3→v4 purely additive — absent `contents` backfills to an empty box; a v3 build refuses a v4 save non-destructively to `.newer.bak`) — covered by the dedicated `save_migration` fixture (v1/v2/v3→v4 plus the future-version refusal) — plus ATOMIC temp+rename writes and non-destructive corrupt/future-version recovery (`.corrupt.bak`/`.newer.bak`, `save_recovery` plus a version-refusal `warning`), a non-losing full-party capture HOLD retrievable from the party screen, biome/battle music routing, traces, and smoke scenario bootstrapping are centralized. The exit-time ObjectDB/resource leak (an OGG stream still playing at process exit) is fixed at the source with a headless audio gate (plus a windowed `_exit_tree` stop as defense-in-depth). The runtime exposes the smoke-only `seed_for_smoke(seed)` determinism seam (pins the encounter and battle rngs together; real play never calls it), and the corrupt-save recovery parses quietly (`JSON.new().parse` returns an error code without the engine printing) so a handled refusal emits no spurious engine error line (miss-002). |
| `world_runtime` | `runtime` | `world` | 3 | 3 | 3 | 3 | Progressive biome generation with data-driven traversal rules, species-level spawn tables with source-token aliases, navigable spawn with reachability proof, field-move-gated walkability, and day/night presentation. `validate_invariants` is exercised by the `biome_probe` smoke scenario. |
| `pokemon_data` | `data` | `data` | 3 | 2 | 3 | 3 | 954 species, 299 moves, and 114 items parsed from the source tree (base stats, evolutions, spawn biomes, field-move flags, dex data; the item count includes the runtime `potion` supplement the source i18n omits); parsing is split into single-purpose parsers with a summary warning trace. |
| `pokemon_progression` | `domain` | `progression` | 2 | 2 | 3 | 3 | Growth-curve EXP, level-up move learning, level/item/happiness evolution checks, and gender assignment from species ratios (drives infatuation) are extracted into pure domain rules. |
| `battle_loop` | `runtime` | `battle` | 3 | 3 | 3 | 3 | Battle runs on mainline formulas (type chart, stat stages, status and volatile conditions, capture, EXP yield, level evolution, priority turn order) with per-move source animations (frame-paced and deterministic, KO blows included), species cries, integer-scaled crisp presentation, and scenario coverage (`wild_battle`, `battle_anim`, `ui_render_audit`). HP bar alignment (2026-07-21): the programmatic enemy/player HP bars were translated -11 stage px so they now sit on the GSC art's baked black tracks (enemy x32..79 y18..21, player x96..143 y74..77, width 48 preserved) — a scene-only `BattleView.tscn` change with NO .gd or render-model change (the model encodes no bar rects; sidecar `draw_order` is live-captured), exactly the four battle baselines (09-12) regenerated with overworld/menu baselines byte-identical, and visual_sweep green at 0/16 changed. Scores unchanged: alignment only, no product-behavior change. |
| `menu_ui` | `ui` | `ui` | 3 | 2 | 3 | 3 | Start menu now hosts a full party screen (swap lead, summary, field moves, and a campsite `RETRIEVE` action for held Pokemon) and bag screen (item use on party); New Game is confirm-gated through the message box (the menu closes on confirm, stays open on cancel), all trace-backed and cooperating with battle transitions. |
| `harvest_loop` | `runtime` | `harvest` | 3 | 3 | 3 | 2 | Harvesting resolves cut/dig/smash on faced tiles with party capability, permanent sparse overrides, and save v3 persistence. Phase 5 added a DIG BONUS POOL parallel to the unchanged base yield (`harvest_resolver.DIG_BONUS_POOLS`/`bonus_for`/`_dig_draw`, a pure step-counter SplitMix over total_steps+tile — NO shared `_rng` consumed; `harvest_runtime` emits `dig_item_found {tile, item_id, biome}`): FAITHFUL for the beach (SAND → the `fresh-beach.md` "Dig Items" pool big_pearl/water_stone/clear_glass/revive), with FLAGGED PORT DIVERGENCES for GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP stone pools + the invented `DIG_BONUS_RARITY := 8` rate (PLAINS deliberately pool-less; ice/dawn stones unassigned — SNOW isn't diggable). Stone acquisition is proven by `field_moves_flow`'s dig group; completeness stays 2 by design (the harvest economy's consumer story is still building/crafting-driven, and the non-Beach pools are divergences, not scraped fidelity). |
| `building_loop` | `runtime` | `building` | 3 | 3 | 3 | 2 | Phase 1 placement: pure-domain structure definitions + faithful per-biome costs (`scripts/domain/structures.gd`), a runtime placement orchestrator that enforces Fighting-type Build capability and consumes bag materials through the session item API (`scripts/runtime/build_runtime.gd`), and a ghost-preview/build-mode/occupancy layer that COMPOSES `world_view.gd` without extending it (`scripts/runtime/structure_layer.gd`), with the overworld-context and party-screen `FIELD MOVE` entry points extracted into `scripts/app/field_action_router.gd` (the party-screen move id is now consumed, not decorative). Placements ride the world's sparse override map as a "placed" entry kind and persist under a NEW ADDITIVE v3 `structures` save key (version stays 3; the clears key is untouched), rendered in place through the world view's existing prop pipeline via a merged `mutations_for_view()` mirror — so logic/render/collision occupancy agrees by construction and `world_view.gd` is never extended. Legibility is trace-backed (build_mode_entered / structure_placed / structure_refused with a reason / materials_consumed / structure_demolished / materials_refunded / placement_flow_passed, refusals never silent); validation is mechanical (the `placement_flow` scenario proves refusal, wall+door placement with exact payloads, occupancy blocking pathing, save round-trip, the would-trap guard, and the demolish/refund round-trip; `world_consistency_audit` extends its three-way check to placed structures; `save_migration` round-trips a structures-bearing v3 payload; `visual_sweep` gains a deterministic `13_built_house.png` baseline). Product completeness stays 2 by Phase-1 design: a walled house with a door is buildable from harvested materials (the exit object), but bed/storage-box interactivity, the indoor-enclosure flood-fill, and full per-biome sprite coverage are deferred to Phases 2-3 (tech-debt-tracker), and the bed is unbuildable until the Phase-2 soft_bedding craft. SOFTLOCK FIX (review-found MAJOR, 2026-07-23): a player could permanently enclose themselves with their own placements (no escape existed — harvest ignores built tiles, demolition was Phase 2+). Both complementary fixes landed: (A) ORIGINAL-FAITHFUL DEMOLITION — Cut on a built tile refunds the full `cost_for(id, biome)` and hard_stone-cost shells need Smash (`structures.demolish_move_for`), wired into the harvest Z-action flow (`game_runtime.harvest_tile` routes placed tiles to `build_runtime.try_demolish`; `structure_demolished` + `materials_refunded` traces; the removal deletes the PLACEMENT entry only via `world_generator.remove_placement`, so the two-map v3 design keeps the underlying clear fact automatically); (B) the WOULD_TRAP guard in `try_place` — a hypothetical stamp + flood-fill from the PLAYER tile (`reachable_walkable_count`, budget 2) refuses `structure_refused{reason: would_trap}` for any solid placement that would leave the player walled in on their own tile (pens enclosing mons are intentional — player-tile reachability only; doors never trip it). `placement_flow` was extended with the exact four-sided trap attempt (the enclosing wall refused) and a build->demolish->refund round-trip (exact refunds, tile walkable again, clear fact intact); the demolition checks are extracted into `scripts/app/placement_flow_demolition.gd` for the app-layer line budget. HARDENING PASS (review-minor batch, 2026-07-23): the trap guard's hypothetical is now mutation-free BY CONSTRUCTION — it passes the candidate tile as one extra unwalkable tile via `reachable_walkable_count`'s new `blocked` seam instead of add/remove-stamping the live placements map (a future signal/trace/cap change to `add_placement` can no longer leak a phantom placement); the demolition proof's clear-fact assertion rides a public `tile_has_clear` seam instead of reaching into the runtime's private generator; and the load-bearing material->demolition WITNESS invariant behind the region-scale enclosure escape (log->Cut, hard_stone->Smash — every shell class has a cost material only its demolish move yields) is mechanized in `unwitnessed_demolish_moves()` and asserted on every `placement_flow` run, with the spec documenting that no non-harvest material source or party-release mechanic may ship without extending the guard. Scores unchanged: hardening + mechanized contract, no product-behavior change. Determinism and the save schema are unchanged. |
| `camping_loop` | `runtime` | `camping` | 3 | 3 | 3 | 2 | Phase 2 camping/night survival composed over the Phase-1 placement system: the single day/night source of truth mirroring the world-view tint keyframes (`scripts/domain/day_phase.gd`, with `night_cycle` asserting the 269/270/1229/1230 boundaries agree so the duplication cannot drift), a light model (`scripts/runtime/night_system.gd`: lit campfire/torch within Manhattan 4 or a Flash-capable Fire-type party member — a capability READ, not the Phase-4 Flash action), night ghost encounters at half the light-less cadence on the shared `_rng` (type-derived sorted pool, deterministic under `seed_for_smoke`, the original's Damp/Mystic/Cursed-Soil dwell replaced by night-without-light — documented), shadow battles that block retreat for their duration via an injected callable (battle_runtime never imports night_system — the layering holds), rest/heal/campsite in `scripts/runtime/camping_runtime.gd` (sleeping bag 50% HP + fainted revive, NO status cure — the faithful answer; bed = full heal + status cure; resting writes the `campsite_tile` anchor the overflow-mon hold and blackout return both read, and blackout now returns to the campsite instead of world origin), the lit light-source glow composed INSIDE `structure_layer.gd` (a static generated gradient, night-only, no per-frame animation — the byte-stability contract; the design's documented fallback path, so world_view.gd stays untouched), and the encounter level/fallback helpers extracted into `scripts/domain/encounter_selection.gd`. Legibility is trace-backed (campfire_lit / rested / campsite_established / night_hazard_spawned / retreat_blocked / evolution_time_gate, refusals never silent); validation is mechanical (camp_survival + night_cycle + time_evolution scenarios + the visual_sweep_camping baselines, all seed-pinned, under verify_all). The budget-full files are touched only by extraction/same-line edit (game_runtime keeps its 320 budget by freeing the campsite accessors + encounter helpers into `camping_runtime.gd`/`encounter_selection.gd` before the new wiring, main.gd one same-line setup argument, pokemon_rules untouched — the Espeon/Umbreon/Frosmoth evolution fix is caller-side with the empty-context fail-open retained and documented). Product completeness stays 2 by design: the ghost trigger is the dev-attested simplification, the retreat block lasts the battle (time freezes mid-battle), dawn/dusk map to DAY, the overworld ghost sprites and the Flash field-move action are deferred, and the evolution gate's fail-open empty context is a documented guarantee, not a mechanic. |
| `crafting` | `runtime` | `crafting` | 3 | 3 | 3 | 2 | Phase 2 campfire crafting: a pure-domain faithful recipe table (`scripts/domain/recipes.gd` — Poke Ball 1 magnet + 1 hard_shell, Soft Bedding 3 soft_feather + 3 silky_thread, Old Rod 1 log + 1 silky_thread, Good Rod 1 old_rod + 2 metal_coat, Super Rod 1 good_rod + 3 magnet — scraped from the wiki Campfire/Items pages with line citations) plus the kiln-gated Great Ball (poke_ball + metal_coat) DEFINED but UNAVAILABLE (no kiln structure in any ported phase; the exec-plan parenthetical that conflated it with the Poke Ball recipe is corrected in the spec, not flattened). `crafting_runtime.gd` crafts all-or-nothing through the session item API with reason-carrying `craft_refused` refusals (no_station / wrong_station / missing / no_recipe), the torch joins `structures.gd` appended LAST (stable cycle indices; faithful 1 Log + 1 Grass → log + dry_soil), and the campfire menu (`scripts/ui/camp_menu.gd` + `scenes/ui/CampMenu.tscn`, ≤220/≤250, ui-references-only) lists exactly the 5 campfire recipes with missing counts greyed — no discovery state (the wiki has none), so nothing about recipes persists. The ingredient source is a minimal deterministic INTERIM (`scripts/domain/material_drops.gd`: ELECTRIC→magnet, WATER→hard_shell, STEEL→metal_coat, BUG→silky_thread, FLYING→soft_feather — mapped 1:1 from the wiki Materials drop table, 100% on a type-matched victory/capture, pure/no-rng, granted by game_runtime with a `material_dropped` trace), witness-safe by construction (the table NEVER contains log or hard_stone — craft_flow asserts the drop-witness invariant every run via crafting_runtime.drop_witness_clean (is_drop_material)) and flipped to the documented SECONDARY by the Phase-5 faithful habitat drops (header flipped, kept not deleted; tech-debt + spec handoff). Legibility is trace-backed (item_crafted / craft_refused / material_dropped); validation is mechanical (the craft_flow scenario: refusals, exact bag deltas, the station listing, can_afford("bed") after soft_bedding, save round-trip). Product completeness stays 2 by design: crafted ball tiers are bag inventory only until the Phase-8 ball tiers, rods craft but do not fish (Phase 5), kiln balls/repel/apricorn balls are out, and the bag heal is flat 50% (indoor bonus + terrain/aggressive restrictions deferred with the enclosure flood-fill and the Phase-6 aggressive-chase system). |
| `storage` | `runtime` | `storage` | 3 | 3 | 3 | 2 | Phase 3 storage boxes & party management composed over the Phase-1 placement system: each craftable Storage Box (2 Log, placed like any structure) is an INDEPENDENT container — contents live as an additive array ON the placement entry keyed by tile (the `"lit"` precedent; `WorldOverrides.is_valid_placement` checks only kind + structure_id, so the round-trip needs ZERO validator changes and the generator's save/load plumbing is untouched), never a shared PC (faithful: "the contents of the box are not shared with other built boxes", `.firecrawl/buildings-scrape.md:294`). `scripts/runtime/storage_runtime.gd` (RefCounted; box identity IS its tile, so two boxes can never share by construction) drives open/deposit/withdraw/release through reason-carrying `storage_refused` refusals (`no_box`/`no_such_mon`/`last_party_member`/`party_full`/`would_strand_demolition` — never silent); the storage screen (`scripts/ui/storage_screen.gd` + `scenes/ui/StorageScreen.tscn`, ui-references-only, WIRED through the campfire-menu Z-route pattern in `field_action_router.gd` — the `BOX_ID` arm in `_route_camp_object` opens it, `_overlay_open` covers BOTH overlays, the `closed` signal re-enables + saves and feeds `input_router`'s generalized latch via `main.gd`'s `bind_ui_consumers`, and `storage_flow` drives the box loop through the real Z seam) adds a CONFIRM-GATED release through the MessageBox sibling (the 0.2 destructive-action contract; "It will be gone for good."), with the release-confirm MOUSE-bypass gated at `_on_entry_clicked` (a click on the still-visible action list mid-confirm cannot mutate the box, so the keyboard confirm releases only the mon it named); and `build_runtime.try_demolish` refuses a non-empty box (`demolish_refused{box_not_empty}`, checked before `remove_placement` — faithful to `.firecrawl/pokewilds-build.md:38`'s filled-chest rule reduced to the single-floor port), so demolition can NEVER destroy contents and an empty box Cuts to the full 2 Log refund. Legibility is trace-backed (box_opened / mon_deposited / mon_withdrawn / mon_released / storage_refused / demolish_refused); validation is mechanical (the `storage_flow` scenario: box round-trip opened through the real overworld Z seam (the router's `BOX_ID` arm), confirm-gated release on both branches, the release-confirm MOUSE-bypass group (`storage_release_mouse_check.gd`: a click mid-confirm mutates nothing; the keyboard confirm releases the mon it named), every refusal, the non-empty Cut refusal, box INDEPENDENCE in the world AND across save, save round-trip with corrupt-contents normalization, the overflow NO-ROUTING proof (full-party capture → campsite hold, never a box), and the dynamic witness guard; `save_migration` gains the v4 fixture; `visual_sweep_storage` gains the deterministic `18_storage_box` baseline; `ui_render_audit` gains a `storage` state). Save schema v4 is PURELY ADDITIVE (the v3→v4 bump the building spec pre-pinned; `SAVE_VERSION := 4`, absent `contents` backfills to `[]`, `_normalize_structures` sanitizes corrupt contents to an empty box — never a crash or a torn entry). Party management grew arbitrary REORDER (MOVE, replacing swap-lead-only; `set_party_lead` delegates to `move_party_member`) + DEPOSIT to the nearest adjacent box (`box_tile_near` deterministic scan — the "right next to an entrance" rule), and the load-bearing demolition-witness invariant is extended DYNAMICALLY: deposit/party-release refuse to strand the party's last witnessed Cut/Smash carrier while structures stand (the party pool counts, box contents NEVER — a box may be sealed away; the static `unwitnessed_demolish_moves` audit is unchanged and `storage_flow` asserts the dynamic half every run). Budget-full files were touched only by extraction + same-line edit (game_runtime freed ~23 lines by extracting `harvest_tile` into `harvest_runtime.gd` first; phase0_scenarios extracted `wild_battle`; party_screen extracted `party_actions.gd`; main.gd four same-line edits — the `bind_ui_consumers` line, the `_field_router.setup` argument, the `overworld_idle` StorageScreen check, and the `_toggle_menu` StorageScreen guard; start_menu dict-only). Product completeness stays 2 by design: release is permanent removal (the original's world-drop needs overworld-mon entities — Phase 6) behind double-emphasized confirm wording, nickname is deferred (mons already carry names — an additive RENAME later), there is no per-box mon cap (faithful: mons stack until moved), and overflow captures stay on the campsite hold (the always-exists no-loss anchor; boxes are MANUAL — the retrieve→deposit loop is the player's path from hold to box). |
| `field_moves` | `runtime` | `field_moves` | 3 | 3 | 3 | 2 | Phase 4 field-move completion: the eight moves that had capability rules but ZERO runtime callers (Flash, Teleport + Way Stones, Ride, Fly, Attack, Charm, Repel, Power) gain runtime callers, traces, and scenario coverage, completing the Phase-2 night design and wiring the hooks Phase 6/7 consume. The capability MODEL (`field_moves.gd`) is UNCHANGED — the spec records the fly/ride explicit-flag-only fidelity DECISION (no AUTO_TYPES broadening: fly stays 30 species, ride 32, the faithful db model; can_perform already returns false on an empty auto-type), so the all-moves party rides explicit flags (Charizard fly=1, Rhyperior ride=1). Orchestration is EXTRACTED into the NEW `scripts/runtime/field_move_runtime.gd` because `game_runtime.gd` is at its 320 budget (same-line instantiation + shared `_rng` injection only); player-facing entry rides `field_action_router.gd` (has room) with same-line `main.gd` edits (220/220). Flash keeps its faithful PASSIVE light (a Fire-type party member lights `LIGHT_RADIUS=4`, "the same range as a campfire"); Phase 4 adds the active caller + `flash_lit` trace + a Flash-only-party-keeps-the-dark-lit scenario, never diverging from the campfire range. Way Stones (new `way_stone` structure; the registry is the placed entries themselves on the `structures` save key — no separate session key) are intra-world warp targets for Teleport (`teleport_used`) and Fly (`fly_used`, visited-only with unvisited refusal); the world-edge Teleport Beacons + edge-fly chaining are explicitly Phase 7 (the Way-Stone/Beacon naming split is kept airtight). Ride adds a mount speed mode + sprite beyond the 0.09s run (`mount_summoned`, speed-only — no ledge-climbing); Power moves a NEW movable boulder prop (`structures.gd` `BOULDER_ID`, riding the placements map / `structures` save key, no build cost — distinct from Smash's destructible rock); Repel sets an additive `session_state.repel_steps` counter that short-circuits `generate_wild_encounter` before consuming encounter rng (`repel_active`, structural suppression — a documented fidelity gap vs the original's crafted low-level-only item). Attack/Charm are HOOKS only (`overworld_attack` / `charm_used`, the latter level-gated) — the overworld-Pokémon entities land in Phase 6, so the scenarios assert the hook is reached + traced + refuses gracefully with no target, never any mon state. Legibility is trace-backed (the eight domain events + the three scenario pass markers with their symmetric `_failed`, refusals never silent); validation is mechanical (the `field_moves_flow` scenario's precondition witness asserts `party_has_field_move_ability` for all 13 moves against the live catalog so drift fails loud, then the eight per-move happy-path + refusal checks PLUS the Phase-5 dig-acquisition group LAST (`field_moves_dig_checks.gd` — the stone-pool contract witness, a Beach SAND water_stone hunt predicted via the resolver (faithful pool), the first-found divergent biome pinned to the SHIPPED `DIG_BONUS_POOLS` table (a flagged port divergence), and a PLAINS negative control; `dig_acquisition_ok` in the payload, the witness for the harvest loop's `dig_item_found`); `build_house_flow` harvests→builds a house with a door→demolishes with exact refund under the all-moves party; `playtest_field_soak` extends the bot's repertoire, self-guarded; all seed-pinned + dispatcher save-guarded under verify_all). The all-moves party (`FIELD_MOVES_PARTY` = RHYPERIOR/CHARIZARD/MACHOP/CALYREX/DEDENNE/DRAPION, the DP-proven 6-species minimum cover) is a single shared constant swapped via `smoke_scenario_runner.swap_party` and restored on every exit. Product completeness stays 2 by design: Attack/Charm are hooks awaiting Phase-6 entities, Fly/Teleport are intra-world only (edge chaining = Phase 7), Ride is speed-only, Power is movable boulders only, Repel is the field-move simplification, and the active Flash is a convenience over the faithful passive light. |
| `breeding_loop` | `runtime` | `breeding` | 3 | 3 | 3 | 2 | Phase 5 Pokemon systems (shinies, breeding, habitat drops; the evolution-stone bag-use seam WIRED AND stone ACQUISITION landed — faithful Beach-Dig + flagged port additions): the breed→hatch→shiny-hunt loop composed over the Phase-1 fence (pens are sealed fence rings detected by ONE SHARED flood both runtimes ride — `Breeding.detect_pens`, `PEN_FLOOD_BUDGET` 256, min-y-then-x anchor; a gated ring is no pen) with NO overworld-mon entities (the pasture abstraction stands in until Phase 6). Shinies: an additive `is_shiny` instance field rolled 1/256 at creation (`pokemon_rules.roll_shiny` — the injected shared `_rng` when threaded, a deterministic nonce fallback otherwise; pinned by `seed_for_smoke`), rendering as a faithful battle PALETTE SWAP (front.pal→shiny.pal exact-match remap over the strip before the AtlasTexture crop, cached; sparkle overlay at battle start) plus a status/egg icon badge (shiny.png/shiny_inverse.png beside gender), with the overworld rendering shinies identically to normals for anything WILD (faithful — `wiki-overworld-encounters.md:230`; the Ride mount and pen egg entities swap `overworld-shiny.png` when it exists). Breeding: female + compatible male sharing an egg group (parsed at `species_file_parser.gd:114-118`) in a type-matched habitat pen, both happy (≥220 — the shared happiness gate that also feeds the wired happiness evolution), Ditto breeds with anything, egg moves from the FATHER (a NEW `parse_egg_moves` over the previously unparsed `egg_moves.asm`), eggs fully visible pre-hatch (gender/moveset/shiny), step-based hatching at level 5 (2560-step minimum — hatch-cycle data unparsed), and `ALLOW_UNDISCOVERED_BREEDING := false` — the FAITHFUL default (the exec plan's genderless/Undiscovered workaround stays one flagged const flip away); exactly ONE compatible pair per pen breeds (a third compatible mon breaks attraction — faithful; extra incompatible mons ignored). The lay gate rides the SAME habitat scan as drops (`HabitatDrops.pen_habitat_tags` over the pen interior + one-tile ring, `types_satisfied` per parent, AND'd — drops-comfortable ⟺ the lay-gate passes by construction), and the pasture store is ONE session key held BY REFERENCE (`breeding_runtime._pastures` IS `session.pastures`; `habitat_runtime` reads/writes the same dicts in place, so the daily +10 reaches the 220 gate the SAME step — no save roundtrip; the withdraw erase guard keeps breeding's eggs). Habitat drops: the FAITHFUL primary material source (comfortable happy penned mons drop the type→material table once per in-game day — dual-types drop each type's material; the species-override families ALL implemented: Honey/Soft Wool/Star Piece/Berry Juice/Ancientpowder + Miltank→`moo_moo_milk` via a `RUNTIME_ITEM_SUPPLEMENTS` entry), flipping the Phase-2 battle-end `material_drops.gd` table to the documented SECONDARY source (kept, not deleted), with the type→pen-tile habitat table mapped to the port's biomes (documented basic-ground placeholders for GHOST/STEEL where no faithful tile exists; the Dragonite line prefers water only) and the load-bearing demolition-witness invariant reconciled (Rock→hard_stone WITHHELD from drops; `HabitatDrops.witness_clean()` asserted every `habitat_drops` run; the Phase-5 Steel-type shiny_stone cadence drop is witness-safe by construction — shiny_stone is neither log nor hard_stone, and `is_habitat_drop_material` walks it). DIVERGENCE (Phase 5 stone acquisition, flagged): a happy comfortable Steel-type mon also yields ONE shiny_stone per ≥4 in-game-day window (`habitat_drops.STEEL_STONE_DROP`/`STEEL_STONE_CADENCE`, a second `_day_tick` gate that reuses `item_dropped`, never `drops_for`'s daily path — `TYPE_MATERIALS["STEEL"]` stays exactly `[metal_coat]`; `wiki-materials.md:393` documents Metal Coat ONLY). Comfort re-evaluates on each crossed in-game day boundary (+10 happiness per comfortable day tick, no decay — a documented divergence from the original's deposit-time freeze, ~9.5x slower than the design's first hourly model, pacing the once-per-day drop rhythm, so the "becomes uncomfortable → forfeits the drop" quirk is observable). Evolution stones: the bag-use seam is WIRED — the new `scripts/runtime/stone_evolution_runtime.gd` (validate → consume → swap → trace — a refused bag decrement is the distinct `no_item` refusal and applies nothing) behind the one-line `game_runtime.use_stone_on_mon` delegation (the exact `breed_flow_checks.STONE_SEAM_METHODS` name, so `run_stone_case` auto-arms), with a bag-screen stone branch reusing the party picker; `evolution_stone_used {item_id, species_id, evolved}` (source `GameRuntime`, registry-required on `pokemon_progression`) now has its FIRST live caller — the stone is consumed BEFORE the swap, `is_shiny` + level/exp/moves/happiness ride the species swap while stats/max_hp/current_hp/catch_rate RECOMPUTE off the target entry (the exact level-evolution path, `battle_runtime._try_evolve`), and a no-effect use ("It would have no effect.") consumes nothing and emits no trace (the Potion precedent). All ten stone ids resolve from `item.properties:80-89` (thunderstone has NO underscore). Stone ACQUISITION is IMPLEMENTED (tech-debt item 11 RESOLVED): the faithful Beach-Dig pool (SAND → water_stone among big_pearl/clear_glass/revive, `fresh-beach.md`; surfaced via `dig_item_found`) plus flagged PORT ADDITIONS — the other-biome Dig pools (GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP), the invented `DIG_BONUS_RARITY` rate, and the Steel-type shiny_stone cadence drop above (reusing `item_dropped`; `wiki-materials.md:393` documents Metal Coat ONLY) — all pure step-counter draws that consume NO shared `_rng` (seven scenarios re-ran byte-identical, the shiny_odds pin holding); ice/dawn stones stay grant-only (SNOW isn't diggable — the item-11 residual). Legibility is trace-backed (shiny_rolled / egg_laid / egg_hatched / egg_picked_up / mon_penned / breeding_refused / habitat_happiness_changed / item_dropped / pasture_deposited / pasture_withdrawn / pasture_refused / evolution_stone_used — refusals are reason-carrying events, never silent; the pen-lost recovery — on load AND live on demolition — rides the warning tier); validation is mechanical (the `breed_flow` scenario proves the contract witness, pairing→egg→hatch with the father's egg move, the Ditto rule, the unbreedable case, the wrong-egg-group negative (a happy EEVEE×ABRA — both basic-recessive, so habitat cannot confound the gate — with catalog-asserted disjoint groups AND catalog-asserted habitat-neutrality lays nothing across eight cadences), the Flying/Water ring-scan proofs (a PIDGEY pair lays in a pen built AROUND a live tree prop; water proven at the domain layer through `pen_habitat_tags` + `types_satisfied` with a no-water control, the runtime MAGIKARP pen auto-arming if a seeded pond site exists — else a named skip), egg pickup, the armed stone group (`evolution_stone_used` + FLAREON), and the save round-trip; `shiny_odds` is STATISTICAL but SEEDED — exact precomputed expectation on the pinned seed, never flaky, plus the adjustable-odds hook proof (`set_shiny_odds(2)` flips the rate); `habitat_drops` proves satisfied/unsatisfied comfort, the daily drop + cadence, the milk override, the witness audit, AND the Steel-type shiny_stone cadence control (`run_steel_drop_control` — first-drop-day stone, three-day suppression, re-arm; `steel_ok`); `visual_sweep_pokemon` baselines the shiny battle sprite + the egg in pen; all under verify_all). All Phase-5 state is ADDITIVE on save v4 (no bump — v5 stays reserved for Phase 7; eggs ride the party array as zero-HP additive mon keys the healthy-party scans skip naturally, pastures ride ONE additive session key held BY REFERENCE by both runtimes (validation stays single-entry in `breeding_runtime.apply_save_state` — a lost pen relocates its mons to the campsite hold, never loses them; `is_shiny` backfills absent→false). Budget-full files touched only by extraction + same-line edit (breeding.gd / habitat_drops.gd / breeding_runtime.gd / habitat_runtime.gd extracted new; pokemon_rules gains only the shiny roll + is_shiny key; game_runtime same-line wiring; main.gd same-line tick; session_state one additive key). Product completeness stays 2 by design: no overworld-mon entities (pasture abstraction; Phase 6 owns wild egg nests + egg-theft aggression + the Attack-on-egg shiny check), Rock drops withheld (witness invariant), the holding-item farm loop + talk-to-mon interaction deferred, hatch-cycle parsing deferred, evolution-stone ACQUISITION landed (bag USE wired + the faithful Beach-Dig pool + flagged port-addition Dig pools and the Steel-type shiny_stone cadence drop; ice/dawn stones stay grant-only — the item-11 residual), and the adjustable-odds MENU entry is future UI (the `set_shiny_odds` domain hook exists today). |
| `fishing` | `runtime` | `fishing` | 3 | 3 | 3 | 2 | Phase 5 rod-tiered fishing composed over the Phase-2 crafted rods (Old/Good/Super already craft into the bag at the campfire — `recipes.gd:40-42`, exact wiki match): face a water tile and press `Z` to cast the BEST rod held (a bag rod-USE entry is NOT wired — the overworld Z is the only trigger) — fishing works FROM SHORE without Surf (water tiles are `walkable=false`/`encounter=false`/surf-gated, `biome_defs.gd:62`), so fishing is PURELY ADDITIVE (no existing encounter stream changes). Each cast rolls a per-tier BITE on the shared seeded `_rng` (Old 55% / Good 70% / Super 85% — the wiki describes no minigame, so the wait is this single roll, `fishing_cast` traced on every rng-consuming cast); a bite draws from the rod tier's CUMULATIVE pool (Old: MAGIKARP/TENTACOOL; Good + HORSEA/CORSOLA; Super + QWILFISH — five Beach-scraped species, the documented Beach-anchored approximation, `fresh-beach.md:63` the ONLY scraped biome table) filtered to battle-viable species, at a FIXED per-tier level band (`TIER_LEVEL_RANGE`: Old 3-7 / Good 8-14 / Super 15-22); the shiny roll APPLIES to hooked mons (water shiny hunts are real). The hooked mon rides a PENDING-ENCOUNTER seam (`generate_wild_encounter` returns it BEFORE the wild draw — Repel and unlit-night ghosts never touch fishing), and `fish_caught` traces a fishing-battle capture (`consume_fishing_battle` resets on every finished battle, so the mark never leaks). Orchestration is EXTRACTED into the NEW `scripts/domain/fishing.gd` + `scripts/runtime/fishing_runtime.gd` (game_runtime at its 320 budget — same-line instantiation + the pending-seam + the capture trace); player-facing entry rides `field_action_router.gd` (a fishing arm ahead of the "nothing left here." fall-through). Legibility is trace-backed (`fishing_cast` / `fish_hooked` / `fish_caught`; `fishing_refused` reason-carrying — no_rod/no_water/no_species — never silent); validation is mechanical (the `fishing_flow` scenario: craft→cast→bite→hook→battle, the strict tier-gate containment under reseed, the refusals + the seeded bite-miss, the pending-seam proof under Repel, the shiny application, seeded determinism; under verify_all). Product completeness stays 2 by design: biome-agnostic tables (Beach anchor only — per-biome tables need fresh scrapes), the single bite roll (no cast/wait minigame), and the Lure Ball (3× fished) / Dive Ball (3.5× water) catch bonuses wait on the Phase-8 ball tiers. |
| `overworld_mons` | `runtime` | `overworld_mons` | 3 | 3 | 3 | 2 | Phase 6 visible roaming Pokémon (the original's signature interaction) composed over the Phase-2 grass stream, the Phase-4 Attack/Charm hooks and the Phase-5 egg payload. PURE domain rules in `scripts/domain/overworld_mons.gd` (slot/cell spawn model, disposition resolution, Charm math, the egg-theft provocation rule, nest/Alpha rolls); the entity lifetime + forced-battle pending seam + every trace in `scripts/runtime/overworld_mons_runtime.gd` with the step engine extracted to `overworld_mons_sim.gd`; the y-sort render node `scripts/runtime/entity_layer.gd` (a STATIC self-wired child of `Main` — ZERO lines in main.gd/world_view.gd; the feet-origin `world_draw_order` contract EXTENDED and audited). THE LOAD-BEARING INVARIANT: NO entity roll consumes the shared `_rng` — every draw is a pure SplitMix-style hash of `(world_seed, total_steps, slot)` (the `harvest_resolver._dig_draw` precedent); the runtime's `setup()` takes NO rng parameter (structural), and the `overworld_mons` scenario pins it twice over (an identical `entity_set_hash` across a seeded double run + the CONTROL that N seeded wild draws are byte-identical with entities active vs inert — the wild stream provably unconsumed, so every other pinned scenario + the shiny_odds 6/1024 expectation stay untouched). Persistence is TRANSIENT by explicit decision (NO save keys, NO `SAVE_VERSION` bump — v5 reserved for Phase 7): entities re-derive from `(world_seed, total_steps)` + clock; recruits/taken eggs persist via the party. Validation is mechanical: the gate scenario (determinism, per-biome spawn off the LIVE catalog with the named wiki dispositions, charm-recruit + the full-party refusal :262, the provoked:false/+3 forced-seam battles :280/:284, the Repel seam ordering, the :248 egg Attack/TAKE binary, the level-5 hatch, EVERY-creation `shiny_rolled{origin:overworld}`, the transient save round-trip), the audit extension (world_consistency_audit + world_spatial_audit reach mon entities: spawn determinism, logic/render agreement, the z-order contract on entity sprites, per-biome coverage, the guardian probe — mutation lane extracted to `world_entity_audit.gd` at the 220 wall), and the deterministic 22/23 visual sweep. Product completeness stays 2 by design: ALL spawn/despawn numbers, radii and the nest/Alpha model are flagged divergences (the scrapes publish none; the nest/Alpha ships per exec-plan mandate, justified against the +3-stage buff :284, the faithful strewn-egg + TAKE/Attack binary kept to the letter); the recruitable subset is catalog-driven first-forms + wiki examples (no exhaustive table until fresh scrapes); overworld shinies render faithful-silent (:230 — the pen-egg sparkle stays the sole isolated visual divergence); asset coverage is tier-1 ow-sheet / tier-2 the source game's own placeholder, flagged never silent. |
| `vision_fidelity` | `app` | `vision` | 3 | 3 | 3 | 2 | Capture honesty for windowed verification: a `frame_post_draw` readback guard added after (never replacing) scenario settle waits, a capture validity oracle (undersize/blank/uniform/magenta) with transport-vs-regression classification, a quarantine-tier duplicate-capture hook, a root-viewport-crop fallback for the Godot 4.6 magenta SubViewport regression (#115402), and head_sha/godot_version/window/renderer stamps on the playtest report. Slice 2 lands determinism pinning (verify-first): `project.godot` pins `msaa_2d=0` + `snap_2d_transforms_to_pixel=true`, measured byte-identical across 2/2 consecutive windowed sweeps and all 16 baselines, display_matrix green across all 6 sizes, canary 100% block-uniform, and `tools/determinism_verify.py` asserts the pins (the Nearest/integer-stretch candidate was rejected on measured 12/16-shot pixel deltas). The subsystem's current claims — guard, oracle, transport honesty, stamps, determinism, (Slice 3) semantic sidecars plus the explainable per-region diff (canary rect at ~0 tolerance = first red-tier coded region, near-zero ink/string tiers armed only after a measured-zero 3-run DUPCHECK noise floor, cursor zones masked, global 0.5%/tolerance-8 backstop and visual_diff.py untouched, runner-recorded verdicts + quarantine-tier region_drift, sidecar↔trace join at cursor+1), and (Slice 4) the coded legibility oracles — are now all mechanically supported, which is the rubric's bar for validation 3. Slice 4 lands `scripts/app/text_oracle.gd` (glyph template match via TextServer raster readback) with the duplicate-run raster-equivalence proof on file (raw draw_string == in-engine Label at 7px: 0 gap, 0 cross-launch, on both white and #f8f8f8; white-vs-#f8 binarization fringe ≤1 px/string; min-stroke 1px; stamp 4.6.1-stable (official)/forward_plus/Apple M4) and tolerances calibrated from those numbers (T_str = 2 px/string, T_glyph = 1 px/glyph — measured N with 2x headroom, never assumed); `tools/contrast_check.py` (WCAG linearized-sRGB contrast, AA 4.5:1 for 7px text) whose determinism is proven by construction (pure reduction over sorted color clusters ⇒ identical ratios on the Slice-2 byte-identical captures, zero run-to-run variance) and coded-gradeable only after that proof plus a clean-run history; and `tools/cvd_sim.py` (Machado, Oliveira & Fernandes 2009 severity-1.0 matrices, cited + import-time row-sum assert — quarantine-forever accessibility evidence, never red), with Contrast + CVD sections added to `tools/generate_legibility_report.py` and a fresh-pixels runner post-step on visual_sweep. The coded `text_oracle_passed` event is registered, and per-state `GRADUATED_STATES` replaces the single GRADUATED flag in `ui_render_audit`. Slice 5 lands the Lane-4 automation (`tools/vision_review.py`, stdlib-only): per-changed-shot evidence bundles (full-frame before/after copies, native-resolution cluster and region crops, numbered Set-of-Mark overlays, the expected-strings manifest, the rubric excerpt) feed a deterministic sidecar-consistency DEFAULT reviewer (no model, CI-safe, byte-stable per seed; `--reviewer-cmd` is the optional model plugin), and pipeline-side grounding enforcement guarantees 100% of emitted findings cite a sidecar region id and intersect its bbox (a mock reviewer's ungrounded output is dropped and counted). The `vision-review/2` output carries `finding_id` ledger join keys for the Slice-6 graduation ledger, and the shot-hash manifest drives staleness refusal; the runner post-step (`apply_vision_review`) records `vision_review_written` plus kind-`vision_review` quarantine entries — report-tier only, quarantine-forever (only tool errors fail red). The oracle spec's Lane-4 success criterion — the review produces its structured findings file on every sweep whose shots change AND its rubric catches at least one seeded visual defect all coded oracles miss — is PROVEN in the Slice-5 validation pass: the post-step regenerates `vision-review.json` on every compare run (even with 0 changed shots), and a seeded moves-menu cursor deletion on `10_battle_moves` — geometry fully inside the baseline cursor mask and outside every coded gate (canary ~0, ink/string/label rects, glyph tolerances, contrast rects, CVD palettes, and the global 0.5% backstop all silent) — is caught as a `cursor_missing` vision_review quarantine finding grounded on `cursor:move_0` with the run green and zero coded findings. Slice 6 lands graduation + calibration + the optional capture extra: `tools/graduation_ledger.py` (`record`/`status`/`calibration`, stdlib-only) banks windowed `ui_render_audit` runs into the committed evidence binder `docs/generated/graduation-ledger.json` (head_sha, boot-delimited session identity, per-state finding counts + ≤8 sample payloads, `text_oracle_passed` payload — self-contained evidence even if the machine-local user:// JSONL is deleted), computes per-state trailing clean-streaks against the threshold of 5 with the exact blocker and next step on file, and computes calibration cycles over the quarantine→graduation pipeline — the free VLM calibration loop — via Lane-4 `finding_id` and state+kind+region joins (`visual_explain.rects_overlap` the single geometry home); zero denominators are reported honestly ("undefined (0 of 0 — no quarantine findings in the window)", never 0.0/1.0), the first cycle is the baseline, and a two-cycle TREND needs the next legibility-garden cycle (weekly, Mon 14:00 UTC) and is never claimed from a single cycle. The optional vision extra ships behind its own registry entry and the ONE documented core-tools stdlib-guard exemption: `pyproject.toml` (uv; `dependencies=[]` — the core stays dependency-free) with `[project.optional-dependencies] vision = [scikit-image]`, and `tools/vision_metrics.py` (SSIM-map corroboration) is importable-graceful (`SKIMAGE_AVAILABLE` guard: absent → skipped section, never an error), quarantine-forever, and structurally NEVER a gate (no runner wiring, standalone exit 0, advisory localization only — 8–16 px window averaging dilutes exactly the sprite-/glyph-scale defects the coded oracles catch at exact pixels). The four optional capture spikes (Movie Writer motion lane, DAP `godot/put_msg` bridge, ScriptBacktrace, live NDJSON-over-TCP introspection endpoint) stay DEFERRED — explicitly off the critical path; the endpoint is specced as build-only-if in-process sidecars prove insufficient, and they have not. Per-state flips proceed on ledger-recorded evidence only (5 consecutive clean windowed runs at the current HEAD; documented flip order; battle_message requires the documented BOX judgment) and change the harness tiering, not the game. Slice-6 Graduate phase (2026-07-21, HEAD 7b733946): all four battle states flipped on ledger runs 1-5 with the evidence on file (`docs/generated/graduation-ledger.json` flips[]), and calibration cycle 1 committed as the honest zero-denominator baseline. Product completeness stays 2: `contrast_low` awaits its coded promotion, and Lane-4 findings are quarantine-forever judgment. NOTE (Workstream L.1): the one-command local gate `tools/verify_all.py` now MECHANIZES the subsystem's refuse-on-mismatch and freshness hooks — R1 report `head_sha`==HEAD, R3 windowed `godot_version`/`renderer`/`window` stamps vs the baseline sidecar capture_env plus the canonical 1152x648 window (a sweep captured under a different binary/renderer/window is refused, not diffed), and R6 `vision-review.json` freshness via `review_is_fresh` — which were "human/agent-enforced policy until verify_all"; this is the rubric's "mechanically supported" bar, so scores are unchanged (product completeness stays 2: it changes verification tiering and orchestration, not game behavior). SYSTEMIC FIX for the 11 stage-px HP-bar escape (the suite was fully green and fully self-consistent yet shipped the misalignment, because every automated check class is self-referential and the bars were never modeled): this slice closes the two structural gaps as one. G1 (programmatic-on-baked elements had no anchor to the source art they overlay) — a source-art anchor registry (docs/registry/art-anchors.toml) whose stage_rects are a pure integer function of the art BYTES re-derived at check time by tools/art_geometry.py (dark-run scan of battle_screen2.png verified to reproduce the fixed HP tracks (32,18,48,4) and (96,74,48,4) and exclude the misaligned 43/107), a mechanical [[scenes]] overlay inventory so any future overlay is caught on day one, a tools/check_art_anchors.py static RED gate folded into check_repo_contracts (completeness plus art-freshness re-derivation plus the art_sha256 pin — art is a submodule, so the sha is the per-file enforcement), an anchor_drift Lane-4 class grounded on the first art-truth anchor:<id> region, and a baseline-regeneration refusal gate (apply_anchor_gate) that structurally inverts the 3-days-green-on-wrong-baseline hole. G2 (the rubric art-fidelity questions were answered by no agent) — the Lane-4 --reviewer-cmd socket is populated by tools/vlm_reviewer.py (Qwen3-VL 8b, pure-stdlib, quarantine-forever WITNESS never the geometric oracle, composite degrade so coverage never depends on the model running) and a rubric-coverage ledger makes "unanswered" a first-class COUNTED state (q1- question ids, QUESTION_ANSWERERS, EXPECTED_QUESTION_COUNTS backstop, rubric_coverage manifest block). A miss-postmortem protocol plus tracked ledger (docs/references/miss-postmortem-protocol.md, docs/generated/miss-postmortems.json, entry #1 = the HP-bar episode) binds every future miss to a class-level mechanism. Validation stays 3 (the anchor gate, baseline refusal, and coverage backstop are mechanical — the rubric's bar); product completeness stays 2 (verification tiering and orchestration, not game behavior; anchor_drift is quarantine pre-graduation, VLM findings quarantine-forever). |
