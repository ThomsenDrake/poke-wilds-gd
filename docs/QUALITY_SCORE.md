Status: current
Last verified: 2026-08-19
Review cadence days: 14
Source paths: .github/workflows/feedback-relay-deploy.yml, docs/registry/subsystems.toml, docs/registry/art-anchors.toml, docs/registry/agent-surface.toml, services/feedback-relay, scripts, scenes, tools

# Quality Score

Scores use `0-3` where `3` means strong, mechanically supported coverage.

Feedback maintainability hardening (2026-08-16): scores remain unchanged. The
capture snapshot is now a plain read-only service instead of a marker child on
`GameRuntime`; one outbox owner atomically commits and recovers paired artifacts;
trace and bundle truncation share one complete-record JSONL primitive; the relay's
exact v1 TypeScript schema and typed HTTP errors prevent permanent validation
failures from entering the retryable 5xx class; and `feedback_flow` drives explicit
editor-only seams through the canonical `SmokeTap` helper.

Feedback review repairs (2026-08-17): scores remain unchanged. Linux and Windows
exports now embed their PCK so the packaging tool's single reported executable is
complete, and pre-dialog UI capture snapshots the common UI parent so simultaneous
visible siblings are preserved. `feedback_flow` pins a MessageBox-over-menu capture,
and the Python suite pins both desktop export options.

Feedback review follow-up (2026-08-17): scores remain unchanged. Distributed-build
cleanliness now includes untracked exportable resources while `.gitignore` continues
to exclude private/generated state, and pre-outbox failures return an explicit
`unsaved` result whose player copy never claims a local artifact exists. Python and
`feedback_flow` regression checks pin both distinctions.

Feedback persistence follow-up (2026-08-17): scores remain unchanged. Bundle
creation now propagates every ZIP entry write, entry close, and archive close error,
while malformed persisted install IDs are regenerated through same-directory atomic
replacement. The Python contract test and `feedback_flow` malformed-ID journey pin
these failure and recovery paths.

Feedback retry follow-up (2026-08-17): scores remain unchanged. Direct submissions
and retry uploads now share one owner lock, every upload cycle freshly reconciles the
outbox before stopping its timer, and atomic sidecar publication refuses short writes.
`feedback_flow` suspends one retry, submits a second report, and proves the second
bundle stays scheduled and later sends.

Feedback routing/retention follow-up (2026-08-17): scores remain unchanged. A
local-only private route preserves each queued report's original package authorization
without placing it in its ZIP, upload metadata, trace, or logs; the real journey changes
synthetic package identity between queue and retry and proves route cleanup on success.

Feedback deployment automation (2026-08-18): scores remain unchanged. A
path-filtered GitHub Actions workflow now gates relay changes with the locked Worker
checks and both Wrangler dry-runs, then serializes D1 migration, deploy, and typed
health verification through staging before production. GitHub receives only the
least-privilege Cloudflare deployment credential; Worker runtime secrets remain in
Cloudflare.

The relay cleanup now drains up to ten deterministic 100-row pages with one bulk R2
delete and guarded D1 update per page, with failure-order and bounded-cap tests. Relay
message validation now shares Godot's Unicode-code-point unit, and explicit HTTP 408
responses remain retryable.

Feedback surface/responsiveness follow-up (2026-08-17): scores remain unchanged.
Storage, camp, and waystone overlays now retain distinct capture labels, and the real
feedback journey pins all seven supported surfaces. The six-size display matrix opens
the dialog and proves its panel/editor stay inside the viewport down to 438x383. Relay
expiry comparisons normalize stored ISO timestamps through SQLite `datetime()` so the
daily cleanup does not miss already-expired same-day rows.

Feedback size/Unicode follow-up (2026-08-17): scores remain unchanged. Runtime bundle
reduction now enforces the relay's 16 MiB compressed and 24 MiB uncompressed caps before
outbox commit, while public issue body and title truncation count Unicode code points and
cannot split astral characters.

Feedback packaging-safety follow-up (2026-08-17): scores remain unchanged. Package
creation validates an HTTPS-only credential-free relay endpoint before sending admin
authorization or embedding it, and a cross-platform advisory lock prevents concurrent
exports from overwriting or deleting shared tester build metadata.

Feedback retry/redaction/fetch follow-up (2026-08-17): scores remain unchanged. The
same HTTPS boundary now protects maintainer bundle fetches and the game upload sink;
failed permanent-rejection sidecar writes quarantine and suppress the retry pair, and
machine redaction targets semantic identity fields and paths without rewriting common
substrings in player prose.

Feedback retry-isolation/expiry follow-up (2026-08-17): scores remain unchanged.
Queued failures no longer stop later independently routed reports in the same scan, and
the relay rejects expired report IDs before R2 so private bundles cannot be resurrected.
The concurrency journey ignores pre-existing outbox entries, leaving them queued and
untouched while it verifies only the two reports created by that run.

Feedback cleanup-ownership follow-up (2026-08-18): scores remain unchanged. Relay
uploads now own a guarded `uploading` lease before R2, while cleanup atomically claims
`expiring` rows before deletion. Worker tests pin both transition orderings, cleanup
failure recovery, stale-read rejection without an R2 write, and immediate admin denial.

Feedback credential/configuration follow-up (2026-08-18): scores remain unchanged.
Both privileged Python clients reject redirects before an admin credential can cross a
hop. The real journey proves an unconfigured export returns `unsaved` before bundle or
outbox creation and cannot leave a permanently retrying private route.

Feedback trace-schema follow-up (2026-08-18): scores remain unchanged. Both the
current-session slicer and the bundle-size reducer now emit truncation markers with the
canonical `event`/`ts_msec`/`source`/`payload` shape. The real feedback journey forces
both paths and parses their markers, while the pure source contract pins each producer.
Oversized engine logs now discard a leading partial line before UTF-8 decoding and
redaction, preventing an arbitrary tail offset from separating sensitive data from its
redaction cue; the focused journey pins that ordering with an isolated fixture.
Public issue composition also HTML-encodes and Markdown-escapes every untrusted display
field, so player delimiters cannot hide or restructure the agent handoff; Worker tests
pin comments, headings, fences, table separators, and handoff visibility.
Successful uploads now atomically persist a terminal `sent` marker before checked
route/bundle/metadata deletion. Any failed cleanup is quarantined and reported as a
distinct remote-success/local-cleanup result that retains the issue number rather than
emitting the normal sent trace; the focused journey injects a ZIP-delete failure and
proves the report cannot upload twice. Runtime endpoint validation now parses DNS/IP
host and bounded numeric port before any bundle or outbox creation; the shared Python
validator rejects the same empty explicit-port form before packaging or admin fetch.

Legendary-dungeon validation layer (2026-08-11): validation remains at the existing score, now backed by the rng-free warp/resolver/chamber core, additive dungeon-context persistence, explicit in-dungeon mutation/refusal guards, the dedicated end-to-end scenario, and the 44/45 presentation baselines.

Navigation-audit fixture hardening (2026-08-11): `app_bootstrap` scores remain unchanged. The battle half now owns its seeded new game, rebuilt `WorldView`, and avatar resync, then bounded-waits asynchronous turn playback before deciding whether a terminal battle must restart. This supersedes the older row's `(code, save, seed)` shorthand for `nav_audit`: its encounter scope/level and battle decisions are a function of (code, seed), never inherited boot geometry.

Seeded-smoke creation-order repair (2026-08-11): `app_bootstrap` now poisons the creation order before `save_stability`, `session_runtime.seed_for_smoke` resets it with the RNG streams, and `pokemon_progression` exposes only that narrow reset seam. Scores are unchanged: this repairs deterministic verification rather than player behavior.

Created-game dungeon journey (2026-08-12): `app_bootstrap`'s real title/creation `new_game_flow` now reaches the `legendary_dungeons` verification helper after its existing identity, beach, and persistence witnesses. On the dual-contract fixture seed 2026080001, the helper proves all seven canonical entrances with live resolver stamps and Main-routed avatar entry/exit steps; it also proves the Regigigas empty-bag refusal before restoring temporary tablets. `new_game_flow_passed` carries `dungeon_warps_ok`. Scores for both `app_bootstrap` and `legendary_dungeons` remain unchanged: this closes an end-to-end validation gap without changing production behavior or architecture.

Collapsed-poll C+Z leak (2026-08-13): `app_bootstrap`'s `input_router.poll` drops `context_ok` after a same-frame `build_toggle` so C+Z cannot harvest while opening the overlay (the pre-collapse `poll_build_toggle` then re-read `is_active()` order). `building_loop`'s `dig_silence` part G pins it. Scores unchanged: same-frame regression fix + existing-scenario part, not new behavior.

Collapsed-poll Enter-close latch leak (2026-08-13): `app_bootstrap`'s `input_router.poll` clears callback-raised `_ui_ate_press` before return so Enter-close via `_toggle_menu`/`hide_menu` cannot swallow the next frame's first press (the pre-collapse context poll owned that reset). `input_gate` part F pins a latch raised during poll and a next-frame Enter. Scores unchanged: same-frame regression fix + existing-scenario part, not new behavior.

Cloud Dummy-audio leak gate (2026-08-14): `session_runtime` music/cry playback now also returns early when `GODOT_AUDIO_DRIVER=Dummy` (the playtest/Cloud driver). Dummy still "plays" and holds the OGG chain at quit — the same leak class as headless — which miss-002 `ERROR:` capture would otherwise false-red after ALSA is silenced. `music_track_selected` still emits before the gate. Scores unchanged: verification honesty, not game behavior.

| Subsystem | Layer | Quality bucket | Legibility | Validation | Architecture | Product completeness | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `feedback_reporting` | `app` | `feedback` | 3 | 3 | 3 | 3 | Release-build `F` reporting captures a pre-dialog screenshot, current-launch trace, sanitized engine log, in-memory save, and visible UI tree into a checksummed v1 ZIP while preserving exact pause state. Revocable invite tokens reach a typed Cloudflare Worker; GitHub App credentials remain server-side, raw artifacts stay in private R2, D1 owns idempotency/rate limits/expiry, and public issues carry only sanitized handoff data. `feedback_flow` drives real F input across title/menu/storage/camp/waystone/battle/overworld and parses the bundle through a fake transport; `display_matrix` pins responsive feedback layout at all six sizes. Python bundle tests, Worker TypeScript/Vitest, and production/staging Wrangler dry-runs gate the implementation. Production and staging were provisioned previously, and the successful canary issues #32 and #33 were closed without being rerun for this port. |
| `app_bootstrap` | `app` | `bootstrap` | 3 | 3 | 3 | 2 | Boot and scene wiring are explicit, trace-backed, and smoke scenarios are extracted into `smoke_scenarios.gd`. The oracle suite runs four lanes: journey/soak playtests, nav/texture/data/layout audits, world-consistency and UI-render-model audits, and a deterministic visual sweep with baseline diffing plus mandatory vision-capable LLM review. Sweep, UI-render, and display-matrix captures now delegate to the `vision_fidelity` capture contract (frame_post_draw readback guard, validity oracle, transport-honest skips), and the sweep's determinism is pinned in `project.godot` (Slice 2: `msaa_2d=0` + `snap_2d_transforms_to_pixel=true`, measured byte-identical consecutive sweeps; the Nearest-default-filter + integer-stretch candidate was rejected on measured pixel deltas). Slice 3 adds per-shot semantic sidecars (canonical JSON, trace-correlated) and a runner-recorded explainable per-region gate on top of the unchanged global diff — seeded canary and deleted-label perturbations turn the suite red via region gates with no per-bug assertion. Slice 4 wires the `ui_render_audit` pixel half to the `vision_fidelity` glyph template oracle (`scripts/app/text_oracle.gd`): the single GRADUATED flag becomes a per-state `GRADUATED_STATES` dict, so a graduated battle state's glyph mismatch / garble / low_ink fails red with no per-bug assertion while non-graduated states stay quarantine-tier; the audit sheds its pixel-half detail by delegation and stays ≤220 lines. Slice 6 lands the graduation machinery: `tools/graduation_ledger.py` (`record`/`status`/`calibration`) banks each windowed `ui_render_audit` run — head_sha, boot-delimited session identity, per-state finding counts, `text_oracle_passed` payload — into the committed evidence binder `docs/generated/graduation-ledger.json`, so streaks and flippability are COMPUTED from recorded entries, never asserted (unstamped historical sessions are never backfilled into flip-qualifying streaks). Per-state `GRADUATED_STATES` flips (line 16) then proceed on ledger-recorded evidence only — 5 consecutive clean windowed runs at the current HEAD, in the documented order (battle_moves + battle_item FIRST: ANCHOR glyph-template match; battle_action NEXT: lint cleanliness on ACTION_ROWS; battle_message LAST: BOX mode with a required documented judgment) — and each flip graduates that state's entire pixel lint (glyph + lint findings go red together); the flip changes the verification harness's tiering, not the game. Slice-6 Graduate phase (2026-07-21, HEAD 7b733946): all four battle states flipped on ledger runs 1-5 (moves + item anchor, action lint, message box with the documented engine-owned ~1px pen judgment), run 6 the post-flip clean confirmation (streak 6/5); the Graduate-phase seeded-defect proof went RED via the graduated gate with no per-bug assertion while the same defect class stayed quarantine in a temporarily un-graduated state. Product completeness stays 2: the flips change the verification harness's tiering only, not game behavior. NOTE (Workstream L.1): the one-command local gate `tools/verify_all.py` now MECHANIZES the refuse-on-mismatch (report `head_sha`==HEAD; windowed `godot_version`/`renderer`/`window` vs the baseline capture_env plus the canonical window) and freshness (`vision-review.json` via `review_is_fresh`) refusals previously "human/agent-enforced policy until verify_all" — the rubric's "mechanically supported" bar; scores unchanged (it changes verification orchestration and tiering, not game behavior). NOTE (miss-002, 2026-07-22): the nav_audit false-red closed at the class level — the battle half pins BOTH runtime rngs via `seed_for_smoke(NAV_AUDIT_SEED)` (the house seeding convention; the pass event is a pure function of (code, save, seed), never per-process wall-clock rng), the RUN check is total (a legitimate "Can't escape!" trap refusal is proven, the trap expired, and escape re-proven — new coverage, never a suite failure), and the shared harness now collects any `<scenario>_failed` trace plus captured `push_error` "ERROR: " lines as named failures (ERROR_MARKERS single-sourced across both transports), so a red can never be reasonless; the two-part class-level plant executed and reverted byte-identical (ledger entry #2). Scores unchanged: verification determinism and reporting honesty, not game behavior. Phase-3 controls (exec plan A1–A5, generalized in the Phase 3 final review): `input_router.gd`'s same-frame latch is now GENERALIZED (`_ui_ate_press`, set by `bind_ui_consumers` connecting the argless closed/confirmed/cancelled signals of all four input-phase UIs — CampMenu, StartMenu, MessageBox, StorageScreen), killing EVERY same-press double-fire at the shared root (Enter no longer opens the start menu under a closing camp menu; Z-on-Demolish no longer re-fires harvest/build on the bare former campfire tile; a start-menu CLOSE no longer re-harvests the faced tile; a MessageBox NEW GAME confirm no longer harvests the brand-new world's spawn-facing tile; an inert party-screen FIELD MOVE no longer harvests the faced cut-tile its capability message just declined — `main.gd::_toggle_menu` and the `overworld_idle` poll additionally guard on camp-menu AND StorageScreen visibility for direct-call paths, four same-line edits only), the party-screen FIELD MOVE entries for non-harvest moves are explicitly capability display with a degrade message (verified species-flag counts + the inert-move deferrals in menu-and-save.md), and the `input_gate` scenario (real input-phase injection via `Input.use_accumulated_input` and the shared `smoke_tap.gd`: press/release in separate frames, injection witnesses, proven red-before/green-after) covers the camp-menu race frames (parts A/B) plus the start-menu CLOSE / inert-FIELD-MOVE / NEW-GAME-confirm leak paths (parts C/E/D in `input_gate_menu_checks.gd`) that `smoke_scenarios._press` structurally cannot reach. The latch's BATTLE-END arm (the surviving same-press leak the menu arm left open): a press that ENDS A BATTLE re-fired the overworld context action the same frame — Z-on-RUN / a ball-select Z capture resolve SYNCHRONOUSLY in `battle_view._unhandled_input` (input phase) and `main._on_battle_finished` re-enables the overworld within it, so the same-frame `poll` harvested/built the faced tile (the harvest toast superseded "Got away safely!") — is closed at the shared root: `input_router.gd` gains the public `note_press_consumed()` latch setter and `main._on_battle_finished`'s `_in_battle = false` line calls it (one same-line edit; main.gd stays 220/220), covering EVERY end path (RUN, capture, victory, defeat — the only end classes; any future press-driven end, e.g. a message advance, is covered by the same unconditional single-endpoint latch) uniformly, and the new `battle_end_input` scenario (same real-input-phase mechanism via `smoke_tap.gd`: press/release in separate frames, injection witnesses, seeded by `seed_for_smoke`, proven red-before/green-after) proves the escape AND capture end-presses no longer re-fire (the faced cut trees stand, the escape/capture toasts survive) AND that a fresh deliberate press the next frame still harvests (the latch is reset by the context poll, so the fix never over-suppresses). Scores unchanged: a bug fix + regression scenario + doc co-mods, no new game behavior. NOTE (pre-Phase-7 suite expansion, landed pending Verify): `verify_all` gains a double-run determinism lane after S6 (the six rng-consumer scenarios twice headless, ts-stripped canonical trace JSONL compared via a new `determinism_verify.py cmp` verb extending the previously unwired `cmd_cmp`; ≤90s; miss-002 mismatch payload), the four satellite sweep families + the new `visual_sweep_fishing` satellite in S9 (shots 15-30 one-command), a per-soak WARNING-COUNT TRIPWIRE (each soak ≤ a committed pin under `docs/generated/`, exceedance RED with the warning list), and a RED sidecar `crafted_state.world_seed`/`shot_seq` equality gate in the `run_playtests` region-gate post-step (`--update` validates the source sidecar BEFORE the baseline rewrite). The new `rng_joint_pin` (cross-consumer `_rng` interleaving byte-stability across two in-scenario runs) and `save_stability` (canonical v4 save↔reload↔save byte-compare + the committed `docs/generated/golden-saves/v4_golden.json` golden fixture) scenarios land on this subsystem, and `check_repo_contracts` gains the no-`RandomNumberGenerator`-in-`*_sim.gd`-setup rule (codifying `overworld_mons_sim.gd:11`) + a shot-numbering completeness check consuming the single-sourced shot-range registry (retired 17 whitelisted). Scores unchanged: verification coverage + determinism pins, not game behavior. NOTE (Phase 7 Build 3): `save_migration`'s `v_ok` grows to five (the pinned non-trivial v5 fixture + the two byte witnesses) and `save_stability`'s v5 work (the update-mode downshift guard + the world_chain round-trip sublane) extracts into `scripts/app/save_stability_support.gd` for the 220-line budget (the wild_battle_scenario precedent). Scores unchanged: migration coverage + extraction, not game behavior. NOTE (Phase 7 R4/R11 post-Build-3 audit): the world-depth landmark shots were pixel-UNDER-gated (only the 0.5% global drift; pre-capture asserts read tile-logic dicts, not pixels) and three Phase-7 states were UNSHOT (Heart Tower, any non-origin chained world, a legendary guardian sprite). R4 lands the regional pixel oracle `scripts/app/visual_sweep_world_depth_oracle.gd` (extracted at the app-220 wall): it projects the in-scenario landmark/beacon/legendary tiles to screen rects off the camera tile + canonical window (TILE_SIZE 16 × Camera2D zoom 3 = 48 px/tile) and the sweep + beacon-shot merge them into each sidecar's `expected_regions.ink` (31 statue pair, 32 statue grid + open doors, 33 beacon way-stones, 34 tower wall/decor) — a strict `canary_rect` for 31/32, while 33 (beacon selector overlay) + 34 (tower decor alpha edge) are ink-only (`visual_region_diff.py:40` treats `ink` as red-tier) — so a dropped sprite reds via `visual_region_diff.py`'s red tier (feet-origin integer tiles make the gated regions self-equal — measured 0 region failures / 0 quarantine on re-compare). R11 captures the unshot states after 33: `34_heart_tower` joins the origin `world_depth` satellite (registry [31,34]; tower asserted on rendered props, no encounter token — `visual_sweep_world_depth_expand.gd`), and the derived-world shots form their OWN `visual_sweep_world_chain` satellite (registry [35,36], seed = `world_seed_for(2026072907,(0,-1))` single-sourced) because R3's one-world-per-run sidecar gate forbids a derived seed sharing a shots dir with the origin shots — `35_chained_world` (re-anchored landmarks via `session.active_chain`+`world_seed` before `rebuild`) + `36_legendary_guardian` (entity layer opted IN + `stamp_legendaries()` since `craft_state` bypasses it; one ring≥60 static framed out of its widened sight so the step-driven chase never moves it and the two-sample rest probe settles byte-stable). Both satellites register in `qa_scenarios.gd` + smoketest windowed sets + `SCENARIO_REQUIREMENTS` + `SATELLITE_SWEEP_SCENARIOS`/`PLAYTEST_SCENARIOS` (verify_all S9 + the R3 RED gate cover them by construction); the shared rest probe + legendary-framing + R3 registry single-source are extracted (`visual_sweep_world_depth_expand.gd` / `visual_sweep_registry_support.gd`, the latter also fixing an R3 latent app-220 overage in `visual_sweep_baselines.gd`); the 33 beacon shot now `close_selector()`s so the picker overlay does not bleed into later shots. Baselines captured in update mode + re-compared at 0.0% drift (global + per-region). Scores unchanged: verification coverage + pixel-gating, not game behavior. NOTE (Phase 7 audit R13/R14 docs-honesty repairs): two claim/reality reconciliations — CONTROLS.md struck its Ruins loot-props claim (ONLY the Mansion's one-shot loot shelf exists in code; the overclaim was deleted rather than implementing Ruins loot to match the docs), and the Heart Tower host-biome restore on exit is NOW truly trace-asserted (R10's `music_track_selected{kind: <host biome>}` seam, emitted by `play_biome_track` before the headless gate and asserted by `world_depth_checks`' tower case — registry + trace-events.md + spec wording reconciled off the old "no seam"); and the double-run lane header records the night-ghost rng exclusion (`night_system.gd:123,128`) as a LOUD NAMED skip (miss-002 spirit): the two draws ride the already-pinned shared `game_runtime._rng` stream and are self-pinned via `seed_for_smoke` in `night_cycle` (S7 single-run, requirements-gated), and no consumer ever enters unlit night (new games start 10:00; the soak never reaches NIGHT_START 20:30) — initially recorded as a LOUD NAMED exclusion rather than a tenth consumer (SUPERSEDED by the next note). Scores unchanged: docs/claim honesty + named-skip loudness, not game behavior. NOTE (Phase 7 audit R-INFRA night_cycle, SUPERSEDING the R14 deferral above): the LITERAL repair landed — `night_cycle` IS the double-run lane's TENTH consumer (`DOUBLE_RUN_SCENARIOS` nine → ten, regroup [4,5] → [5,5], per-group budgets [80,100] → [100,100]s), covering the unlit-night ghost draws (`night_system.gd:123,128` — the shared `game_runtime._rng` randf gate + the randi_range species pick), the ONE shared-stream consumer none of the other nine ever enters (new games start 10:00; the soaks never reach NIGHT_START 20:30), so a ghost-localized regression can no longer replay green in the lane and only red in S7. The scenario self-pins the breed_flow way (`seed_for_smoke` BEFORE `new_game` + `rebuild`) so its crafted-dark draws replay cross-process (a boot world would ride the wall-clock `root_seed` and red the cmp); the verify_all exclusion header was replaced by the tenth-consumer rationale and the lane re-stamped through the miss-002 ritual. Scores unchanged: determinism-lane coverage, not game behavior. Showcase + beacon wiring (2026-08): `qa_scenarios.gd` registers the windowed-only `showcase_capture` driver (NOT a baseline sweep — no SHOT_REGISTRY entry, deliberately outside the reconcile/region-diff gate; frames + sidecars land in `docs/generated/showcase/`), and `Main.tscn` instances the BeaconSelector UI node (hidden at boot; the screen self-wires the input latch via the documented `bind_ui_consumers` reach-in — `main.gd` stays at its frozen 220 budget with zero added lines). Scores unchanged: dispatch registration + scene wiring, not boot behavior. NOTE (configurable encounters, 2026-08): `qa_scenarios.gd` registers the `encounter_config` gate scenario (collision-only default + opt-in modes + additive save round-trip; it joins the double-run lane as the eleventh consumer), and `smoke_scenarios.gd`'s `menu_save` + `input_gate_menu_checks.gd` re-target the start menu's NEW GAME/CLOSE rows after the OPTIONS entry inserted at index 3. Scores unchanged: dispatch registration + index co-mods, not boot behavior. NOTE (infinite-world slice): world chaining RETIRED for a seamless infinite plane (save_migration extracted to save_migration_scenario.gd; v5→v6 save — truly chained saves refused+preserved non-destructively, chain-less flatten losslessly). The review-fix round closed a pre-existing avatar-input race: `main._on_menu_closed` re-checks the WayStoneSelector modal's visibility before re-enabling input (walking-under-the-modal pinned red by `waystone_selector`). NOTE (new-game-flow slice): `main.gd` restructures the boot tail — the scenario request is consumed BEFORE `ensure_initialized(silent_new_game)`, `_enter_world` + the title/creation signal handlers, same-line title/creation visibility guards — and extracts `smoke_context()` into `main_smoke_context.gd`; scenario boots keep the pre-slice path byte-identical (silent new_game on absent/corrupt/refused save, boot toast, world sync at boot) while player boots gain the splash/title and never auto-start a game or write a save. Scores unchanged: the scenario-boot contract is byte-identical; the player boot is the new flow. NOTE (GBC restyle wave 0, 2026-08): the app-side co-mods for the title/creation stage rebuild (the `title_flow` row owns the screens): `new_game_flow_scenario.gd` + `new_game_flow_checks.gd` retarget the retired node reads to restyle seams (`title.entry_labels()/selected_entry()/entry_row_text(i)`, `creation.step_title_label()/step_value_label()`, `creation.seed_edit_active()` — the old ItemList/Panel-node/SeedPrompt witnesses) with the typed-digit unicode loop + grid walks + ALL trace pins VERBATIM, and the NEW `new_game_flow_geo.gd` geometry witnesses (name/avatar plates fully on-screen) close the geometry-blind class that let the ce1571b 0×0-root anchor collapse ship — proven red-before on the old screens, green on the stage rebuild. Five NEW title baselines (37_title_splash, 38_title_entries, 39_creation_seed, 40_creation_name, 41_creation_avatar) join the MAIN sweep via the NEW `visual_sweep_title.gd` capture module + SHOT_REGISTRY main-extra growth — it drives the real screens like `new_game_flow` but NEVER confirms creation (no session_created, no session/world mutation), and the sidecars inherit the sweep's CRAFTED_STATE + shot_seq, so the RED-tier sidecar seed-equality gate is green by construction (no second world, NO tools/** edits, no miss-002 re-stamp). Scores unchanged: seam retargets + verification coverage, not game behavior. NOTE (GBC restyle wave 2, 2026-08): the app-side co-mods for the pause/menu stage rebuild (the `menu_ui`/`crafting`/`storage`/`landmarks` rows own the screens + the per-screen extractions): the app readers retarget off the retired ItemList/Panel node reads to the restyle seams — `input_gate_scenario.gd` reads `camp_menu.selected_row_text()`, `input_gate_menu_checks.gd` reads `start_menu.selected_row_text()` + `party_screen.action_row_text(i)`, and `layout_audit.gd` + `ui_render_audit.gd` take the menu/party/bag(/storage) bounds off `stage_root()` and measure TEXT EXTENTS (the font's `get_string_size` / `UiRenderModel.ink_rect` — the restyled stage labels carry oversized bounding rects under theme font overrides while their ink fits), skipping the deliberate `clip_text` row labels (a one-line GBC row clips at the plate interior by design; the `.text` contract survives intact); `storage_release_mouse_check.gd`'s retarget rides the `storage` row. Every injection witness, tap shape, and assertion is unchanged; the committed menu baselines (06/07/08/28/29 + the camping/storage satellites) are still the pre-restyle captures — the re-pins land with the wave-3 `visual_sweep_update` pass. Scores unchanged: seam retargets + measurement honesty, not game behavior. NOTE (agent-neutral integration, 2026-08-09): Phase 1 lands the machine-readable agent-surface contract — `docs/registry/agent-surface.toml` (protocols DAP 6006 / LSP 6005, the scenario CLI, the trace stream, visual baselines, determinism conventions, the golden save, preflight, the error-as-directive convention) with its prose companion `docs/references/agent-integration.md` (linked from AGENTS.md's Read First), mechanically gated by a NEW `agent_surface_issues` fold into `check_repo_contracts.run()` (schema_version int, the eight required sections, integer ports, every referenced repo path must exist — refuse-on-unreadable, the art_anchor_issues progressive-arming precedent). Phase 2 lands the `ui_tree_dump` scenario (`scripts/app/ui_tree_dump_scenario.gd` — the accessibility-snapshot analog: title/menu/party/bag/battle driven through the established scenario seams, each visible Control subtree written to `.godot-smoke/ui_tree/<screen>.json` with path/type/text/rect/disabled + screen id + cursor — paths in the transcript, never binary) with the symmetric `ui_tree_dump_passed`/`ui_tree_dump_failed` pair (PLAYTEST_SCENARIOS only, the nav_audit pairing precedent), and self-pins the three advisory scenarios (`battle_anim`/`smoke_biome`/`wild_battle` via `seed_for_smoke` BEFORE pass-gating draws — the new_game_flow precedent, NOT double-run consumers), closing the seed_convention_advisories debt. The strict-review F2 remedy deduped the triplicated `_guaranteed_capture_mon` fixture into the new shared `scripts/app/battle_scenario_fixtures.gd` (all three battle/storage scenario callers retargeted, zero assertion changes), and F8 used the freed headroom to restore one-statement-per-line in `battle_end_input_scenario.gd` (220→212) — both extraction/decompression only, no behavior change. Scores unchanged: legibility surfaces + verification coverage, not game behavior. NOTE (agent-surface completion sprint, 2026-08-10): the sprint closes the contract-completeness gaps the Aug-10 legibility work left beside the suite — the manifest gains the `[ui_tree]`/`[monitors]`/`[vision_review]`/`[play_agent]` sections (the play_agent driver marked OPTIONAL — an optional reference driver, never part of the agent-neutral contract); BOTH harnesses + `verify_all` now emit the structured error-as-directive envelope `{code, retryable, hint}` on every red (per-scenario codes `dap_endpoint_unreachable`/`exceptions_captured`/`scenario_failed`/`missing_required_events` + the Lane-4 trio `vision_review_failed`/`vision_review_stale`/`vision_review_incomplete`; single-sourced via `error_envelope` in `tools/godot_dap_smoketest.py`), so a red names a stable machine code, never just prose; the previously-orphaned `dig_silence` scenario is harness-selectable (SCENARIO_REQUIREMENTS + PLAYTEST_SCENARIOS); and the NEW `legibility_soak` scenario (`scripts/app/legibility_soak_scenario.gd` + `legibility_soak_checks.gd`, the 220-wall split) actively GATES the Phase-2 surfaces — the five agent-facing screens driven through the ui_tree_dump seams with the ui_tree dumps, the `game/*` Performance monitors (`current_screen` read WHILE each screen is up), and the trace lifecycle asserted in agreement (292 checks, self-pinned 2026080905, headless-runnable, not a double-run consumer). The gated suite is now 64 scenarios (51 playtest + 13 smoke). Scores unchanged: error/surface legibility + verification coverage, not game behavior. NOTE (C-under-waystone, 2026-08-13): Main's overworld-free poll excludes `$UI/WayStoneSelector.visible` so C cannot arm build mode behind Teleport/Fly; `waystone_selector` pins it. Scores unchanged: input-gate fix, not new behavior. |
| `session_runtime` | `runtime` | `runtime` | 3 | 3 | 3 | 2 | Save schema v4 (bag, time of day, steps, world overrides, the campsite hold, placed structures, and since Phase 3 per-placement storage-box `contents`) with v1/v2/v3 migration (v3→v4 purely additive — absent `contents` backfills to an empty box; a v3 build refuses a v4 save non-destructively to `.newer.bak`) — covered by the dedicated `save_migration` fixture (v1/v2/v3→v4 plus the future-version refusal) — plus ATOMIC temp+rename writes and non-destructive corrupt/future-version recovery (`.corrupt.bak`/`.newer.bak`, `save_recovery` plus a version-refusal `warning`), a non-losing full-party capture HOLD retrievable from the party screen, biome/battle music routing, traces, and smoke scenario bootstrapping are centralized. The exit-time ObjectDB/resource leak (an OGG stream still playing at process exit) is fixed at the source with a headless audio gate (plus a windowed `_exit_tree` stop as defense-in-depth). The runtime exposes the smoke-only `seed_for_smoke(seed)` determinism seam (pins the encounter and battle rngs together — and since the pre-Phase-7 expansion ALSO `player_avatar._rng`, wall-clock-seeded previously (`player_avatar.gd:42,47`; the trigger draw :205), via `game_runtime.gd:207`, with `boot`/`overworld_step`/`menu_save` smoke scenarios gaining seed calls; real play never calls it), and the corrupt-save recovery parses quietly (`JSON.new().parse` returns an error code without the engine printing) so a handled refusal emits no spurious engine error line (miss-002). NOTE (Phase 7 Build 3, save v5): the first STRUCTURAL bump since v2 — `SAVE_VERSION := 5` adds chain identity (`root_seed`/`active_chain`) + the per-world `chained_worlds` dict keyed by the bare `"<cx>,<cy>"` grammar; the ACTIVE world keeps v4 keying EXACTLY (the golden byte-proof), every world's `landmark_state` nests under `chained_worlds["<cx>,<cy>"].landmark_state`. The pure transform lives in the NEW `scripts/domain/save_migration.gd` (a `duplicate(true)` deep COPY that never mutates its argument — the aliasing guard; `world_id_for`/`chain_for`, the `downshift` inverse, and the two pinned `byte_witness_issues`); `session_payload.apply_into` migrates FIRST then reads the v5 keys with new-game defaults; the frozen seam's resolution extends to `chained_worlds[world_id_for(chain)].landmark_state` in `session_state.gd` (landmark_runtime untouched). Scores unchanged: schema + migration mechanics, not game behavior. NOTE (configurable encounters, 2026-08): `session_state.gd` gains the v5-ADDITIVE `encounter_settings` key (NO `SAVE_VERSION` bump — `{}`/absent == `"off"`, the contact-only default; `get/set_encounter_settings` validate the mode + snap the rate to the 0.02 ladder), and `session_payload.gd` marshals it write-only-when-non-default (a never-touched save keeps its exact byte shape — the v4 golden carries no key). Scores unchanged: additive schema mechanics, not game behavior. NOTE (infinite-world slice): v6 save — chain identity keys retired, landmark_state back at its top-level seat, truly chained saves refused+preserved (.chained.bak), chain-less v4/v5 flatten losslessly. NOTE (new-game-flow slice): `session_state.gd` gains the creation identity constants + vars (player_name/player_avatar/shiny_odds_choice — persisting across resets, the encounter_settings precedent) and `game_runtime` gains `ensure_initialized(silent_new_game)` / `has_loaded_save()` / `begin_created_game` (identity + odds committed BEFORE the pinned new_game draw order); the three additive v6 keys (player_name/player_avatar/shiny_rate) marshal write-only-when-non-default, NO schema bump. Scores unchanged: additive schema mechanics, not game behavior. NOTE (agent-neutral integration, 2026-08-09): Phase 2 lands three custom Performance monitors (`game/party_size`, `game/world_seed`, `game/current_screen` — queryable via `Performance.get_custom_monitor` even in release builds, the live-probe complement to the JSONL file artifact; `game/current_screen`'s screen ids are kept in sync with the ui_tree_dump scenario's screen ids — the agreement is now CONSTRUCTED by the `legibility_soak` gate (2026-08-10), which reads the monitor WHILE each screen is dumped and reds on drift) in `scripts/runtime/performance_monitors.gd`, the ONE self-registering child node (registers in `_ready`, values read off `get_parent()`, `_exit_tree` unregisters — BOTH lambda forms segfaulted at engine teardown on 4.6.1; callables are destroyed while the script is alive, probed headless), `game_runtime.gd` spending exactly one add_child line. Phase 3 lands the opt-in `addons/agent_trace/` editor plugin (disabled by default, project.godot untouched): `trace_logger.gd` gains an ADDITIVE `EngineDebugger.send_message("agent_trace:event", [line])` hook behind `EngineDebugger.is_active()` (a strict no-op headless/exported — file/stdout behavior byte-identical), and the editor side renders the live stream in a per-session debugger tab plus a bottom-panel activity log. Scores unchanged: legibility surfaces + an editor-only viewer over the existing trace contract, not game behavior. |
| `world_runtime` | `runtime` | `world` | 3 | 3 | 3 | 3 | Progressive biome generation with data-driven traversal rules, species-level spawn tables with source-token aliases, navigable spawn with reachability proof, field-move-gated walkability, and day/night presentation. `validate_invariants` is exercised by the `biome_probe` smoke scenario. NOTE (configurable encounters, 2026-08): `player_avatar._try_trigger_encounter` is now GATED on `session.encounter_settings` (mode `off` — the DEFAULT — never rolls; `classic` rolls on encounter tiles; `anywhere` on any standable tile; both at the configured rate; the fast-gait modifier unchanged), and the walk/run frame build extracted to `scripts/runtime/player_sprite_frames.gd` at the 320 wall. The default flip (grass roll off) is documented product behavior recorded on `overworld_mons`; the gate + extraction here are mechanics. Scores unchanged. NOTE (infinite-world slice): the WORLD_RADIUS disc edge RETIRED — the world is a seamless infinite plane (walkable far tiles refuse on their own terrain); walk/run frame build extracted to player_sprite_frames.gd. NOTE (infinite-world slice 4): find_walkable_spawn gains a beach-preference first pass (SAND + cardinal surf neighbor + _spawn_meets_neighbors, falling back to the walkable scan) — faithful "start on a beach"; the spawn shift re-pinned the spawn-dependent baselines + save_stability golden. NOTE (new-game-flow slice): `player_sprite_frames.build(avatar_name)` parameterizes the walk/run sheet paths (requested → ben → kris fallback chain) and `player_avatar.set_avatar` re-applies frames on boot/reset — the default stays ben (the pre-slice hardcoded sheet), so every existing render keeps its pixels. Scores unchanged: parameterization, baseline-neutral. |
| `pokemon_data` | `data` | `data` | 3 | 2 | 3 | 3 | 954 species, 730 moves, and 120 items loaded from the generated catalog `assets/data/catalog/{species,moves,items}.json` (base stats, evolutions, spawn biomes, field-move flags, dex data; the item count includes source-i18n omissions supplied by importer overrides, including the five braille tablets), authored offline by `tools/import_pokeapi.py` from the pinned PokeAPI/api-data checkout + the vendored tree's custom fields, with `python3 tools/import_pokeapi.py --check` gating committed-JSON freshness in verify_all (S4.5) and `pokemon_catalog.gd` as the identical-schema JSON loader with a summary warning trace. |
| `pokemon_progression` | `domain` | `progression` | 2 | 2 | 3 | 3 | Growth-curve EXP, level-up move learning, level/item/happiness evolution checks, and gender assignment from species ratios (drives infatuation) are extracted into pure domain rules. |
| `battle_loop` | `runtime` | `battle` | 3 | 3 | 3 | 3 | Battle runs on mainline formulas (type chart, stat stages, status and volatile conditions, capture, EXP yield, level evolution, priority turn order) with per-move source animations (frame-paced and deterministic, KO blows included), species cries, integer-scaled crisp presentation, and scenario coverage (`wild_battle`, `battle_anim`, `ui_render_audit`). HP bar alignment (2026-07-21): the programmatic enemy/player HP bars were translated -11 stage px so they now sit on the GSC art's baked black tracks (enemy x32..79 y18..21, player x96..143 y74..77, width 48 preserved) — a scene-only `BattleView.tscn` change with NO .gd or render-model change (the model encodes no bar rects; sidecar `draw_order` is live-captured), exactly the four battle baselines (09-12) regenerated with overworld/menu baselines byte-identical, and visual_sweep green at 0/16 changed. Scores unchanged: alignment only, no product-behavior change. NOTE (pre-Phase-7 expansion pins): `seed_for_smoke` (`game_runtime.gd:207`) now pins the FULL rng surface — encounter + battle AND `player_avatar._rng` (wall-clock at boot previously) — and the new `rng_joint_pin` scenario pins the shared-`_rng` encounter/fishing/dig interleaving byte-stable across two in-scenario runs; the double-run determinism lane covers the six rng consumers. Scores unchanged: pins, not battle behavior. NOTE (Phase 0 static-gate cleanup): the pure-data effect tables extracted to `scripts/domain/battle_effect_tables.gd` (`battle_rules.gd` back to exactly 320/320); zero behavior change — scores unchanged: extraction only. |
| `menu_ui` | `ui` | `ui` | 3 | 2 | 3 | 3 | Start menu now hosts a full party screen (swap lead, summary, field moves, and a campsite `RETRIEVE` action for held Pokemon) and bag screen (item use on party); New Game is confirm-gated through the message box (the menu closes on confirm, stays open on cancel), all trace-backed and cooperating with battle transitions. NOTE (configurable encounters, 2026-08): the start menu grows an OPTIONS entry (index 3, before NEW GAME) hosting the NEW `scripts/ui/options_screen.gd` + `scenes/ui/OptionsScreen.tscn` child submenu (cycle OFF/CLASSIC/ANYWHERE + nudge the rate along the 0.02 ladder; write-through + save on every change; the session is the single source; the mode/rate constants ride `SessionState`, single-sourced), with the context-resolver extracted to `scripts/ui/menu_context.gd` at the 220 wall. Scores unchanged: a new menu surface, mechanical. NOTE (infinite-world slice): new title-screen OPTIONS submenu (options_screen.gd + menu_context.gd extraction) for the configurable-encounter opt-in; BeaconSelector renamed WayStoneSelector (edge beacons retired, way-stone teleport kept). NOTE (infinite-world slice 4): NEW GAME gains a creation-time seed choice — Z stays the one-press RANDOM default (the menu_save + input_gate-D drivers pin that tap), a ←/→ confirm-phase gesture opens the sceneless seed_prompt.gd (typed digits / cursor / digit-bump, capped 0x7fffffff on typed entry AND digit-bump); game_runtime.new_game(custom_seed) skips ONLY the world-seed randi draw (starter roll stays first); new seed_choice scenario. NOTE (GBC restyle wave 2, 2026-08): the start menu, party screen, bag screen, options submenu, and the pause-menu SeedPrompt rebuilt on the battle 160×144 stage idiom (the wave-0 `gbc_stage.gd`/`gbc_widgets.gd` library; the MessageBox textbox band rode wave 1, committed 36ffd71): integer-scaled NEAREST displays, `fonts.ttf`@7 black ink on white plates, explicit integer offsets only (never `set_anchors_preset` on a parented node — the ce1571b anchor-collapse defect class), every art load guarded (menu1.png / item_menu_gsc1.png / gsc background — plate/black-backing fallback, never a crash). The scenes shrink to root + script (+ timers); the 220 ui wall is paid by six extractions — `start_menu_stage.gd`, `menu_list_stage.gd` (the SHARED single-list composition, also consumed by the camp menu / way-stone selector / storage action plate; owned here with its home screens), `party_screen_stage.gd`, `party_action_dispatch.gd`, `bag_screen_stage.gd`, `bag_item_use.gd`. FROZEN: every public API, entry list, trace, latch signal, input semantic, and i18n string — row strings are NEVER rewritten (over-long rows clip at the plate interior via `clip_text`, so the scenario `.text` contract holds); the click convenience retargets from `ItemList.item_clicked` to a root `_gui_input` + `stage_point` inverse map + `row_rect` hit tests (handler name/signature preserved); NEW restyle seams `stage_root()`/`row_texts()`/`selected_row_text()`/`row_rect()` (+ the party screen's `action_row_text()`) serve the app readers (the `app_bootstrap` row's note). The committed menu baselines (06/07/08/28/29) are still the pre-restyle captures — re-pins land with the wave-3 `visual_sweep_update` pass. Scores unchanged: presentation rebuild + seam preservation, not game behavior. NOTE (Phase 0 static-gate cleanup): the row style constants + HP-bar builder extracted to `scripts/ui/party_row_style.gd` (`party_rows.gd` 209/220); the layout_audit child-order contract unchanged — scores unchanged: extraction only. |
| `harvest_loop` | `runtime` | `harvest` | 3 | 3 | 3 | 2 | Harvesting resolves cut/dig/smash on faced tiles with party capability, permanent sparse overrides, and save v3 persistence. Phase 5 added a DIG BONUS POOL parallel to the unchanged base yield (`harvest_resolver.DIG_BONUS_POOLS`/`bonus_for`/`_dig_draw`, a pure step-counter SplitMix over total_steps+tile — NO shared `_rng` consumed; `harvest_runtime` emits `dig_item_found {tile, item_id, biome}`): FAITHFUL for the beach (SAND → the `fresh-beach.md` "Dig Items" pool big_pearl/water_stone/clear_glass/revive), with FLAGGED PORT DIVERGENCES for GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP stone pools + the invented `DIG_BONUS_RARITY := 8` rate (PLAINS deliberately pool-less; ice/dawn stones unassigned — SNOW isn't diggable). Stone acquisition is proven by `field_moves_flow`'s dig group; completeness stays 2 by design (the harvest economy's consumer story is still building/crafting-driven, and the non-Beach pools are divergences, not scraped fidelity). |
| `building_loop` | `runtime` | `building` | 3 | 3 | 3 | 2 | Phase 1 placement: pure-domain structure definitions + faithful per-biome costs (`scripts/domain/structures.gd`), a runtime placement orchestrator that enforces Fighting-type Build capability and consumes bag materials through the session item API (`scripts/runtime/build_runtime.gd`), and a ghost-preview/build-mode/occupancy layer that COMPOSES `world_view.gd` without extending it (`scripts/runtime/structure_layer.gd`), with the overworld-context and party-screen `FIELD MOVE` entry points extracted into `scripts/app/field_action_router.gd` (the party-screen move id is now consumed, not decorative). Placements ride the world's sparse override map as a "placed" entry kind and persist under a NEW ADDITIVE v3 `structures` save key (version stays 3; the clears key is untouched), rendered in place through the world view's existing prop pipeline via a merged `mutations_for_view()` mirror — so logic/render/collision occupancy agrees by construction and `world_view.gd` is never extended. Legibility is trace-backed (build_mode_entered / structure_placed / structure_refused with a reason / materials_consumed / structure_demolished / materials_refunded / placement_flow_passed, refusals never silent); validation is mechanical (the `placement_flow` scenario proves refusal, wall+door placement with exact payloads, occupancy blocking pathing, save round-trip, the would-trap guard, and the demolish/refund round-trip; `world_consistency_audit` extends its three-way check to placed structures; `save_migration` round-trips a structures-bearing v3 payload; `visual_sweep` gains a deterministic `13_built_house.png` baseline). Product completeness stays 2 by Phase-1 design: a walled house with a door is buildable from harvested materials (the exit object), but bed/storage-box interactivity, the indoor-enclosure flood-fill, and full per-biome sprite coverage are deferred to Phases 2-3 (tech-debt-tracker), and the bed is unbuildable until the Phase-2 soft_bedding craft. SOFTLOCK FIX (review-found MAJOR, 2026-07-23): a player could permanently enclose themselves with their own placements (no escape existed — harvest ignores built tiles, demolition was Phase 2+). Both complementary fixes landed: (A) ORIGINAL-FAITHFUL DEMOLITION — Cut on a built tile refunds the full `cost_for(id, biome)` and hard_stone-cost shells need Smash (`structures.demolish_move_for`), wired into the harvest Z-action flow (`game_runtime.harvest_tile` routes placed tiles to `build_runtime.try_demolish`; `structure_demolished` + `materials_refunded` traces; the removal deletes the PLACEMENT entry only via `world_generator.remove_placement`, so the two-map v3 design keeps the underlying clear fact automatically); (B) the WOULD_TRAP guard in `try_place` — a hypothetical stamp + flood-fill from the PLAYER tile (`reachable_walkable_count`, budget 2) refuses `structure_refused{reason: would_trap}` for any solid placement that would leave the player walled in on their own tile (pens enclosing mons are intentional — player-tile reachability only; doors never trip it). `placement_flow` was extended with the exact four-sided trap attempt (the enclosing wall refused) and a build->demolish->refund round-trip (exact refunds, tile walkable again, clear fact intact); the demolition checks are extracted into `scripts/app/placement_flow_demolition.gd` for the app-layer line budget. HARDENING PASS (review-minor batch, 2026-07-23): the trap guard's hypothetical is now mutation-free BY CONSTRUCTION — it passes the candidate tile as one extra unwalkable tile via `reachable_walkable_count`'s new `blocked` seam instead of add/remove-stamping the live placements map (a future signal/trace/cap change to `add_placement` can no longer leak a phantom placement); the demolition proof's clear-fact assertion rides a public `tile_has_clear` seam instead of reaching into the runtime's private generator; and the load-bearing material->demolition WITNESS invariant behind the region-scale enclosure escape (log->Cut, hard_stone->Smash — every shell class has a cost material only its demolish move yields) is mechanized in `unwitnessed_demolish_moves()` and asserted on every `placement_flow` run, with the spec documenting that no non-harvest material source or party-release mechanic may ship without extending the guard. Scores unchanged: hardening + mechanized contract, no product-behavior change. Determinism and the save schema are unchanged. Main.tscn co-mod (2026-08): the scene gains the BeaconSelector UI child + the windowed-only `showcase_capture` dispatch registration — neither touches `structures.gd` / `build_runtime.gd` / `structure_layer.gd` / `field_action_router.gd`; costs, the would-trap guard, demolition/refund, the landmark-refusal clause, and the v4 schema are unchanged. Scores unchanged: scene-tree co-mod only. NOTE (infinite-world slice): field_move_actions warp routing reworked to way-stones (edge-beacon concept retired with chaining). NOTE (Phase 0 static-gate cleanup): camp/storage placed-object routes extracted to `scripts/app/field_object_routes.gd` (`field_action_router.gd` 153/220) and the f98a7cab-late `dig_silence_scenario.gd` registered; the `campfire_lit` trace source string deliberately kept `"App.FieldActionRouter"` — scores unchanged: extraction + registry coverage only. NOTE (C-toggle build entry, 2026-08-13): overworld `C` (`build_toggle`) is the live Build path; `Z` stays harvest/interact so Dig on walkable DIG-biome tiles cannot shadow Build. `dig_silence` D/E/F pin the player-facing C path (Z does not open build; C opens without placing; overlay Z places) and close the Z-entry over-fire. Scores unchanged: disjoint-key fix + regression, completeness still 2. |
| `camping_loop` | `runtime` | `camping` | 3 | 3 | 3 | 2 | Phase 2 camping/night survival composed over the Phase-1 placement system: the single day/night source of truth mirroring the world-view tint keyframes (`scripts/domain/day_phase.gd`, with `night_cycle` asserting the 269/270/1229/1230 boundaries agree so the duplication cannot drift), a light model (`scripts/runtime/night_system.gd`: lit campfire/torch within Manhattan 4 or a Flash-capable Fire-type party member — a capability READ, not the Phase-4 Flash action), night ghost encounters at half the light-less cadence on the shared `_rng` (type-derived sorted pool, deterministic under `seed_for_smoke`, the original's Damp/Mystic/Cursed-Soil dwell replaced by night-without-light — documented), shadow battles that block retreat for their duration via an injected callable (battle_runtime never imports night_system — the layering holds), rest/heal/campsite in `scripts/runtime/camping_runtime.gd` (sleeping bag 50% HP + fainted revive, NO status cure — the faithful answer; bed = full heal + status cure; resting writes the `campsite_tile` anchor the overflow-mon hold and blackout return both read, and blackout now returns to the campsite instead of world origin), the lit light-source glow composed INSIDE `structure_layer.gd` (a static generated gradient, night-only, no per-frame animation — the byte-stability contract; the design's documented fallback path, so world_view.gd stays untouched), and the encounter level/fallback helpers extracted into `scripts/domain/encounter_selection.gd`. Legibility is trace-backed (campfire_lit / rested / campsite_established / night_hazard_spawned / retreat_blocked / evolution_time_gate, refusals never silent); validation is mechanical (camp_survival + night_cycle + time_evolution scenarios + the visual_sweep_camping baselines, all seed-pinned, under verify_all). The budget-full files are touched only by extraction/same-line edit (game_runtime keeps its 320 budget by freeing the campsite accessors + encounter helpers into `camping_runtime.gd`/`encounter_selection.gd` before the new wiring, main.gd one same-line setup argument, pokemon_rules untouched — the Espeon/Umbreon/Frosmoth evolution fix is caller-side with the empty-context fail-open retained and documented). Product completeness stays 2 by design: the ghost trigger is the dev-attested simplification, the retreat block lasts the battle (time freezes mid-battle), dawn/dusk map to DAY, the overworld ghost sprites and the Flash field-move action are deferred, and the evolution gate's fail-open empty context is a documented guarantee, not a mechanic. NOTE (new-game-flow slice): `encounter_selection.gd` (this subsystem's extraction HOME) gains the `STARTER_SPECIES_ID := "MACHOP"` / `FALLBACK_SPECIES_ID := "CHIKORITA"` split + `build_starter_traced` (the game_runtime extraction, exact `"GameRuntime"` source string kept); `build_starter`'s rng shape is unchanged, so no pinned stream shifts. Scores unchanged: starter faithfulness fix + extraction, no encounter-stream behavior change. |
| `crafting` | `runtime` | `crafting` | 3 | 3 | 3 | 2 | Phase 2 campfire crafting: a pure-domain faithful recipe table (`scripts/domain/recipes.gd` — Poke Ball 1 magnet + 1 hard_shell, Soft Bedding 3 soft_feather + 3 silky_thread, Old Rod 1 log + 1 silky_thread, Good Rod 1 old_rod + 2 metal_coat, Super Rod 1 good_rod + 3 magnet — scraped from the wiki Campfire/Items pages with line citations) plus the kiln-gated Great Ball (poke_ball + metal_coat) DEFINED but UNAVAILABLE (no kiln structure in any ported phase; the exec-plan parenthetical that conflated it with the Poke Ball recipe is corrected in the spec, not flattened). `crafting_runtime.gd` crafts all-or-nothing through the session item API with reason-carrying `craft_refused` refusals (no_station / wrong_station / missing / no_recipe), the torch joins `structures.gd` appended LAST (stable cycle indices; faithful 1 Log + 1 Grass → log + dry_soil), and the campfire menu (`scripts/ui/camp_menu.gd` + `scenes/ui/CampMenu.tscn`, ≤220/≤250, ui-references-only) lists exactly the 5 campfire recipes with missing counts greyed — no discovery state (the wiki has none), so nothing about recipes persists. The ingredient source is a minimal deterministic INTERIM (`scripts/domain/material_drops.gd`: ELECTRIC→magnet, WATER→hard_shell, STEEL→metal_coat, BUG→silky_thread, FLYING→soft_feather — mapped 1:1 from the wiki Materials drop table, 100% on a type-matched victory/capture, pure/no-rng, granted by game_runtime with a `material_dropped` trace), witness-safe by construction (the table NEVER contains log or hard_stone — craft_flow asserts the drop-witness invariant every run via crafting_runtime.drop_witness_clean (is_drop_material)) and flipped to the documented SECONDARY by the Phase-5 faithful habitat drops (header flipped, kept not deleted; tech-debt + spec handoff). Legibility is trace-backed (item_crafted / craft_refused / material_dropped); validation is mechanical (the craft_flow scenario: refusals, exact bag deltas, the station listing, can_afford("bed") after soft_bedding, save round-trip). Product completeness stays 2 by design: crafted ball tiers are bag inventory only until the Phase-8 ball tiers, rods craft but do not fish (Phase 5), kiln balls/repel/apricorn balls are out, and the bag heal is flat 50% (indoor bonus + terrain/aggressive restrictions deferred with the enclosure flood-fill and the Phase-6 aggressive-chase system). NOTE (GBC restyle wave 2, 2026-08): the campfire menu rebuilt on the battle 160×144 stage idiom — the stage composition rides menu_ui's SHARED `menu_list_stage.gd` single-list build (one owner per file), and the runtime-access block EXTRACTS to `scripts/ui/camp_menu_access.gd` at the 220 ui wall (behavior-identical: the crafting runtime/session/catalog reads, the REFLECTIVE recipe-ingredient read off the crafting runtime's pinned `Recipes` domain const, the lit placement read, the MessageBox toast). The five-recipe listing contract (missing counts greyed, no discovery state), the Craft/Extinguish-or-Light/Demolish routing, every trace, the latch, and the click/input semantics are FROZEN (`input_gate` part B now reads `camp_menu.selected_row_text()`); the scene shrinks to root + script; the `16_craft_menu` baseline re-pin is PENDING (`visual_sweep_update`). Scores unchanged: presentation rebuild + extraction, not crafting behavior. |
| `storage` | `runtime` | `storage` | 3 | 3 | 3 | 2 | Phase 3 storage boxes & party management composed over the Phase-1 placement system: each craftable Storage Box (2 Log, placed like any structure) is an INDEPENDENT container — contents live as an additive array ON the placement entry keyed by tile (the `"lit"` precedent; `WorldOverrides.is_valid_placement` checks only kind + structure_id, so the round-trip needs ZERO validator changes and the generator's save/load plumbing is untouched), never a shared PC (faithful: "the contents of the box are not shared with other built boxes", `.firecrawl/buildings-scrape.md:294`). `scripts/runtime/storage_runtime.gd` (RefCounted; box identity IS its tile, so two boxes can never share by construction) drives open/deposit/withdraw/release through reason-carrying `storage_refused` refusals (`no_box`/`no_such_mon`/`last_party_member`/`party_full`/`would_strand_demolition` — never silent); the storage screen (`scripts/ui/storage_screen.gd` + `scenes/ui/StorageScreen.tscn`, ui-references-only, WIRED through the campfire-menu Z-route pattern in `field_action_router.gd` — the `BOX_ID` arm in `_route_camp_object` opens it, `_overlay_open` covers BOTH overlays, the `closed` signal re-enables + saves and feeds `input_router`'s generalized latch via `main.gd`'s `bind_ui_consumers`, and `storage_flow` drives the box loop through the real Z seam) adds a CONFIRM-GATED release through the MessageBox sibling (the 0.2 destructive-action contract; "It will be gone for good."), with the release-confirm MOUSE-bypass gated at `_on_entry_clicked` (a click on the still-visible action list mid-confirm cannot mutate the box, so the keyboard confirm releases only the mon it named); and `build_runtime.try_demolish` refuses a non-empty box (`demolish_refused{box_not_empty}`, checked before `remove_placement` — faithful to `.firecrawl/pokewilds-build.md:38`'s filled-chest rule reduced to the single-floor port), so demolition can NEVER destroy contents and an empty box Cuts to the full 2 Log refund. Legibility is trace-backed (box_opened / mon_deposited / mon_withdrawn / mon_released / storage_refused / demolish_refused); validation is mechanical (the `storage_flow` scenario: box round-trip opened through the real overworld Z seam (the router's `BOX_ID` arm), confirm-gated release on both branches, the release-confirm MOUSE-bypass group (`storage_release_mouse_check.gd`: a click mid-confirm mutates nothing; the keyboard confirm releases the mon it named), every refusal, the non-empty Cut refusal, box INDEPENDENCE in the world AND across save, save round-trip with corrupt-contents normalization, the overflow NO-ROUTING proof (full-party capture → campsite hold, never a box), and the dynamic witness guard; `save_migration` gains the v4 fixture; `visual_sweep_storage` gains the deterministic `18_storage_box` baseline; `ui_render_audit` gains a `storage` state). Save schema v4 is PURELY ADDITIVE (the v3→v4 bump the building spec pre-pinned; `SAVE_VERSION := 4`, absent `contents` backfills to `[]`, `_normalize_structures` sanitizes corrupt contents to an empty box — never a crash or a torn entry). Party management grew arbitrary REORDER (MOVE, replacing swap-lead-only; `set_party_lead` delegates to `move_party_member`) + DEPOSIT to the nearest adjacent box (`box_tile_near` deterministic scan — the "right next to an entrance" rule), and the load-bearing demolition-witness invariant is extended DYNAMICALLY: deposit/party-release refuse to strand the party's last witnessed Cut/Smash carrier while structures stand (the party pool counts, box contents NEVER — a box may be sealed away; the static `unwitnessed_demolish_moves` audit is unchanged and `storage_flow` asserts the dynamic half every run). Budget-full files were touched only by extraction + same-line edit (game_runtime freed ~23 lines by extracting `harvest_tile` into `harvest_runtime.gd` first; phase0_scenarios extracted `wild_battle`; party_screen extracted `party_actions.gd`; main.gd four same-line edits — the `bind_ui_consumers` line, the `_field_router.setup` argument, the `overworld_idle` StorageScreen check, and the `_toggle_menu` StorageScreen guard; start_menu dict-only). Product completeness stays 2 by design: release is permanent removal (the original's world-drop needs overworld-mon entities — Phase 6) behind double-emphasized confirm wording, nickname is deferred (mons already carry names — an additive RENAME later), there is no per-box mon cap (faithful: mons stack until moved), and overflow captures stay on the campsite hold (the always-exists no-loss anchor; boxes are MANUAL — the retrieve→deposit loop is the player's path from hold to box). NOTE (GBC restyle wave 2, 2026-08): the storage screen rebuilt on the battle 160×144 stage idiom — the stage composition EXTRACTS to `scripts/ui/storage_screen_stage.gd` at the 220 ui wall (two two-line Columns off the `party_rows.gd` formats, title/detail/hint plates, the actions popup plate riding menu_ui's shared `menu_list_stage.gd` Rows, the summary overlay), seams `stage_root()`/`row_texts()`/`selected_row_text()`/`row_rect()`; `storage_release_mouse_check.gd` retargets — the click point derives from the stage-local `row_rect` mapped through the `ScreenDisplay` integer scale (`item_clicked` is gone with the restyle) and the direct `_on_entry_clicked` call rides ALONGSIDE the synthesized GUI event as the guaranteed witness (the `storage_mouse_fallback` trace reason updated) — the mid-confirm gate stays the assertion target, so `release_mouse_ok` proves the same contract; the `ui_render_audit` storage state measures off `stage_root()` (ink-rect text extents, deliberate `clip_text` rows skipped). The box loop, the confirm-gated RELEASE, every `storage_refused` reason, the latch + Z-route, and every trace are FROZEN; the scene's full-rect `Dim` retires with the rebuild — the wave-1 MessageBox band (`z_index` over later `$UI` siblings) draws the confirm at full contrast, so the dimmed-confirm readability deviation is RESOLVED. Baselines 18/19 re-pins PENDING (`visual_sweep_update`). Scores unchanged: presentation rebuild + extraction, not storage behavior. |
| `field_moves` | `runtime` | `field_moves` | 3 | 3 | 3 | 2 | Phase 4 field-move completion: the eight moves that had capability rules but ZERO runtime callers (Flash, Teleport + Way Stones, Ride, Fly, Attack, Charm, Repel, Power) gain runtime callers, traces, and scenario coverage, completing the Phase-2 night design and wiring the hooks Phase 6/7 consume. The capability MODEL (`field_moves.gd`) is UNCHANGED — the spec records the fly/ride explicit-flag-only fidelity DECISION (no AUTO_TYPES broadening: fly stays 30 species, ride 32, the faithful db model; can_perform already returns false on an empty auto-type), so the all-moves party rides explicit flags (Charizard fly=1, Rhyperior ride=1). Orchestration is EXTRACTED into the NEW `scripts/runtime/field_move_runtime.gd` because `game_runtime.gd` is at its 320 budget (same-line instantiation + shared `_rng` injection only); player-facing entry rides `field_action_router.gd` (has room) with same-line `main.gd` edits (220/220). Flash keeps its faithful PASSIVE light (a Fire-type party member lights `LIGHT_RADIUS=4`, "the same range as a campfire"); Phase 4 adds the active caller + `flash_lit` trace + a Flash-only-party-keeps-the-dark-lit scenario, never diverging from the campfire range. Way Stones (new `way_stone` structure; the registry is the placed entries themselves on the `structures` save key — no separate session key) are intra-world warp targets for Teleport (`teleport_used`) and Fly (`fly_used`, visited-only with unvisited refusal); the world-edge Teleport Beacons + edge-fly chaining are explicitly Phase 7 (the Way-Stone/Beacon naming split is kept airtight). Ride adds a mount speed mode + sprite beyond the 0.09s run (`mount_summoned`, speed-only — no ledge-climbing); Power moves a NEW movable boulder prop (`structures.gd` `BOULDER_ID`, riding the placements map / `structures` save key, no build cost — distinct from Smash's destructible rock); Repel sets an additive `session_state.repel_steps` counter that short-circuits `generate_wild_encounter` before consuming encounter rng (`repel_active`, structural suppression — a documented fidelity gap vs the original's crafted low-level-only item). Attack/Charm are HOOKS only (`overworld_attack` / `charm_used`, the latter level-gated) — the overworld-Pokémon entities land in Phase 6, so the scenarios assert the hook is reached + traced + refuses gracefully with no target, never any mon state. Legibility is trace-backed (the eight domain events + the three scenario pass markers with their symmetric `_failed`, refusals never silent); validation is mechanical (the `field_moves_flow` scenario's precondition witness asserts `party_has_field_move_ability` for all 13 moves against the live catalog so drift fails loud, then the eight per-move happy-path + refusal checks PLUS the Phase-5 dig-acquisition group LAST (`field_moves_dig_checks.gd` — the stone-pool contract witness, a Beach SAND water_stone hunt predicted via the resolver (faithful pool), the first-found divergent biome pinned to the SHIPPED `DIG_BONUS_POOLS` table (a flagged port divergence), and a PLAINS negative control; `dig_acquisition_ok` in the payload, the witness for the harvest loop's `dig_item_found`); `build_house_flow` harvests→builds a house with a door→demolishes with exact refund under the all-moves party; `playtest_field_soak` extends the bot's repertoire, self-guarded; all seed-pinned + dispatcher save-guarded under verify_all). The all-moves party (`FIELD_MOVES_PARTY` = RHYPERIOR/CHARIZARD/MACHOP/CALYREX/DEDENNE/DRAPION, the DP-proven 6-species minimum cover) is a single shared constant swapped via `smoke_scenario_runner.swap_party` and restored on every exit. Product completeness stays 2 by design: Attack/Charm are hooks awaiting Phase-6 entities, Fly/Teleport stay intra-world (their TARGETS are same-world way stones only — edge chaining landed in Phase 7 Build 3 but Teleport can NEVER cross worlds, see the landmarks row), Ride is speed-only, Power is movable boulders only, Repel is the field-move simplification, and the active Flash is a convenience over the faithful passive light. NOTE (Phase 7 Build 3, the faithful way-stone beacon deltas — this subsystem's co-mod surface is `field_move_runtime.gd` at its 320 wall + `field_move_actions.gd`): a way stone built within `TELEPORT_EDGE_MARGIN := 8` of the `WORLD_RADIUS := 96` edge IS a Teleport Beacon — `register_way_stone`'s `beacon_registered_hook` fires `world_chain_runtime.note_way_stone_registered`, which traces `beacon_placed{tile, chain, beacon_index}` (an inland stone keeps `waystone_registered` only — beacon vs way-stone distinguished by edge proximity); `use_teleport`/`use_fly` refuse `field_move_refused{reason: "edge_suppressed"}` inside the margin band (the injected `world_chain_gate` seam, the Phase-6 hook precedent — ZERO lines of beacon policy at field_move_runtime's wall); and `field_move_actions._route_warp` routes teleport/fly through the multi-beacon SELECTOR ($UI/BeaconSelector, registration order, closing the last-registered-only divergence `fresh-faq.md:180`) when more than one beacon is registered, warps directly on exactly one, and keeps the v4 last-way-stone warp on zero. Beacons register PER-WORLD (the registry reads the ACTIVE world's placements), so a beacon list never spans worlds — the cross-world ban by construction (`fresh-faq.md:186`). The chain mechanic itself (edge crossing, per-world persistence, save v5) is the landmarks subsystem's. Scores unchanged: faithful beacon mechanics + a closed divergence, not new game behavior. NOTE (infinite-world slice): edge beacons + edge-suppression retired with chaining; way-stone teleport kept via the renamed WayStoneSelector (intra-world warp points), whose multi-stone CHOICE + avatar-input ownership (the menu-close re-enable re-checks the modal) is witnessed by the `waystone_selector` scenario. NOTE (2026-08-11 CI hardening): `field_moves_flow` now pins and rebuilds its own world/avatar before the Flash proof and asserts the dark control has no authored encounter token; a loaded random boot world can no longer route the control around the night-ghost branch. Scores unchanged: harness determinism and a named non-vacuity guard, not game behavior. |
| `breeding_loop` | `runtime` | `breeding` | 3 | 3 | 3 | 2 | Phase 5 Pokemon systems (shinies, breeding, habitat drops; the evolution-stone bag-use seam WIRED AND stone ACQUISITION landed — faithful Beach-Dig + flagged port additions): the breed→hatch→shiny-hunt loop composed over the Phase-1 fence (pens are sealed fence rings detected by ONE SHARED flood both runtimes ride — `Breeding.detect_pens`, `PEN_FLOOD_BUDGET` 256, min-y-then-x anchor; a gated ring is no pen) with NO overworld-mon entities (the pasture abstraction stands in until Phase 6). Shinies: an additive `is_shiny` instance field rolled 1/256 at creation (`pokemon_rules.roll_shiny` — the injected shared `_rng` when threaded, a deterministic nonce fallback otherwise; pinned by `seed_for_smoke`), rendering as a faithful battle PALETTE SWAP (front.pal→shiny.pal exact-match remap over the strip before the AtlasTexture crop, cached; sparkle overlay at battle start) plus a status/egg icon badge (shiny.png/shiny_inverse.png beside gender), with the overworld rendering shinies identically to normals for anything WILD (faithful — `wiki-overworld-encounters.md:230`; the Ride mount and pen egg entities swap `overworld-shiny.png` when it exists). Breeding: female + compatible male sharing an egg group (parsed at `species_file_parser.gd:114-118`) in a type-matched habitat pen, both happy (≥220 — the shared happiness gate that also feeds the wired happiness evolution), Ditto breeds with anything, egg moves from the FATHER (a NEW `parse_egg_moves` over the previously unparsed `egg_moves.asm`), eggs fully visible pre-hatch (gender/moveset/shiny), step-based hatching at level 5 (2560-step minimum — hatch-cycle data unparsed), and `ALLOW_UNDISCOVERED_BREEDING := false` — the FAITHFUL default (the exec plan's genderless/Undiscovered workaround stays one flagged const flip away); exactly ONE compatible pair per pen breeds (a third compatible mon breaks attraction — faithful; extra incompatible mons ignored). The lay gate rides the SAME habitat scan as drops (`HabitatDrops.pen_habitat_tags` over the pen interior + one-tile ring, `types_satisfied` per parent, AND'd — drops-comfortable ⟺ the lay-gate passes by construction), and the pasture store is ONE session key held BY REFERENCE (`breeding_runtime._pastures` IS `session.pastures`; `habitat_runtime` reads/writes the same dicts in place, so the daily +10 reaches the 220 gate the SAME step — no save roundtrip; the withdraw erase guard keeps breeding's eggs). Habitat drops: the FAITHFUL primary material source (comfortable happy penned mons drop the type→material table once per in-game day — dual-types drop each type's material; the species-override families ALL implemented: Honey/Soft Wool/Star Piece/Berry Juice/Ancientpowder + Miltank→`moo_moo_milk` via a `RUNTIME_ITEM_SUPPLEMENTS` entry), flipping the Phase-2 battle-end `material_drops.gd` table to the documented SECONDARY source (kept, not deleted), with the type→pen-tile habitat table mapped to the port's biomes (documented basic-ground placeholders for GHOST/STEEL where no faithful tile exists; the Dragonite line prefers water only) and the load-bearing demolition-witness invariant reconciled (Rock→hard_stone WITHHELD from drops; `HabitatDrops.witness_clean()` asserted every `habitat_drops` run; the Phase-5 Steel-type shiny_stone cadence drop is witness-safe by construction — shiny_stone is neither log nor hard_stone, and `is_habitat_drop_material` walks it). DIVERGENCE (Phase 5 stone acquisition, flagged): a happy comfortable Steel-type mon also yields ONE shiny_stone per ≥4 in-game-day window (`habitat_drops.STEEL_STONE_DROP`/`STEEL_STONE_CADENCE`, a second `_day_tick` gate that reuses `item_dropped`, never `drops_for`'s daily path — `TYPE_MATERIALS["STEEL"]` stays exactly `[metal_coat]`; `wiki-materials.md:393` documents Metal Coat ONLY). Comfort re-evaluates on each crossed in-game day boundary (+10 happiness per comfortable day tick, no decay — a documented divergence from the original's deposit-time freeze, ~9.5x slower than the design's first hourly model, pacing the once-per-day drop rhythm, so the "becomes uncomfortable → forfeits the drop" quirk is observable). Evolution stones: the bag-use seam is WIRED — the new `scripts/runtime/stone_evolution_runtime.gd` (validate → consume → swap → trace — a refused bag decrement is the distinct `no_item` refusal and applies nothing) behind the one-line `game_runtime.use_stone_on_mon` delegation (the exact `breed_flow_checks.STONE_SEAM_METHODS` name, so `run_stone_case` auto-arms), with a bag-screen stone branch reusing the party picker; `evolution_stone_used {item_id, species_id, evolved}` (source `GameRuntime`, registry-required on `pokemon_progression`) now has its FIRST live caller — the stone is consumed BEFORE the swap, `is_shiny` + level/exp/moves/happiness ride the species swap while stats/max_hp/current_hp/catch_rate RECOMPUTE off the target entry (the exact level-evolution path, `battle_runtime._try_evolve`), and a no-effect use ("It would have no effect.") consumes nothing and emits no trace (the Potion precedent). All ten stone ids resolve from `item.properties:80-89` (thunderstone has NO underscore). Stone ACQUISITION is IMPLEMENTED (tech-debt item 11 RESOLVED): the faithful Beach-Dig pool (SAND → water_stone among big_pearl/clear_glass/revive, `fresh-beach.md`; surfaced via `dig_item_found`) plus flagged PORT ADDITIONS — the other-biome Dig pools (GRASSLAND/FOREST/SAVANNA/DESERT/SWAMP), the invented `DIG_BONUS_RARITY` rate, and the Steel-type shiny_stone cadence drop above (reusing `item_dropped`; `wiki-materials.md:393` documents Metal Coat ONLY) — all pure step-counter draws that consume NO shared `_rng` (seven scenarios re-ran byte-identical, the shiny_odds pin holding); ice/dawn stones stay grant-only (SNOW isn't diggable — the item-11 residual). Legibility is trace-backed (shiny_rolled / egg_laid / egg_hatched / egg_picked_up / mon_penned / breeding_refused / habitat_happiness_changed / item_dropped / pasture_deposited / pasture_withdrawn / pasture_refused / evolution_stone_used — refusals are reason-carrying events, never silent; the pen-lost recovery — on load AND live on demolition — rides the warning tier); validation is mechanical (the `breed_flow` scenario proves the contract witness, pairing→egg→hatch with the father's egg move, the Ditto rule, the unbreedable case, the wrong-egg-group negative (a happy EEVEE×ABRA — both basic-recessive, so habitat cannot confound the gate — with catalog-asserted disjoint groups AND catalog-asserted habitat-neutrality lays nothing across eight cadences), the Flying/Water ring-scan proofs (a PIDGEY pair lays in a pen built AROUND a live tree prop; water proven at the domain layer through `pen_habitat_tags` + `types_satisfied` with a no-water control, the runtime MAGIKARP pen auto-arming if a seeded pond site exists — else a named skip), egg pickup, the armed stone group (`evolution_stone_used` + FLAREON), and the save round-trip; `shiny_odds` is STATISTICAL but SEEDED — exact precomputed expectation on the pinned seed, never flaky, plus the adjustable-odds hook proof (`set_shiny_odds(2)` flips the rate); `habitat_drops` proves satisfied/unsatisfied comfort, the daily drop + cadence, the milk override, the witness audit, AND the Steel-type shiny_stone cadence control (`run_steel_drop_control` — first-drop-day stone, three-day suppression, re-arm; `steel_ok`); `visual_sweep_pokemon` baselines the shiny battle sprite + the egg in pen; all under verify_all). All Phase-5 state is ADDITIVE on save v4 (no bump — v5 stays reserved for Phase 7; eggs ride the party array as zero-HP additive mon keys the healthy-party scans skip naturally, pastures ride ONE additive session key held BY REFERENCE by both runtimes (validation stays single-entry in `breeding_runtime.apply_save_state` — a lost pen relocates its mons to the campsite hold, never loses them; `is_shiny` backfills absent→false). Budget-full files touched only by extraction + same-line edit (breeding.gd / habitat_drops.gd / breeding_runtime.gd / habitat_runtime.gd extracted new; pokemon_rules gains only the shiny roll + is_shiny key; game_runtime same-line wiring; main.gd same-line tick; session_state one additive key). Product completeness stays 2 by design: no overworld-mon entities (pasture abstraction; Phase 6 owns wild egg nests + egg-theft aggression + the Attack-on-egg shiny check), Rock drops withheld (witness invariant), the holding-item farm loop + talk-to-mon interaction deferred, hatch-cycle parsing deferred, evolution-stone ACQUISITION landed (bag USE wired + the faithful Beach-Dig pool + flagged port-addition Dig pools and the Steel-type shiny_stone cadence drop; ice/dawn stones stay grant-only — the item-11 residual), and the adjustable-odds MENU entry is future UI (the `set_shiny_odds` domain hook exists today). NOTE (new-game-flow slice): the adjustable-odds knob LANDED as the creation screen's SHINY RATE step — the FLAGGED ladder `SHINY_ODDS_CHOICES := [32, 64, 128, 256, 512, 1024]` around the faithful 1/256, applied as `PokemonRules.shiny_odds =` in `begin_created_game` BEFORE the pinned draw order (the `set_shiny_odds` hook lineage's effect; zero new rng) — so the completeness note above now reads: a start-MENU entry per se still does not exist, but the creation flow carries the knob. Scores unchanged: the knob rides the title_flow slice's seam, breeding behavior untouched. |
| `fishing` | `runtime` | `fishing` | 3 | 3 | 3 | 2 | Phase 5 rod-tiered fishing composed over the Phase-2 crafted rods (Old/Good/Super already craft into the bag at the campfire — `recipes.gd:40-42`, exact wiki match): face a water tile and press `Z` to cast the BEST rod held (a bag rod-USE entry is NOT wired — the overworld Z is the only trigger) — fishing works FROM SHORE without Surf (water tiles are `walkable=false`/`encounter=false`/surf-gated, `biome_defs.gd:62`), so fishing is PURELY ADDITIVE (no existing encounter stream changes). Each cast rolls a per-tier BITE on the shared seeded `_rng` (Old 55% / Good 70% / Super 85% — the wiki describes no minigame, so the wait is this single roll, `fishing_cast` traced on every rng-consuming cast); a bite draws from the rod tier's CUMULATIVE pool (Old: MAGIKARP/TENTACOOL; Good + HORSEA/CORSOLA; Super + QWILFISH — five Beach-scraped species, the documented Beach-anchored approximation, `fresh-beach.md:63` the ONLY scraped biome table) filtered to battle-viable species, at a FIXED per-tier level band (`TIER_LEVEL_RANGE`: Old 3-7 / Good 8-14 / Super 15-22); the shiny roll APPLIES to hooked mons (water shiny hunts are real). The hooked mon rides a PENDING-ENCOUNTER seam (`generate_wild_encounter` returns it BEFORE the wild draw — Repel and unlit-night ghosts never touch fishing), and `fish_caught` traces a fishing-battle capture (`consume_fishing_battle` resets on every finished battle, so the mark never leaks). Orchestration is EXTRACTED into the NEW `scripts/domain/fishing.gd` + `scripts/runtime/fishing_runtime.gd` (game_runtime at its 320 budget — same-line instantiation + the pending-seam + the capture trace); player-facing entry rides `field_action_router.gd` (a fishing arm ahead of the "nothing left here." fall-through). Legibility is trace-backed (`fishing_cast` / `fish_hooked` / `fish_caught`; `fishing_refused` reason-carrying — no_rod/no_water/no_species — never silent); validation is mechanical (the `fishing_flow` scenario: craft→cast→bite→hook→battle, the strict tier-gate containment under reseed, the refusals + the seeded bite-miss, the pending-seam proof under Repel, the shiny application, seeded determinism; under verify_all). Product completeness stays 2 by design: biome-agnostic tables (Beach anchor only — per-biome tables need fresh scrapes), the single bite roll (no cast/wait minigame), and the Lure Ball (3× fished) / Dive Ball (3.5× water) catch bonuses wait on the Phase-8 ball tiers. NOTE (pre-Phase-7 expansion): the `visual_sweep_fishing` satellite (shots `26_fishing_cast` + `27_fishing_bite`, per-sweep frozen date-shaped seed 2026072804, a fixed Beach water tile via ring-scan, an Old Rod through the REAL resolver, `encounter_chance` 0, the pending seam DISARMED at capture) baselines the fishing presentation; windowed-only, in the smoke list, marker pinned on this subsystem. Scores unchanged. |
| `overworld_mons` | `runtime` | `overworld_mons` | 3 | 3 | 3 | 2 | Phase 6 visible roaming Pokémon (the original's signature interaction) composed over the Phase-2 grass stream, the Phase-4 Attack/Charm hooks and the Phase-5 egg payload. PURE domain rules in `scripts/domain/overworld_mons.gd` (slot/cell spawn model, disposition resolution, Charm math, the egg-theft provocation rule, nest/Alpha rolls); the entity lifetime + forced-battle pending seam + every trace in `scripts/runtime/overworld_mons_runtime.gd` with the step engine extracted to `overworld_mons_sim.gd`; the y-sort render node `scripts/runtime/entity_layer.gd` (a STATIC self-wired child of `Main` — ZERO lines in main.gd/world_view.gd; the feet-origin `world_draw_order` contract EXTENDED and audited). THE LOAD-BEARING INVARIANT: NO entity roll consumes the shared `_rng` — every draw is a pure SplitMix-style hash of `(world_seed, total_steps, slot)` (the `harvest_resolver._dig_draw` precedent); the runtime's `setup()` takes NO rng parameter (structural), and the `overworld_mons` scenario pins it twice over (an identical `entity_set_hash` across a seeded double run + the CONTROL that N seeded wild draws are byte-identical with entities active vs inert — the wild stream provably unconsumed, so every other pinned scenario + the shiny_odds 6/1024 expectation stay untouched). Persistence is TRANSIENT by explicit decision (NO save keys, NO `SAVE_VERSION` bump — v5 reserved for Phase 7): entities re-derive from `(world_seed, total_steps)` + clock; recruits/taken eggs persist via the party. Validation is mechanical: the gate scenario (determinism, per-biome spawn off the LIVE catalog with the named wiki dispositions, charm-recruit + the full-party refusal :262, the provoked:false/+3 forced-seam battles :280/:284, the Repel seam ordering, the :248 egg Attack/TAKE binary, the level-5 hatch, EVERY-creation `shiny_rolled{origin:overworld}`, the transient save round-trip), the audit extension (world_consistency_audit + world_spatial_audit reach mon entities: spawn determinism, logic/render agreement, the z-order contract on entity sprites, per-biome coverage, the guardian probe — mutation lane extracted to `world_entity_audit.gd` at the 220 wall), and the deterministic 22/23 visual sweep. Product completeness stays 2 by design: ALL spawn/despawn numbers, radii and the nest/Alpha model are flagged divergences (the scrapes publish none; the nest/Alpha ships per exec-plan mandate, justified against the +3-stage buff :284, the faithful strewn-egg + TAKE/Attack binary kept to the letter); the recruitable subset is catalog-driven first-forms + wiki examples (no exhaustive table until fresh scrapes); overworld shinies render faithful-silent (:230 — the pen-egg sparkle stays the sole isolated visual divergence); asset coverage is tier-1 ow-sheet / tier-2 the source game's own placeholder, flagged never silent. DEEP-DIVE MUTATION LANE (six single-file sabotages, each sabotage-red then reverted clean): proved the entity-timeline determinism hash, the live-catalog disposition probe, the `alpha_provoked` witness and the Attack/TAKE binary; FOUND + CLOSED two holes (the Charm level-gate refusal was unpinned — now an over-leveled refusal case; the shiny tally was payload-buried — now a named failure), both re-proven sabotage-red; the audit's geometrically-dead `entity_outside_band` assert removed and the guardian sublane made loud on a no-nest skip. NOTE (pre-Phase-7 expansion): the new `playtest_entity_soak` scenario rides a NEW bot band (`scripts/runtime/playtest_bot_entity.gd` — extracted from the at-320-budget `playtest_bot.gd` exactly like `playtest_bot_breeding.gd`) looping steal-egg / provoke-Alpha / charm-recruit / flee-despawn over hundreds of seeded iterations with postconditions (sprite count bounded, y-sort keys never NaN, pending seam never left armed, despawn hygiene, zero `entity_at` on the player tile), pinned here alongside `overworld_mons_passed`; `visual_sweep_overworld` gains `30_night_roamers` (tod=0, entities ACTIVE — the night×entities×tint triple `04_night` leaves inert); the soak-warning pin + shot-range registry add pins. Scores unchanged: coverage + pins, not entity behavior. Main.tscn co-mod (2026-08): the BeaconSelector UI node + the windowed-only `showcase_capture` registration cannot perturb entities (iteration stays id-SORTED in `_live_list`; the activation gate is unchanged — entities run INERT for every non-opt-in scenario). The double-run lane's per-scenario cap was retuned 30→45s (`tools/verify_all.py`, miss-002 re-stamped): the gate scenario measures ~31s end-to-end, so the old cap truncated it mid-scenario and the truncation race diverged tails — two COMPLETED runs cmp byte-identical (ts-stripped), all ten consumers green x2 under the retuned cap. Scores unchanged: budget, not entity behavior. NOTE (configurable encounters, 2026-08): wild encounters become CONFIGURABLE with a collision-only DEFAULT — the grass per-step roll is now OPT-IN (`session.encounter_settings` mode `off`/`classic`/`anywhere` + rate, set from the title-screen Options menu), and by default a battle starts only when the player's sprite shares a tile with a wild mon (`scripts/domain/contact_encounter.gd` predicate → `overworld_mons_runtime._check_player_contact` in `note_player_step`, player-initiated `provoked:false`/no-buff; eggs exempt; the aggressive chase-catch unchanged). The player-action surface extracted to `scripts/runtime/overworld_mons_actions.gd` at the 320 wall (one-line public delegators keep every call site). The `encounter_config` gate scenario proves all four contracts + the double-run determinism (contact rides the pending seam, NOT the wild stream — the shared `_rng` stays provably unconsumed). Scores unchanged: documented product behavior + mechanical coverage. NOTE (infinite-world slice 2): the spawn-case sampling is anchor-local (climate blobs are local — the band-ring spread retired); NEST_MIN_RING stays a pure distance gate; the FrameTicker + generation bump fixed the exit-time animation-resource leak the terrain shift exposed (attack_animator/battle_turn_player '2 resources still in use' flake class); soak warning baselines re-pinned deliberately (playtest_soak 3->10 unhandled-effect variety, entity_soak 1->2 ARCEUS placeholder). NOTE (infinite-world slice 4): the disposition-tails respot check repositions onto a cell with a derivable roamer slot (the beach spawn can land in an entity-sparse patch; is_slot_present SplitMix mirror, deterministic) + playtest_soak warning baseline re-pinned 10->8 (beach spawn changed the soak's encountered mons). LEGENDARY-DUNGEON SLICE (2026-08-10): the repeating-lair machinery (`overworld_mons_lairs.gd`, `lair_for_chunk`, the `_lairs` window sync + instance-state map) is RETIRED — the frozen seven move into warp-entered dungeons in later slices; the origin-seven stamping, white-out HP/stages persistence, and the anchor-keyed `removal_key`/`is_removed` grammar are byte-identical. Scores unchanged. |
| `vision_fidelity` | `app` | `vision` | 3 | 3 | 3 | 2 | Capture honesty for windowed verification: a `frame_post_draw` readback guard added after (never replacing) scenario settle waits, a capture validity oracle (undersize/blank/uniform/magenta) with transport-vs-regression classification, a quarantine-tier duplicate-capture hook, a root-viewport-crop fallback for the Godot 4.6 magenta SubViewport regression (#115402), and head_sha/godot_version/window/renderer stamps on the playtest report. Slice 2 lands determinism pinning (verify-first): `project.godot` pins `msaa_2d=0` + `snap_2d_transforms_to_pixel=true`, measured byte-identical across 2/2 consecutive windowed sweeps and all 16 baselines, display_matrix green across all 6 sizes, canary 100% block-uniform, and `tools/determinism_verify.py` asserts the pins (the Nearest/integer-stretch candidate was rejected on measured 12/16-shot pixel deltas). The subsystem's current claims — guard, oracle, transport honesty, stamps, determinism, (Slice 3) semantic sidecars plus the explainable per-region diff (canary rect at ~0 tolerance = first red-tier coded region, near-zero ink/string tiers armed only after a measured-zero 3-run DUPCHECK noise floor, cursor zones masked, global 0.5%/tolerance-8 backstop and visual_diff.py untouched, runner-recorded verdicts + quarantine-tier region_drift, sidecar↔trace join at cursor+1), and (Slice 4) the coded legibility oracles — are now all mechanically supported, which is the rubric's bar for validation 3. Slice 4 lands `scripts/app/text_oracle.gd` (glyph template match via TextServer raster readback) with the duplicate-run raster-equivalence proof on file (raw draw_string == in-engine Label at 7px: 0 gap, 0 cross-launch, on both white and #f8f8f8; white-vs-#f8 binarization fringe ≤1 px/string; min-stroke 1px; stamp 4.6.1-stable (official)/forward_plus/Apple M4) and tolerances calibrated from those numbers (T_str = 2 px/string, T_glyph = 1 px/glyph — measured N with 2x headroom, never assumed); `tools/contrast_check.py` (WCAG linearized-sRGB contrast, AA 4.5:1 for 7px text) whose determinism is proven by construction (pure reduction over sorted color clusters ⇒ identical ratios on the Slice-2 byte-identical captures, zero run-to-run variance) and coded-gradeable only after that proof plus a clean-run history; and `tools/cvd_sim.py` (Machado, Oliveira & Fernandes 2009 severity-1.0 matrices, cited + import-time row-sum assert — quarantine-forever accessibility evidence, never red), with Contrast + CVD sections added to `tools/generate_legibility_report.py` and a fresh-pixels runner post-step on visual_sweep. The coded `text_oracle_passed` event is registered, and per-state `GRADUATED_STATES` replaces the single GRADUATED flag in `ui_render_audit`. Slice 5 lands the Lane-4 automation (`tools/vision_review.py`, stdlib-only): per-changed-shot evidence bundles (full-frame before/after copies, native-resolution cluster and region crops, numbered Set-of-Mark overlays, the expected-strings manifest, the rubric excerpt) feed a deterministic sidecar-consistency DEFAULT reviewer (no model, CI-safe, byte-stable per seed; `--reviewer-cmd` is the optional model plugin), and pipeline-side grounding enforcement guarantees 100% of emitted findings cite a sidecar region id and intersect its bbox (a mock reviewer's ungrounded output is dropped and counted). The `vision-review/2` output carries `finding_id` ledger join keys for the Slice-6 graduation ledger, and the shot-hash manifest drives staleness refusal; the runner post-step (`apply_vision_review`) records `vision_review_written` plus kind-`vision_review` quarantine entries — report-tier only, quarantine-forever (only tool errors fail red). The oracle spec's Lane-4 success criterion — the review produces its structured findings file on every sweep whose shots change AND its rubric catches at least one seeded visual defect all coded oracles miss — is PROVEN in the Slice-5 validation pass: the post-step regenerates `vision-review.json` on every compare run (even with 0 changed shots), and a seeded moves-menu cursor deletion on `10_battle_moves` — geometry fully inside the baseline cursor mask and outside every coded gate (canary ~0, ink/string/label rects, glyph tolerances, contrast rects, CVD palettes, and the global 0.5% backstop all silent) — is caught as a `cursor_missing` vision_review quarantine finding grounded on `cursor:move_0` with the run green and zero coded findings. Slice 6 lands graduation + calibration + the optional capture extra: `tools/graduation_ledger.py` (`record`/`status`/`calibration`, stdlib-only) banks windowed `ui_render_audit` runs into the committed evidence binder `docs/generated/graduation-ledger.json` (head_sha, boot-delimited session identity, per-state finding counts + ≤8 sample payloads, `text_oracle_passed` payload — self-contained evidence even if the machine-local user:// JSONL is deleted), computes per-state trailing clean-streaks against the threshold of 5 with the exact blocker and next step on file, and computes calibration cycles over the quarantine→graduation pipeline — the free VLM calibration loop — via Lane-4 `finding_id` and state+kind+region joins (`visual_explain.rects_overlap` the single geometry home); zero denominators are reported honestly ("undefined (0 of 0 — no quarantine findings in the window)", never 0.0/1.0), the first cycle is the baseline, and a two-cycle TREND needs the next legibility-garden cycle (weekly, Mon 14:00 UTC) and is never claimed from a single cycle. The optional vision extra ships behind its own registry entry and the ONE documented core-tools stdlib-guard exemption: `pyproject.toml` (uv; `dependencies=[]` — the core stays dependency-free) with `[project.optional-dependencies] vision = [scikit-image]`, and `tools/vision_metrics.py` (SSIM-map corroboration) is importable-graceful (`SKIMAGE_AVAILABLE` guard: absent → skipped section, never an error), quarantine-forever, and structurally NEVER a gate (no runner wiring, standalone exit 0, advisory localization only — 8–16 px window averaging dilutes exactly the sprite-/glyph-scale defects the coded oracles catch at exact pixels). The four optional capture spikes (Movie Writer motion lane, DAP `godot/put_msg` bridge, ScriptBacktrace, live NDJSON-over-TCP introspection endpoint) stay DEFERRED — explicitly off the critical path; the endpoint is specced as build-only-if in-process sidecars prove insufficient, and they have not. Per-state flips proceed on ledger-recorded evidence only (5 consecutive clean windowed runs at the current HEAD; documented flip order; battle_message requires the documented BOX judgment) and change the harness tiering, not the game. Slice-6 Graduate phase (2026-07-21, HEAD 7b733946): all four battle states flipped on ledger runs 1-5 with the evidence on file (`docs/generated/graduation-ledger.json` flips[]), and calibration cycle 1 committed as the honest zero-denominator baseline. Product completeness stays 2: `contrast_low` awaits its coded promotion, and Lane-4 findings are quarantine-forever judgment. NOTE (Workstream L.1): the one-command local gate `tools/verify_all.py` now MECHANIZES the subsystem's refuse-on-mismatch and freshness hooks — R1 report `head_sha`==HEAD, R3 windowed `godot_version`/`renderer`/`window` stamps vs the baseline sidecar capture_env plus the canonical 1152x648 window (a sweep captured under a different binary/renderer/window is refused, not diffed), and R6 `vision-review.json` freshness via `review_is_fresh` — which were "human/agent-enforced policy until verify_all"; this is the rubric's "mechanically supported" bar, so scores are unchanged (product completeness stays 2: it changes verification tiering and orchestration, not game behavior). SYSTEMIC FIX for the 11 stage-px HP-bar escape (the suite was fully green and fully self-consistent yet shipped the misalignment, because every automated check class is self-referential and the bars were never modeled): this slice closes the two structural gaps as one. G1 (programmatic-on-baked elements had no anchor to the source art they overlay) — a source-art anchor registry (docs/registry/art-anchors.toml) whose stage_rects are a pure integer function of the art BYTES re-derived at check time by tools/art_geometry.py (dark-run scan of battle_screen2.png verified to reproduce the fixed HP tracks (32,18,48,4) and (96,74,48,4) and exclude the misaligned 43/107), a mechanical [[scenes]] overlay inventory so any future overlay is caught on day one, a tools/check_art_anchors.py static RED gate folded into check_repo_contracts (completeness plus art-freshness re-derivation plus the art_sha256 pin — art is a submodule, so the sha is the per-file enforcement), an anchor_drift Lane-4 class grounded on the first art-truth anchor:<id> region, and a baseline-regeneration refusal gate (apply_anchor_gate) that structurally inverts the 3-days-green-on-wrong-baseline hole. G2 (the rubric art-fidelity questions were answered by no agent) — the Lane-4 --reviewer-cmd socket is populated by tools/vlm_reviewer.py (Qwen3-VL 8b, pure-stdlib, quarantine-forever WITNESS never the geometric oracle, composite degrade so coverage never depends on the model running) and a rubric-coverage ledger makes "unanswered" a first-class COUNTED state (q1- question ids, QUESTION_ANSWERERS, EXPECTED_QUESTION_COUNTS backstop, rubric_coverage manifest block). A miss-postmortem protocol plus tracked ledger (docs/references/miss-postmortem-protocol.md, docs/generated/miss-postmortems.json, entry #1 = the HP-bar episode) binds every future miss to a class-level mechanism. Validation stays 3 (the anchor gate, baseline refusal, and coverage backstop are mechanical — the rubric's bar); product completeness stays 2 (verification tiering and orchestration, not game behavior; anchor_drift is quarantine pre-graduation, VLM findings quarantine-forever). NOTE (pre-Phase-7 expansion): the rubric's five MENU-group model-only questions convert to DETERMINISTIC answerers over the menu sidecars' `expected_regions.strings` (the existing `deterministic-sidecar-consistency` kind — NO new machinery, NO new `[KIND_MODEL]` questions; counts unchanged), leaving ten model-only questions; the two `overworld_mons` questions stay flagged model-only with a bank-on-VLM-restoration note. Shot numbering is single-sourced in a shot-range registry const (`visual_sweep_baselines.gd`: per-sweep ranges + seeds + retired numbers; `_foreign_shot` guards derive; biome-shots floor ≥3 loud-fail); the shot set grows with 24-30 (17 formally retired — the sole whitelisted gap); the RED sidecar `crafted_state.world_seed`/`shot_seq` equality gate joins the region-gate post-step. Scores unchanged: verification coverage, not game behavior. NOTE (agent-surface completion sprint, 2026-08-10): `verify_all`'s refusals R1–R6 now carry the structured `{code, retryable, hint}` envelope (`stale_head_sha`/`report_invalid`/`stamp_mismatch`/`windowed_lane_missing`/`dap_endpoint_unreachable`/`vision_review_stale` — detail text and exit-code semantics unchanged), and the manifest names `.godot-smoke/vision-review.json` + `tools/vlm_reviewer.py` as the Lane-4 surface (the `[vision_review]` section; quarantine-forever semantics recorded on the entry). Scores unchanged: refusal legibility + manifest completeness, not gating behavior. NOTE (Cursor Cloud VLM path, 2026-08-14): windowed capture + Lane-4 Command Code (`cmd` + COMMAND_CODE_API_KEY) can run on Cloud; a mismatched capture_env.adapter_name skips PNG/region compare (warn `adapter_mismatch`, never a silent pass) so lavapipe cannot false-red against Apple M4 baselines. Scores unchanged: verification honesty, not game behavior. NOTE (Lane-4 fallback, 2026-08-18): when `cmd` / luna is unavailable the same Command Code review harness points at an OpenAI-compatible endpoint (`mistral-medium-3-5` at reasoning effort `high`; default `https://api.mistral.ai/v1`; key env-only). Replaces the unused `qwen3-vl:4b` slot. Scores unchanged: reviewer transport, not game behavior. NOTE (Lane-4 auto walk, 2026-08-19): `VLM_RUNTIME=auto` now advances to later locally-ready backends after a live model-call failure (not only a local probe miss); pinned runtimes stay fail-closed on that backend. `reviewer_meta.prior_backends` is presence/error-only. Scores unchanged: reviewer transport, not game behavior. |
| `landmarks` | `runtime` | `landmarks` | 3 | 3 | 3 | 2 | Phase 7 Build 1 of 3 (world-depth.md § Landmarks): the world gains DESTINATIONS — all three landmarks (Pokémon Mansion 13x10, desert Ruins 15x11 outer/inner/underground, Heart Tower 9x8) generate in EVERY world (origin + chained) from day one through the frozen chain-parametric generator seam (`Landmarks.tile_logic_for(world_seed, chain, map_pos, base)` / `landmarks_in_world(world_seed, chain)` / `anchor_for(...)`, the origin filling `chain=(0,0)`; any-world generation is a REQUIREMENT, not a later flip — user decision). Anchors are pure SplitMix (`overworld_mons._mix` convention, pinned `SALT_LANDMARK_ANCHOR 0x23`, bounded `ANCHOR_SEARCH_BUDGET := 400` with a fixed-ring fallback — never an absent landmark, never an unbounded loop); footprints stamp at the single `world_generator` mutation boundary BEFORE clears/placements (empty maps byte-identical; the hook was paid by EXTRACTING `_pick_prop` + `_coord_noise` + the rock-cliff tail), and `Structures.can_place_on` refuses landmark tiles for life (extracted at its 320/320 wall). The Mansion puzzle (a flagged port invention over faithful assets — frozen mini-contract) is PURE transitions in `landmarks.gd` (`MANSION_STATUES := 3` toggles, locked→statues_set→unlocked, stays-unlocked, no argument mutation): the mansion_key gates the room grid, the third simultaneous statue ON flips the sewer door walkable through the resolver overlay, journals are lore pickups. Puzzle state rides the FROZEN location-keyed state seam (`SessionState.landmark_state_for(chain)` / `set_landmark_state(chain, state)`) — Build 1 resolves both to the v4-ADDITIVE top-level `landmark_state` key (NO `SAVE_VERSION` bump; `session_state`'s marshalling extracted into the NEW `session_payload.gd`), and `landmark_runtime` is written against the seam, NEVER the keying, so Build 3's `chained_worlds` relocation touches ZERO runtime lines. Encounter scope is footprint-LOCAL: the dormant `PKMNMANSION`/`RUINS_OUTER`/`RUINS_INNER` tokens direct-match verbatim through `filter_species_ids`'s new `landmark_token` path (Ditto becomes faithfully mansion-exclusive — the located source breeding's universal-parent story rested on), with the flagged `RUINS_INNER_CURATED` {VOLCARONA 38-45}, with sprite-less GOLURK culled by the shared battle-viability rule high-level table + the aggressive DUSCLOPS underground via the Phase-6 stationary-entity mechanism (the dormant disposition live, the location the port's); token `""` outside footprints ⇒ `generate_wild_encounter` byte-identical to pre-Phase-7. Heart Tower ships footprint + entry + the dedicated field music (`Wilds_HeartTower.ogg` on entry, host-biome restore on exit); INTERNALS DEFERRED (spec § Scope quarantine). Legibility is trace-backed (`landmark_entered` / `puzzle_state_changed`, both from the single source `LandmarkRuntime`; the documented `key_item_used` auxiliary; symmetric `landmark_flow_passed`/`landmark_flow_failed` (miss-002; every tile derived from `landmarks_in_world`, never hardcoded); the sweep marker — refusals/failures never silent, the sweep's failure paths ride `push_error` + the harness ERROR_MARKERS). Validation is mechanical: `landmark_flow` (SEED 2026072907 seeded BEFORE `new_game`) SOLVES the Mansion puzzle on the fixed seed, proves footprint-local scope both sides of the boundary, footprint protection, and the state round-trip THROUGH THE SEAM — and JOINS the double-run determinism lane (regrouped to the spec's [4,4] by Build 2's `legendary_spawn`, the ≤80s per-group budget holding); the `visual_sweep_world_depth` satellite baselines `31_ruins_interior` + `32_mansion` (puzzle state crafted through the frozen seam, never played live; shot range + seed single-sourced in the registry); `check_repo_contracts` extends the `*_sim.gd` rng ban to `scripts/domain/**` + `landmark_runtime.gd` (NO new `RandomNumberGenerator` anywhere — static-rule-enforced; the shared `_rng` stream untouched). Product completeness stays 2 by design (Build 1 of 3): Heart Tower internals quarantined (element floors, the floor puzzle, the Calm-music trigger, the Ho-Oh/Lugia reward), the Mansion puzzle + the ruins high-level curation + the level bands (18-26/22-30/floor 30) + Ultra-Ball-props-grant-`poke_ball` loot are flagged divergences, and legendaries (Build 2) + chaining/save v5/the `33_beacon` shot (Build 3) are pending. Phase 7 Build 2 DOMAIN + RUNTIME + SCENARIO lanes landed (scores unchanged — the frozen-seven stamping runtime, the `battle_kind` music seam, the `legendary_encounter` trace and the `legendary_spawn` gate scenario are in; the scenario witnesses the domain lane's EMPIRICAL FLAG loud: origin worlds generate ZERO LAVA tiles, so the LAVA four resolve `NO_ANCHOR` — contract-legal, payload-witnessed via `lava_absent`; the exclusion's pool shift legitimately re-pinned the soak warning baseline + the overworld_mons nest alpha, miss-002-loud): `scripts/domain/legendary_placement.gd` (frozen seven `LEGENDARY_IDS`, flagged SNOW/LAVA affinity, PURE `_mix` ring-≥60 anchors bounded to `LEGENDARY_ANCHOR_BUDGET := 300` over the mirrored generator noise channels, `legendaries_for_world` suppressed by the persistent removal set, the `"<cx>,<cy>:<SPECIES>"` `removal_key`/`is_removed` grammar); `biome_encounters`' `LEGENDARY_IDS` never-encounter exclusion guarding BOTH the type fallback and the otherwise-unguarded full-catalog fallback (a static legendary can never surface in any pool — mutation-proven by `legendary_spawn`); and the v4-additive `legendary_removals` marshalling in `session_payload` (write-only-when-non-empty; absent → `[]`; golden byte-stable). The gone-for-good-per-world KO/catch removal is the flagged PORT DECISION (deliberately inverting wiki :224's re-battleable stationary legendary; the white-out-leaves-it-standing half stays faithful), flagged in the module header. Phase 7 Build 3 SAVE/MIGRATION lane landed (scores unchanged — the pure `scripts/domain/save_migration.gd` transform (migrate/world_id_for/downshift/the byte witnesses), the SAVE_VERSION 4→5 bump + the `root_seed`/`active_chain`/`chained_worlds` session vars, the seam resolution extended to `chained_worlds[world_id_for(chain)].landmark_state`, the `save_stability` golden guard's one pinned line change (canonicalize `migrate(golden)`) + the update-mode downshift + the world_chain round-trip sublane, and the pinned non-trivial v5 migration fixture): the frozen v4 golden stays the migration WITNESS — `canonical(migrate(golden))` == golden + exactly the three identity keys, proven against the committed fixture. Phase 7 Build 3 CHAINING + BEACON lanes landed (scores unchanged — the pure `scripts/domain/world_chain.gd` (origin-unmixed `world_seed_for`, the flagged `WORLD_RADIUS := 96` Manhattan extent, `TELEPORT_EDGE_MARGIN := 8`, `ENTRY_TILE_BUDGET := 200`, the seed-derived entry-edge deposit), the `world_chain_runtime.try_cross_edge` swap (persist the outgoing maps + `landmark_state` into `chained_worlds` → derive the incoming seed → re-apply its overrides/placements/campsite/pastures → RE-STAMP its legendaries + RE-APPLY its puzzle state through the frozen seam → entry-edge reposition; the `world_edge_crossed` + `world_chained` traces), the `player_avatar` edge-cross trigger (Surf over water / Fly airborne cross; walk refuses `traversal_blocked{reason: "world_edge"}` — only Surf/Fly cross), the faithful way-stone beacon deltas (the PER-WORLD registry — the cross-world ban by construction; `beacon_placed` registry-REQUIRED on an edge-band registration; the `edge_suppressed` teleport/fly refusal inside the margin band; the multi-beacon SELECTOR `scripts/ui/beacon_selector.gd` + `BeaconSelector.tscn` wired through `field_move_actions._route_warp`, closing the last-registered-only divergence `fresh-faq.md:180`), and the `world_chain` gate scenario (SEED 2026072913): the faithful loop witnessed end to end — the in-scenario double-run byte-identity of both worlds, the pure-hash seed witness, the unconsumed-stream CONTROL, per-world persistence across crossing + return + the v5 round trip, the legendary re-stamp, landmark-state independence, the three beacon deltas — joined as the NINTH double-run consumer (regrouped [4,5]; self-pinned), and `check_repo_contracts`' rng ban now arms over `world_chain_runtime.gd`; PHASE 7 COMPLETE — the deferred Heart Tower internals stay the sole quarantine; the `33_beacon` sweep shot LANDED with Build 3 (registry range [31,33]; the shot extracted to `scripts/app/visual_sweep_world_depth_beacon.gd` at the app-220 wall — a world edge with two registered way-stone beacons + the multi-beacon selector open, NO rng in the capture path; baseline captured, 0.0% re-compare drift). NOTE (Phase 7 post-Build-3 suite audit + repair, 2026-07-31): the 19-gap audit landed EVERY repair (scores unchanged — assertion non-vacuity + pixel coverage, not game behavior). The registry shot ranges are NOW `world_depth` [31,34] + the derived-world `world_chain` [35,36] (baselines 31-36: R11 `34_heart_tower` joins the origin satellite; `35_chained_world` + `36_legendary_guardian` form their OWN `visual_sweep_world_chain` satellite under `world_seed_for(2026072907,(0,-1))` = 1746331193 — the SplitMix mix & 0x7fffffff, NOT the raw 63-bit output — its own sweep so R3's one-world-per-run sidecar gate holds; all six sidecars carry R4 regional `expected_regions.ink` off `visual_sweep_world_depth_oracle.gd` — 31/32/35/36 ALSO carry a strict `canary_rect`, while `33_beacon` + `34_heart_tower` are ink-only (`canary_rect` empty: the beacon selector overlay + the tower decor's alpha edge are not strict-canary-safe, so the ink overlap gate is their red tier, `visual_region_diff.py:40`) — so a dropped statue/decor/beacon/tower/guardian sprite reds via `visual_region_diff.py`'s red tier instead of hiding under the 0.5% global gate; baselines captured, 0.0% drift). The new `beacon_selector` scenario (R5, seed 2026073101; its `beacon_selector_passed`/`_failed` pair registry-required) closes the multi-beacon SELECTOR choice gap shot 33 (screenshot-only) and `world_chain` (direct `use_teleport(tile)`, selector bypassed) both left open: two edge-band beacons registered, the `beacon_tiles()` listing pinned == registration order, the APP teleport route driven so `UI/BeaconSelector` opens, a REAL `smoke_tap` input pick of index 1 asserts the avatar lands on THAT tile, plus the cancel-changes-nothing control. R1 (room/sewer door NOT-walkable negative pre-states), R2 (guardian disposition AGGRESSIVE load-bearing on `landmark_entity_spawned` + the provoked +3 chase-catch witness, `landmark_guardian_checks.gd` extracted at the app-220 wall), R6 (the legendary never-encounter exclusion extended to `encounter_scope_for` curated/extra_ids — the path the per-biome pool scan cannot see), R7 (the post-reload RETURN witness — reload mid-chain, cross back, origin re-enters `newly_generated: false` with edits intact), R8 (surf-vs-fly cross-method duality through the PRODUCTION `player_avatar._try_start_step` gate + the spec §66 entry-edge DEPOSIT position asserted, `world_chain_cross_checks.gd`), R9 (the Mansion loot-shelf one-shot grant) and R10 (the tower EXIT host-biome restore now trace-asserted via `music_track_selected{kind: <host biome>}` off `play_biome_track`'s pre-gate emit — no longer seamless) folded into the three plan scenarios; the entity soak enters anchored dungeons and counts at least one chamber-legendary encounter. The R3 RED-tier sidecar seed-equality + source-art anchor gates now cover EVERY satellite family (the tuple single-sourced from `run_playtests.py` via importlib); the dormant `field_move_runtime` `_rng` injection is DELETED (the re-injection route named — the `fishing_runtime` precedent); CONTROLS.md struck its Ruins-loot overclaim. Product completeness stays 2: the deferred Heart Tower internals remain the sole quarantine. Showcase capture (2026-08): `showcase_capture` (driver + the `showcase_landmarks` / `showcase_guardian` / `showcase_pen` satellites — the `showcase_beacon` / `showcase_chained` frames RETIRED with world chaining, infinite-world slice 1) crafts the world-depth locales deterministically (seed 2026072907 mansion / ruins / guardian / heart tower; pen seed 2026072605) and writes frames + sidecars to `docs/generated/showcase/` — deliberately NOT a baseline sweep (no SHOT_REGISTRY, no reconcile/region-diff; windowed-only, transport-skips under PLAYTEST_FORCE_HEADLESS). Product completeness unchanged: showcase artifacts, not gating. NOTE (configurable encounters, 2026-08): `session_payload.gd` (this subsystem's extraction HOME) marshals the additive `encounter_settings` key alongside `legendary_removals` (write-only-when-non-default; normalize-on-load mirrors the `SessionState` setter; the v5 golden stays byte-identical). Scores unchanged: marshalling, not landmark behavior. NOTE (infinite-world slice): the `chain` param is frozen at origin (seamless plane); the world_chain/beacon_selector scenarios + edge-beacon machinery retired (way-stone teleport kept) — `beacon_selector` is SUCCEEDED by the `waystone_selector` scenario (the multi-stone choice + the avatar-input ownership pin; the `waystone_selector_passed`/`_failed` pair registry-required). NOTE (slice 2): the climate field resolves the LAVA empirical flag — ALL SEVEN legendaries anchor (guided min-margin lattice scan + sibling-exclusion chain; LEGENDARY_RING_MIN stays as the progression floor); EXPECTED_ANCHORED re-pinned to the full seven with the synthetic reach-1 NO_ANCHOR witness; showcase frames re-captured (06/09/10 retired). NOTE (slice 3): chunk-hash content scattering landed — content_scatter.gd + landmark_scatter.gd (landmark instances beyond the origin core, SCATTER_MIN_RING 112) + repeating legendary lairs (overworld_mons_lairs.gd, window-scoped, per-instance anchor-keyed removals); landmark_state re-keyed to "id@x,y" with lossless v6 load normalization; the origin core (3 landmarks + 7 anchors) stays byte-identical (baselines did NOT move); new content_scatter scenario + audit content_density metric. NOTE (infinite-world slice 4): seed_choice scenario registered on this subsystem (custom-seed determinism + beach spawn + random-path pin witnesses). NOTE (new-game-flow slice): `session_payload.gd` (this subsystem's marshalling HOME) marshals the three creation identity keys (`player_name`/`player_avatar`/`shiny_rate`, write-only-when-non-default, normalized on load) alongside `encounter_settings`; NO `SAVE_VERSION` bump — the goldens stay byte-identical with the keys absent. Scores unchanged: marshalling, not landmark behavior. NOTE (GBC restyle wave 2, 2026-08): the WayStoneSelector + the Options submenu (both carried on this entry's registry paths) rebuilt on the battle 160×144 stage idiom via the SHARED `menu_list_stage.gd` single-list composition (menu_ui-owned — one owner per file): `fonts.ttf`@7 black ink on white plates, integer-scaled NEAREST displays, explicit integer offsets only; scenes shrink to root + script; seams `stage_root()`/`row_texts()`/`selected_row_text()`/`row_rect()`. FROZEN: the multi-stone listing (`way_stone_tiles()` registration order), the opener-supplied rows + resolve Callable (the ui-may-not-import-domain contract), the self-wired input latch, and every trace — the `waystone_selector` scenario (the multi-stone choice + the avatar-input ownership pin) drives the rebuild unchanged; the options write-through/save-on-close contract is unchanged. Presentation-only: no landmark/legendary/warp/save surface touched. Scores unchanged: presentation rebuild, not landmark behavior. NOTE (legendary-dungeon slice, 2026-08-11 — DOMAIN layer only): `scripts/domain/dungeon_layouts.gd` + `scripts/domain/dungeon_maps.gd` join this entry's code_paths as PURE data/API (the seven hand-authored dungeon grids, the entrance facades + seal table, the curated scopes culled to battle-viable keys, the parsed-cell/warp/anchor API delegating to `legendaries_for_world`) — NO runtime consumer yet; they move to the slice's own `legendary_dungeons` entry when its validation layer lands. Scores unchanged: inert domain data, no behavior. NOTE (legendary-dungeon slice — predicate retirement): the chunk-hash LAIR + dungeon-SITE predicate legs leave `content_scatter.gd` (the warp-entered hand-authored dungeons supersede both; the 0x51/0x53/0x47/0x49 salts stay RESERVED so no future family collides with a pinned stream). Scores unchanged: retirement, not behavior. NOTE (C-under-waystone, 2026-08-13): `waystone_selector` also pins that C does not open build mode under the open selector (Main's overworld-free poll excludes the selector). Scores unchanged: input-gate pin, not landmark behavior. |
| `legendary_dungeons` | `runtime` | `legendary_dungeons` | 3 | 3 | 3 | 2 | Legendary-dungeon slice (world-depth.md § Legendaries, the dungeon LOOP): the seven hand-authored dungeons landed as PURE domain data/API (`scripts/domain/dungeon_layouts.gd` const ASCII grids + themes + curated scopes + entrance facades; `scripts/domain/dungeon_maps.gd` the landmarks-grammar parser, the species<->dungeon mapping, the entrance-anchor derivation delegating to `LegendaryPlacement.legendaries_for_world` with EMPTY removals so entrances stamp even after a catch and anchors stay byte-identical to the `legendary_spawn` pins, the warp tables, and the enforcing invariants `validate_map`/`validate_all` — exactly-one S/E/P markers, flood-fill spawn→chamber + spawn→exit, no orphan walkable cells, the exit south band, nonempty curated scopes whose `extra_ids` exactly match present, battle-viable catalog keys and contain no legendary — ridden by the world_gen_audit entry's ENFORCING lane) plus the runtime (`scripts/runtime/dungeon_runtime.gd`: `active_area`, the tile-logic choke point, the step-hook warps, the Regigigas five-tablet seal (READ-ONLY, never consumes), the chamber stamp through overworld_mons_runtime's dungeon seam with removal/`legendary_kos` suppression + the `REGI_RESTAND_STEPS` re-stand valve, the session-scoped whittle, the per-dungeon music arm). Legibility is trace-backed: `dungeon_entered`/`dungeon_exited`/`dungeon_entry_refused` (source `DungeonRuntime`), `tablet_claimed` (`OverworldMonsRuntime`), the auxiliary `dungeon_music` headless witness, and the symmetric `legendary_dungeon_passed`/`legendary_dungeon_failed` pair (miss-002) — all six registry-REQUIRED on this row. Validation is mechanical: the `legendary_dungeon` gate scenario (SEED 2026073001, self-pinned per the legendary_spawn precedent; checks split into `legendary_dungeon_checks.gd` and `legendary_dungeon_journey_checks.gd` at the app-220 wall) proves the seven entrance stamps at the pinned anchors with distinct warp tiles (pure derivation AND the live resolver), the REAL-step warp round-trip (entrance → spawn, exit → entrance with the pinned trace payloads), chamber-legendary dungeon-locality, the catch → exactly-one-tablet → gone-forever lane, the tablet-Regi KO valve (no tablet, `legendary_kos` at the KO's total_steps, cooldown suppression, a FRESH re-stand after a synthetic step advance), the 0..4→5 seal ladder (refusals carry the shrinking sorted missing list; tablets never consumed), overworld-sim suppression while real shared-tile contact and ordinary-Z provocation remain live, the in-dungeon field-action refusals (teleport/fly + build traced, harvest/dig message-only and camping refused), and the `active_area`/`legendary_kos` save round-trip plus invalid loaded-tile normalization into a FRESH session. The chamber-battle proofs the landmarks-owned `legendary_spawn` scenario drives (`legendary_spawn_battle_checks.gd`) are OWNED here — they exercise only dungeon-runtime behavior. Product completeness stays 2: the maps are hand-authored (no generator variety), the seal is the sole gate mechanic, and dungeon encounter scopes are one- or two-species curated tables filtered by the shared viability rule, with an invalid/empty scope yielding no encounter; the white-out re-battle / KO gone-for-good rules ride the documented port decisions on the `landmarks` row. |
| `world_gen_audit` | `domain` | `world` | 3 | 3 | 3 | 2 | Comprehensive world-generation AUDIT harness (the world-generation workflow; audit-first — the world-gen FIXES land in later slices). Three pure domain families measure the procedural world across a fixed 9-seed list, NO rng (a pure function of code + catalog + seeds — deterministic by construction, deliberately NOT a double-run consumer; its `determinism_density` check IS the in-engine determinism proof): `world_gen_cohesion.gd` (one cached disc scan/seed → disc determinism density, ring admission, biome-adjacency histogram + hostile-pair count, the diamond ring-seam metric vs control rings, region-size/speck distribution, per-ring biome histogram + SNOW/LAVA extreme reach); `world_gen_spawns.gd` (the FIRST base-stat-total computation anywhere in the codebase; type↔biome coherence + the legendary/EGG no-leak invariant, fallback-disjoint leaks, BST↔depth + level-band↔strength analysis); `world_gen_dungeons.gd` (per-biome footprint site availability incl. LAVA, anchors-in-extent, spawn-disc exclusion, spawn reachability, the unimplemented spec §19(c) gen-time reachability gate). The runtime facade `world_gen_audit_runner.gd` builds a FRESH `WorldGenerator` per seed (never the live `_world_gen`/shared `_rng`) and re-exposes the domain consts the app scenario reaches (app may not preload domain). TIER RULE: `world_gen_audit_passed` gates on the ENFORCING tier ONLY (structural invariants green today: disc determinism, ring admission, pool no-leak, landmark-in-extent, spawn reachability); every gap needing a future fix rides the warning-tier `world_gen_audit_advisory` event (NEVER gates) — the `advisory_findings` ARE the fix-slice punch-list. Measured headline advisories today: `lava_site_gap` (LAVA never generates on origin ⇒ zero LAVA-dungeon sites), `hostile_adjacency_count`/`ring_seam`/`speck_count` (no biome blending/transition logic — hard-edged biomes + diamond seams at rings 10/28/60), `bst_depth`/`level_band_strength` (stats play no role in spawn assignment — high-BST mons reachable at ring-0 level 2-5), `legendary_no_anchor`/`legendary_anchors_out_of_extent` (the 60-134 anchor budget can exhaust even for SNOW on some seeds), `spawn_disc_intrusion` (a ring-34 footprint can reach manhattan ≤24 of origin), `landmark_reachability` (§19(c) unimplemented). Scores: legibility/validation/architecture 3 (deterministic, mechanically-gated, layer-pure); product completeness 2 (this is the MEASUREMENT layer — the cohesion/blending, stat-aware-spawn, and LAVA/dungeon-site fixes are the follow-on slices, each promoting its advisory checks to enforcing as it lands; the LAVA fix re-pins `legendary_spawn_checks.gd` EXPECTED_LAVA_ABSENT in lockstep). NOTE (infinite-world slice 2 — the LAVA fix LANDED): the climate field (`biome_field.gd`) replaced the radial ring model — ring admission/seam/extent checks RETIRED; the enforcing tier is now disc determinism + the biome-distribution contract (ten common biomes per 400-radius stride-4 seed window + LAVA in >= `LAVA_WINDOWS_MIN` of 9 windows cross-seed, the joint tail's honest shape — a cold-climate window may lack LAVA) + pool no-leak + spawn reachability; the spawns advisories are per-biome (depth tiers dead); the headline `lava_site_gap` flipped from expected-zero to rare-but-nonzero; slice 3 added the `content_density` advisory (scattered landmarks / repeating lairs / dungeon-site rates over the beyond-core chunk sample) + instance-keyed landmark reachability — origin-core preservation is the audit's new pinning discipline. Findings: `res://.godot-smoke/world-gen-audit-findings.json` (per-run) + the curated `docs/generated/world-gen-audit-findings.md` (committed). |
| `title_flow` | `ui` | `bootstrap` | 3 | 3 | 3 | 2 | New-game flow + startup splash (title_flow slice, 2026-08-04; spec `docs/product-specs/bootstrap-and-overworld.md`, divergences ledger `docs/product-specs/infinite-world.md` § Flagged divergences 7-11): a black SPLASH card ("POKÉWILDS" + "a fan remake of PokeWilds — original game by SheerSt" — FLAGGED invention, no logo/copyright strings exist anywhere in the sources; 2.0s or any key) → the TITLE over COMPOSED art (gsc background + white wordmark plate + textbox entry band — the shipped `title_bg1.png` RETIRED by pixel probe, see the spec) with CONTINUE (shown only when a save is loaded) / NEW GAME → the MessageBox save-wipe confirm (the pinned pause-menu wording) → CreationScreen steps SEED → SHINY RATE → NAME → AVATAR → GO! (the faithful "Generating... please wait..." string + a FLAGGED 0.6s beat) → `game_runtime.begin_created_game` (identity + odds committed BEFORE the pinned new_game draw order) → beach spawn with the faithful level-5 MACHOP starter (`.firecrawl/wiki-getting-started.md:60`; the former CHIKORITA starter was an unflagged invention, corrected). Screens: `scripts/ui/title_screen.gd` + `scenes/ui/TitleScreen.tscn` (the splash is a PHASE of the title scene — one input handler, one `$UI/TitleScreen.visible` guard in main), `scripts/ui/creation_screen.gd` + `scenes/ui/CreationScreen.tscn`, and the sceneless `name_entry.gd` (7×4 A-Z+DEL+OK grid keyboard — a type-to-enter keyboard would collide with the InputMap; 8-char cap, "PLAYER" default) + `avatar_picker.gd` (the 24 shipped sets, default ben — the pre-slice hardcoded sheet, deliberately baseline-neutral); the SEED step carries the in-stage `gbc_digit_row` (SeedPrompt semantics ported verbatim; `seed_prompt.gd` itself untouched — the pause menu keeps its instance). Boot split: scenario boots (a `.godot-smoke/scenario.json` request present) keep the pre-slice path byte-identical (silent new_game on absent/corrupt/refused save, boot toast, world sync at boot); PLAYER boots show the splash/title and never auto-start a game or write a save; the pause-menu NEW GAME stays the pinned one-confirm reset. Legibility is trace-backed (splash_shown / splash_closed{reason} / title_shown{has_save, entries} / title_continued{party_size} / title_new_game_chosen / creation_confirmed{player_name, player_avatar, shiny_odds, world_seed} — pure trace.emit_event before any render gate; the `new_game_flow_passed`/`_failed` pair rides the requirements row). Validation is mechanical: the `new_game_flow` gate scenario (PIN 2026080601, dual-contract WORLD_SEED 2026080001, name ASH, avatar kris, odds 64) drives the REAL screens via `smoke_tap` injected input with injection witnesses everywhere (degraded delivery fails red, never vacuous), then the extracted helper proves all seven live canonical entrance records and real Main-routed avatar entry/exit round trips, including Regigigas's empty-bag refusal followed by a tablet-restored success; `new_game_flow_passed` carries `dungeon_warps_ok: true`. Green headless AND windowed; NOT a double-run consumer (the seed_choice precedent — custom-seed creation skips the world-seed draw, and the only pinned-stream draw is re-pinned before it; `verify_all.py` untouched). Architecture: every file sits within budget (ui scripts ≤220, scenes ≤250), ui→{ui,runtime,core} layer rules hold (the screens self-wire via `/root/GameRuntime` + the MessageBox sibling reach, the start_menu precedent), and both screens join `bind_ui_consumers` with argless `closed` latch signals (the splash-skip press and the final GO press never re-fire the menu/context polls same-frame). Creation identity (player_name/player_avatar/shiny_odds_choice) persists across resets and saves (additive v6 keys, write-only-when-non-default, NO schema bump — the encounter_settings precedent). Product completeness stays 2 by design: HOST/JOIN multiplayer NOT ported (`strings.properties:13` — flagged scope), and the flagged inventions (splash-card content; CONTINUE/NEW-GAME title over the original's NEW/LOAD + file list; the file-name creation step dropped — single-save port; the shiny ladder [32,64,128,256,512,1024] around the faithful 1/256; the name grid + 8-char cap + "PLAYER" default; the avatar default ben; the 0.6s generating beat; the wave-0 composed wordmark plate — no logo asset exists in the submodule) are named in the spec, never silent. NOTE (GBC restyle wave 0, 2026-08): both screens + both overlays rebuilt on the battle 160×144 stage idiom — the NEW shared library `gbc_stage.gd` + `gbc_widgets.gd` (integer-scale layout, stage_point inverse map, fonts.ttf@7 apply_font with an ink parameter, NEAREST on every art TextureRect) + `gbc_digit_row.gd`, and the per-screen 220-wall extractions `title_screen_stage.gd` / `creation_screen_stage.gd` / `creation_screen_render.gd`; the scenes shrink to root + script + timers. The retired-asset title art becomes COMPOSED art (gsc/background1.png load-guarded full stage + white wordmark plate + textbox entry band); the wordmark is a FLAGGED invention (composed text — no logo asset exists) with the É glyph `has_char`-verified and the pre-approved "POKEWILDS" degrade (fonts.ttf carries it today); the creation SEED step's second SeedPrompt instantiation is DELETED in favor of the in-stage digit row (SeedPrompt digit-edit semantics ported verbatim — typed digits/bump/backspace/MAX_SEED cap/`[d]` marking; input semantics + all traces + the creation_confirmed payload byte-frozen); the name/avatar grids become white stage plates with explicit integer offsets, making the ce1571b anchor-preset 0×0-collapse defect class structurally impossible (ink contract: black ink on white plates, white ink ONLY on the pure-black splash). Baselines RESOLVED: title shots 37-41 ride the main sweep (the `app_bootstrap` row's note). Scores unchanged: presentation rebuild + seam preservation, not game behavior. NOTE (Phase 0 static-gate cleanup): the creation-screen step-label accessor cluster (scenario seams + committed-name/seed-line) moved into the existing `creation_screen_render.gd` (`creation_screen.gd` 218/220); the three app readers retargeted with zero assertion changes — scores unchanged: extraction only. NOTE (agent-neutral integration Phase 4, 2026-08-09): the shared GBC library annotates every widget it builds with AccessKit metadata — PROPERTY-ONLY (`accessibility_name`/`accessibility_description`/`accessibility_live`; never the low-level RID API the engine's per-class updates would race), zero geometry/visual/input change, so the committed baselines are unaffected by construction; ONE contract serves the player-facing screen-reader tree AND the `ui_tree_dump` agent surface (which reads the same properties — the two agree by construction). Contract + new-widget rule: `docs/references/accessibility.md` (flagged experimental upstream). Scores unchanged: metadata-only annotations, not game behavior. |
