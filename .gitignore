# Godot 4+ specific ignores
.godot/
/android/
# Godot-regenerated import sidecars next to vendored source assets
*.import
.firecrawl/
.godot-smoke/
.DS_Store
.tmp_kris_frame_*.png
.tmp_kris_strip.png

# Python caches
__pycache__/

# PokeAPI api-data cache (270MB dump; re-fetch via tools/import_pokeapi.py --refresh)
tools/.cache/

# Claude Code session transcripts exported to the repo root (do NOT delete the user files; just untrack).
*-local-command-caveat*.txt

# Stray root screenshots from manual play (do NOT delete the user files; just untrack).
# NOTE: the filename uses a narrow no-break space (U+202F) before "PM" — matched exactly here.
Screenshot 2026-07-18 at 4.12.58 PM.png
Screenshot 2026-07-18 at 4.12.58 PM.png.import
